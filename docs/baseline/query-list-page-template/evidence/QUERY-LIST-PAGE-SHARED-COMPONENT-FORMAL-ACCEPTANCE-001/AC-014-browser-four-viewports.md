# AC-014 真实前后端 + 真实浏览器，四个正式视口

前端 `0.0.0.0:5173`（隔离工作区 vite）、后端 `8080`（30 条真实记录），无头 Chrome 通过 CDP 驱动，逐视口执行：正常首载 → 查询 → 重置 → 立即刷新 → 加载态几何 → Tooltip 单实例/宽度/切换/视口约束 → 表格与查询区响应式 → 路由槽不泄漏 → 控制台与网络。

## 1. 逐步结果

| 视口 | 首载真实数据 | 查询 | 重置 | 立即刷新 | 加载态几何 | Tooltip | 响应式 | 路由槽不泄漏 | 阻塞性控制台错误 | 非 GET 写请求 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1280×800 | 30 行 | 通过 | 通过 | 通过 | 58/58 | 通过 | 通过（表格 1175px 溢出内容区 965px，横向滚动生效） | 通过 | 无 | 无 |
| 1700×920 | 30 行 | 通过 | 通过 | 通过 | 58/58 | 通过 | 通过 | 通过 | 无 | 无 |
| 1920×1080 | 30 行 | 通过 | 通过 | 通过 | 58/58 | 通过 | 通过 | 通过 | 无 | 无 |
| 2560×1440 | 30 行 | 通过 | 通过 | 通过 | 58/58 | 通过 | 通过 | 通过 | 无 | 无 |

页面请求方法集合实测为 `{GET}`；无任何非 GET 写请求。控制台唯一一条错误是本验证主动注入的 `status of 500`（用于首载失败态），属于预期并已排除。

## 2. 逐视口关键几何

| 视口 | 页面壳直接子节点 | 查询按钮 | 重置按钮 | 立即刷新按钮 | 结果区 body `overflow-x` | 内容区 `scrollbar-gutter` | 表头列数 |
|---|---|---|---|---|---|---|---|
| 1280×800 | 3（HEADER / .ql-q-panel / .ql-result-panel） | 62×30 | 62×30 | 110×32 | auto | stable | 7 |
| 1700×920 | 3 | 62×30 | 62×30 | 110×32 | auto | stable | 7 |
| 1920×1080 | 3 | 62×30 | 62×30 | 110×32 | auto | stable | 7 |
| 2560×1440 | 3 | 62×30 | 62×30 | 110×32 | auto | stable | 7 |

表头列数恒为 7；表格 `min-width: 1175px`，在 1280 视口内确实产生横向溢出并由结果区 body 承担，未出现额外公共包裹层。

## 3. 首载失败态

四个视口下均确认：`.ql-page` 直接元素子节点 = 2（页头 + 错误卡片），无表格、无查询栏，出现数据加载失败文案，`role="alert"` 恰好 1 个；错误卡片 class 与「重新加载」按钮几何/样式与已批准基线严格一致。

## 4. 网络拦截的使用边界

浏览器内仅在临时窗口内拦截一次列表请求并返回 500，用于触发首载失败态；随后恢复真实后端并重新验证正常态。所有拦截都发生在浏览器运行时，未修改任何源码、未写入 Git 工作区、未改变后端状态。
