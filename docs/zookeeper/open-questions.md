# ZooKeeper 分析待确认问题

> 分析时间：2026-07-17 16:39 CST
> 答复人：待项目负责人确认

---

## ZK-Q-001：alive 临时节点是新增机制还是版本差异

- **问题**：hosp-006 有 alive 临时节点，hosp-007 没有。alive 是程序版本升级后新增的心跳机制，还是 hosp-007 因异常而无法创建 alive？
- **相关路径**：/bsoft-cdc/clients/hosp-006/alive
- **观察数据**：hosp-006 alive=临时节点（ephemeralOwner≠0），hosp-007 无 alive 节点
- **无法确认原因**：两个客户端实例几乎同时注册（7月17日），但一个有心跳一个没有
- **候选解释**：
  1. hosp-007 启动时因 ORA-00257 错误未能完成初始化，未到达创建 alive 的阶段
  2. 两个客户端版本不同，hosp-007 仍使用旧版协议
- **是否阻塞**：部分阻塞。影响在线状态判断逻辑的通用性
- **建议确认人**：CDC 后端开发

---

## ZK-Q-002：Server001 是否正常运行

- **问题**：Server001 的 updateTime 停留在 7月14日（3天前），无 alive 节点，status.code=1001（系统初始化）。服务端当前是否正常运行？
- **相关路径**：/bsoft-cdc/servers/Server001
- **观察数据**：status updateTime 和 ip updateTime 均为 2026-07-14 15:23:02
- **无法确认原因**：仅一个实例无法对比，不清楚 1001 是"已初始化完成"还是"正在初始化"
- **候选解释**：
  1. 服务端正常运行但不需要周期性更新状态
  2. 服务端已停但状态未被更新
- **是否阻塞**：部分阻塞。影响中心端页面的在线状态判断
- **建议确认人**：CDC 运维

---

## ZK-Q-003：状态码完整枚举值

- **问题**：当前仅观察到 4 种 status.code（1001/1002/1101/9001），完整的状态码枚举值有哪些？
- **相关路径**：所有 status 节点
- **观察数据**：
  - 1001：系统初始化（Server）
  - 1002：进程运行正常（Client）
  - 1101：增量模式运行中（Job）
  - 9001：进程异常（Client，含异常堆栈）
- **无法确认原因**：开发环境实例数少，无法覆盖全量状态
- **候选解释**：
  - 1xxx：正常运行状态族
  - 9xxx：异常状态族
- **是否阻塞**：否。可按已知状态枚举开发，后续补充
- **建议确认人**：CDC 后端开发

---

## ZK-Q-004：detailInfo 字段的内容范围

- **问题**：detailInfo 正常时固定为 "everything under control"，异常时包含完整 Java 异常堆栈。其内容范围和使用约定是什么？是否会在生产环境泄露敏感信息？
- **相关路径**：hosp-007/status → detailInfo
- **观察数据**：`"java.sql.SQLException: ORA-00257: Archiver error. Connect AS SYSDBA only until resolved.\n"`
- **无法确认原因**：仅一个异常实例，不清楚是否还有其他异常格式
- **是否阻塞**：否。但建议页面设计时对 detailInfo 做长度限制或折叠展示
- **建议确认人**：CDC 后端开发

---

## ZK-Q-005：scn 节点的生命周期

- **问题**：my-19c/scn 当前为空 JSON `{}`，上一轮分析（7月16日）有 offset 值。scn 数据何时写入？是否在增量阶段才会出现 offset？
- **相关路径**：/bsoft-cdc/clients/hosp-006/jobs/my-19c/scn
- **观察数据**：上一轮 `{"offset":"31120290432","updateTime":"2026-07-16 16:00:04"}` → 本轮 `{}`
- **无法确认原因**：探针于今天 10:30 重建，可能尚在快照阶段或未开始采集
- **候选解释**：快照阶段 scn 为空，进入增量阶段后写入 offset
- **是否阻塞**：否。页面可处理空值
- **建议确认人**：CDC 后端开发

---

## ZK-Q-006：signals 节点已消失

- **问题**：上一轮分析（7月16日）job 下有 signals 子节点（空数据），本轮已完全消失。signals 是已被移除还是按需创建？
- **相关路径**：/bsoft-cdc/clients/hosp-006/jobs/my-19c/signals（NoNodeException）
- **观察数据**：上一轮 dataLength=0，本轮 stat 返回 NoNode
- **无法确认原因**：不清楚 signals 的生命周期管理逻辑
- **是否阻塞**：否。不纳入当前页面设计
- **建议确认人**：CDC 后端开发

---

## ZK-Q-007：hosp-007 无采集任务的原因

- **问题**：hosp-007 的 jobs 为空（numChildren=0）。这是因为 ORA-00257 错误导致无法加载任务配置，还是该探针确实未分配任务？
- **相关路径**：/bsoft-cdc/clients/hosp-007/jobs
- **观察数据**：jobs 子节点数为 0，status.code=9001
- **无法确认原因**：不清楚任务加载与 Oracle 连接的依赖顺序
- **是否阻塞**：否
- **建议确认人**：CDC 后端开发

---

## ZK-Q-008：alive 节点的 updateTime 是否会持续更新

- **问题**：当前 alive 的 updateTime 等于 startTime（16:29:12），未观察到后续更新。alive 是一次性创建还是周期性更新？
- **相关路径**：/bsoft-cdc/clients/hosp-006/alive
- **观察数据**：dataVersion=0，mtime=ctime
- **无法确认原因**：alive 刚创建不久，无法判断是否会被后续更新
- **候选解释**：alive 依赖临时节点机制本身（会话存活=节点存在），不需要更新数据
- **是否阻塞**：否。不影响在线判断
- **建议确认人**：CDC 后端开发

---

## 问题汇总

| 编号 | 问题 | 是否阻塞 |
|------|------|----------|
| ZK-Q-001 | alive 机制通用性 | 部分阻塞 |
| ZK-Q-002 | Server001 运行状态 | 部分阻塞 |
| ZK-Q-003 | 状态码完整枚举 | 否 |
| ZK-Q-004 | detailInfo 内容范围 | 否 |
| ZK-Q-005 | scn 生命周期 | 否 |
| ZK-Q-006 | signals 已消失 | 否 |
| ZK-Q-007 | hosp-007 无任务 | 否 |
| ZK-Q-008 | alive 是否更新 | 否 |

阻塞问题：2 个（ZK-Q-001、ZK-Q-002），均为"部分阻塞"——不影响页面框架开发，但影响在线状态判断的准确性和通用性。
