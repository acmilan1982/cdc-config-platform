# 空白与冲突标记检查

## git diff --check
exit_code=0

## git diff --cached --check
exit_code=0

## 新增行行尾空白（相对基准新增的 '+' 行；须为 0）
0

## 新增行行尾空白明细（须为空）
  （无输出 = 本任务未引入任何行尾空白）

## 本任务触碰文件中基准即存在的行尾空白（历史遗留，本任务未引入亦未清理）
  （无输出 = 无历史遗留行尾空白）
