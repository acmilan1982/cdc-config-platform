# DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001 执行报告

- 任务编号：`DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001`
- 任务名称：数据订阅正式验收前 UI 交互基线对齐草案
- 任务性质：纯文档、正式验收前的需求/验收/设计/UI 定向对齐草案
- 目标分支：`develop`
- 基准提交：`25d60eed6df31ece0b2c43f5b4e67d8861efc4c3`
- 结果提交：本报告不预填本任务尚未产生的提交号；本任务真实结果提交、远程提交与推送状态由最终执行控制台输出（见 §15 说明）
- 前序任务：后端实现及真实数据库集成验证、前端 R1/R2/R3 实现、R3-R1 报告元数据收口（均获 ChatGPT 正式批准）
- 结论：本任务把已确认、已实现、已复审通过的最终交互事实同步为待正式复审的文档草案，**不代表**正式验收通过；仍需 ChatGPT 对本对齐草案正式复审，复审通过后先批准收口，再执行 126 条正式验收。

---

## 1. 基准提交与执行前状态

- 基准提交：`25d60eed6df31ece0b2c43f5b4e67d8861efc4c3`（任务开始前已验证本地 HEAD、`origin/develop` 与远程 `refs/heads/develop` 三者一致，ahead/behind=`0 0`）。
- 执行前完整 `git status --short` 已记录；任务开始前已存在的无关修改与未跟踪文件原样保留，未修改、未覆盖、未暂存、未提交。

## 2. 为什么正式验收前必须先对齐基线

正式验收基线仍存在少量已过期 UI 文字，且 R2 已批准实现的 Shift 连选尚未同步到需求/验收/设计。若不先修正文档，正式验收将使用过期标准，导致已获产品负责人批准的最终页面被错误判定失败。本任务只把已经确认、已经实现、已经复审通过的最终交互事实同步为待正式复审的文档草案，不创造任何新功能。

## 3. 三个确定冲突

1. `DSUB-REQ-053`、`DSUB-AC-058`、`UI.md` 仍写弹窗约 `94vw × 92vh`，最终批准实现为桌面默认宽 `1280px`、高 `82vh`，并受视口约束（宽度上限 `calc(100vw - 64px)`、小屏退化为 `calc(100vw - 32px)`、高度受 `calc(100vh - 48px)` 约束、不超出浏览器可视区域）。
2. `DSUB-REQ-066`、`DSUB-AC-073`、`UI.md` 仍要求目标库选中卡片使用“浅蓝背景”，最终批准实现已改为白色主体，仅用主题蓝边框、左侧主题蓝复选框与克制的淡蓝灰轻阴影表达选中，不使用大面积浅蓝或蓝色渐变背景。
3. R2 已实现并复审通过的 Shift 连续范围选择尚未写入 `DSUB-REQ-073`、`DSUB-AC-082` 和 UI/设计正文。

## 4. REQUIREMENTS 调整的准确编号

仅定向扩展以下 6 条需求业务行及其紧邻说明（编号保持 `DSUB-REQ-001~107` 连续唯一，数量保持 107，不增加、不删除、不重编号）：

- `DSUB-REQ-053`：最终弹窗尺寸（`1280px`/`82vh` 及视口约束、固定头尾中间滚动）；
- `DSUB-REQ-054`：空间优先级与顶部布局（描述单行必填最大 255、桌面源库/目标库同行约 34%/剩余、小屏整组换行、统一水平中轴、源表区占主要空间、Schema 区约 240~260px、不恢复最右侧“已选源表”面板）；
- `DSUB-REQ-057`：订阅描述单行输入框、必填、最大 255 字符、不使用 textarea；
- `DSUB-REQ-065`：紧凑目标库卡片布局（约 `200×48px`、两行、唯一左侧复选框、3 卡 1K/2K 同排、最多 5 个、无搜索/折叠/查看更多）；
- `DSUB-REQ-066`：目标库卡片最终视觉状态（白色主体四态，选中不使用大面积浅蓝/蓝色渐变背景，不使用健康/告警语义色）；
- `DSUB-REQ-073`：追加 Shift 连续范围选择权威规则（10 条，含起点/目标状态、范围包含首尾、连续 Shift 不移动起点、范围边界、跳过禁选/disabled、无起点退化、清除起点时机、单次集合单次提交、低干扰提示）。

其余 101 条需求业务行相对基准逐行零变化。

## 5. ACCEPTANCE 调整的准确编号

仅定向扩展以下 6 条验收业务行（编号保持 `DSUB-AC-001~126` 连续唯一，数量保持 126，全部状态仍为 `NOT_RUN`）：

- `DSUB-AC-058`（对应 `DSUB-REQ-053`）：最终弹窗尺寸与视口约束；
- `DSUB-AC-061`（对应 `DSUB-REQ-057`）：描述单行输入框、无 textarea、最大 255 字符；
- `DSUB-AC-069`（对应 `DSUB-REQ-054`）：固定头尾 + 桌面源库/目标库同行与小屏整组换行 + 统一水平中轴 + 源表区占主要空间 + Schema 区约 240~260px；
- `DSUB-AC-072`（对应 `DSUB-REQ-065`）：紧凑两行卡片、唯一左侧复选框、3 卡同排、最多 5 个按空间换行、无搜索/折叠/查看更多；
- `DSUB-AC-073`（对应 `DSUB-REQ-066`）：选中卡片白色主体 + 主题蓝边框 + 左侧主题蓝复选框 + 克制轻阴影，不使用大面积蓝色背景，未选中/悬停/禁用状态符合 §6.5；
- `DSUB-AC-082`（对应扩展后的 `DSUB-REQ-073`）：保留四个既有批量操作并追加 Shift 连选验收（正向与反向范围包含首尾、普通选中/取消后 Shift 范围、连续 Shift 保持起点、下一次普通点击更新起点、当前 Schema 当前可见结果边界、搜索结果边界、不影响其他 Schema、跳过保留字符与 disabled 表、无起点/起点不可见退化、各类切换与批量操作后清除起点、单次范围只提交一次集合、120~240 张表无明显卡顿）。

其余 120 条验收业务行相对基准逐行零变化；§5 追踪说明同步了上述对应关系。

## 6. DESIGN/UI 的对齐范围

- `DESIGN.md`：新增 §7.7“正式验收前 UI 交互基线对齐（最终实现事实）”，记录最终弹窗尺寸与源表空间优先级、桌面源库/目标库同行与统一水平中轴、订阅描述单行输入、目标库卡片白色主体四态（明确该规则只针对目标库卡片，源表选中行浅蓝背景保持不变）、Shift 连选状态模型与范围边界（含“Shift 操作只在前端内存中一次更新选中集合并只触发一次状态提交，不产生逐表网络请求”）、既有契约（`SourceTableInput[]`、`PRESERVE/REPLACE`、现有元数据加载与当前弹窗会话缓存规则、null-safe CSV 契约、无并发保护边界）保持不变、不新增后端接口；§8 需求追踪中 6 条需求的设计覆盖补充 §7.7。
- `UI.md`：同步最终视觉规格——弹窗 `1280px`/`82vh` 及视口约束；描述单行输入；源库/目标库桌面同行、小屏换行、统一水平中轴；目标库卡片约 `200×48px`、两行、唯一左侧复选框、3 个同排；目标库状态白色主体、选中蓝边框/蓝复选框/轻阴影、禁止浅蓝整块背景；源表区占主要空间、Schema 约 `240~260px`；Shift 连选完整交互和低干扰提示；不恢复最右侧“已选源表”面板；源表选中行“复选框 + 整行浅蓝背景”保持不变。

## 7. 107/126 数量与连续性验证

- 需求 `DSUB-REQ-001~107` 恰好 107 条、连续唯一；
- 验收 `DSUB-AC-001~126` 恰好 126 条、连续唯一；
- 验收→需求映射无悬空（6 条定向调整行的映射均已同步）。

## 8. 126 条验收仍全部 NOT_RUN

126 条验收执行状态全部为 `NOT_RUN`，非 `NOT_RUN` 数量为 0；本任务不执行任何正式验收，不代表验收已通过。

## 9. API/DATABASE 零变化

`API.md`、`DATABASE.md` 相对基准提交零 diff，保持 `APPROVED`。API 仍为 10 项能力、25 个业务错误码；DATABASE 设计不变；未引入版本令牌、内容指纹、快照版本、行锁或 `40910`；列表查询三类数据源 ID 语义不变；元数据 API 使用 query 参数；物化视图显式排除；`SourceTableInput[]`、`PRESERVE/REPLACE`、UUID32、`IdType.INPUT` 不变。

## 10. 前后端业务代码与测试代码零变化

- `frontend/src/**` 相对基准零 diff；
- `backend/src/**` 相对基准零 diff；
- 任何测试代码零变化；
- 只读核对了 `frontend/src/views/data-subscribe/components/SubscribeFormDialog.vue` 与 `SourceTableSelector.vue`（弹窗尺寸 CSS、`.sf-top-row`/`.sf-source-item`/`.sf-target-item` 的 `align-items: center`、目标库卡片四态样式、描述单行输入、Shift 连选锚点与 `applyShiftRange` 逻辑），仅用于保证文档准确，未修改任何代码。

## 11. 未运行测试/构建/服务

本任务为纯文档任务，未运行前端测试、未运行 `npm run build`、未运行任何 Maven 命令、未启动/停止/重启任何服务。

## 12. 未访问数据库及外部系统

未访问数据库、未执行 DDL/DML、未操作 `CDC_DATA_SUBSCRIBE` 或其备份表；未操作 ZooKeeper、Kafka、sync-client；未执行大屏调整。

## 13. 大屏延期状态

大屏调整保持 `DEFERRED_AFTER_DATA_SUBSCRIPTION_FEATURE_ACCEPTANCE`，未执行。

## 14. 本次只是草案，等待 ChatGPT 正式复审

- 需求/验收/设计/UI 状态更新为 `DRAFT_PENDING_USER_REVIEW`（上一正式批准版本及其提交证据作为历史事实保留）；
- 实现事实更新为 `IMPLEMENTED_REVIEW_APPROVED_PENDING_FORMAL_ACCEPTANCE`（非 `IMPLEMENTED_ACCEPTED`）；
- 126 条正式验收仍为 `NOT_RUN`；
- 本草案不代表正式验收通过；本任务完成后的唯一下一入口为 ChatGPT 对本对齐草案的正式复审，复审通过后先执行批准收口，再进入 126 条正式验收，不得跳过批准收口直接执行验收。

## 15. Git 执行结果说明

本任务结果提交、远程提交与推送状态以最终执行控制台输出为准（本报告不预填本任务尚未产生的提交号，避免悬空引用）。推送后应满足本地 HEAD、`origin/develop`、远程 `refs/heads/develop` 三者一致，ahead/behind=`0 0`。
