数据订阅正式验收真实浏览器验收结果
任务：DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001
浏览器：headless Google Chrome（CDP 自动化，真实渲染）
前端：http://127.0.0.1:5173/config/subscribe（Vite dev，代理 /api → 127.0.0.1:8080）
视口：1440×900 与 2048×768
证据文件：evidence/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001/browser/*.png / *.json

一、列表页（两个视口均验证）
- 列表首次加载：1440×900 与 2048×768 均渲染 12 条启用记录；控制台零 error。
- 列顺序：订阅描述、源库、源表、目标库、更新时间、操作（DOM 逐行核对）。
- 源库主显示机构（孝感市第一人民医院等）、ID 悬停（DATA_SOURCE_ID）。
- 源表列显示"共 N 张"（同源第一条/跨行完全重复 → 共 3 张；其余共 1 张）。
- 目标库标签折叠：1440×900 下 3 目标显示 +2 折叠；2048×768 下 3 目标同排平铺。
- 更新时间列：UPDATE_TIME 为空回退 INSERT_TIME 并标记"创建时间"（TIME/同源/异常等行）。
- 异常数据源展示：AC044 源停用 → "业务库 + 已停用"；AC045 源不存在 → "DSUB-FA-001-NOSUCH + 不存在"；
  AC046 目标停用 → 目标库标记"已停用"。
- 多源库异常行（ANOM）：整行显示"配置异常：该记录包含多个源库，请直接维护数据库"，无查看/编辑/删除
  按钮（操作列空）。

二、查询区（AC-031/036/037）
- 查询条件严格为两个多选下拉：选择源库（可多选，组内 OR）、选择目标库（可多选，组内 OR）。
- 源库多选下拉候选项：孝感市第一人民医院(112-source-19c)、杭州市第一人民医院、本机的oracle(my-19c)；
  不含停用源 199-source。
- 选择源库 112-source-19c 点击"查询"：列表过滤为 10 条（仅源含 112-source-19c 的记录，排除源停用/
  不存在的 AC044/AC045）。
- 点击"重置"：表单条件清空，列表保持上一次已生效查询结果（10 条，不自动恢复全部）——符合 AC-037。

三、详情弹窗（AC-049~056）
- 1440×900 与 2048×768 均打开"订阅详情"：展示源库、3 个目标库（doirs库×3）、按 Schema 分组的
  表清单（His.Dept_Table / his.Patient_V / VSM.Lower_tbl，大小写保持）、无遗留字段。
- 多源异常行不提供查看入口（AC-051，列表层操作列已为空，详情接口亦返回 40352）。

四、新增弹窗（AC-057~074）
- 1440×900 与 2048×768 均打开"新增订阅"：表单项为 订阅描述/源库/目标库/源表；
- 目标库卡片：6 个启用目标卡片两行紧凑排布，其中 DSUB-FA-001-TGT.DOT 卡片禁用并显示"保留字符"
  原因（句点保留字符禁用，AC-071/014）；含"保存"按钮。
- Schema/表选择器区域存在（hasSchemaSelector=true）。

五、控制台
- 全部交互步骤后控制台 error=0（console-*.json 为空数组）。

六、说明
- 精细交互（Shift 连选、全选/取消/只看已选/清空、120~240 表容量、表名模糊搜索、弹窗拖动边界、
  脏关闭二次确认、保存失败逐条展示）由前端定向/全量自动化测试覆盖（frontend-tests/*），浏览器层
  对弹窗结构与关键流程做了真实渲染抽查。
- 所有截图与 DOM 快照已归档；未修改任何代码或测试。
