# 探针端管理 Feature（client-config）导航与状态

## 1. Feature 身份

| 项目 | 值 |
|---|---|
| 用户可见名称 | 探针端管理（页面标题、菜单、面包屑已统一为“探针端管理”；路由 `meta.title`、`menu.ts` 与页面标题均为该名称，更名已实施） |
| Feature 内部标识 | `client-config`（内部目录标识不变） |
| 既有路由 | `/config/client`（保持不变） |
| 既有实现事实 | `IMPLEMENTED_PENDING_USER_ACCEPTANCE`（既有页面与后端 CRUD 已实现并通过自动化与真实页面复验，**尚待项目负责人验收**；来源见 `reports/CLIENT-CONFIG-LIST-UI-ADJUSTMENT-001-R2.md`、`reports/CLIENT-CONFIG-USER-ACCEPTANCE-PREPARATION-001-R1.md`；本节此前“占位实现、实现未开始”表述已随本节更新纠正，历史章节不改写） |
| 实现状态 | 分层口径：`existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE`（既有 Feature 实现已完成、尚待项目负责人验收）；`adjustment_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE`（**仅指**本轮页面级调整实现已由 `CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-IMPLEMENTATION-001` 于 2026-09-23 完成、并经 ChatGPT 从远程 Git 独立代码复审通过，当前等待项目负责人页面目测/接受；其 2026-09-23 代码提交时点状态 `IMPLEMENTED_PENDING_CHATGPT_REVIEW` 属历史记录；**不**代表既有实现状态，也**不**代表已目测、已验收或已接受） |
| 正式验收执行 | `formal_acceptance_execution_status=NOT_RUN`（**当前**全文件 135 条验收用例全部未执行，含既有 76 条、第一轮新增 13 条、第二轮新增 15 条、第三轮新增 13 条与第四轮新增 18 条；本行原记 117 条，已随第四轮草案建立同步为当前值，历史值不改写，见 §1.9） |
| 本轮调整任务编号 | `CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-001`（R1 修订：`-001-R1`；R2 证据纠错：`-001-R2`；R3 最小纠错：`-001-R3`；批准收口：`CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-APPROVAL-CLOSEOUT-001`） |
| 本轮调整基线状态 | `adjustment_baseline_status=APPROVED`（经 ChatGPT 远程 R3 复审 `APPROVED` 后由项目负责人于 2026-09-22 批准；批准前为 `DRAFT_PENDING_USER_REVIEW`） |
| 本轮调整批准状态 | `adjustment_approval_status=APPROVED_BY_PROJECT_OWNER`；`adjustment_approval_date=2026-09-22`；`adjustment_approved_reviewed_commit=5066c761f8a9400d5841222cb73b0c03f56a82d0` |
| 本轮调整实现状态 | `adjustment_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE`（批准后的页面级调整已由 `CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-IMPLEMENTATION-001` 于 2026-09-23 实现，定向测试与前端构建通过，并已经 ChatGPT 从远程 Git 独立代码复审通过，当前等待项目负责人页面目测/接受；2026-09-23 代码提交时点状态为历史值 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`；**不**等于已目测、已验收或已接受） |
| 本轮正式验收执行状态 | `formal_acceptance_execution_status=NOT_RUN`（新增 `CCFG-AC-077~089` 亦全部 `NOT_RUN`） |
| 第二轮调整任务编号 | `CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-001-V2`（2026-09-23，第二轮主列表视觉调整**草案**，对应报告见 §2 导航） |
| 第二轮调整基线状态 | `adjustment2_baseline_status=APPROVED`（经 ChatGPT 从远程 Git 对 V2 草案 R1 纠错结果的独立复审后，由项目负责人于 2026-09-23 批准收口，见 §1.5；批准前为 `DRAFT_PENDING_USER_REVIEW`，V2 草案复审曾为 `CHANGES_REQUIRED` 并由 R1 纠正任务 `CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-001-V2-R1` 承接） |
| 第二轮调整批准状态 | `adjustment2_approval_status=APPROVED_BY_PROJECT_OWNER`；`adjustment2_approval_date=2026-09-23`；`adjustment2_approved_reviewed_commit=3830cba16142b4e2ad88f1fa96682ea8397a1203`（该提交经 ChatGPT 从远程 Git 独立复审，结论 `APPROVED`——R1 纠错通过） |
| 第二轮调整实现状态 | `adjustment2_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE`（本轮已批准基线已于 2026-09-23 由 `CLIENT-CONFIG-VISUAL-FOLLOWUP-IMPLEMENTATION-001` 完成前端实现，并经 ChatGPT 从远程 Git 对实现提交 `fecf5a06d90c690b5ee984a9e487f05322e1dd70` 独立代码复审通过，**当前等待项目负责人页面目测/接受**，见 §1.6；其实现完成时点状态 `IMPLEMENTED_PENDING_CHATGPT_REVIEW` 与批准任务 `CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-APPROVAL-CLOSEOUT-001` 时点的 `NOT_STARTED` 属历史状态。**复审通过不等于已目测、已验收或已接受**） |
| 第三轮调整任务编号 | `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001`（R1 定向纠错：`-001-R1`；批准收口：`CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-APPROVAL-CLOSEOUT-001`，2026-09-23；第三轮 `+N` 清单与新增／编辑弹窗视觉调整，对应报告见 §2 导航） |
| 第三轮调整基线状态 | `adjustment3_baseline_status=APPROVED`（草案曾为 `DRAFT_PENDING_USER_REVIEW`；该草案经 ChatGPT 从远程 Git 复审、结论 `CHANGES_REQUIRED`，由 R1 纠错任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001-R1` 定向修订后，ChatGPT 对 R1 结果提交 `1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244` 复审、结论 `APPROVED`，项目负责人于 2026-09-23 回复“批准”，由批准收口任务收口为 `APPROVED`，见 §1.7） |
| 第三轮调整批准状态 | `adjustment3_approval_status=APPROVED_BY_PROJECT_OWNER`；`adjustment3_approval_date=2026-09-23`；`adjustment3_approved_reviewed_commit=1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244`（该提交经 ChatGPT 从远程 Git 独立复审，结论 `APPROVED`——R1 纠错通过）。**批准的是第三轮调整基线**；该批准为**纯文档收口**，批准收口时点尚未实现第三轮调整，其实现已另行由独立实现任务承接（当前实现状态见上行与 §1.8） |
| 第三轮调整实现状态 | `adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`（第三轮已批准基线已于 2026-09-23 由 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-IMPLEMENTATION-001` 完成前端实现，定向测试与前端构建通过、并已做真实浏览器核对，**当前停在 ChatGPT 远程代码复审入口**，见 §1.8；该任务时点之前的 `NOT_STARTED` 属历史状态。`CCFG-REQ-113~122`、`CCFG-DESIGN-054~060`、`CCFG-UI-043~049` 已实现，`CCFG-AC-105~117` 仍**未执行**）；本轮正式验收 `adjustment3_formal_acceptance_execution_status=NOT_RUN`（该任务时点全文件 117 条验收全部 `NOT_RUN`；**当前**全文件 135 条全部 `NOT_RUN`，见 §1.9） |
| 第四轮调整任务编号 | `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001`（2026-09-24，新增／编辑探针弹窗的字段级校验、数据源提示与弹窗稳定性、弹窗视口居中与标签右对齐、探针 ID 32 位与描述 256 字符输入／截断调整，对应报告见 §2 导航）；R1 定向纠错：`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001-R1`（纯文档：按钮旧规则冲突、`CCFG-AC-131` 不可能输入、后续模板追踪映射三处一致性纠错）；批准收口：`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001`（2026-09-24，纯文档批准收口，见 §1.9） |
| 第四轮调整基线状态 | `adjustment4_baseline_status=APPROVED`（草案曾为 `DRAFT_PENDING_USER_REVIEW`；该草案经 ChatGPT 从远程 Git 对提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 复审、结论 `CHANGES_REQUIRED`，由 R1 定向纠错任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001-R1` 定向修订后，ChatGPT 对 R1 结果提交 `9daf03848d53008d3ac73e6a43c1368fa5d72750` 复审、结论 `APPROVED`，项目负责人于 2026-09-24 回复“批准”，经批准收口任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001` 收口为 `APPROVED`，见 §1.9）；`adjustment4_approval_status=APPROVED_BY_PROJECT_OWNER`；`adjustment4_approval_date=2026-09-24`；`adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750`；`adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`（第四轮已批准基线的前端实现已于 2026-09-24 由 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001` 完成，定向测试、全量前端测试与前端构建通过、并已做真实无头浏览器只读核对，**当前停在 ChatGPT 远程代码复审入口**，见 §1.10；其首次远程代码复审结论 `CHANGES_REQUIRED`（仅主提交按钮加载态蓝黑跳色一项）已由 R1 定向纠偏任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001-R1` 修复、现行下一入口为 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_IMPLEMENTATION_R1_REVIEW`，见 §1.10 与 §5；批准收口时点的 `NOT_STARTED` 属历史时点，照实保留）；`adjustment4_formal_acceptance_execution_status=NOT_RUN`（草案建立时点的分层状态 `DRAFT_PENDING_USER_REVIEW`／`NOT_APPROVED`／`NOT_STARTED`／`NOT_RUN` 属历史时点，照实保留） |

### 1.1 本轮页面级调整分层状态（2026-09-22，R1 修订；同日批准收口后更新为 `APPROVED`）

本轮任务 `CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-001`（R1：`-001-R1`，纯文档）按项目负责人已明确确认的 9 项页面调整决定、以及 R1 新增的两项冻结决定（行选中能力整体取消、历史异常 `FG_ACTIVE` 红色 `异常：{原始值}`），为 `/config/client` 单页建立页面级调整基线。该基线经 R2 证据纠错、R3 最小纠错后，已由 ChatGPT 从远程 Git 复审（R3 结论 `APPROVED`）并由项目负责人于 2026-09-22 批准收口。当前分层状态如下：

```text
existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment_baseline_status=APPROVED
adjustment_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment_approval_date=2026-09-22
adjustment_approved_reviewed_commit=5066c761f8a9400d5841222cb73b0c03f56a82d0
adjustment_implementation_status=NOT_STARTED
formal_acceptance_execution_status=NOT_RUN
PENDING_USER_CONFIRMATION=0
next_entry=CHATGPT_REMOTE_BASELINE_APPROVAL_CLOSEOUT_R2_REVIEW
```

- **历史状态与当前状态必须区分（不得删除或伪装 R0/R1/R2/R3 记录）**：本轮新增/修订条款在批准前曾为 `DRAFT_PENDING_USER_REVIEW`；R1 阶段 ChatGPT 对 R0 的复审结论为 `CHANGES_REQUIRED`、R2 阶段对 R1 为 `CHANGES_REQUIRED`、R3 阶段对 R2 为 `CHANGES_REQUIRED`，R3 结果随后获 ChatGPT `APPROVED` 并由项目负责人批准。当前 `adjustment_baseline_status=APPROVED` 只表示**页面调整基线**获批，**不**表示页面已实现、已目测、已测试或验收已通过。
- **不得抹除旧实现事实**：本 Feature 的既有实际实现状态为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`（既有实现已完成、尚待项目负责人验收），该事实保持原样、**不**因本轮页面调整改写；只有**本轮页面级调整实现**为 `NOT_STARTED`。
- **验收分层**：全部 89 条（既有 76 条 `CCFG-AC-001~076` + 本轮新增 13 条 `CCFG-AC-077~089`）**全部 `NOT_RUN`**。不得把本轮调整基线批准写成“已实现”“已测试”或“验收通过”。
- **页面级授权 ≠ 模板级全局授权（2026-09-22 批准收口后口径）**：项目负责人**已授予** `/config/client` 的**页面级选择性接入授权**（查询列表页模板侧采用页面壳/查询面板/操作区/结果面板四组件、**不**接入刷新工具栏；列表表格视觉模板侧仅覆盖本页**主列表**，不含新增/编辑弹窗与弹窗内控件）。本次批准收口**不新增、不扩大**该授权范围；该页面级授权与本次页面调整基线批准**均不等于**本轮实现已授权或已完成。两套公共模板的**模板级全局迁移状态**（`page_migration_status=NOT_STARTED`、`page_migration_authorization_status=NOT_GRANTED`、`pilot_page_selection_status=NOT_DECIDED`）**保持不变**，**其他页面未被授权**。
- **待确认项清零**：`PENDING_USER_CONFIRMATION=0`；行单选/选中行高亮/“已选择：{探针ID}”取消与异常 `FG_ACTIVE` 红色 `异常：{原始值}` 均为项目负责人本轮已冻结决定，不再作为待确认项、风险项或实现自由度。

### 1.2 本轮页面级调整基线批准收口（2026-09-22，纯文档）

| 项目 | 值 |
|---|---|
| 批准任务编号 | `CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-APPROVAL-CLOSEOUT-001` |
| 证据链 | R0 草案提交 `fba09d17a471f7a4d1c7e56c07cfe9e6d1420dd1` → R1 修订提交 `2c2b2a71fcd049a68f86339d225f659af69e81c3` → R2 证据纠错提交 `5e0731aef0e36c1be9b87eba660e9b8c4a052555` → R3 最小纠错提交 `5066c761f8a9400d5841222cb73b0c03f56a82d0` |
| ChatGPT 复审对象 / 结论 | 远程提交 `5066c761f8a9400d5841222cb73b0c03f56a82d0`；结论 `APPROVED` |
| 项目负责人批准 | 于 2026-09-22 明确回复原话：`批准本轮探针端管理页面调整基线` |
| 批准对象 | `CCFG-REQ-091~103` 及本轮对既有需求的定向修订；`CCFG-AC-077~089` 及本轮对既有验收的定向修订；`CCFG-DESIGN-038~046` 及相关定向修订；`CCFG-UI-027~035` 及相关定向修订；R1 冻结的两项项目负责人决定（取消行单选/选中高亮/“已选择：{探针ID}”；异常 `FG_ACTIVE` 红色 `异常：{原始值}`，异常行“更多”仅含“停用”和“删除”）。**仅限** `/config/client` 单页，**不**扩大到 API、数据库契约、其他页面或模板级全局迁移 |
| 状态变化 | 仅 `adjustment_baseline_status` 由 `DRAFT_PENDING_USER_REVIEW` 变为 `APPROVED`（并记录 `adjustment_approval_status=APPROVED_BY_PROJECT_OWNER`） |
| 保持不变的计数与状态 | 需求/验收/设计/UI 定义 103/89/46/35；89 条验收全部 `NOT_RUN`；覆盖 103/103；`PENDING_USER_CONFIRMATION=0`；`adjustment_implementation_status=NOT_STARTED`；`formal_acceptance_execution_status=NOT_RUN` |
| 下一入口 | 2026-09-22 批准收口时的**历史入口**：`CHATGPT_REMOTE_BASELINE_APPROVAL_CLOSEOUT_REVIEW`（该远程复审已完成并返回 `CHANGES_REQUIRED`，由 R1/R2 纠错任务承接）；该记录时点的下一入口为 `CHATGPT_REMOTE_BASELINE_APPROVAL_CLOSEOUT_R2_REVIEW`（已由后续独立页面调整实现任务承接，**当前**下一入口见 §1.4） |

### 1.3 本轮页面级调整实现（2026-09-23）

独立实现任务 `CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-IMPLEMENTATION-001` 已按已批准基线实现 `/config/client` 单页调整（起始提交 `40d28125de5d7977936a35bca193ccf4489d65b7`，对应报告见 §2 导航）。该实现任务**完成时点**的分层状态如下（该实现其后已经 ChatGPT 从远程 Git 独立代码复审通过，**当前**分层状态见 §1.4）：

```text
existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment_baseline_status=APPROVED
adjustment_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment_approval_date=2026-09-22
adjustment_approved_reviewed_commit=5066c761f8a9400d5841222cb73b0c03f56a82d0
adjustment_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW
formal_acceptance_execution_status=NOT_RUN
PENDING_USER_CONFIRMATION=0
next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_PAGE_ADJUSTMENT_IMPLEMENTATION_REVIEW
```

- **实现范围**：仅在 `frontend/src/views/client-config/ClientConfigPage.vue` 与 `ClientConfigPage.spec.ts` 两文件实施页面级调整（无新增定向测试文件），并同步当前状态文档；两套公共模板实现、数据源管理参考页、路由/菜单、前端 API 类型与接口、后端代码、数据库对象与构建依赖**均未修改**。
- **实现内容**：接入 `QueryListPageShell`/`QueryListQueryPanel`/`QueryListActions`/`QueryListResultPanel` 四组件（**不**接入刷新工具栏/倒计时/最近刷新时间）；主列表六列固定顺序 序号｜探针 ID｜探针描述｜采集数据源｜数据源数量｜操作（最右 `fixed="right"`）；结果区左侧摘要、右侧“新增探针”；取消“删除所选”、行单选、选中高亮与“已选择：{探针ID}”及独立“状态”列；探针 ID 后按 `FG_ACTIVE` 三态显示（`'1'` 无标记、`'0'` 与数据源管理同款“停用”标记、其余历史异常红色 `异常：{原始值}`，不可见单空白以可见定界符如实呈现）；“更多”菜单按行态给出 停用/启用 + 删除；主表以显式根类接入列表表格视觉预设（仅主表）；页面“刷新失败”提示改为普通加载语义“列表加载失败 / 重试”。
- **状态边界**：`adjustment_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW` 只表示**代码实现已完成并停在 ChatGPT 远程复审入口**；**不**表示已通过项目负责人目测、已执行正式验收或已获最终接受。全部 89 条验收（既有 76 条 + `CCFG-AC-077~089` 13 条）仍**全部 `NOT_RUN`**，覆盖保持 103/103，`PENDING_USER_CONFIRMATION=0` 保持。浏览器只读实机目测因运行条件不具备（无可用开发服务与浏览器自动化工具，且不得擅自安装）记为 `BROWSER_BLOCKED_RUNTIME_UNAVAILABLE`，如实留待用户页面目测，未伪造任何截图或视觉通过结论。
- **页面级授权 ≠ 模板级全局授权（本次实现后口径不变）**：项目负责人**已授予** `/config/client` 的页面级选择性接入授权（查询列表页模板侧四组件、**不**接入刷新工具栏；列表表格视觉模板侧仅覆盖本页**主列表**）。本次实现**不新增、不扩大**该授权范围；两套公共模板的**模板级全局迁移状态**（`page_migration_status=NOT_STARTED`、`page_migration_authorization_status=NOT_GRANTED`、`pilot_page_selection_status=NOT_DECIDED`）**保持不变**，数据源管理参考页最终接受状态**不变**，**其他页面未被授权**。
- **时序说明（2026-09-23 追加，不改写以上历史表述）**：本轮实现起始提交为 `de23b68d1999425d14c2753515b237711147b826`；截至 2026-09-23，该页面级调整实现**已**经 ChatGPT 从远程 Git 独立代码复审通过，`next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_PAGE_ADJUSTMENT_IMPLEMENTATION_REVIEW` **属该时点的历史入口**。本节此前所述实现状态 `IMPLEMENTED_PENDING_CHATGPT_REVIEW` 为历史状态表述；此处仅追加时序说明，**不**改写实现定义、**不**宣布页面最终接受、**不**改变 89 条验收 `NOT_RUN` 的执行状态。

### 1.4 第二轮主列表视觉调整（2026-09-23 草案建立 → V2 复审 `CHANGES_REQUIRED` → R1 纠错 → 2026-09-23 批准收口为 `APPROVED`）

第二轮任务 `CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-001-V2`（纯文档）依据项目负责人查看 `/config/client` 与 `/config/data-source` 页面后**已明确确认的五项视觉调整决定**，为 `/config/client` **主列表**建立新一轮调整**草案**；该草案经 ChatGPT 从远程 Git 独立复审（结论 `CHANGES_REQUIRED`）、由 R1 纠正任务 `CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-001-V2-R1` 定向修订后，已由项目负责人于 2026-09-23 批准收口为 `APPROVED`（见 §1.5）。本提示词完整取代此前被项目负责人取消的 `...BASELINE-001-Agent-Prompt.md`（V1），V1 不作为现行任务执行、提交或引用。当前分层状态如下：

```text
existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment_baseline_status=APPROVED
adjustment_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment2_baseline_status=APPROVED
adjustment2_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment2_approval_date=2026-09-23
adjustment2_approved_reviewed_commit=3830cba16142b4e2ad88f1fa96682ea8397a1203
adjustment2_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
formal_acceptance_execution_status=NOT_RUN
PENDING_USER_CONFIRMATION=0
next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_APPROVAL_CLOSEOUT_REVIEW
```

五项调整：① 结果区最右侧“新增探针”改为参考页“新增数据源”同款**黑色实心按钮**（保留加号/文案/位置/行为，不改“查询”“重置”）；② “探针 ID”**正文**采用参考页“数据源 ID”同款字重与颜色（停用/异常标识语义与视觉保持，省略与编辑入口不变）；③ 主列表行高跟随参考页“数据源管理”主列表**实际行高规则**（移除本页固定像素行高，改由公共表格视觉预设的单元格内边距与内容决定；不改全局模板与公共预设、公共层不新增行高令牌）；④ “采集数据源”标签借用参考页“角色”标签视觉语言并采用**绿色／红色／中性色**三态（有项级 `anomalies` → 红；否则整行 `COMMA_PROTOCOL_AMBIGUOUS` → 中性；否则 → 绿，语义为“当前未检测到异常”而非“目标库”角色；红色异常不因整行歧义降级，整行歧义提示保留；ORG／ID 回退、异常优先、最多 6 个、`+N` 与完整清单语义保持，尺寸变更须同步核准测量盒模型）；⑤ 操作列**全部行**的“更多”文字统一改为**水平三点图标（Ellipsis）**（菜单柔和圆角/弥散阴影/适当内边距/清晰 Hover 与焦点、以分隔线单独隔开红色“删除”、可键盘操作、右边缘不裁切；点击/双击触发器与菜单不冒泡触发行双击编辑；下拉条目仍严格按 `FG_ACTIVE` 三态）。**不改项**：“探针描述”列宽维持现状（长文本省略）、现有查询条件、**刷新能力缺席**、六列顺序、CRUD 合同、Tooltip 业务信息与弹窗功能不变。

- **状态分层与批准边界（2026-09-23 批准收口后；实现状态已于 2026-09-23 由 §1.6 更新）**：本节建立时的分层状态为：本轮基线 `adjustment2_baseline_status=APPROVED`（2026-09-23 由项目负责人批准收口，见 §1.5）、本轮实现 `adjustment2_implementation_status=NOT_STARTED`、本轮验收 `NOT_RUN`（`CCFG-AC-090~104`，15 条新增，全文件 104 条全部 `NOT_RUN`）。**批准的是本轮视觉调整基线，不等于已实现、已目测、已验收或已接受**；本节其余“草案基线 `DRAFT_PENDING_USER_REVIEW`、需另行复审与批准”的表述属草案阶段（截至 V2 复审 `CHANGES_REQUIRED` 与 R1 纠错）的真实状态，已由 §1.5 记载的 2026-09-23 批准收口取代。**当前实现状态见 §1.6：`adjustment2_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE`**（第二轮已批准基线的前端实现已完成并经 ChatGPT 从远程 Git 对实现提交 `fecf5a06d90c690b5ee984a9e487f05322e1dd70` 独立代码复审通过，当前等待项目负责人页面目测/接受；其实现完成时点状态 `IMPLEMENTED_PENDING_CHATGPT_REVIEW` 属历史记录。本轮验收仍全部 `NOT_RUN`）。
- **参考来源效力边界**：参考页 `/config/data-source` **仅作视觉对照**，本轮规则**只**作用于 `/config/client` 主列表；**不**修改数据源管理的代码、文档、状态、行为，**不**修改模板级全局配置。所有 `#hex`/像素数值均为**参考页实现事实**，用户提供截图是**期望视觉来源**而非测试通过证据；实现须以真实参考页样式与实际视口为准。
- **历史保持不改写**：§1.1/§1.2/§1.3 记载的第一轮已批准基线（`adjustment_baseline_status=APPROVED`）、既有实现事实（`IMPLEMENTED_PENDING_USER_ACCEPTANCE`）与第一轮实现事实**保持真实、不改写、不抹除**；`CCFG-REQ-001~103`、`CCFG-AC-001~089`、`CCFG-DESIGN-001~046`、`CCFG-UI-001~035` 定义行**逐字节零变化**。本轮**不**自动继承上一轮批准；本轮已另行完成 ChatGPT 远程复审（对象提交 `3830cba16142b4e2ad88f1fa96682ea8397a1203`，结论 `APPROVED`——R1 纠错通过）并经项目负责人 2026-09-23 批准收口（见 §1.5）。
- **不涉及范围**：本轮**不改** `API.md`/`DATABASE.md`、**不改**任何业务代码/测试/前端或后端源文件、**不改** `docs/baseline/**` 与模板级全局状态（`NOT_STARTED`/`NOT_GRANTED`/`NOT_DECIDED`）；`PENDING_USER_CONFIRMATION=0`。

### 1.5 第二轮主列表视觉调整基线批准收口（2026-09-23，纯文档）

| 项目 | 值 |
|---|---|
| 批准任务编号 | `CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-APPROVAL-CLOSEOUT-001` |
| 证据链 | V2 草案提交 `328a7ac56fabb3f3ef5ff5f087537a8d828df84b`（ChatGPT 复审结论 `CHANGES_REQUIRED`）→ R1 纠错提交 `3830cba16142b4e2ad88f1fa96682ea8397a1203` |
| ChatGPT 复审对象 / 结论 | 远程提交 `3830cba16142b4e2ad88f1fa96682ea8397a1203`；结论 `APPROVED`（R1 纠错通过；该结论仅针对文档草案复审） |
| 项目负责人批准 | 于 2026-09-23 明确回复原话：`批准本轮五项视觉调整基线` |
| 批准对象 | `CCFG-REQ-104~112`（9 条）、`CCFG-AC-090~104`（15 条）、`CCFG-DESIGN-047~053`（7 条）、`CCFG-UI-036~042`（7 条）及 R1 纠正后的口径。批准范围为 `/config/client` **主列表**下列五项：① 新增探针按钮与数据源管理新增按钮同款黑色；② 探针 ID 正文字重与颜色对齐数据源 ID；③ 行高跟随数据源管理的实际规则；④ 采集数据源标签采用绿／红／中性三态（独立的行级歧义警示保持红色）；⑤ 操作列使用水平三点图标及改进菜单。**仅限** `/config/client` 主列表，**不**扩大到 API、数据库契约、数据源管理参考页、其他页面或模板级全局迁移 |
| 状态变化 | 仅 `adjustment2_baseline_status` 由 `DRAFT_PENDING_USER_REVIEW` 变为 `APPROVED`（并记录 `adjustment2_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment2_approval_date=2026-09-23`、`adjustment2_approved_reviewed_commit=3830cba16142b4e2ad88f1fa96682ea8397a1203`） |
| 保持不变的计数与状态 | 需求 112 条 / 验收 104 条 / 设计 53 条 / 界面 42 条定义行相对 `3830cba` **逐字节零变化**（含 R1 修正的 `CCFG-AC-093`、`CCFG-DESIGN-050`）；104 条验收全部 `NOT_RUN`；覆盖 112/112；`PENDING_USER_CONFIRMATION=0`；`adjustment2_implementation_status=NOT_STARTED`；第一轮已批准基线与既有实现事实保持 `IMPLEMENTED_PENDING_USER_ACCEPTANCE` |
| 下一入口 | `CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_BASELINE_APPROVAL_CLOSEOUT_REVIEW`（本轮批准收口的远程复审入口；只有该复审通过后才进入第二轮代码实现任务） |

批准对象**仅为本轮视觉调整基线**，**不**代表代码已实现、已测试、已目测或验收已执行通过；第一轮已批准基线与第一轮实现事实保持真实不改写；历史 V2/R1 草案文字与报告照实保留，其“草案／待复审”措辞属各自阶段的真实状态；`API.md`/`DATABASE.md` 本轮**未**修改；两套模板的模板级全局迁移状态（`NOT_STARTED`/`NOT_GRANTED`/`NOT_DECIDED`）**未变**。

### 1.6 第二轮主列表视觉调整实现（2026-09-23，前端实现）

独立实现任务 `CLIENT-CONFIG-VISUAL-FOLLOWUP-IMPLEMENTATION-001` 已按已批准基线实现 `/config/client` **主列表**的五项视觉调整（起始提交 `81bb8f172f612a2a6edf91181c59b4388da2c999`，对应报告见 §2 导航）。该实现任务**完成时点**的分层状态如下（**该实现其后已经 ChatGPT 从远程 Git 独立代码复审通过，`adjustment2_implementation_status` 当前值为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`，项目负责人查看页面后提出第三轮调整；当前状态见 §1.7**）：

```text
existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment_baseline_status=APPROVED
adjustment_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment2_baseline_status=APPROVED
adjustment2_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment2_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW
formal_acceptance_execution_status=NOT_RUN
PENDING_USER_CONFIRMATION=0
next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_IMPLEMENTATION_REVIEW
```

| 已批准条款 | 实现落点 | 结果 |
|---|---|---|
| `CCFG-DESIGN-047`/`CCFG-UI-036`/`CCFG-REQ-104` | 结果区最右“新增探针”去掉 `type="primary"`，改由 `.cc-btn-add` 声明与参考页“新增数据源”同款黑色实心样式（`#09090b` 底 / 白字 / 圆角 6px / 字重 500，hover+focus `#27272a`、active `#18181b`）；加号图标、文案、位置与新增行为不变；“查询”“重置”未改 | 已实现 |
| `CCFG-DESIGN-048`/`CCFG-REQ-105` | `.cc-id` 仅新增 `font-weight: 600` 与 `color: #09090b` 对齐参考页“数据源 ID”正文；未改字体族；“停用”标识与历史异常 `异常：{原始值}` 的语义、色彩与 Tooltip 保持不变 | 已实现 |
| `CCFG-DESIGN-049`/`CCFG-UI-038`/`CCFG-REQ-106` | 移除本页 `.cc-table :deep(.el-table__row){height:60px}` 与 `.cc-src` 的固定 `30px`，行高改由公共预设单元格内边距（`12px 0`）与内容决定；未改公共模板/全局预设/弹窗内表格，未新增 `--lt-*` 令牌 | 已实现 |
| `CCFG-DESIGN-050`/`CCFG-UI-039`/`CCFG-REQ-107`/`108` | “采集数据源”标签改为参考页“角色”标签语言（高 20px、12px/600、圆角 4px、无边框、左右内边距 9px）；新增 `.cc-dstag--ok/--neutral/--bad` 三态取色：项级 `anomalies` → 红（`#fef0f0`/`#d54949`）、否则整行 `COMMA_PROTOCOL_AMBIGUOUS` → 中性（`#f4f4f5`/`#606266`）、否则 → 绿（`#ecfdf5`/`#047857`，语义为“当前未检测到异常”）；独立行级歧义警示 `.cc-rowbad` 保持原有红色（`#fef0f0`/`#d54949`、边框 `#f1a7a7`、27px）与 Tooltip 文案不变；ORG／ID 回退、异常优先、最多 6 项、`+N` 与完整清单语义保持；`CHIP_BOX` 测量基准同步校准（含新增 `fontWeight` 支持） | 已实现 |
| `CCFG-DESIGN-051~053`/`CCFG-UI-040~042`/`CCFG-REQ-109`/`110`/`112` | 操作列入口由“更多”文字改为水平三点图标（`MoreFilled`，`.cc-more-link` 28×28、`role="button"`、`tabindex="0"`、`aria-label="更多操作：{探针ID}"`、`:focus-visible` 可见焦点）；菜单经 `popper-class="cc-more-popper"` 声明柔和圆角（8px）、弥散阴影、圆角内边距与 Hover／焦点反馈，`divided` 分隔线单独隔开红色“删除”；三态条目（`'1'` → 停用/删除；`'0'` → 启用/删除；异常值 → 停用/删除）与行级忙碌、二次确认、失败处理、既有接口 E5~E7 全部保持；触发器与菜单的 click/dblclick 不触发行双击编辑 | 已实现 |
| `CCFG-REQ-111` 及既有回归 | “探针描述”列宽、六列顺序、表格横向滚动、省略规则、最右固定操作列、刷新能力缺席、CRUD 合同、查询/重置、双击与键盘编辑、单实例 Tooltip 均保持不变 | 保留 |

- **实现范围**：仅在 `frontend/src/views/client-config/ClientConfigPage.vue`、`ClientConfigPage.spec.ts` 与同目录 `listLayout.ts` 三文件实施（`listLayout.ts` 为同在 `frontend/src/views/client-config/` 下的本页私有纯函数模块，因标签盒模型变更需同步校准测量基准而扩展，见报告 §2.1 说明）；并同步当前状态文档。数据源管理参考页、两套公共模板实现与模板级全局状态、路由/菜单、前端 API 类型与接口、后端代码、数据库对象与构建依赖**均未修改**。
- **状态边界**：`adjustment2_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`（**实现完成时点值**；该入口其后已复审通过，**当前值**为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`）只表示**代码实现已完成并停在 ChatGPT 远程复审入口**；**不**表示已通过项目负责人目测、已执行正式验收或已获最终接受。全部 104 条验收（含本轮 `CCFG-AC-090~104` 15 条）仍**全部 `NOT_RUN`**，覆盖保持 112/112，`PENDING_USER_CONFIRMATION=0` 保持。
- **时序说明（2026-09-23 追加，不改写以上历史表述）**：本实现完成时点 `next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_IMPLEMENTATION_REVIEW` **属该时点的历史入口**；该远程代码复审**其后已经 ChatGPT 从远程 Git 独立复审通过**（结论 `APPROVED`），`adjustment2_implementation_status` **当前值**为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`，**当前等待项目负责人页面目测/接受，尚未最终接受页面**（项目负责人随后查看实际页面并提出第三轮 `+N` 清单与新增／编辑弹窗视觉调整，见 §1.7）。**当前下一入口**为第三轮批准收口的远程复审 `CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_APPROVAL_CLOSEOUT_REVIEW`。此处仅追加时序说明，**不**改写实现定义、**不**宣布页面最终接受、**不**改变 104 条验收 `NOT_RUN` 的执行状态。
- **浏览器证据（本次与上一轮不同）**：本机存在可用的 `google-chrome`，故以真实无头浏览器（CDP 驱动）在 1440×900 与 1920×1080 两个视口核对了主列表与菜单，结论 `BROWSER_VERIFIED_HEADLESS_CDP`；核对用的静态服务与 API 桩为临时进程，核对后已停止并清理，未改动仓库文件。**该结论为代码 Agent 自测证据，不构成项目负责人目测或正式验收**。
- **页面级授权 ≠ 模板级全局授权（本次实现后口径不变）**：本次实现**不新增、不扩大**任何授权；两套公共模板的模板级全局迁移状态（`page_migration_status=NOT_STARTED`、`page_migration_authorization_status=NOT_GRANTED`、`pilot_page_selection_status=NOT_DECIDED`）**保持不变**，数据源管理参考页最终接受状态**不变**，**其他页面未被授权**。

### 1.7 第三轮 `+N` 清单与新增／编辑弹窗视觉调整（2026-09-23 草案建立 → ChatGPT 复审 `CHANGES_REQUIRED` → R1 定向纠错 → 2026-09-23 批准收口为 `APPROVED`）

第三轮任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001`（纯文档）依据项目负责人**已明确确认的四项调整决定**，为 `/config/client` 的 `+N` 完整清单弹层与新增／编辑探针弹窗建立新一轮调整。该草案经 ChatGPT 从远程 Git 独立复审、结论 `CHANGES_REQUIRED`，由 R1 纠错任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001-R1` 定向纠错后，ChatGPT 从远程 Git 对 R1 结果提交 `1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244` 复审、结论 `APPROVED`，项目负责人于 2026-09-23 回复“批准”，由批准收口任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-APPROVAL-CLOSEOUT-001` 收口为 `adjustment3_baseline_status=APPROVED`。该轮**不实现任何代码、不声明验收**，**批准的是调整基线，尚未实现第三轮调整**。当前分层状态如下：

```text
existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment_baseline_status=APPROVED
adjustment_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment2_baseline_status=APPROVED
adjustment2_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment3_baseline_status=APPROVED
adjustment3_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment3_approval_date=2026-09-23
adjustment3_approved_reviewed_commit=1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244
adjustment3_implementation_status=NOT_STARTED
adjustment3_formal_acceptance_execution_status=NOT_RUN
formal_acceptance_execution_status=NOT_RUN
PENDING_USER_CONFIRMATION=0
next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_APPROVAL_CLOSEOUT_REVIEW
```

四项调整：① `+N` 点击弹层完整清单每项按“孝感市第一人民”标签 Tooltip 的内容层级呈现（首行为完整机构名 `DATA_SOURCE_ORG`，次行为弱化前缀 `数据源 ID：` + 完整 `DATA_SOURCE_ID`，不再同行拼接；机构名缺失时以原始 ID 为主行且不重复），项级异常原因/冲突探针信息保留在项内可辨识位置、沿用既有红色警示语义，含逗号歧义行的既有提示文案与事实边界保持；清单仍按原存储顺序展示全部数据源、异常不隐藏、`+N` 计数准确、保留“点击 `+N` 展开”，不改主表行高与单个标签悬停 Tooltip，项与项之间以清晰间距或浅色分隔线区分；弹层按内容自然增高，实现时**移除** `.cc-full-list` 的 `max-height: 320px` 与 `overflow-y: auto`（生产常见每个探针约 5~6 个数据源，不得为臆想的大数据量牺牲“无内部滚动”）；② 新增／编辑弹窗两模式使用同一放大尺寸（当前 `width="680px"`，桌面目标约 **900px** 并受视口限制保留两侧安全边距），数据源选择区可见高度适度增加以露出更多候选项，“可选数据源”较“已选”获得略多横向空间以改善机构名、ID 与不可选原因的阅读，窄视口收缩到可用空间、表单项/候选区可用既有受控滚动，标题、关闭按钮与底部取消／创建或保存保持可操作；**该弹窗内部滚动属允许项，上一项的“无内部滚动”仅约束 `+N` 弹层，不得误套用到弹窗候选列表**；探针 ID 锁定/修改、描述自动生成、搜索、已选项、异常展示、保存校验、未保存关闭确认保持不变；③ 弹窗配置项名称与底部主按钮对齐数据源管理（配置项名称对齐参考页 `.editor-dialog .el-form-item__label` 的 `14px/500/#3f3f46`，保持页面默认无衬线字体族、不对表单套用主表探针 ID 的等宽加粗样式、保留必填红色星号与既有校验语义；底部主提交按钮对齐参考页 `.editor-submit-button` 黑色实心 `#09090b`/白字/6px 圆角/字重 500，hover/focus `#27272a`、active `#18181b`，禁用与加载态清晰可辨且不被常态规则覆盖；**仅该主按钮**变色，取消/自动生成/修改探针 ID 等不变；**保留既有文案**，新增探针按钮仍为“创建”、编辑探针按钮仍为“保存”）；④ 操作菜单“删除”**不加粗**（保持菜单项字重一致，仍以红色文字、上方分隔线与 hover/focus 反馈体现危险语义，不改启用/停用/删除业务语义、确认弹窗与三点触发器）。

- **状态分层与获批边界**：本轮为**纯文档基线**，`adjustment3_baseline_status=APPROVED`、`adjustment3_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment3_approval_date=2026-09-23`、`adjustment3_approved_reviewed_commit=1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244`、`adjustment3_implementation_status=NOT_STARTED`、本轮验收 `NOT_RUN`（`CCFG-AC-105~117`，13 条新增，全文件 117 条全部 `NOT_RUN`）。**批准基线 ≠ 已实现 ≠ 已目测 ≠ 已验收**：**批准的是第三轮调整基线，尚未实现第三轮调整**。
- **与已批准第二轮的关系**：第二轮的 `adjustment2_baseline_status=APPROVED` 与实现 `adjustment2_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE`（第二轮实现已完成并经 ChatGPT 远程代码复审通过、当前等待项目负责人页面目测/接受）**保持真实不改写**；其实现完成时点的历史值 `IMPLEMENTED_PENDING_CHATGPT_REVIEW` 保留于 §1.6 历史记录。本轮为独立的第三轮基线，具有独立的批准信息，**不**改写第二轮历史、**不**自动继承第二轮批准。
- **R1 定向纠错（2026-09-23，`CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001-R1`）**：ChatGPT 从远程 Git 对 R0 草案提交 `a585071d86eeee47dbd8d986fdb6f90b32161033` 的复审结论为 `CHANGES_REQUIRED`，R1 仅定向修订三处——① `ACCEPTANCE.md` 的 `CCFG-AC-113` 验收步骤（新增模式探针 ID 可直接填写、编辑模式默认锁定与“修改探针 ID”解锁/“取消修改”恢复锁定；不修改页面行为）；② R0 报告 §4.2 需求侧计数口径勘误（正确为 10 条新增定义行 `113~122` 加 1 条修改的既有定义行 `016`，见 R1 报告）；③ 两份文档中作为当前值的第二轮实现状态/入口同步。**本轮四项产品决定不重新设计**，除 `CCFG-AC-113` 外全部定义行相对 `a585071` **逐字节零变化**。
- **定向修订（保留原文）**：本轮对既有规则中与本轮决定冲突的条目（`REQUIREMENTS.md` 的 `CCFG-REQ-016`、`ACCEPTANCE.md` 的 `CCFG-AC-011`、`UI.md` 的 `CCFG-UI-009` 与 `CCFG-UI-024`）采取**目标标注 + 修订**：保留原决定与变更轨迹、不改写为“从未存在”，并指明由 `CCFG-REQ-115`/`CCFG-AC-109`/`CCFG-UI-044` 承接；这些标注**已随本轮基线于 2026-09-23 获批而生效**，第三轮实现以本轮已批准规则为准。
- **参考来源效力边界**：参考页 `/config/data-source` **仅作视觉对照**，本轮规则**只**作用于 `/config/client`；**不**修改数据源管理的代码、文档、状态、行为。参考页的 `#hex`/像素数值为**参考页实现事实**。
- **不涉及范围**：本轮**不改** `API.md`/`DATABASE.md`、**不改**任何业务代码/测试/前端或后端源文件、**不改** `docs/baseline/**`、`docs/features/README.md`、历史报告与 `docs/prompts/**`，**不改**模板级全局状态（`NOT_STARTED`/`NOT_GRANTED`/`NOT_DECIDED`）；`PENDING_USER_CONFIRMATION=0`。
- **时序说明（2026-09-23 追加，不改写以上历史表述）**：本节所记 `adjustment3_implementation_status=NOT_STARTED` 与 `next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_APPROVAL_CLOSEOUT_REVIEW` 均为**批准收口时点**的真实状态与入口；其三份实现事实（`existing_feature_implementation_status`/`adjustment_implementation_status`/`adjustment2_implementation_status` 均为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`）当时有效、现在仍有效。该批准收口入口**其后已完成**（ChatGPT 从远程 Git 复审第三轮 R1 提交 `1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244` 的结论 `APPROVED`，项目负责人 2026-09-23 批准），第三轮代码实现已另行由独立实现任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-IMPLEMENTATION-001` 承接，**当前** `adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`（见 §1.8）。此处仅追加时序说明，**不**改写上述批准收口阶段的定义、**不**宣布第三轮已验收或页面已获最终接受、**不**改变 117 条验收 `NOT_RUN` 的执行状态。

### 1.8 第三轮 `+N` 清单与新增／编辑弹窗视觉调整实现（2026-09-23，前端实现）

独立实现任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-IMPLEMENTATION-001` 已按已批准基线实现 `/config/client` 的 `+N` 完整清单弹层与新增／编辑探针弹窗视觉调整（起始提交 `cf0817f23c77190275b7f58c755c4e2c4619adc9`，对应报告见 §2 导航）。本任务完成时点的分层状态如下：

```text
existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment_baseline_status=APPROVED
adjustment_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment2_baseline_status=APPROVED
adjustment2_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment3_baseline_status=APPROVED
adjustment3_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW
adjustment3_formal_acceptance_execution_status=NOT_RUN
formal_acceptance_execution_status=NOT_RUN
PENDING_USER_CONFIRMATION=0
next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_IMPLEMENTATION_REVIEW
```

| 已批准条款 | 实现落点 | 结果 |
|---|---|---|
| `CCFG-REQ-113`/`CCFG-AC-105~106`/`CCFG-DESIGN-054`/`CCFG-UI-043` | `+N` 清单每项改为两级信息：主行 `.cc-full-org` 放完整 `DATA_SOURCE_ORG`，次行 `.cc-full-id` 以可见前缀 `数据源 ID：` 引导完整原始 `DATA_SOURCE_ID`（两条均 `display: block`）；遍历仍取 `row.dataSources`，保持 E1 返回的**原存储顺序**、全部条目与异常项（含原隐藏集合内的异常项）均展示 | 已实现 |
| `CCFG-REQ-114`/`CCFG-AC-107~108`/`CCFG-DESIGN-054`、`CCFG-DESIGN-055`/`CCFG-UI-043`、`CCFG-UI-044` | 机构名为空（`org` 为 `null`/空白）时主行只放原始 ID，`v-if="hasOrg(ds)"` 使次行不渲染、不重复；项级 `anomalies` 与冲突探针信息由 `.cc-full-bad` 原样保留（沿用既有红色语义），含逗号歧义行的 `.cc-full-note` 既有文案与事实边界不变 | 已实现 |
| `CCFG-REQ-115`/`CCFG-AC-109~110`/`CCFG-DESIGN-055`/`CCFG-UI-044` | **移除** `.cc-full-list` 的 `max-height: 320px` 与 `overflow-y: auto`（未以其他选择器或行内样式重新加回内部滚动或固定最大高度）；清单按内容自然增高，条目以 `.cc-full-item + .cc-full-item { border-top: 1px solid #f0f0f0 }` 与 `padding: 6px 0` 区分，长 ID 允许 `overflow-wrap: anywhere` 换行；`+N` 计数、点击触发方式、外层单标签 Tooltip 与主表行高不变 | 已实现 |
| `CCFG-REQ-116`/`CCFG-AC-111`/`CCFG-DESIGN-056`/`CCFG-UI-045` | 新增／编辑共用弹窗 `width` 由 `680px` 改为 `900px`，并以 `:deep(.cc-dialog) { max-width: calc(100vw - 48px) }` 在窄视口收缩、保留左右安全边距 | 已实现 |
| `CCFG-REQ-117`/`CCFG-AC-112`/`CCFG-DESIGN-057`/`CCFG-UI-045` | `.cc-opt-list` 与 `.cc-pane--chosen .cc-chosen-list` 受控高度由 `200px` 提高到 `260px` 以露出更多候选；“可选数据源”经 `.cc-pane--options { flex: 1.15 }` 略宽于 `.cc-pane--chosen { flex: 1 }`；候选机构名、原始 ID、不可选原因与搜索／置灰／选中／移除语义保持；弹窗内部（表单与候选区）受控滚动保留：`.cc-form { max-height: calc(100vh - 240px); overflow-y: auto }`，“无内部滚动”仅约束 `+N` 清单、未误套用到弹窗候选列表 | 已实现 |
| `CCFG-REQ-118`/`CCFG-AC-113`/`CCFG-DESIGN-060`/`CCFG-UI-013~017` | 弹窗既有功能未因本轮尺寸与样式调整改变：探针 ID 锁定／经“修改探针 ID”解锁修改、探针描述自动生成、候选搜索、已选项展示与移除、异常显示、保存前校验阻断、未保存关闭确认均保持现有行为与语义 | 保留 |
| `CCFG-REQ-119`/`CCFG-AC-114`/`CCFG-DESIGN-058`/`CCFG-UI-046` | 弹窗标签“探针 ID”“探针描述”“采集数据源”对齐参考页 `.editor-dialog .el-form-item__label`：`.cc-form-label` 为 `14px / 500 / #3f3f46`，沿用页面默认无衬线字体族（未施加等宽或加粗），必填红星 `.cc-form-label::before` 保持 | 已实现 |
| `CCFG-REQ-120`/`CCFG-AC-115`/`CCFG-DESIGN-059`/`CCFG-UI-047` | 底部主提交按钮新增页内作用域类 `cc-dialog-submit`，以 `.cc-dialog-submit:not(.is-disabled)` 声明参考页 `.editor-submit-button` 黑色实心样式（背景／边框 `#09090b`、白字、6px 圆角、字重 500，hover／focus `#27272a`、active `#18181b`）；非禁用选择器使禁用与 loading 态仍由 Element Plus 呈现、明显不可点；仅该主按钮变色，取消／自动生成／“修改探针 ID”未改 | 已实现 |
| `CCFG-REQ-121`/`CCFG-AC-116`/`CCFG-DESIGN-060`/`CCFG-UI-048` | 主提交按钮文案保持 `{{ mode === 'edit' ? '保存' : '创建' }}`（新增模式为“创建”、编辑模式为“保存”），本轮未改动任何按钮文案 | 保留 |
| `CCFG-REQ-122`/`CCFG-AC-117`/`CCFG-DESIGN-060`/`CCFG-UI-049` | 操作菜单“删除”**不加粗**：`.cc-more-popper .el-dropdown-menu__item { font-weight: 400 }` 使“删除”与“启用／停用”字重一致，危险语义仍由红色文字、`divided` 上方分隔线与 hover／focus 反馈承载；三点触发器、事件边界、二次确认与 E5~E7 接口语义不变 | 已实现 |
| 第一轮、第二轮已批准条款与既有回归 | 六列顺序、序号、固定操作列、`FG_ACTIVE` 三态 ID 标识、采集数据源标签三态与异常优先、结果区摘要与黑色“新增探针”、查询／重置、双击与键盘编辑、单实例 Tooltip、刷新能力缺席、CRUD 合同、行级忙碌与二次确认均保持不变 | 保留 |

- **实现范围**：仅在 `frontend/src/views/client-config/ClientConfigPage.vue` 与同目录 `ClientConfigPage.spec.ts` 两文件实施（无新增定向测试文件），并同步本 Feature 当前状态文档与新增实现报告。数据源管理参考页、两套公共模板实现与模板级全局状态、路由／菜单、前端 API 类型与接口、后端代码、数据库对象与构建依赖**均未修改**。
- **状态边界**：`adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW` 只表示**代码实现已完成、测试与构建已通过并停在 ChatGPT 远程复审入口**；**不**表示已通过项目负责人目测、已执行正式验收或已获最终接受。全部 117 条验收（含本轮 `CCFG-AC-105~117` 13 条）仍**全部 `NOT_RUN`**，覆盖保持 122/122，`PENDING_USER_CONFIRMATION=0` 保持。
- **浏览器证据（与第一轮实现报告时的条件不同）**：本机存在可用的 `google-chrome`，且存在已安装的 `playwright-core`（未新增或替换任何浏览器／服务依赖）。以真实无头浏览器加载 `/config/client`、用 route 拦截**伪造的 E1/E2 响应**在 1440×900 与 1920×1080 两个视口核对了 `+N` 清单的普通位置、视口底边附近位置与两类弹窗，并在 1100×800、900×700 收窄视口核对按钮可达性，结论 `BROWSER_VERIFIED_HEADLESS`。核对用的开发服务为临时进程，核对后已停止，未改动仓库文件。**数据经接口桩注入而非取自真实后端**，且该结论为代码 Agent 自测证据，**不构成项目负责人目测或正式验收**；`+N` 清单在 1440×900 首行锚点下超过约 500px 时的裁切边界见报告 §5 风险项。
- **页面级授权 ≠ 模板级全局授权（本次实现后口径不变）**：本次实现**不新增、不扩大**任何授权；两套公共模板的模板级全局迁移状态（`page_migration_status=NOT_STARTED`、`page_migration_authorization_status=NOT_GRANTED`、`pilot_page_selection_status=NOT_DECIDED`）**保持不变**，数据源管理参考页最终接受状态**不变**，**其他页面未被授权**。
- **时序说明（2026-09-23 追加，不改写以上历史表述）**：上条所记“`+N` 清单在 1440×900 首行锚点下超过约 500px 时的裁切边界”为**初次实现时点**的真实状态，照实保留。该裁切已由定向修复任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-IMPLEMENTATION-001-R1` 于 2026-09-23 消除：以 `preventOverflow { boundary: 'viewport', rootBoundary: 'viewport', mainAxis: true, altAxis: true, tether: false, padding: 8 }` 交给 `+N` 弹层（`placement="top"` 仍为首选方向），真实无头浏览器复验 1440×900 首行 9 项由 6/9 提升到 **9/9 完整可见**、上溢 0，长 ID/异常场景由 5/9 提升到 9/9，底边与常见 5～6 项场景维持全部完整可见；`.cc-full-list` 仍 `max-height: none`、`overflow-y: visible`、无内部滚动，未引入固定最大高度、内部滚动或隐藏条目。已批准基线与 122/117/60/49 定义行**逐字节不变**，`adjustment3_implementation_status` 保持 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`，117 条验收仍全部 `NOT_RUN`；R1 报告见 §2 导航。**此处“尚未提交、尚未推送”为 R1 修复当时（2026-09-23）的历史状态**，该时点入口为工作区差异审阅 `WORKTREE_DIFF_REVIEW_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_IMPLEMENTATION_R1`；该工作区差异审阅**其后确已发生**并附完整证据包，本次第三轮实现（含上述 R1 裁切修复）**其后已以普通提交 `6cf7a554b457ed6c8edb827ef1971054df3354fb` 提交并推送**到 `origin/develop`（起始提交 `cf0817f23c77190275b7f58c755c4e2c4619adc9`，恰一提交、九文件）。**当前状态（2026-09-23 起）**：该提交的远程复审中**代码实现与 R1 裁切修复通过**，**但整次提交的文档复审为 `CHANGES_REQUIRED`**，原因仅为本节映射表的引用错位与 R1 时序表述；现处于**远程复审后的文档纠错阶段**，由任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-REMOTE-REVIEW-DOC-R1` 仅定向修订本 README，未提交的工作区差异待 ChatGPT 复审（见 §5 当前下一入口）。以上均为历史与当前状态的**时序说明**，**不**宣布验收通过、**不**宣称页面已获最终接受：全部 117 条验收仍 `NOT_RUN`，正式验收未执行，项目负责人尚未目测接受。

### 1.9 第四轮新增／编辑弹窗校验与交互调整基线（2026-09-24，草案建立 → R1 纠错 → 2026-09-24 批准收口为 `APPROVED`）

纯文档草案任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001` 按项目负责人**已明确确认的调整规则**，为 `/config/client` 的**新增／编辑探针弹窗**（两种模式共用）建立第四轮页面级调整草案：① 探针 ID／探针描述／采集数据源三类业务校验错误改为**字段级反馈**（对应控件红色边框 + 错误文字在该控件正下方红色显示），点击“创建／保存”一次校验全部字段、显示各自错误并定位第一个错误，修正字段即清除或更新该字段错误，**不再**用页面顶端临时消息代替字段内反馈、也不对同一错误重复弹顶端消息；主提交按钮**始终呈现与数据源管理主提交按钮一致的黑色可点击样式**（表单不完整仍可点击、点击后显示字段级错误），请求处理中防重复提交且加载态不得在蓝色与黑色之间跳色，**不**因未选数据源而预先禁用；可明确归属某字段的业务接口校验／冲突错误在该字段下方反馈，网络不可用或无法归属的系统错误可保留全局提示，既有前后端校验、防重提交与成功行为不放宽。② “采集数据源”未选且尚未尝试提交时显示**中性灰色**说明“至少选择 1 个数据源”，选中即隐藏、全部取消即恢复（不得用红／绿表示未完成的正常状态）；点击“创建／保存”仍未选时同一字段反馈区转为**红色错误**，选中后消除，再次清空时按是否已尝试提交正确恢复错误或中性说明；字段反馈区**预留稳定空间**，选中／取消选择及普通错误状态切换不得造成弹窗底边／底部按钮明显跳动，少量需换行的真实错误文案保留可读性、不硬裁剪，视口较小时仍须遵守现有弹窗可操作、页面不异常横溢的规则。③ 弹窗相对**浏览器可见视口水平居中**，侧栏正常展开或收起、不同宽度视口下均不得产生约半个侧栏宽度的左偏，保留约 900px 桌面目标宽度与窄视口安全边距，**不**通过写死与某一侧栏宽度相关的偏移量修补；“探针 ID”“探针描述”“采集数据源”等配置项名称参考“新增数据源”弹窗统一**右对齐**、右边缘整齐（左边缘无须对齐），必填红星在名称前，现有字体规格保留、输入控件区域对齐。④ **探针 ID 输入／粘贴最多接受 32 位**（已有首位与允许字符、判空、大小写不敏感唯一性及后端校验不变，超长不再无限录入，非法字符以字段级错误明确指出格式问题、不得悄悄改成别的 ID；新增直接可填、编辑默认锁定并可按既有“修改探针 ID／取消修改”操作解锁）；**探针描述最多 256 个字符**（**256 个汉字也是 256 个字符**，按 Unicode 完整字符计数，截断不得拆开代理对或破坏字符，手工输入及粘贴都执行同一限制，输入提示改为“最多 256 个字符”，保留最终描述原文保存、Trim 仅判空与后端原有 `VARCHAR2(1024 BYTE)`／UTF-8 原文字节 `<=1024` 校验，数据库字段、API 与服务端上限**不**随界面限制改为 256 字节）；“自动生成”先按原有数据源选择顺序用每个 `DATA_SOURCE_ORG` 生成原规则的完整描述，再**直接保留前 256 个字符**写入描述输入框（超过时不失败、不弹超长提示，不改变原有未选数据源时“完全无动作”及无有效机构名时的其他既有规则），对旧版“生成完整结果超 1024 字节则失败”条款作**目标标注 + 定向修订**并以最终准备写入的 256 字符文本继续执行实际 UTF-8 字节检查，末尾机构名或分隔符可能因直接截断而不完整属项目负责人明确接受的截断行为；编辑历史记录时若原 `CLIENT_DESC` 超过 256 字符，**打开弹窗仅将前 256 个字符回显为本次编辑草稿**，不写数据库、不偷偷修改列表原记录，保存则按该截断后草稿走现有表单校验与保存流程，关闭不保存不改持久化数据。

本草案**仅**调整 `/config/client` 页面级基线，**不**改 `/config/data-source`（仅作字段错误与标签布局的视觉参考且未被修改）、其他功能页面、全局组件、后端、数据库，**不**改两套既有查询／列表视觉模板的全局状态，**不**改 `API.md`/`DATABASE.md`。项目负责人认可“新增／编辑表单弹窗模板”方向，**须在本轮页面实现并经项目负责人实际目测认可后**另开任务提炼可复用共性规则（字段错误显示、标签对齐、提示稳定性、按钮与弹窗定位）；探针专有的 ID 32 位、描述 256 字符与数据源分配规则**不**进入全局模板；本轮只在文档登记该后续入口，**不**提前创建／批准模板，**不**声称其他页面已接入。**草案建立时点**的分层状态如下（该时点状态照实保留；其后已由批准收口任务更新为获批状态，见下方批准记录）：

```text
existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment_baseline_status=APPROVED
adjustment_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment2_baseline_status=APPROVED
adjustment2_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment3_baseline_status=APPROVED
adjustment3_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW
adjustment4_baseline_status=APPROVED
adjustment4_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment4_approval_date=2026-09-24
adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750
adjustment4_implementation_status=NOT_STARTED
adjustment4_formal_acceptance_execution_status=NOT_RUN
formal_acceptance_execution_status=NOT_RUN
PENDING_USER_CONFIRMATION=0
next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW
```

> **上列状态块为第四轮基线批准收口时点的值（历史时点，照实保留）**：`adjustment4_implementation_status=NOT_STARTED` 与 `next_entry=..._BASELINE_APPROVAL_CLOSEOUT_REVIEW` 均为**收口时点值**。第四轮实现已于 2026-09-24 完成，**当前值**为 `adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`、`next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_IMPLEMENTATION_REVIEW`（见 §1.10）。

- **本轮定义行（草案建立，逐项映射见各自文档）**：`CCFG-REQ-123~136`（14 条，`REQUIREMENTS.md` §7.13）、`CCFG-AC-118~135`（18 条，`ACCEPTANCE.md` §1.8）、`CCFG-DESIGN-061~071`（11 条，`DESIGN.md` §16）、`CCFG-UI-050~059`（10 条，`UI.md` §18）；编号总数更新为 需求 **136** / 验收 **135** / 设计 **71** / UI **59**，覆盖 **136/136** 与 **135/135**。
- **本轮定向修订（原文保留不改写，标注 `**【本轮（第四轮）定向修订 · 待批准】**`）**：`CCFG-REQ-037`/`CCFG-REQ-039`/`CCFG-REQ-060`（3 条需求）、`CCFG-DESIGN-059`（1 条设计）、`CCFG-UI-013`/`CCFG-UI-015`/`CCFG-UI-017`（3 条界面）；其中既有 `CCFG-UI-045`/`CCFG-UI-046`/`CCFG-UI-024` 的口径继续有效，未因本轮被取代；既有**验收**定义行零修订。
- **R1 定向纠错（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001-R1`，纯文档）**：ChatGPT 从远程 Git 对草案提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 复审结论 `CHANGES_REQUIRED`，仅修正三处文档一致性。① **R1-01 按钮旧规则冲突**——`CCFG-REQ-120`/`CCFG-AC-115`/`CCFG-UI-047`（并核 `CCFG-DESIGN-059` 与 `CCFG-DESIGN-062` 状态矩阵）最小定向修订：区分“**表单未完成／校验未通过 → 仍可点击且呈黑色**”与“**提交请求处理中 → 暂不可重复提交、加载反馈清楚可辨、底色不蓝黑跳色**”两种状态，`CCFG-AC-115` 的“校验未通过预先禁用”场景标注为**由 `CCFG-AC-120` 取代**，并给出**现行可执行验收步骤**；`CCFG-REQ-124`/`CCFG-AC-120`/`CCFG-DESIGN-062`/`CCFG-UI-051` 同步把“始终”限定为**非提交处理中**的常态，两条验收期望**同时成立、无冲突**；② **R1-02 `CCFG-AC-131` 不可能输入**——256 个字符的 UTF-8 字节数**最多恰好 1024、不能超过 1024**，原“接近或超过 1024”分支不可构造，仅改写该条为三类互不重叠边界（256 汉字＝768 字节允许保存 / 恰好 256 个四字节码点＝恰好 1024 字节上限可接受 / 前端拦截第 257 字符且**绕过前端**直连后端提交 >1024 字节仍被拒绝），`VARCHAR2(1024 BYTE)` 契约与 Trim 仅判空、原文保存不变；③ **R1-03 追踪映射不实**——`CCFG-REQ-136`/`CCFG-AC-135` 仅登记未来“新增／编辑表单弹窗模板”入口，属**文档治理／范围声明**、本轮**无新增 UI 元素**，从 `CCFG-UI-059` 与 `DESIGN.md` §12.1/§12.2 的对应单元格**移除**无依据映射，改由 `CCFG-DESIGN-071` 承载。第四轮分层状态**不变**（`DRAFT_PENDING_USER_REVIEW`／`NOT_APPROVED`／`NOT_STARTED`／`NOT_RUN`）；`CCFG-AC-001~135`（135 条）连续唯一、**全部 `NOT_RUN`**；除明列定向行外其余定义行**逐字节零变化**；**不**回写 R0 报告。
- **批准记录（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001`，纯文档批准收口）**：时序可追溯——① R0 草案提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 的 ChatGPT 远程复审为 `CHANGES_REQUIRED`；② R1 定向纠错提交 `9daf03848d53008d3ac73e6a43c1368fa5d72750` 的 ChatGPT 远程**基线文档复审**为 `APPROVED`；③ 项目负责人随后于 **2026-09-24** 明确回复“**批准**”。本收口据此把第四轮基线统一记为 `adjustment4_baseline_status=APPROVED`、`adjustment4_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment4_approval_date=2026-09-24`、`adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750`；`adjustment4_implementation_status=NOT_STARTED`、`adjustment4_formal_acceptance_execution_status=NOT_RUN` 保持。曾处于 `DRAFT_PENDING_USER_REVIEW`、`NOT_APPROVED` 及旧 `next_entry` 的表述作为**历史时点**照实保留、不改写。**批准的是第四轮页面级调整基线，不是代码、测试、目测结果或最终接受**：本轮实现与正式验收均未开始，`CCFG-AC-118~135`（18 条）连同全文件 135 条验收仍**全部 `NOT_RUN`**。批准范围为第四轮已复审基线的：字段级校验错误呈现与主提交按钮黑色可点击态及提交中防重复、灰色／红色数据源提示与稳定布局、弹窗视口居中与配置项名称右对齐、探针 ID 32 位、描述 256 个 Unicode 码点（256 汉字可输入、按完整字符截断）、自动生成与历史编辑草稿截断、原有后端 UTF-8 `<=1024 BYTE` 防线及其验收定义，以及 R1 的按钮旧规则与新规则定向关系、`CCFG-AC-131` 可执行边界、后续模板入口追踪关系；任何人不得把 R0 的错误映射或不可能输入带回。对“新增／编辑表单弹窗通用模板”仅保留后续入口：**须在本轮页面实现完成并经项目负责人实际目测认可后**另开任务提炼，本收口**不**创建、**不**批准、**不**推广任何模板；参考页 `/config/data-source` **仅作视觉对照且未被修改**。
- **状态边界**：`adjustment4_baseline_status=APPROVED`（草案时点曾为 `DRAFT_PENDING_USER_REVIEW`，属历史时点）仅表示**第四轮调整基线已获项目负责人批准**；**不**表示本轮已实现、已目测或已验收；`adjustment4_implementation_status=NOT_STARTED` 表示**本轮尚未实现**；`adjustment4_formal_acceptance_execution_status=NOT_RUN` 与全文件 135 条 `formal_acceptance_execution_status=NOT_RUN` 表示**没有任何用例被标记执行或通过**。本轮**不修改代码**、**不**执行正式验收；前几轮实现事实保持各自时点口径，**不**因本轮改写。
- **分层状态保持真实**：`existing_feature_implementation_status`/`adjustment_implementation_status`/`adjustment2_implementation_status` 均为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`，`adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`（均为各轮实现完成时点的真实事实）。
- **改动范围**：仅 `docs/features/client-config/{README,REQUIREMENTS,ACCEPTANCE,DESIGN,UI}.md` 与新增报告 `reports/CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001.md`（R1 纠错另有 `reports/CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001-R1.md`，批准收口另有 `reports/CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001.md`）；`API.md`/`DATABASE.md`、`docs/baseline/**`、`docs/features/README.md`、历史报告、`docs/prompts/**`、前后端代码与服务、两套模板的模板级全局状态**均未修改**；`PENDING_USER_CONFIRMATION=0`。
- **下一入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW`（原草案入口 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_REVIEW` 已完成并返回 `CHANGES_REQUIRED`、R1 入口 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_R1_REVIEW` 已完成并返回 `APPROVED`，均降为历史入口；由 ChatGPT **从远程 Git** 对本批准收口结果提交做独立复审，详见 §5）。该远程复审完成后方可另行组织第四轮页面代码实现；本收口**不**宣布第四轮已实现或已验收。

### 1.10 第四轮新增／编辑弹窗校验与交互调整实现（2026-09-24，前端实现）

独立前端实现任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001` 已按第四轮已批准基线（`adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750`）实现 `/config/client` **新增／编辑探针弹窗**（两模式共用）的字段级校验、数据源提示与弹窗稳定性、弹窗视口居中与配置项名称右对齐、探针 ID 32 位与探针描述 256 字符输入／自动生成截断／历史记录草稿回显调整（起始提交 `b35ea6974adad3f61b6599de0c8b48f124125ae6`，对应报告见 §2 导航）。本任务完成时点的分层状态如下：

```text
existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment_baseline_status=APPROVED
adjustment_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment2_baseline_status=APPROVED
adjustment2_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE
adjustment3_baseline_status=APPROVED
adjustment3_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW
adjustment4_baseline_status=APPROVED
adjustment4_approval_status=APPROVED_BY_PROJECT_OWNER
adjustment4_approval_date=2026-09-24
adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750
adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW
adjustment4_formal_acceptance_execution_status=NOT_RUN
formal_acceptance_execution_status=NOT_RUN
PENDING_USER_CONFIRMATION=0
next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_IMPLEMENTATION_REVIEW
```

> **上列状态块为第四轮实现任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001`（提交 `9da9620`）完成时点的值（历史时点，照实保留）**：`next_entry=..._IMPLEMENTATION_REVIEW` 为**该时点值**。该实现随后经 ChatGPT 远程代码复审返回 `CHANGES_REQUIRED`（仅主提交按钮加载态蓝黑跳色一项目标，见 §5 历史入口），已由 R1 定向纠偏任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001-R1` 修复；**当前值**为 `adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`（不变，仍停在远程复审入口）、`next_entry=CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_IMPLEMENTATION_R1_REVIEW`（见下条 R1 纠偏说明）。

| 已批准条款 | 实现落点 | 结果 |
|---|---|---|
| `CCFG-REQ-123`/`CCFG-AC-118`/`CCFG-DESIGN-061`/`CCFG-UI-050` | 探针 ID／探针描述／采集数据源三类业务校验错误改为**字段级反馈**：`idFieldError`／`descFieldError`／`sourceFieldError` 三个状态 + `idControlEl`／`descControlEl`／`sourceControlEl` 三个模板 ref；错误时对应控件容器加 `.cc-field--error`（控件红框）、其下 `.cc-field-feedback` 内以 `.cc-field-error`（红字、`role="alert"`）就地显示；点击“创建／保存”由 `submitDialog()` **一次校验全部字段**并逐项呈现，`focusFirstError()` 在 `nextTick` 后定位第一个错误控件；修正某字段由对应 `watch` 只清除／更新该字段错误 | 已实现 |
| `CCFG-REQ-123`/`CCFG-AC-119`/`CCFG-DESIGN-061`/`CCFG-UI-050` | 字段级反馈**取代**页面顶端临时消息，且**不**对同一错误重复弹顶端消息：业务校验失败路径不再调用 `ElMessage`；仅成功提示（`ElMessage.success`）、无法归属字段的系统错误与网络异常保留顶层提示 | 已实现 |
| `CCFG-REQ-124`/`CCFG-AC-120`/`CCFG-DESIGN-062`/`CCFG-UI-051` | 主提交按钮 `.cc-dialog-submit` **常态不预禁用**（`:disabled` 仅绑 `submitting`），未选数据源时仍为黑色可点击；点击后原位显示字段级错误并拒绝写库（不触发创建／更新接口）；`.cc-dialog-submit:not(.is-disabled)` 声明 `#09090b`／白字／6px／500 与既有 hover／focus／active；禁用与加载态仍由 Element Plus 呈现、不被正常态规则覆盖 | 已实现 |
| `CCFG-REQ-124`/`CCFG-AC-120`/`CCFG-DESIGN-062`/`CCFG-UI-051` | 提交请求处理中**防重复**：`submitDialog()` 首行守卫（`mode` 为空或 `submitting` 为真时直接返回），`submitting` 期间按钮 `:disabled` + `:loading`；真实无头浏览器双击场景下写入请求数等于场景数（未出现重复提交） | 已实现 |
| `CCFG-REQ-125`/`CCFG-AC-121`/`CCFG-DESIGN-063`/`CCFG-UI-052` | 服务端错误按**真实错误码**归属字段：`applyServerFieldError()` 以 `ID_ERROR_CODES{40100,40101,40940}`→ID、`DESC_ERROR_CODES{40102}`→描述、`SOURCE_ERROR_CODES{40103,40104,40105,40941}`→数据源；归属成功即落该字段下方；无法归属（如 `40942` 异常选择、`50000` 等）仍走全局 `ElMessage.error`，未改动任何错误码或接口契约 | 已实现 |
| `CCFG-REQ-126`/`CCFG-AC-122`/`CCFG-DESIGN-064`/`CCFG-UI-053` | `sourceFeedback` 计算属性驱动数据源提示：未选且**尚未尝试提交** → 中性灰色 `至少选择 1 个数据源`；选中任一即隐藏；全部取消后按 `submitAttempted` 正确恢复灰色（不用红／绿表示未完成的正常状态） | 已实现 |
| `CCFG-REQ-127`/`CCFG-AC-123`/`CCFG-DESIGN-065`/`CCFG-UI-054` | 点击提交仍未选 → **同一**反馈区转**红色错误**且 `.cc-split--error .cc-pane` 显红框；选中后消除；`resetDialog()` 在关闭／重开时复位 `submitAttempted` 与三处字段错误，重开为新会话（未尝试提交 → 中性灰） | 已实现 |
| `CCFG-REQ-128`/`CCFG-AC-124/125`/`CCFG-DESIGN-066`/`CCFG-UI-055` | 字段反馈区预留**稳定空间**：`.cc-field-feedback { min-height: 20px }` 且 `overflow: visible`，长错误文案 `overflow-wrap: anywhere` 完整换行不硬裁剪；真实浏览器核对 idle／多项错误／修正后／提交中四个状态弹窗底边与 footer 底边每个视口内**完全一致**（footer 底边 1440×900 为 606.16、1920×1080 为 633.16、1024×720 为 579.16），未出现明显跳动 | 已实现 |
| `CCFG-REQ-129`/`CCFG-AC-126`/`CCFG-DESIGN-067`/`CCFG-UI-056` | 弹窗水平居中交由 Element Plus 既有 `.el-overlay { position: fixed }` 与 `.el-dialog { margin: auto }` 完成，**未**写死任何与侧栏宽度相关的偏移量（本页源文件不出现 `220px`／`64px` 等偏移常量）；保留 `width="900px"` 桌面宽度与 `:deep(.cc-dialog) { max-width: calc(100vw - 48px) }` 窄视口安全边距。真实无头浏览器在 1440×900、1920×1080、1024×720 且侧栏展开／收起共六个组合下核对，弹窗中心与可见视口中心偏差均为 **0** | 已实现 |
| `CCFG-REQ-130`/`CCFG-AC-127`/`CCFG-DESIGN-068`/`CCFG-UI-057` | “探针 ID”“探针描述”“采集数据源”等配置项名称**右对齐**：`.cc-form-label { text-align: right }`，`14px / 500 / #3f3f46` 与必填红星 `.cc-form-label::before` 保持；真实浏览器下三个标签右边缘一致（1440×900 均为 370），左边缘不强制对齐 | 已实现 |
| `CCFG-REQ-131`/`CCFG-AC-128/129`/`CCFG-DESIGN-069`/`CCFG-UI-058`、`CCFG-REQ-037` | 探针 ID 输入／粘贴**最多 32 位**：`clientIdModel` 可写计算属性的 setter 以 `slice(0, 32)` 截断，原生 `maxlength="32"` 同步生效；`ID_PATTERN = /^[A-Za-z0-9][A-Za-z0-9._-]{0,31}$/` 继续保持首位、字符集与大小写不敏感唯一性口径；非法字符**不**被静默改写，`9bad!` 提交后在 ID 字段下方以字段级错误指出格式问题；编辑模式默认锁定与“修改探针 ID”解锁／“取消修改”恢复锁定的既有语义保持，解锁后改空 ID 报错、取消修改恢复原 ID 并清除错误 | 已实现 |
| `CCFG-REQ-132`/`CCFG-AC-130/131`/`CCFG-DESIGN-070`/`CCFG-UI-013/059` | 探针描述**最多 256 个 Unicode 完整字符**：`DESC_MAX_CHARS = 256`，`truncateCodePoints(value, 256)` 以 `Array.from` 按码点截断、不拆散代理对；`clientDescModel` setter 同口径截断（手工输入与粘贴一致）；输入提示改为 `探针用途描述（最多 256 个字符）`；Trim 仅用于判空、提交原文，前端 UTF-8 字节校验与后端 `VARCHAR2(1024 BYTE)`、API 上限**未**改动；`'甲'.repeat(300)` 与 `'😀'.repeat(300)` 均截断为 256 个完整字符且不含孤立代理项，256 个汉字可正常保存 | 已实现 |
| `CCFG-REQ-133`/`CCFG-AC-132`/`CCFG-DESIGN-070`/`CCFG-UI-015/059` | “自动生成”按既有规则**先**依 `DATA_SOURCE_ORG` 原选择顺序拼出完整描述，**再**直接保留前 256 个完整字符写入描述输入框；未选数据源仍“完全无动作”、无有效机构名的既有失败处理保持；超长**不**报错、**不**弹超长提示，旧“生成完整结果超 1024 字节则失败”口径不再阻断 | 已实现 |
| `CCFG-REQ-134`/`CCFG-AC-133`/`CCFG-DESIGN-071`/`CCFG-UI-059` | 编辑历史记录 `CLIENT_DESC` 超 256 字符时，`openEdit()` 仅以 `truncateCodePoints(row.clientDesc ?? '', 256)` 把**前 256 个完整字符回显为本次编辑草稿**，不写数据库、不改列表原记录；未保存关闭不改持久化数据；保存按截断后草稿走既有更新接口（`'甲'.repeat(256)`） | 已实现 |
| `CCFG-REQ-135`/`CCFG-AC-134`/`CCFG-DESIGN-071` | 边界与范围：仅 `/config/client` 新增／编辑弹窗；编辑历史**逗号歧义**与**异常数据源**既有的阻断／提示语义保留（阻断由“预先禁用主按钮”改为“点击后就地报错并拒绝写库”，未静默放行）。`+N` 清单、主列表、两套公共模板、其他页面均未改动 | 已实现 |
| `CCFG-REQ-136`/`CCFG-AC-135`/`CCFG-DESIGN-071` | 仅文档登记未来“新增／编辑表单弹窗通用模板”入口，本轮**未**创建、**未**批准、**未**推广任何模板，本轮亦无新增 UI 元素 | 登记保留 |
| 第一轮至第三轮已批准条款与既有回归 | 列顺序与序号、固定操作列、`FG_ACTIVE` 三态标识、采集数据源标签三态与异常优先、查询／重置、双击与键盘编辑、单实例 Tooltip、刷新能力缺席、CRUD 合同、行级忙碌与二次确认、“新增探针”黑色实心按钮、`+N` 清单两级信息与自然增高、弹窗 900px 与数据源区调整、操作菜单“删除”不加粗均保持不变 | 保留 |

- **实现范围**：仅在 `frontend/src/views/client-config/ClientConfigPage.vue` 与同目录 `ClientConfigPage.spec.ts` 两文件实施（无新增辅助文件、无新增定向测试文件），并同步本 Feature 当前状态文档与新增实现报告。数据源管理参考页、两套公共模板实现与模板级全局状态、路由／菜单、前端 API 类型与接口、后端代码、数据库对象与构建依赖**均未修改**。
- **状态边界**：`adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW` 只表示**代码实现已完成、测试与构建已通过并停在 ChatGPT 远程复审入口**；**不**表示已通过项目负责人目测、已执行正式验收或已获最终接受。全文件 135 条验收仍**全部 `NOT_RUN`**，覆盖保持 136/136 与 135/135，`PENDING_USER_CONFIRMATION=0` 保持。
- **验证结果**：定向测试 `ClientConfigPage.spec.ts` **136/136 通过**；全量前端测试 57 个测试文件 / **1116 个用例通过**；`npm run build`（`vue-tsc --noEmit && vite build`）**成功**。上述为自动化证据，**不构成**项目负责人目测或正式验收。
- **浏览器证据**：以仓库外临时静态服务 + 真实无头 Chrome（CDP，未新增或替换任何浏览器／服务依赖）加载构建产物 `/config/client`，用接口桩注入**伪造的**列表与数据源选项响应，在 1440×900、1920×1080、1024×720 三个视口 × 侧栏展开／收起共六个组合下核对，结论 `BROWSER_VERIFIED_HEADLESS`：弹窗中心与可见视口中心偏差均为 0；多项错误态下恰好三处字段错误且 ID／描述控件 `box-shadow: rgb(245,108,108) 0 0 0 1px inset`、数据源区 `border-color: rgb(245,108,108)`、错误文字 `rgb(245,108,108)`；数据源提示由中性灰 `rgb(144,147,153)` 在提交尝试后转为红色、选中后隐藏；字段反馈区 `min-height: 20px`、`overflow: visible`；同一视口内 idle／多项错误／修正后／提交中四态弹窗与 footer 底边不变（footer 底边 1440×900 为 606.16、1920×1080 为 633.16、1024×720 为 579.16）；按钮常态 `rgb(9,9,11)`／6px／500／白字且 `disabled=false`；窄视口下标题与底部按钮仍可操作。核对用临时进程已停止，未改动仓库文件。**数据经接口桩注入而非取自真实后端**，该结论为代码 Agent 自测证据，**不构成项目负责人目测或正式验收**。
- **加载态配色口径（`9da9620` 实现时点的已知残余解释差异，已由下条 R1 纠偏消除）**：真实浏览器下提交处理中按钮由 Element Plus 呈现为浅蓝（`rgb(160,207,255)`）＋4px 圆角。本实现**未**覆盖该外观，依据是 `CCFG-UI-047`（本轮未被修订的保留条款）要求“禁用态与 loading 态**沿用 Element Plus 既有视觉且不被正常态规则覆盖**”，且 `CCFG-UI-051` 要求“加载态**不得**在蓝色与黑色之间跳色”，把“跳色”理解为**闪烁／来回跳变**（本实现以 `:not(.is-disabled)` 作用域使加载态单次稳定呈现，无跳变）；若把“不得在蓝色与黑色之间跳色”理解为“加载态也须为黑色”，则与同轮保留的“沿用 Element Plus 既有视觉”相冲突。此处按保留条款口径实现，并**照实登记为待项目负责人目测确认项**，不擅自改判已批准基线。**该口径已被 ChatGPT 远程代码复审否决**：`9da9620` 的复审结论为 `CHANGES_REQUIRED`，`CHANGES_REQUIRED` 明确指出“只发生一次、没有闪烁”**不能**作为符合 `CCFG-REQ-124`/`CCFG-AC-120`/`CCFG-DESIGN-062`/`CCFG-UI-051`“不得在蓝色与黑色之间跳色”的解释；该残余差异已由下条 R1 纠偏任务**改正**（加载态改为黑色系），本历史条目**不**再作为现行实现口径。
- **R1 定向纠偏（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001-R1`，前端纠偏，起始提交 `9da96206cae51a891e326e586793e5e641082952`）**：按 ChatGPT 远程代码复审结论 `CHANGES_REQUIRED`，**仅**修复新增／编辑探针弹窗**主提交按钮**在提交请求处理中由黑色跳为浅蓝（Element Plus `--el-button-disabled-bg-color` = `--el-color-primary-light-5` = `#a0cfff`）的问题，**不**修改任何需求／验收／设计／UI 定义行。落点：`ClientConfigPage.vue` 新增**仅加载态**定向规则 `.cc-dialog-submit.is-loading{, :hover, :focus, :active}`（深灰黑系 `#3f3f46` 底与边框、白字、6px 圆角、`font-weight: 500`、`cursor: not-allowed`），并把 Element Plus 加载态的 30% 白遮罩 `::before` 在本按钮内置为透明；沿用 scoped CSS 的 `[data-v-*]` 使该规则（(0,3,0)）稳压 EP `.el-button.is-disabled`（(0,2,0)），**不**新增全局覆盖、**不**使用 `!important`、**不改**正常态 `:not(.is-disabled)` 作用域、**不改** `:loading`／`:disabled` 与 `submitDialog()` 防重复逻辑、**不改**按钮文案与取消／自动生成／修改探针 ID 行为。真实无头浏览器（1440×900）在**新增**与**编辑**两条路径上分别核对三态：**常态** `background-color/border-color = rgb(9,9,11)`、`color = rgb(255,255,255)`、`border-radius = 6px`、无 `is-loading`／`is-disabled`、`cursor: pointer`；**挂起** `rgb(63,63,70)`（黑系深灰，**非**浅蓝）、`color` 白、6px、`is-disabled is-loading`、加载图标存在、`cursor: not-allowed`、`::before` 为透明；**结束**（写请求以业务错误码收尾，弹窗保持打开）恢复 `rgb(9,9,11)` 常态；挂起期间连点 3 次写入请求仍各为 **1** 次（未重复提交）。回归保留项经真实浏览器与单测复核未变：弹窗视口居中偏差 0（24 个弹窗打开场景）、字段反馈区 `min-height: 20px`／`overflow: visible`、footer 底边 1440×900 为 606.16、1920×1080 为 633.16、1024×720 为 579.16，探针 ID 32 位与描述 256 个 Unicode 字符截断行为不变。定向测试 `ClientConfigPage.spec.ts` **140/140 通过**、全量前端测试 57 文件 / **1120 用例通过**、`npm run build` 成功；四类定义行相对 `9da9620` **逐字节零变化**，135 条验收仍**全部 `NOT_RUN`**。该纠偏**仅**证明代码口径已改正，**不**代表已通过项目负责人目测或正式验收。
- **页面级授权 ≠ 模板级全局授权（本次实现后口径不变）**：本次实现**不新增、不扩大**任何授权；两套公共模板的模板级全局迁移状态（`page_migration_status=NOT_STARTED`、`page_migration_authorization_status=NOT_GRANTED`、`pilot_page_selection_status=NOT_DECIDED`）**保持不变**，数据源管理参考页最终接受状态**不变**，**其他页面未被授权**。项目负责人认可的“新增／编辑表单弹窗模板”方向**须在本轮实现并经项目负责人实际目测认可后**另开任务提炼，本轮仅在文档登记入口（`CCFG-REQ-136`／`CCFG-AC-135`／`CCFG-DESIGN-071`）。
- **定义行完整性**：`CCFG-REQ-001~136`、`CCFG-AC-001~135`、`CCFG-DESIGN-001~071`、`CCFG-UI-001~059` 相对批准收口提交 `b35ea6974adad3f61b6599de0c8b48f124125ae6` **逐字节零变化**（本实现任务不修订任何定义行）；`API.md`/`DATABASE.md` 未修改。

## 2. 文档导航

| 文档 | 职责 | 状态 |
|---|---|---|
| `README.md`（本文件） | Feature 定位、文档导航与状态 | 已随 Feature 基线状态更新（2026-09-03）；R1 定向修订同步（2026-09-04）；并发口径调整草案同步（2026-09-04）；并发口径调整 R1 定向修订同步（2026-09-04）；并发口径调整批准收口同步（2026-09-04）；设计并发口径调整同步（2026-09-04）；设计并发口径调整批准收口同步（2026-09-04）；页面级调整草案同步（2026-09-22）；页面级调整草案 R1 偏差修正同步（2026-09-22）；页面级调整基线批准收口同步（2026-09-22）；页面级调整实现同步（2026-09-23）；第二轮主列表视觉调整草案同步（2026-09-23）；第二轮视觉调整草案 R1 纠错（行级歧义警示颜色勘误、`CCFG-AC-093` 窄视口预期收窄、当前状态/入口与历史时点一致）同步（2026-09-23）；第二轮视觉调整基线批准收口（2026-09-23，`adjustment2_baseline_status=APPROVED`）同步（2026-09-23）；新增 §1.6 第二轮视觉调整实现（2026-09-23，`adjustment2_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`）同步（2026-09-23）；新增 §1.7 第三轮 `+N` 清单与新增／编辑弹窗视觉调整草案（2026-09-23，`adjustment3_baseline_status=DRAFT_PENDING_USER_REVIEW`）同步（2026-09-23）；第三轮草案 R1 定向纠错（2026-09-23，ChatGPT 对 R0 复审 `CHANGES_REQUIRED`；第二轮实现当前状态改为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`、现行下一入口统一为 `CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_R1_REVIEW`）同步（2026-09-23）；第三轮视觉调整基线批准收口（2026-09-23，`adjustment3_baseline_status=APPROVED`、`adjustment3_approval_status=APPROVED_BY_PROJECT_OWNER`、批准依据提交 `1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244`，现行下一入口更新为 `CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_APPROVAL_CLOSEOUT_REVIEW`）同步（2026-09-23）；新增 §1.8 第三轮 `+N` 清单与新增／编辑弹窗视觉调整实现（2026-09-23，`adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`，现行下一入口更新为 `CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_IMPLEMENTATION_REVIEW`）同步（2026-09-23）；§1.8 追加第三轮实现 R1 定向修复时序说明（2026-09-23，`CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-IMPLEMENTATION-001-R1`：消除 1440×900 首行 `+N` 弹层上缘裁切、`adjustment3_implementation_status` 保持 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`、现行入口为工作区差异审阅 `WORKTREE_DIFF_REVIEW_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_IMPLEMENTATION_R1`）同步（2026-09-23）；README §1.8 映射表引用与 R1 时序勘误（2026-09-23，`CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-REMOTE-REVIEW-DOC-R1`：按已批准定义行修正 `CCFG-REQ-113~122` 对 `CCFG-AC-105~117`／`CCFG-DESIGN-054~060`／`CCFG-UI-043~049` 的引用并把合并过宽的行拆分逐条对应，R1“尚未提交、尚未推送”限定为历史状态并补充当前已提交推送、现处远程复审后文档纠错阶段，当前下一入口更新为 `WORKTREE_DIFF_REVIEW_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_REMOTE_REVIEW_DOC_R1`）同步（2026-09-23）；新增 §1.9 第四轮新增／编辑弹窗校验与交互调整草案（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001`，纯文档：`adjustment4_baseline_status=DRAFT_PENDING_USER_REVIEW`、`adjustment4_approval_status=NOT_APPROVED`、`adjustment4_implementation_status=NOT_STARTED`，编号总数更新为 需求 136 / 验收 135 / 设计 71 / UI 59，当前下一入口更新为 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_REVIEW`）同步（2026-09-24）；§1.9 追加第四轮草案 R1 定向纠错（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001-R1`：ChatGPT 对 `51be9aa` 复审 `CHANGES_REQUIRED`；按钮旧规则冲突、`CCFG-AC-131` 不可能输入、后续模板追踪映射三处一致性纠错，第四轮分层不变，当前下一入口更新为 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_R1_REVIEW`）同步（2026-09-24）；§1.9 追加第四轮弹窗校验与交互调整基线批准收口（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001`：ChatGPT 对 R1 提交 `9daf03848d53008d3ac73e6a43c1368fa5d72750` 复审 `APPROVED`、项目负责人 2026-09-24 批准，`adjustment4_baseline_status=APPROVED`、`adjustment4_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment4_approval_date=2026-09-24`、`adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750`，本轮实现仍 `NOT_STARTED`、135 条验收仍全部 `NOT_RUN`，当前下一入口更新为 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW`）同步（2026-09-24） |
| `REQUIREMENTS.md` | 需求基线（`CCFG-REQ-001~090` 已批准；本轮新增 `CCFG-REQ-091~103` 已随本轮页面调整基线于 2026-09-22 批准） | `APPROVED`（2026-09-03 曾 `APPROVED`，因并发口径调整于 2026-09-04 转为待复审草案：取消旧需求的并发“最多一个成功”强承诺（`LOCK TABLE ... WAIT 5` 是后续未批准设计草案的方案，本轮已过时），改为尽力写前检查 + 已接受并发边界；首版调整结果经 ChatGPT 正式复审 `CHANGES_REQUIRED`（R1-01~R1-04），R1 定向修订完成后，ChatGPT 对 R1 结果提交 `f2a4d7d...` 正式复审 `APPROVED`，项目负责人于 2026-09-04 明确回复“批准”，经并发口径调整批准收口重新收口为 `APPROVED`；批准的是需求基线，不代表功能已实现或验收已通过；调整前批准历史见 §1.1，本轮批准信息见 §1.2）——**本轮页面级调整基线（2026-09-22，已批准）**：新增 §1.3 分层状态块与 §7.10（`CCFG-REQ-091~103`，13 条），定向修订 `CCFG-REQ-011/020/021/022/023/025/028/029/034/042`；新增部分与修订部分已随本轮页面调整基线于 2026-09-22 批准（批准前为 `DRAFT_PENDING_USER_REVIEW`），实现仍为 `NOT_STARTED`、验收仍为 `NOT_RUN`；`CCFG-REQ-001~090` 主体历史批准链保留不改写。**第二轮主列表视觉调整草案（2026-09-23，`DRAFT_PENDING_USER_REVIEW`）**：新增 §7.11 与 `CCFG-REQ-104~112`（9 条），并在 §7.10 前言追加时序说明（该节历史 `NOT_STARTED`/`NOT_RUN` 表述属该时点事实；页面级调整实现已于 2026-09-23 完成并通过 ChatGPT 远程代码复审，定义行未改写）；编号总数更新为 `CCFG-REQ-001~112`（112 条）、覆盖 112/112，`CCFG-REQ-001~103` 定义行逐字节零变化；新增部分**尚未**复审、**尚未**批准）。**第三轮 `+N` 清单与弹窗视觉调整（2026-09-23 草案建立 → R1 纠错 → 2026-09-23 批准收口为 `APPROVED`）**：新增 §7.12 与 `CCFG-REQ-113~122`（10 条），并对 `CCFG-REQ-016`（`+N` 弹层“可设最大高度并内部滚动”）做**目标标注 + 修订**（原文保留不改写，由 `CCFG-REQ-115` 承接）；编号总数更新为 `CCFG-REQ-001~122`（122 条）、覆盖 122/122，除 `CCFG-REQ-016` 外其余 111 行定义**逐字节零变化**；该草案已经 ChatGPT 从远程 Git 复审、结论 `CHANGES_REQUIRED`（现由 R1 纠错任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001-R1` 受理，R1 **未**修改任何需求定义行），已经 ChatGPT 从远程 Git 对 R1 结果提交 `1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244` 复审、结论 `APPROVED`，并由项目负责人于 2026-09-23 批准收口为 `APPROVED`（第三轮实现仍 `NOT_STARTED`、第三轮验收仍 `NOT_RUN`））。**第四轮新增／编辑弹窗校验与交互调整基线（2026-09-24 草案建立 → R1 纠错 → 2026-09-24 批准收口为 `APPROVED`）**：新增 §7.13 与 `CCFG-REQ-123~136`（14 条），并对 `CCFG-REQ-037`/`CCFG-REQ-039`/`CCFG-REQ-060` 做**目标标注 + 定向修订**（原文保留不改写）；编号总数更新为 `CCFG-REQ-001~136`（136 条）、覆盖 136/136，除上述三条外其余 119 行定义**逐字节零变化**；新增部分已由批准收口任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001` 于 2026-09-24 收口为 `APPROVED`（`adjustment4_baseline_status=APPROVED`、`adjustment4_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment4_approval_date=2026-09-24`、`adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750`）。**R1 定向纠错（2026-09-24）**：草案已经 ChatGPT 从远程 Git 对提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 复审、结论 `CHANGES_REQUIRED`；R1 追加 1 条需求定义行目标标注 `CCFG-REQ-120`（禁用态适用范围收窄，“表单未完成／校验未通过预先禁用”由 `CCFG-REQ-124` 取代，现行口径仅为提交请求处理中暂不可重复提交），并把 `CCFG-REQ-124` 的“始终”限定为**非提交处理中**的常态；需求编号与数量 `CCFG-REQ-001~136`（136 条）保持连续唯一，除 `CCFG-REQ-120`（R1 追加目标标注）、`CCFG-REQ-124`（R1 最小措辞同步）与本轮既有 3 条定向行（`037`/`039`/`060`）外，其余 131 条定义行相对 `51be9aa` **逐字节零变化**；第四轮基线已由批准收口任务于 2026-09-24 收口为 `APPROVED`（`adjustment4_implementation_status` 仍 `NOT_STARTED`、135 条验收仍全部 `NOT_RUN`）；现行下一入口 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW` |
| `ACCEPTANCE.md` | 验收标准（`CCFG-AC-001~135`，全部 `NOT_RUN`；第四轮新增至 135 条，见本行末） | `APPROVED`（2026-09-03 曾 `APPROVED`，因并发口径调整于 2026-09-04 转为待复审草案，首版调整结果经 ChatGPT 正式复审 `CHANGES_REQUIRED`、R1 定向修订完成后，ChatGPT 对 R1 结果提交 `f2a4d7d...` 正式复审 `APPROVED`、项目负责人于 2026-09-04 明确回复“批准”，经并发口径调整批准收口重新收口为 `APPROVED`；批准的是验收标准，不是验收执行结果；76 条用例仍全部 `NOT_RUN`）——**本轮页面级调整基线（2026-09-22，已批准）**：新增 §1.3 分层状态块与 `CCFG-AC-077~089`（13 条），R0 定向修订 `CCFG-AC-002/009/016/017/018/019/021/022/023/025/026/032`，**R1 修正后的本轮修订用例清单为** `CCFG-AC-002/009/016/017/018/019/020/021/022/023/025/026/032/082/087/088/089`（17 条，含 R0 漏记的 `016`/`023` 与 R1 追加项，见 `ACCEPTANCE.md` §1.4）；验收由 76 条增至 **89 条**，需求→验收覆盖 **103/103（100%）**，**89 条执行状态全部为 `NOT_RUN`**。**第二轮主列表视觉调整草案（2026-09-23，`DRAFT_PENDING_USER_REVIEW`）**：新增 §1.6 分层状态块与 `CCFG-AC-090~104`（15 条），验收总数增至 **104 条**、覆盖更新为 **112/112（100%）**、**104 条执行状态全部为 `NOT_RUN`**；`CCFG-AC-001~089` 定义行逐字节零变化，新增部分**尚未**复审、**尚未**批准。**第三轮 `+N` 清单与弹窗视觉调整（2026-09-23 草案建立 → R1 纠错 → 2026-09-23 批准收口为 `APPROVED`）**：新增 §1.7 分层状态块与 `CCFG-AC-105~117`（13 条），并对 `CCFG-AC-011`（`+N` 弹层“可限高并在内部滚动”）做**目标标注 + 修订**（原文保留不改写，由 `CCFG-AC-109` 承接）；验收总数增至 **117 条**、覆盖更新为 **122/122（100%）**、**117 条执行状态全部为 `NOT_RUN`**，除 `CCFG-AC-011` 外其余 103 行定义**逐字节零变化**；该草案已经 ChatGPT 从远程 Git 复审、结论 `CHANGES_REQUIRED`，R1 纠错任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001-R1` 仅定向修订 `CCFG-AC-113` 的验收步骤（新增模式 ID 可直接填写、编辑模式默认锁定与“修改探针 ID”解锁），其余 116 行定义相对 `a585071` **逐字节零变化**，已经 ChatGPT 从远程 Git 复审、结论 `APPROVED`，并由项目负责人于 2026-09-23 批准收口为 `APPROVED`（**尚未**执行，117 条全部 `NOT_RUN`）。**第四轮新增／编辑弹窗校验与交互调整基线（2026-09-24 草案建立 → R1 纠错 → 2026-09-24 批准收口为 `APPROVED`）**：新增 §1.8 分层状态块与 `CCFG-AC-118~135`（18 条）；验收总数增至 **135 条**、覆盖更新为 **136/136（100%）**、**135 条执行状态全部 `NOT_RUN`**；`CCFG-AC-001~117` 定义行**逐字节零变化**（本轮**不**修订既有验收定义行）；新增部分已由批准收口任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001` 于 2026-09-24 收口为 `APPROVED`。**R1 定向纠错（2026-09-24）**：草案已经 ChatGPT 从远程 Git 对提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 复审、结论 `CHANGES_REQUIRED`；R1 定向修订 **2 条**新增验收定义行——`CCFG-AC-115`（追加第四轮目标标注并给出**现行可执行验收步骤**，其“校验未通过预先禁用”场景由 `CCFG-AC-120` 取代）、`CCFG-AC-131`（原“接近或**超过** 1024 字节”分支不可构造——256 字符 UTF-8 最多恰好 1024 字节——改为 768 字节 / 恰好 1024 字节 / 绕过前端 >1024 字节三类互不重叠边界），`CCFG-AC-120` 就“始终”作最小措辞同步并说明与 `CCFG-AC-115` **同时成立、无冲突**；验收编号与数量 `CCFG-AC-001~135`（135 条）保持连续唯一、**全部 `NOT_RUN`**，除 `CCFG-AC-115`/`CCFG-AC-131`/`CCFG-AC-120` 外其余定义行相对 `51be9aa` **逐字节零变化**；第四轮验收基线已由批准收口任务于 2026-09-24 收口为 `APPROVED`（135 条仍全部 `NOT_RUN`）；现行下一入口 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW` |
| `DESIGN.md` | 逻辑设计（`CCFG-DESIGN-001~037` 已批准；本轮新增 `CCFG-DESIGN-038~046` 已随本轮页面调整基线于 2026-09-22 批准；第二轮新增 `CCFG-DESIGN-047~053` 为 V2 草案，含并发/锁方案、追踪矩阵；第三轮新增 `CCFG-DESIGN-054~060` 为 `+N` 清单与弹窗视觉调整（2026-09-23 批准收口为 `APPROVED`）；第四轮新增 `CCFG-DESIGN-061~071` 为新增／编辑弹窗校验与交互调整（2026-09-24 批准收口为 `APPROVED`）） | `APPROVED`（2026-09-03 设计草案；初版正式设计复审结论 `CHANGES_REQUIRED`，2026-09-04 R1 已定向修订；同日设计并发口径定向调整任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001` 按已重新批准的需求并发口径完成设计调整（移除该过时表锁方案）；调整结果经 ChatGPT 对提交 `ba7c5e9...` 正式复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）结论 `APPROVED`，项目负责人于 2026-09-04 明确回复“批准”，经批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001` 收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`，可用于后续实现；批准的是设计基线，不代表代码已实现、已测试或验收已执行通过）——**本轮页面级调整基线（2026-09-22，已批准）**：新增 §13 与 `CCFG-DESIGN-038~046`（9 条），§12 追踪矩阵更新为 103/103 与 89/89；新增部分已随本轮页面调整基线于 2026-09-22 批准（批准前为 `DRAFT_PENDING_USER_REVIEW`），实现仍为 `NOT_STARTED`；`CCFG-DESIGN-001~037` 业务定义原文保留不改写，仅 `CCFG-DESIGN-021` 追加一条“入口位置由 §13 取代”的定向修订标注（语义不变）。**第二轮主列表视觉调整草案（2026-09-23，`DRAFT_PENDING_USER_REVIEW`）**：新增 §14 与 `CCFG-DESIGN-047~053`（7 条），§12 追踪矩阵更新为 112/112 与 104/104；`CCFG-DESIGN-001~046` 定义行逐字节零变化，新增部分**尚未**复审、**尚未**批准、**尚未**实现。**第三轮 `+N` 清单与弹窗视觉调整（2026-09-23 草案建立 → R1 纠错 → 2026-09-23 批准收口为 `APPROVED`）**：新增 §15 与 `CCFG-DESIGN-054~060`（7 条），§12 追踪矩阵更新为 122/122 与 117/117；`CCFG-DESIGN-001~053` 定义行逐字节零变化；该草案已经 ChatGPT 从远程 Git 复审、结论 `CHANGES_REQUIRED`（现由 R1 纠错任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001-R1` 受理，R1 **未**修改任何设计定义行，60 条定义行相对 `a585071` 逐字节零变化），已经 ChatGPT 从远程 Git 对 R1 结果提交 `1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244` 复审、结论 `APPROVED`，并由项目负责人于 2026-09-23 批准收口为 `APPROVED`（**尚未**实现）。**第四轮新增／编辑弹窗校验与交互调整基线（2026-09-24 草案建立 → R1 纠错 → 2026-09-24 批准收口为 `APPROVED`）**：新增 §16 与 `CCFG-DESIGN-061~071`（11 条），§12 追踪矩阵更新为 136/136 与 135/135；`CCFG-DESIGN-001~060` 定义行**逐字节零变化**，仅 `CCFG-DESIGN-059` 追加一条 `**【本轮（第四轮）定向修订 · 待批准】**` 标注（原文保留不改写，禁用口径由 `CCFG-DESIGN-062` 承接）；新增部分已由批准收口任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001` 于 2026-09-24 收口为 `APPROVED`（本轮实现仍 `NOT_STARTED`）。**R1 定向纠错（2026-09-24）**：草案已经 ChatGPT 从远程 Git 对提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 复审、结论 `CHANGES_REQUIRED`；R1 追加 `CCFG-DESIGN-059` 的 R1 一致性核对标注、`CCFG-DESIGN-062` 就“始终”限定为**非提交处理中**的常态并补充两种状态并存关系说明，并从 §12.1 `CCFG-REQ-136` 行与 §12.2 `CCFG-AC-135` 行**移除**无依据的 `CCFG-UI-059` 映射（新增 §12 的 R1 说明：该入口属文档治理／范围声明、本轮无新增 UI 元素）；设计编号与数量 `CCFG-DESIGN-001~071`（71 条）保持连续唯一，§12 矩阵仍为 136/136 需求、135/135 验收；第四轮设计基线已由批准收口任务于 2026-09-24 收口为 `APPROVED`；现行下一入口 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW` |
| `API.md` | 接口契约（`CCFG-API-001~020`，接口 E1~E7、错误码表） | `APPROVED`（2026-09-03 设计草案；初版正式设计复审结论 `CHANGES_REQUIRED`，2026-09-04 R1 已定向修订；同日设计并发口径定向调整任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001` 按已重新批准的需求并发口径完成设计调整（移除该过时错误码方案）；调整结果经 ChatGPT 对提交 `ba7c5e9...` 正式复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）结论 `APPROVED`，项目负责人于 2026-09-04 明确回复“批准”，经批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001` 收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`，可用于后续实现；批准的是设计基线，不代表代码已实现、已测试或验收已执行通过，实现状态仍 `NOT_STARTED`） |
| `UI.md` | 界面设计（`CCFG-UI-001~026` 已批准；本轮新增 `CCFG-UI-027~035` 已随本轮页面调整基线于 2026-09-22 批准；第二轮新增 `CCFG-UI-036~042` 为 V2 草案，布局/交互/文案；第三轮新增 `CCFG-UI-043~049` 为 `+N` 清单与弹窗视觉调整（2026-09-23 批准收口为 `APPROVED`）；第四轮新增 `CCFG-UI-050~059` 为新增／编辑弹窗校验与交互调整（2026-09-24 批准收口为 `APPROVED`）） | `APPROVED`（2026-09-03 设计草案；初版正式设计复审结论 `CHANGES_REQUIRED`，2026-09-04 R1 已定向修订；同日设计并发口径定向调整任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001` 按已重新批准的需求并发口径完成设计调整（移除该过时文案方案）；调整结果经 ChatGPT 对提交 `ba7c5e9...` 正式复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）结论 `APPROVED`，项目负责人于 2026-09-04 明确回复“批准”，经批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001` 收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`，可用于后续实现；批准的是设计基线，不代表代码已实现、已测试或验收已执行通过）——**本轮页面级调整基线（2026-09-22，已批准）**：新增 §15 与 `CCFG-UI-027~035`（9 条），`CCFG-UI-004/005/006/018/022/024` 部分口径被取代或收窄（原文保留不改写）；**R1（2026-09-22）**：冻结项目负责人两项决定——取消“删除所选”后**行单选/选中行高亮/“已选择：{探针ID}”整体取消**、历史异常 `FG_ACTIVE` 展示为**紧跟探针 ID 的红色 `异常：{原始值}`**（`CCFG-UI-031/035`），并定向清理 `CCFG-UI-004/005/006/032` 的旧行选中表述；新增部分已随本轮页面调整基线于 2026-09-22 批准（批准前为 `DRAFT_PENDING_USER_REVIEW`），实现仍为 `NOT_STARTED`，本轮 `PENDING_USER_CONFIRMATION=0`。**第二轮主列表视觉调整草案（2026-09-23，`DRAFT_PENDING_USER_REVIEW`）**：新增 §16 与 `CCFG-UI-036~042`（7 条），`CCFG-UI-004/005/007/010/029/032` 的部分口径被本轮**定向修订**（原文保留不改写）；`CCFG-UI-001~035` 定义行逐字节零变化，新增部分**尚未**复审、**尚未**批准、**尚未**实现。**第三轮 `+N` 清单与弹窗视觉调整（2026-09-23 草案建立 → R1 纠错 → 2026-09-23 批准收口为 `APPROVED`）**：新增 §17 与 `CCFG-UI-043~049`（7 条），并对 `CCFG-UI-009`、`CCFG-UI-024` 中 `+N` 弹层“设最大高度并内滚”的口径做**目标标注 + 修订**（原文保留不改写，由 `CCFG-UI-044` 承接；弹窗内部滚动与 Tooltip 单实例口径保持有效）；除该两条外 `CCFG-UI-001~042` 定义行**逐字节零变化**；该草案已经 ChatGPT 从远程 Git 复审、结论 `CHANGES_REQUIRED`（现由 R1 纠错任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001-R1` 受理，R1 **未**修改任何界面定义行，49 条定义行相对 `a585071` 逐字节零变化），已经 ChatGPT 从远程 Git 对 R1 结果提交 `1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244` 复审、结论 `APPROVED`，并由项目负责人于 2026-09-23 批准收口为 `APPROVED`（**尚未**实现）。**第四轮新增／编辑弹窗校验与交互调整基线（2026-09-24 草案建立 → R1 纠错 → 2026-09-24 批准收口为 `APPROVED`）**：新增 §18 与 `CCFG-UI-050~059`（10 条），并对 `CCFG-UI-013`/`CCFG-UI-015`/`CCFG-UI-017` 做**目标标注 + 定向修订**（原文保留不改写）；除该三条外 `CCFG-UI-001~049` 定义行**逐字节零变化**；新增部分已由批准收口任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001` 于 2026-09-24 收口为 `APPROVED`（本轮实现仍 `NOT_STARTED`）。**R1 定向纠错（2026-09-24）**：草案已经 ChatGPT 从远程 Git 对提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 复审、结论 `CHANGES_REQUIRED`；R1 对 `CCFG-UI-047` 追加第四轮目标标注（禁用态适用范围收窄，“表单未完成／校验未通过预先禁用”由 `CCFG-UI-051` 取代）、`CCFG-UI-051` 就“始终”限定为**非提交处理中**的常态并补充两种状态并存关系说明、从 `CCFG-UI-059` 定义行单元格**移除**无依据的 `CCFG-REQ-136`／`CCFG-AC-135` 映射（保留 `132`/`133`/`134` 与 `130`/`131`/`132`/`133`）并说明该入口属文档治理／范围声明、本轮无新增 UI 元素；界面编号与数量 `CCFG-UI-001~059`（59 条）保持连续唯一；第四轮界面基线已由批准收口任务于 2026-09-24 收口为 `APPROVED`；现行下一入口 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW` |
| `DATABASE.md` | 数据库使用设计（`CCFG-DB-001~022`，SQL 形态/事务锁矩阵） | `APPROVED`（2026-09-03 设计草案；初版正式设计复审结论 `CHANGES_REQUIRED`，2026-09-04 R1 已定向修订；同日设计并发口径定向调整任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001` 按已重新批准的需求并发口径完成设计调整（移除该过时事务锁方案）；调整结果经 ChatGPT 对提交 `ba7c5e9...` 正式复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）结论 `APPROVED`，项目负责人于 2026-09-04 明确回复“批准”，经批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001` 收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`，可用于后续实现；批准的是设计基线，不代表代码已实现、已测试或验收已执行通过，实现状态仍 `NOT_STARTED`，76 条验收仍全部 `NOT_RUN`） |
| `reports/CLIENT-CONFIG-REQUIREMENTS-BASELINE-001.md` | 需求与验收草案建立执行报告 | 已建立 |
| `reports/CLIENT-CONFIG-REQUIREMENTS-BASELINE-001-R1.md` | R1 定向修订执行报告 | 已建立 |
| `reports/CLIENT-CONFIG-REQUIREMENTS-BASELINE-APPROVAL-001.md` | 需求与验收基线批准收口执行报告 | 已建立 |
| `reports/CLIENT-CONFIG-DESIGN-BASELINE-001.md` | 设计基线草案建立执行报告 | 已建立 |
| `reports/CLIENT-CONFIG-DESIGN-BASELINE-001-R1.md` | 设计基线 R1 定向修订（正式复审 `CHANGES_REQUIRED`）执行报告 | 已建立（2026-09-04） |
| `reports/CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001.md` | 并发口径定向调整草案（纯文档）执行报告 | 已建立（2026-09-04） |
| `reports/CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1.md` | 并发口径调整 R1 定向修订（正式复审 `CHANGES_REQUIRED`）执行报告 | 已建立（2026-09-04） |
| `reports/CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md` | 并发口径调整批准收口执行报告 | 已建立（2026-09-04） |
| `reports/CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001.md` | 设计并发口径调整（正式需求并发口径重新批准驱动）执行报告 | 已建立（2026-09-04） |
| `reports/CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001.md` | 设计并发口径调整批准收口执行报告 | 已建立（2026-09-04） |
| `reports/CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-001.md` | 页面级查询列表页模板 + 列表表格视觉模板选择性接入调整基线草案执行报告 | 已建立（2026-09-22） |
| `reports/CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-001-R1.md` | 页面级调整基线草案 R1 偏差修正（ChatGPT 对 R0 复审 `CHANGES_REQUIRED` 驱动，含项目负责人两项冻结决定）执行报告 | 已建立（2026-09-22） |
| `reports/CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-001-R2.md` | R1 报告证据纠错（ChatGPT 对 R1 复审 `CHANGES_REQUIRED` 驱动的追加式证据纠错）执行报告 | 已建立（2026-09-22） |
| `reports/CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-001-R3.md` | R2 报告摘要最小证据纠错（ChatGPT 对 R2 复审 `CHANGES_REQUIRED` 驱动）执行报告 | 已建立（2026-09-22） |
| `reports/CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-APPROVAL-CLOSEOUT-001.md` | 页面级调整基线批准收口（项目负责人批准驱动，`APPROVED`）执行报告 | 已建立（2026-09-22） |
| `reports/CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-IMPLEMENTATION-001.md` | 页面级调整实现（`/config/client` 单页，接入四组件 + 主列表视觉预设，取消行选中/独立状态列，ID 三态标识与“更多”菜单）执行报告 | 已建立（2026-09-23） |
| `reports/CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-001-V2.md` | 第二轮主列表视觉调整基线草案（五项：黑色实心新增按钮、ID 正文对齐、行高跟随参考页、采集数据源标签三态、操作列三点图标菜单）执行报告 | 已建立（2026-09-23，草案 `DRAFT_PENDING_USER_REVIEW`） |
| `reports/CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-001-V2-R1.md` | 第二轮主列表视觉调整草案 R1 纠错（ChatGPT 对 V2 复审 `CHANGES_REQUIRED` 驱动：行级歧义警示颜色越界勘误、`CCFG-AC-093` 窄视口预期收窄、当前状态/入口与历史时点一致）执行报告 | 已建立（2026-09-23） |
| `reports/CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-APPROVAL-CLOSEOUT-001.md` | 第二轮主列表视觉调整基线批准收口（项目负责人批准驱动，`adjustment2_baseline_status=APPROVED`）执行报告 | 已建立（2026-09-23） |
| `reports/CLIENT-CONFIG-VISUAL-FOLLOWUP-IMPLEMENTATION-001.md` | 第二轮主列表视觉调整实现（`/config/client` 主列表五项：黑色实心新增按钮、ID 正文字重颜色对齐、移除固定行高、采集数据源标签三态、操作列三点图标菜单）执行报告 | 已建立（2026-09-23） |
| `reports/CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001.md` | 第三轮 `+N` 清单与新增／编辑弹窗视觉调整基线草案（四项：`+N` 清单项两级内容与自然增高、弹窗放大至约 900px 与数据源区调整、配置项名称与主按钮对齐数据源管理、操作菜单“删除”不加粗）执行报告 | 已建立（2026-09-23，草案 `DRAFT_PENDING_USER_REVIEW`） |
| `reports/CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001-R1.md` | 第三轮 `+N` 清单与新增／编辑弹窗视觉调整草案 R1 定向纠错（ChatGPT 对 R0 复审 `CHANGES_REQUIRED` 驱动：`CCFG-AC-113` 步骤可执行性、R0 报告计数口径勘误、第二轮当前状态与入口同步；含 R0 报告 §4.2 计数 errata）执行报告 | 已建立（2026-09-23） |
| `reports/CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-APPROVAL-CLOSEOUT-001.md` | 第三轮 `+N` 清单与新增／编辑弹窗视觉调整基线批准收口（项目负责人批准驱动，`adjustment3_baseline_status=APPROVED`）执行报告 | 已建立（2026-09-23） |
| `reports/CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-IMPLEMENTATION-001.md` | 第三轮 `+N` 清单与新增／编辑弹窗视觉调整实现（`/config/client`：`+N` 清单两级信息与自然增高、弹窗放大至 900px 与数据源区调整、弹窗标签与主提交按钮对齐数据源管理、操作菜单“删除”不加粗）执行报告 | 已建立（2026-09-23，`adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`） |
| `reports/CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-IMPLEMENTATION-001-R1.md` | 第三轮 `+N` 弹层视口裁切定向修复执行报告（真实浏览器复现并消除 1440×900 首行 9 项上缘裁切 166.2px；根因为 EP 2.14.2 未开 `preventOverflow.altAxis`、竖直方向不避让；采用 `altAxis: true` + 视口边界 + `padding: 8`；含回归护栏与几何断言命令） | 已建立（2026-09-23） |
| `reports/CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001.md` | 第四轮新增／编辑探针弹窗校验与交互调整基线草案（字段级错误反馈与主提交按钮状态、数据源选择中性提示与弹窗稳定性、弹窗相对视口居中与配置项名称右对齐、探针 ID 32 位与描述 256 字符输入／自动生成截断／历史记录回显）执行报告 | 已建立（2026-09-24，草案 `DRAFT_PENDING_USER_REVIEW`） |
| `reports/CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001-R1.md` | 第四轮新增／编辑探针弹窗校验与交互调整草案 R1 定向纠错（ChatGPT 对 R0 复审 `CHANGES_REQUIRED` 驱动：按钮旧规则冲突、`CCFG-AC-131` 不可能输入、`CCFG-REQ-136` 追踪映射不实三处一致性纠错）执行报告 | 已建立（2026-09-24） |
| `reports/CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001.md` | 第四轮新增／编辑探针弹窗校验与交互调整基线批准收口（项目负责人 2026-09-24 批准驱动，`adjustment4_baseline_status=APPROVED`、复审依据提交 `9daf03848d53008d3ac73e6a43c1368fa5d72750`）执行报告 | 已建立（2026-09-24） |
| `reports/CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001.md` | 第四轮新增／编辑探针弹窗校验与交互调整实现（`/config/client`：字段级校验反馈与主提交按钮常态可点击、数据源提示灰／红四态与反馈区稳定空间、弹窗视口居中与配置项名称右对齐、探针 ID 32 位、描述 256 个 Unicode 字符与自动生成／历史草稿截断）执行报告 | 已建立（2026-09-24，`adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`） |
| `reports/CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001-R1.md` | 第四轮新增／编辑弹窗校验与交互调整实现 **R1 定向纠偏**（主提交按钮加载态由 Element Plus 浅蓝改为黑色系深灰，消除蓝黑跳色；含新增／编辑两路径三态浏览器证据与防重复证据）执行报告 | 已建立（2026-09-24，`adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW` 保持） |

## 3. Feature 定位

“探针端管理”用于维护 Oracle 表 `CDC_CLIENT_MULTIPLE` 的探针配置（探针 ID、探针描述、采集数据源、启停状态）。`sync-client` 进程用自身 `client_id` 命中 `CLIENT_ID` 相同且 `FG_ACTIVE=1` 的记录并读取其采集数据源。本 Feature 只维护数据库配置，不直接启停、通知进程，不操作 ZooKeeper、Kafka 或 Topic。

## 4. 当前状态

- 旧口径批准历史：需求基线 `REQUIREMENTS.md`（`CCFG-REQ-001~090`，90 条）与验收标准 `ACCEPTANCE.md`（`CCFG-AC-001~076`，76 条）曾于 2026-09-03 获项目负责人正式批准（ChatGPT 对 R1 结果正式复审结论 `APPROVED`，项目负责人明确回复“批准”），旧口径状态为 `APPROVED`。该次批准只覆盖旧口径的并发强保证目标（并发“最多一个成功”强承诺；当时需求只要求后续设计确定事务/锁/原子方案，未批准任何具体表锁语句，`LOCK TABLE ... WAIT 5` 是随后形成、始终未获项目负责人批准的设计草案方案），不代表功能已实现或验收已通过；后续因并发口径调整进入本轮待复审草案状态，经 ChatGPT 对 R1 结果正式复审 `APPROVED` 与项目负责人 2026-09-04 批准后重新收口为 `APPROVED`（见下条）；旧批准不自动批准本轮调整。
- 并发口径调整草案（2026-09-04，`CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001`，纯文档）：按项目负责人明确决定，配置平台不再为保证数据源唯一分配执行 Oracle 显式表锁，取消并发“最多一个成功”强承诺，改为“新增/编辑/启用写入前重新读取 + 尽力写前检查 + 已接受极端并发下两笔先后都成功的边界”；运行侧 `sync-client`/`sync-server` 使用配置时的重复检查为最终防线但不属本 Feature 范围。受影响需求 `CCFG-REQ-038/068/071/072/074/077`、验收 `CCFG-AC-030/056/058/059/061/064`；`REQUIREMENTS.md` 与 `ACCEPTANCE.md` 状态由旧口径 `APPROVED` 调整为 `DRAFT_PENDING_USER_REVIEW`。ChatGPT 对首版调整结果（提交 `6071d7a...`）正式复审结论为 `CHANGES_REQUIRED`（R1-01~R1-04）；R1 定向修订（`CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1`，2026-09-04，对应报告见 §2 导航）已完成；ChatGPT 对 R1 结果提交 `f2a4d7d...` 正式复审结论为 `APPROVED`（`CHATGPT_FORMAL_REQUIREMENTS_ADJUSTMENT_R1_REVIEW`），项目负责人于 2026-09-04 明确回复“批准”，`REQUIREMENTS.md` 与 `ACCEPTANCE.md` 经批准收口任务 `CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001` 重新收口为 `APPROVED`（对应报告见 §2 导航）；2026-09-03 批准保留为历史，不自动批准本轮调整（本轮依据独立复审与项目负责人明确回复）。
- **【历史记录 · 已被后续实现与收口取代，不代表当前状态】** 设计基线阶段曾记录“实现状态仍为 `NOT_STARTED`：尚未实现任何页面、接口或写库能力；页面/菜单仍为占位，用户可见名称仍为‘客户端配置’”。该表述属**历史事实**（对应设计草案阶段），已由后续实现事实取代：当前 `existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE`，且页面标题、菜单与面包屑已统一为“探针端管理”（见 §1）；仅**本轮页面级调整实现**为 `adjustment_implementation_status=NOT_STARTED`。
- 正式验收执行状态仍为 `formal_acceptance_execution_status=NOT_RUN`：全部 89 条验收用例（既有 76 条 + 本轮新增 13 条）均未执行，不得把已批准需求/验收标准中的目标规则描述为当前已实施事实，不得写成“验收通过”。（**滞后状态值说明（2026-09-24 补充，不改写上述历史表述）**：本行的“89 条”为第一轮页面级调整章节写入时点的历史值；**当前**全文件验收总数为 **135 条**（含第二轮 15 条、第三轮 13 条与第四轮草案新增 18 条），`formal_acceptance_execution_status` 仍为 `NOT_RUN`，**135 条全部未执行**，见 §1.9。）
- `DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md` 已于 2026-09-03 建立设计基线草案（`CLIENT-CONFIG-DESIGN-BASELINE-001`），状态均为 `DRAFT_PENDING_USER_REVIEW`、`PENDING_USER_CONFIRMATION=0`；当时本 Feature 尚未进入实现阶段，四份文档为草案、其中表锁/锁等待方案从未获项目负责人批准（不得写成原表锁设计曾获批准）；不因建立设计草案而改变已批准需求/验收，也不修改任何数据库基线。因本轮需求并发口径调整（2026-09-04）重新批准后，设计并发口径定向调整任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001`（2026-09-04，纯文档）已从四份设计文档中移除已过时的 `LOCK TABLE ... WAIT 5` 表锁/锁等待错误码（`ORA-30006→50050`）方案并按已重新批准口径调整设计，四份文档整体标记由 `STALE_LOCK_DESIGN_PENDING_DESIGN_REVISION` 更新为 `DESIGN_CONCURRENCY_ADJUSTED_PENDING_FORMAL_REVIEW`（当时仍为 `DRAFT_PENDING_USER_REVIEW`、`PENDING_USER_CONFIRMATION=0`）。该调整结果随后经 ChatGPT 对提交 `ba7c5e9...` 正式复审 `APPROVED`、项目负责人于 2026-09-04 明确回复“批准”，四份设计文档经批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001` 收口为 `APPROVED`（见下两条）。
- ChatGPT 对初版设计草案提交 `21f4729c43d146426e8d4f1b2d6b667cfcf160ff` 的正式设计复审结论为 `CHANGES_REQUIRED`（R1-01~R1-09）。`CLIENT-CONFIG-DESIGN-BASELINE-001-R1`（2026-09-04，纯文档）已完成定向修订：API 设计编号重排为 `CCFG-API-001~020` 连续唯一；四文档编号范围同步为 `CCFG-DESIGN-001~037`/`CCFG-API-001~020`/`CCFG-UI-001~026`/`CCFG-DB-001~022`；数据源数组固定“原存储顺序返回 + 前端非持久化前三项投影”单一契约；`CLIENT_DESC` 固定原文保存、Trim 仅判空、按原文计 UTF-8 字节；关键词 LIKE 增加 `\` 字面量转义与 `ESCAPE '\'`；删除未批准的数据源 ID“其他非法字符”限制；补齐 `CATEGORY_MISMATCH`/`TYPE_MISMATCH`、含逗号歧义（`COMMA_PROTOCOL_AMBIGUOUS`）与历史 NULL/空白 `CLIENT_DESC` 契约。修订后四文档状态保持 `DRAFT_PENDING_USER_REVIEW`、`PENDING_USER_CONFIRMATION=0`；受本轮需求并发口径调整影响，正式 R1 设计复审延后；需求调整 R1 结果已获 ChatGPT 正式复审 `APPROVED` 并由项目负责人于 2026-09-04 批准。设计并发口径定向调整任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001`（2026-09-04，纯文档）已从四份设计文档中移除过时的表锁/锁等待错误码方案并按已批准并发口径调整设计（标记 `DESIGN_CONCURRENCY_ADJUSTED_PENDING_FORMAL_REVIEW`，当时仍为 `DRAFT_PENDING_USER_REVIEW`、`PENDING_USER_CONFIRMATION=0`）。对设计并发口径调整结果的正式设计复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）已于 2026-09-04 完成、结论 `APPROVED`，项目负责人同日明确回复“批准”，四份设计文档经批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001` 收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`（当前状态见下条）。
- 设计批准收口（2026-09-04，`CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001`，纯文档，对应报告见 §2 导航）：对设计并发口径调整结果的正式设计复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）对象为提交 `ba7c5e917b1b9d08208c3e1ceb31285407f5fd5e`、结论 `APPROVED`，项目负责人于 2026-09-04 明确回复“批准”；`DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md` 四份设计文档状态已由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`，整体标记由 `DESIGN_CONCURRENCY_ADJUSTED_PENDING_FORMAL_REVIEW` 收口为 `APPROVED_READY_FOR_IMPLEMENTATION`，可用于后续实现；37/20/26/22 条设计业务定义相对批准提交逐字零差异，覆盖保持需求 90/90、验收 76/76。批准的是设计基线，不代表代码已实现、已测试或验收已执行通过：实现状态仍为 `NOT_STARTED`，页面/菜单仍为占位、用户可见名称仍为“客户端配置”，76 条验收仍全部 `NOT_RUN`。下一入口为 `CLIENT_CONFIG_IMPLEMENTATION`（见 §5）。
- 基线影响项（如旧资料“客户端配置”“管理平台对 `CDC_CLIENT_MULTIPLE` 只读”“编辑时探针 ID 不可改”等表述、`CLIENT_DESC` 长度 256 与 1024 的数据库基线差异）已在 `REQUIREMENTS.md` §9 记录；其中 `CLIENT_DESC` 真实语义已确认为 `VARCHAR2(1024 BYTE)`，256 与 1024 的已批准数据库基线差异继续作为后续独立数据库基线同步事项保留。
- **【历史记录 · 草案建立阶段，已被 2026-09-22 批准收口取代，不代表当前状态】** 页面级调整草案（2026-09-22，`CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-001`，纯文档，对应报告见 §2 导航）：按项目负责人已明确确认的 9 项页面调整决定，为 `/config/client` 单页建立“查询列表页模板选择性接入（页面层）”与“列表表格视觉模板接入（主列表）”的调整基线草案。**可见结果**：列顺序改为 序号｜探针 ID｜探针描述｜采集数据源｜数据源数量｜操作（最右固定列）；“新增探针”移到结果区头部最右侧；取消“删除所选”与“状态”列及其行内启停；删除/启停入口改为“操作”列“更多”下拉（`FG_ACTIVE='1'` → 删除/停用；`'0'` → 删除/启用；历史异常 → 仅删除/停用）；探针 ID 后按 `FG_ACTIVE` 三态显示（`'1'` 无标记、`'0'` “停用”标记、其余历史异常红色 `异常：{原始值}`）；本页不提供任何刷新能力。**分层状态（草案阶段）**：该阶段为 `adjustment_baseline_status=DRAFT_PENDING_USER_REVIEW`、`adjustment_implementation_status=NOT_STARTED`、本轮正式验收 `NOT_RUN`；该草案后经 R2/R3 证据纠错并由项目负责人批准收口为 `APPROVED`（见 §1.1、§1.2）。**旧实现事实不被改写**：`existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE` 保留；全部 89 条验收仍 `NOT_RUN`。**本轮不改** `API.md`/`DATABASE.md`（接口与数据库契约不变）、**不改**任何业务代码或测试。**授权分层**：项目负责人**已授予** `/config/client` 的页面级选择性接入授权（查询列表页模板侧四组件、不接入刷新工具栏；列表表格视觉模板侧仅主列表），**该页面级授权与本次基线批准均不代表本轮实现已完成或已验收**；两套模板的**模板级全局状态保持不变**，**其他页面未被授权**。`PENDING_USER_CONFIRMATION=0`：R1 已冻结行选中能力整体取消与异常 `FG_ACTIVE` 红色 `异常：{原始值}` 两项决定（见 §1.1 与 `UI.md` §15）。
- 页面级调整基线批准收口（2026-09-22，`CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-BASELINE-APPROVAL-CLOSEOUT-001`，纯文档，对应报告见 §2 导航）：ChatGPT 从远程 Git 对 R3 结果提交 `5066c761f8a9400d5841222cb73b0c03f56a82d0` 的复审结论为 `APPROVED`，项目负责人于 2026-09-22 明确回复原话 `批准本轮探针端管理页面调整基线`，本轮页面级调整基线由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`（`adjustment_approval_status=APPROVED_BY_PROJECT_OWNER`）。**批准的是页面调整基线，不是代码、测试、目测结果或最终接受**：`existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE` 保持，该时点 `adjustment_implementation_status=NOT_STARTED` 保持，89 条验收全部 `NOT_RUN` 保持，覆盖 103/103、`PENDING_USER_CONFIRMATION=0` 保持；`API.md`/`DATABASE.md` 未修改，两套模板的模板级全局状态保持不变。
- 页面级调整实现（2026-09-23，`CLIENT-CONFIG-QUERY-LIST-AND-LIST-TABLE-ADJUSTMENT-IMPLEMENTATION-001`，前端实现任务，对应报告见 §2 导航）：按已批准基线实现 `/config/client` 单页调整，`adjustment_implementation_status` 由 `NOT_STARTED` 变为 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`。**实现已完成，但不等同于已目测、已验收或已接受**：`existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE` 保持，全部 89 条验收仍 `NOT_RUN`，覆盖 103/103、`PENDING_USER_CONFIRMATION=0` 保持；`API.md`/`DATABASE.md` 未修改，两套模板的模板级全局状态与数据源管理参考页最终接受状态保持不变。实现细节与分层状态见 §1.3。（**时序说明**：该实现已于 2026-09-23 经 ChatGPT 从远程 Git 独立代码复审通过；此处仅追加时序说明，不改写上述历史表述。）
- 第二轮主列表视觉调整**草案**（2026-09-23，`CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-001-V2`，纯文档，对应报告见 §2 导航）：依据项目负责人查看 `/config/client` 与 `/config/data-source` 页面后**已明确确认的五项调整决定**，为 `/config/client` 主列表建立新一轮视觉调整草案——① “新增探针”改黑色实心按钮；② 探针 ID 正文字重颜色对齐参考页“数据源 ID”；③ 行高跟随参考页“数据源管理”主列表实际规则（移除固定像素、不改全局模板）；④ 采集数据源标签借用“角色”标签视觉语言并采用绿/红/中性色三态；⑤ 操作列“更多”统一改为水平三点图标菜单（含删除分隔线、键盘可访问、事件边界）。**分层状态**：本轮草案基线 `adjustment2_baseline_status=DRAFT_PENDING_USER_REVIEW`、本轮实现 `adjustment2_implementation_status=NOT_STARTED`、本轮验收 `NOT_RUN`（`CCFG-AC-090~104` 15 条新增，全文件 104 条全部 `NOT_RUN`）；覆盖更新为 112/112。**历史不被改写**：第一轮已批准基线、既有实现事实与第一轮实现事实保持真实，`CCFG-REQ-001~103`/`CCFG-AC-001~089`/`CCFG-DESIGN-001~046`/`CCFG-UI-001~035` 定义行逐字节零变化。**本轮不改** `API.md`/`DATABASE.md`、**不改**任何业务代码/测试/前端或后端源文件、**不改** `docs/baseline/**` 与模板级全局状态；`PENDING_USER_CONFIRMATION=0`。**用户同意五项 ≠ 文档已批准 ≠ 已实现 ≠ 已验收**（详见 §1.4）。
- 第二轮主列表视觉调整基线**批准收口**（2026-09-23，`CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-APPROVAL-CLOSEOUT-001`，纯文档，对应报告见 §2 导航）：ChatGPT 从远程 Git 对 V2 草案 R1 纠错提交 `3830cba16142b4e2ad88f1fa96682ea8397a1203` 的复审结论为 `APPROVED`（R1 纠错通过），项目负责人于 2026-09-23 明确回复原话 `批准本轮五项视觉调整基线`，第二轮调整基线由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`（`adjustment2_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment2_approval_date=2026-09-23`、`adjustment2_approved_reviewed_commit=3830cba16142b4e2ad88f1fa96682ea8397a1203`）。**批准的是第二轮视觉调整基线，不是代码、测试、目测结果或最终接受**：`adjustment2_implementation_status=NOT_STARTED` 保持，第一轮已批准基线与第一轮实现事实 `IMPLEMENTED_PENDING_USER_ACCEPTANCE` 保持，104 条验收全部 `NOT_RUN`、覆盖 112/112、`PENDING_USER_CONFIRMATION=0` 保持；`API.md`/`DATABASE.md` 未修改，两套模板的模板级全局状态保持不变，数据源管理参考页自身未修改（详见 §1.5）。
- 第二轮主列表视觉调整**实现**（2026-09-23，`CLIENT-CONFIG-VISUAL-FOLLOWUP-IMPLEMENTATION-001`，前端实现任务，对应报告见 §2 导航）：按已批准基线实现 `/config/client` 主列表五项视觉调整，`adjustment2_implementation_status` 由 `NOT_STARTED` 变为 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`。**实现已完成，但不等同于已目测、已验收或已接受**：`existing_feature_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE` 与第一轮 `adjustment_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE` 保持，全部 104 条验收仍 `NOT_RUN`，覆盖 112/112、`PENDING_USER_CONFIRMATION=0` 保持；`API.md`/`DATABASE.md` 未修改，两套模板的模板级全局状态与数据源管理参考页最终接受状态保持不变。实现细节与分层状态见 §1.6。（**时序说明（2026-09-23 追加，不改写上述历史表述）**：该第二轮实现其后已经 ChatGPT 从远程 Git 对实现提交 `fecf5a06d90c690b5ee984a9e487f05322e1dd70` 独立代码复审通过，`adjustment2_implementation_status` **当前值**为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`；项目负责人随后查看实际页面并提出第三轮 `+N` 清单与新增／编辑弹窗视觉调整，**尚未最终接受页面**。现行下一入口为第三轮批准收口的远程复审 `CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_APPROVAL_CLOSEOUT_REVIEW`。）
- 第三轮 `+N` 清单与新增／编辑弹窗视觉调整**草案**（2026-09-23，`CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001`，纯文档，对应报告见 §2 导航）：依据项目负责人**已明确确认的四项调整决定**，为 `/config/client` 的 `+N` 完整清单弹层与新增／编辑探针弹窗建立新一轮调整草案——① `+N` 弹层每项按“孝感市第一人民”标签 Tooltip 的内容层级呈现（主行完整机构名、次行弱化前缀 + 完整数据源 ID），异常/歧义提示保留在项内，弹层按内容自然增高、**不**设内部滚动与固定最大高度；② 新增／编辑弹窗两模式统一放大至桌面约 **900px**、受视口限制留安全边距，数据源区可见高度与“可选/已选”横向配比调整（弹窗内部受控滚动允许）；③ 弹窗配置项名称对齐参考页 `.editor-dialog .el-form-item__label`、底部主提交按钮对齐 `.editor-submit-button` 黑色实心（文案仍“创建”/“保存”）；④ 操作菜单“删除”**不加粗**，仍以红色文字、分隔线与 hover/focus 体现危险语义。**分层状态**：本轮草案基线 `adjustment3_baseline_status=DRAFT_PENDING_USER_REVIEW`、`adjustment3_approval_status=NOT_APPROVED`、本轮实现 `adjustment3_implementation_status=NOT_STARTED`、本轮验收 `NOT_RUN`（`CCFG-AC-105~117` 13 条新增，全文件 117 条全部 `NOT_RUN`）；覆盖更新为 122/122。**历史不被改写**：第一轮、第二轮已批准基线与两轮实现事实保持真实，除目标标注的 `CCFG-REQ-016`/`CCFG-AC-011`/`CCFG-UI-009`/`CCFG-UI-024` 外，既有 112/104/53/42 条定义行**逐字节零变化**。**本轮不改** `API.md`/`DATABASE.md`、**不改**任何业务代码/测试/前端或后端源文件、**不改** `docs/baseline/**`、`docs/features/README.md`、历史报告与 `docs/prompts/**`，**不改**模板级全局状态；`PENDING_USER_CONFIRMATION=0`。**用户同意四项 ≠ 文档已批准 ≠ 已实现 ≠ 已验收**（详见 §1.7）；参考页 `/config/data-source` **仅作视觉对照且未被修改**。
- 第三轮 `+N` 清单与新增／编辑弹窗视觉调整基线**批准收口**（2026-09-23，`CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-APPROVAL-CLOSEOUT-001`，纯文档，对应报告见 §2 导航）：ChatGPT 从远程 Git 对第三轮 R1 纠错提交 `1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244` 的复审结论为 `APPROVED`，项目负责人于 2026-09-23 明确回复“批准”，第三轮调整基线由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`（`adjustment3_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment3_approval_date=2026-09-23`、`adjustment3_approved_reviewed_commit=1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244`）。**批准的是第三轮调整基线，不是代码、测试、目测结果或最终接受**：`adjustment3_implementation_status=NOT_STARTED` 保持，第一轮、第二轮已批准基线与两轮实现事实 `IMPLEMENTED_PENDING_USER_ACCEPTANCE` 保持，117 条验收全部 `NOT_RUN`、覆盖 122/122、`PENDING_USER_CONFIRMATION=0` 保持；本轮四类定义行（`CCFG-REQ-001~122`/`CCFG-AC-001~117`/`CCFG-DESIGN-001~060`/`CCFG-UI-001~049`）相对提交 `1df0ef7` **逐字节零变化**；`API.md`/`DATABASE.md` 未修改，两套模板的模板级全局状态保持不变，数据源管理参考页自身未修改（详见 §1.7）。

- 第三轮 `+N` 清单与新增／编辑弹窗视觉调整**实现**（2026-09-23，`CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-IMPLEMENTATION-001`，前端实现任务，对应报告见 §2 导航）：按已批准基线实现 `/config/client` 的 `+N` 完整清单弹层与新增／编辑探针弹窗视觉调整，`adjustment3_implementation_status` 由 `NOT_STARTED` 变为 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`。**实现已完成，但不等同于已目测、已验收或已接受**：三份既有实现事实（`existing_feature_implementation_status`/`adjustment_implementation_status`/`adjustment2_implementation_status` 均为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`）与第一轮、第二轮已批准基线保持；全部 117 条验收仍 `NOT_RUN`，覆盖 122/122、`PENDING_USER_CONFIRMATION=0` 保持；`API.md`/`DATABASE.md` 未修改，两套模板的模板级全局状态与数据源管理参考页最终接受状态保持不变。实现细节与分层状态见 §1.8。现实浏览器核对结论 `BROWSER_VERIFIED_HEADLESS`（数据经接口桩注入，属 Agent 自测证据，非项目负责人目测或正式验收）。**当前下一入口**为 `CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_IMPLEMENTATION_REVIEW`（见 §5）。（**时序说明（2026-09-23 追加，不改写上述历史表述）**：该实现（含 R1 裁切修复）其后已以普通提交 `6cf7a554b457ed6c8edb827ef1971054df3354fb` 提交并推送到 `origin/develop`；ChatGPT 从远程 Git 复审该提交时，**代码实现与 R1 裁切修复通过**，**整次提交的文档复审为 `CHANGES_REQUIRED`**（原因仅为本 README §1.8 映射表引用错位与 R1 时序表述），现处于**远程复审后的文档纠错阶段**，**当前下一入口**见 §5 末尾；117 条验收仍全部 `NOT_RUN`，正式验收未执行，项目负责人尚未目测接受。）
- 第四轮新增／编辑探针弹窗校验与交互调整**草案**（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001`，纯文档，对应报告见 §2 导航）：依据项目负责人**已明确确认的调整规则**，为 `/config/client` 的**新增／编辑探针弹窗**（两模式共用）建立第四轮页面级调整草案——① 探针 ID／描述／采集数据源三类业务校验改为**字段级反馈**（对应控件红框 + 正下方红字，一次校验全部字段并定位第一个错误，修正即清除，取消页面顶端临时消息），主提交按钮**始终为黑色可点击态**（未选数据源不预先禁用、点击后显示字段级错误、请求中防重复且加载态不跳色），可归属字段的业务错误落字段下方、不可归属的系统错误可保留全局提示；② 未选数据源且未尝试提交时显示**中性灰色**“至少选择 1 个数据源”，选中即隐藏、全取消即恢复，提交后仍未选则转**红色错误**，字段反馈区**预留稳定空间**避免底边／底部按钮跳动；③ 弹窗相对**浏览器可见视口水平居中**（不以写死侧栏宽度偏移量修补，保留约 900px 与窄视口安全边距），`探针 ID`／`探针描述`／`采集数据源` 等配置项名称统一**右对齐**；④ **探针 ID 输入／粘贴上限 32 位**（超长不再无限录入、非法字符以字段级错误指出），**探针描述上限 256 个字符**（256 汉字同为 256 字符，按 Unicode 完整字符截断不破坏字符，输入提示改为“最多 256 个字符”，保留原文保存与后端 UTF-8 `<=1024` 字节校验，不改为 256 字节），**“自动生成”**先按原顺序生成完整描述再**直接保留前 256 个字符**（不失败、不弹超长提示），**编辑历史记录**超 256 字符时**仅回显前 256 字符草稿**、不写库不改原记录。**分层状态**：本轮草案基线 `adjustment4_baseline_status=DRAFT_PENDING_USER_REVIEW`、本轮批准 `adjustment4_approval_status=NOT_APPROVED`、本轮实现 `adjustment4_implementation_status=NOT_STARTED`、本轮验收 `adjustment4_formal_acceptance_execution_status=NOT_RUN`（`CCFG-AC-118~135` 18 条新增，全文件 135 条全部 `NOT_RUN`）；覆盖更新为 136/136 与 135/135。**历史不被改写**：第一轮、第二轮、第三轮已批准基线与三轮实现事实保持真实；除定向修订的 `CCFG-REQ-037`/`-039`/`-060`、`CCFG-DESIGN-059`、`CCFG-UI-013`/`-015`/`-017` 外，既有 122/117/60/49 条定义行**逐字节零变化**（既有验收定义行零修订）。**本轮不改** `API.md`/`DATABASE.md`、**不改**任何业务代码/测试/前端或后端源文件、**不改** `docs/baseline/**`、`docs/features/README.md`、历史报告与 `docs/prompts/**`，**不改**模板级全局状态；`PENDING_USER_CONFIRMATION=0`。**已建立草案 ≠ 已批准 ≠ 已实现 ≠ 已验收**（详见 §1.9）；参考页 `/config/data-source` **仅作视觉对照且未被修改**，其他页面**未被授权**。
- 第四轮新增／编辑探针弹窗校验与交互调整基线**批准收口**（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001`，纯文档，对应报告见 §2 导航）：时序可追溯——① R0 草案提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 的 ChatGPT 远程复审为 `CHANGES_REQUIRED`；② R1 定向纠错提交 `9daf03848d53008d3ac73e6a43c1368fa5d72750` 的 ChatGPT 远程**基线文档复审**为 `APPROVED`；③ 项目负责人随后于 2026-09-24 明确回复“批准”。第四轮调整基线由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`（`adjustment4_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment4_approval_date=2026-09-24`、`adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750`）。**批准的是第四轮页面级调整基线，不是代码、测试、目测结果或最终接受**：`adjustment4_implementation_status=NOT_STARTED` 保持，第一至第三轮已批准基线与三轮实现事实（`existing_feature_implementation_status`/`adjustment_implementation_status`/`adjustment2_implementation_status` 均为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`，`adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`）保持真实不改写，全文件 135 条验收全部 `NOT_RUN`、覆盖 136/136 与 135/135、`PENDING_USER_CONFIRMATION=0` 保持；四类定义行（`CCFG-REQ-001~136`/`CCFG-AC-001~135`/`CCFG-DESIGN-001~071`/`CCFG-UI-001~059`）相对提交 `9daf038` **逐字节零变化**；`API.md`/`DATABASE.md` 未修改，两套模板的模板级全局状态保持不变，数据源管理参考页自身未修改。曾处于 `DRAFT_PENDING_USER_REVIEW`/`NOT_APPROVED` 及旧 `next_entry` 的表述作为历史时点照实保留（详见 §1.9）。
- 第四轮新增／编辑探针弹窗校验与交互调整**实现**（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001`，前端实现任务，对应报告见 §2 导航）：按已批准的第四轮基线实现 `/config/client` 新增／编辑探针弹窗的字段级校验、数据源提示与布局稳定性、弹窗视口居中与配置项名称右对齐、探针 ID 32 位与描述 256 字符输入／自动生成截断／历史草稿回显，`adjustment4_implementation_status` 由 `NOT_STARTED` 变为 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`。**实现已完成，但不等同于已目测、已验收或已接受**：`existing_feature_implementation_status` 与第一轮、第二轮实现事实 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`、第三轮 `adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW` 及第一至第三轮已批准基线保持；全文件 135 条验收仍 `NOT_RUN`，覆盖 136/136 与 135/135、`PENDING_USER_CONFIRMATION=0` 保持；四类定义行相对 `b35ea697` **逐字节零变化**；`API.md`/`DATABASE.md` 未修改，两套模板的模板级全局状态与数据源管理参考页最终接受状态保持不变。定向测试 136/136、全量前端测试 1116 通过、前端构建成功；真实无头浏览器核对结论 `BROWSER_VERIFIED_HEADLESS`（数据经接口桩注入，属 Agent 自测证据，非项目负责人目测或正式验收）。实现细节与分层状态见 §1.10。**该时点下一入口**为 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_IMPLEMENTATION_REVIEW`（该入口其后已完成，ChatGPT 远程代码复审结论 `CHANGES_REQUIRED`，现行下一入口见下条）。
- 第四轮新增／编辑探针弹窗校验与交互调整**实现 R1 定向纠偏**（2026-09-24，`CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001-R1`，前端纠偏任务，对应报告见 §2 导航）：按 ChatGPT 对实现提交 `9da9620` 的远程代码复审结论 `CHANGES_REQUIRED`，**仅**修正新增／编辑弹窗主提交按钮在提交请求处理中由黑色跳为 Element Plus 主色浅蓝（`rgb(160,207,255)`）的**蓝黑跳色**问题——为 `.cc-dialog-submit` 增加**仅加载态**定向黑色系深灰（`#3f3f46`）样式并中和 EP 加载态白色遮罩，**不**改正常态 `:not(.is-disabled)`、**不**新增全局覆盖、**不**用 `!important`、**不**改防重复逻辑与按钮文案，**不**修改任何需求／验收／设计／UI 定义行。真实无头浏览器在新增与编辑两条路径上均核对常态／挂起／结束三态为黑色系（`rgb(9,9,11)` → `rgb(63,63,70)` → `rgb(9,9,11)`，全程无蓝），挂起期间连点 3 次写入请求仍各 1 次。定向测试 `ClientConfigPage.spec.ts` **140/140 通过**、全量前端测试 57 文件 / **1120 用例通过**、`npm run build` 成功；四类定义行相对 `9da9620` **逐字节零变化**，135 条验收仍**全部 `NOT_RUN`**，`PENDING_USER_CONFIRMATION=0` 保持；`adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW` 保持不变（仍停在远程代码复审入口）。**纠偏完成 ≠ 已目测 ≠ 已验收**：项目负责人后续仍须对实际页面做目测；两套模板的模板级全局状态与数据源管理参考页最终接受状态不变，**其他页面未被授权**。**当前下一入口**为 `CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_IMPLEMENTATION_R1_REVIEW`（见 §5）。

## 5. 下一入口

设计并发口径调整任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001` 已于 2026-09-04 完成（纯文档）：从四份设计草案（`DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md`）中移除已过时的 `LOCK TABLE ... WAIT 5`、`ORA-30006→50050` 及并发“最多一个成功”等设计，按已重新批准的需求并发口径调整设计并发表述（需求与验收已随本轮并发口径调整重新批准，ChatGPT 对 R1 结果提交 `f2a4d7d...` 正式复审 `APPROVED`，项目负责人于 2026-09-04 明确回复“批准”，`REQUIREMENTS.md`/`ACCEPTANCE.md` 已收口为 `APPROVED`）。设计并发口径调整结果已于 2026-09-04 完成正式设计复审与批准收口：ChatGPT 对提交 `ba7c5e9...` 的设计并发口径调整结果正式复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）结论 `APPROVED`，项目负责人于 2026-09-04 明确回复“批准”，四份设计文档经批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001` 收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`，整体标记为 `APPROVED_READY_FOR_IMPLEMENTATION`（对应报告见 §2 导航）。下一入口为 `CLIENT_CONFIG_IMPLEMENTATION`：以已批准的需求（90 条）、验收（76 条、全部 `NOT_RUN`）与四份设计（37/20/26/22）为唯一业务基线进入实现阶段；该实现随后已完成（见 §4 既有实现事实 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`），76 条验收保持全部 `NOT_RUN`；不得把已批准设计写成测试已通过或验收已通过。

**历史下一入口（第二轮视觉调整基线批准收口起，覆盖以上历史入口；该入口其后已完成、结论 `APPROVED`）**：`CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_BASELINE_APPROVAL_CLOSEOUT_REVIEW`（对象为本轮批准收口任务 `CLIENT-CONFIG-VISUAL-FOLLOWUP-BASELINE-APPROVAL-CLOSEOUT-001` 的结果提交）。本任务是**纯文档批准收口**：统一第二轮五项视觉调整的当前分层状态为 `adjustment2_baseline_status=APPROVED`、`adjustment2_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment2_approval_date=2026-09-23`（复审对象提交 `3830cba16142b4e2ad88f1fa96682ea8397a1203`，结论 `APPROVED`——R1 纠错通过）；批准范围为 `/config/client` 主列表五项（黑色新增按钮、ID 正文对齐、行高跟随参考页、采集数据源标签绿/红/中性三态且行级歧义警示保持红色、操作列水平三点图标及改进菜单）；覆盖 `CCFG-REQ-104~112`（9 条）、`CCFG-AC-090~104`（15 条）、`CCFG-DESIGN-047~053`（7 条）、`CCFG-UI-036~042`（7 条）；编号总数保持 需求 112 / 验收 104 / 设计 53 / UI 42，覆盖 112/112，**104 条验收全部 `NOT_RUN`**，`PENDING_USER_CONFIRMATION=0`。**状态分层**：第二轮基线 `adjustment2_baseline_status=APPROVED`（本轮实现 `adjustment2_implementation_status=NOT_STARTED`）；第一轮已批准基线与既有实现事实 `IMPLEMENTED_PENDING_USER_ACCEPTANCE` 保持真实不改写。**下一步须由 ChatGPT 从远程 Git 对本收口结果做独立复审**；**不得**把本轮基线批准写成已实现、已目测或已验收，也不得在复审通过前进入第二轮代码实现。参考页 `/config/data-source` **仅作视觉对照**，本轮规则只作用于 `/config/client` 主列表，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_BASELINE_V2_R1_REVIEW`（V2 草案 R1 纠错结果的远程复审，现已完成、结论 `APPROVED`，提交 SHA 由 ChatGPT 从远程 Git 取得，本文件不预造）、更早的 `CHATGPT_REMOTE_CLIENT_CONFIG_PAGE_ADJUSTMENT_IMPLEMENTATION_REVIEW`（2026-09-23 起，该实现已获 ChatGPT 远程代码复审通过）与 `CHATGPT_REMOTE_BASELINE_APPROVAL_CLOSEOUT_R2_REVIEW`、`CHATGPT_REMOTE_BASELINE_R1_REVIEW` 保留不改写。

**历史下一入口（第二轮视觉调整实现起，覆盖以上历史入口；该入口其后已完成、ChatGPT 远程代码复审结论 `APPROVED`）**：`CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_IMPLEMENTATION_REVIEW`（对象为本轮实现任务 `CLIENT-CONFIG-VISUAL-FOLLOWUP-IMPLEMENTATION-001` 的结果提交）。该任务为**前端实现任务**（非纯文档）：按已批准的第二轮五项视觉调整基线实现 `/config/client` **主列表**，`adjustment2_implementation_status` 由 `NOT_STARTED` 变为 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`；范围**仅** `/config/client` 主列表（黑色实心“新增探针”、探针 ID 正文字重颜色对齐、移除本页固定像素行高、采集数据源标签绿/红/中性三态且独立行级歧义警示保持红色、操作列水平三点图标及改进菜单）；覆盖 `CCFG-REQ-104~112`（9 条）、`CCFG-AC-090~104`（15 条）、`CCFG-DESIGN-047~053`（7 条）、`CCFG-UI-036~042`（7 条）；编号总数保持 需求 112 / 验收 104 / 设计 53 / UI 42，覆盖 112/112，**104 条验收全部 `NOT_RUN`**，`PENDING_USER_CONFIRMATION=0`。**状态分层**：第二轮基线 `adjustment2_baseline_status=APPROVED` 与该时点实现状态 `adjustment2_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`（**完成时点历史值**；**当前值**为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`）并存；第一轮已批准基线与既有实现事实 `IMPLEMENTED_PENDING_USER_ACCEPTANCE` 保持真实不改写。该入口的**下一步**（由 ChatGPT 从远程 Git 对本实现结果提交做独立代码复审）**其后已完成、结论 `APPROVED`**；**不得**把本次实现写成已目测、已正式验收或已获项目负责人接受（当前仍为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`、等待项目负责人页面目测/接受）。参考页 `/config/data-source` **仅作视觉对照且未被修改**，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_BASELINE_APPROVAL_CLOSEOUT_REVIEW`（第二轮基线批准收口的远程复审，现已完成、结论 `APPROVED`）、`CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_BASELINE_V2_R1_REVIEW`、`CHATGPT_REMOTE_CLIENT_CONFIG_PAGE_ADJUSTMENT_IMPLEMENTATION_REVIEW`、`CHATGPT_REMOTE_BASELINE_APPROVAL_CLOSEOUT_R2_REVIEW`、`CHATGPT_REMOTE_BASELINE_R1_REVIEW` 保留不改写。

**历史下一入口（第三轮 `+N` 清单与弹窗视觉调整草案建立起，覆盖以上历史入口；该入口其后已完成、ChatGPT 复审结论 `CHANGES_REQUIRED`，由 R1 纠错任务承接）**：`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_REVIEW`（对象为本轮草案任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001` 的结果提交）。本任务是**纯文档草案建立**：为 `/config/client` 的 `+N` 完整清单弹层与新增／编辑探针弹窗建立第三轮调整草案，状态统一为 `adjustment3_baseline_status=DRAFT_PENDING_USER_REVIEW`、`adjustment3_approval_status=NOT_APPROVED`、`adjustment3_implementation_status=NOT_STARTED`、`adjustment3_formal_acceptance_execution_status=NOT_RUN`；四项调整范围为 `+N` 清单项两级内容与自然增高（移除 `.cc-full-list` 固定最大高度与内滚）、新增／编辑弹窗放大至桌面约 900px 与数据源区调整、配置项名称与底部主提交按钮对齐数据源管理（文案仍“创建”/“保存”）、操作菜单“删除”不加粗；覆盖 `CCFG-REQ-113~122`（10 条）、`CCFG-AC-105~117`（13 条）、`CCFG-DESIGN-054~060`（7 条）、`CCFG-UI-043~049`（7 条），并**目标标注修订** `CCFG-REQ-016`/`CCFG-AC-011`/`CCFG-UI-009`/`CCFG-UI-024`；编号总数为 需求 122 / 验收 117 / 设计 60 / UI 49，覆盖 122/122，**117 条验收全部 `NOT_RUN`**，`PENDING_USER_CONFIRMATION=0`。**状态分层**：第一轮已批准基线与既有实现事实 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`、第二轮已批准基线 `adjustment2_baseline_status=APPROVED` 与第二轮实现（完成时点历史值 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`；**当前值** `IMPLEMENTED_PENDING_USER_ACCEPTANCE`）**保持真实不改写**。**下一步须由 ChatGPT 从远程 Git 对本草案结果提交做独立复审**；**不得**把本轮草案写成已批准、已实现、已目测或已验收，也不得在复审与项目负责人批准前进入本轮代码实现。参考页 `/config/data-source` **仅作视觉对照且未被修改**，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_IMPLEMENTATION_REVIEW`（第二轮视觉调整实现的远程代码复审，现已完成、结论 `APPROVED`）、`CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_BASELINE_APPROVAL_CLOSEOUT_REVIEW`、`CHATGPT_REMOTE_CLIENT_CONFIG_VISUAL_FOLLOWUP_BASELINE_V2_R1_REVIEW`、`CHATGPT_REMOTE_CLIENT_CONFIG_PAGE_ADJUSTMENT_IMPLEMENTATION_REVIEW`、`CHATGPT_REMOTE_BASELINE_APPROVAL_CLOSEOUT_R2_REVIEW`、`CHATGPT_REMOTE_BASELINE_R1_REVIEW` 保留不改写。

**历史下一入口（第三轮草案 R1 定向纠错起，覆盖以上历史入口；该入口其后已完成、ChatGPT 复审结论 `APPROVED`）**：`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_R1_REVIEW`（对象为 R1 纠错任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-001-R1` 的结果提交）。本任务为**纯文档 R1 纠错**：ChatGPT 从远程 Git 对 R0 草案提交 `a585071d86eeee47dbd8d986fdb6f90b32161033` 的复审结论为 `CHANGES_REQUIRED`，R1 仅定向修订三处——① `ACCEPTANCE.md` 的 `CCFG-AC-113` 验收步骤（**新增模式**核对探针 ID 可直接填写、**编辑模式**核对默认锁定与经“修改探针 ID”解锁/“取消修改”恢复锁定，不改页面行为）；② R0 报告 §4.2 需求侧计数口径勘误（由新增 R1 报告以 errata 声明，**不**回写 R0 历史报告）；③ 五份 Feature 文档中作为**当前**值的第二轮实现状态与现行下一入口同步（`adjustment2_implementation_status=IMPLEMENTED_PENDING_USER_ACCEPTANCE`，保留其完成时点历史值 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`）。**本轮四项产品决定不重新设计**；除 `CCFG-AC-113` 外，需求 122 / 验收 117 / 设计 60 / UI 49 条定义行相对 `a585071` **逐字节一致**，R0 已定向标注的四条旧定义行不回退；覆盖保持 122/122、**117 条验收全部 `NOT_RUN`**、`PENDING_USER_CONFIRMATION=0`。**状态分层**：第一轮已批准基线与既有实现事实、第二轮已批准基线与第二轮实现当前值 `IMPLEMENTED_PENDING_USER_ACCEPTANCE` **保持真实不改写**；第三轮仍 `adjustment3_baseline_status=DRAFT_PENDING_USER_REVIEW`、`adjustment3_approval_status=NOT_APPROVED`、`adjustment3_implementation_status=NOT_STARTED`。**下一步须由 ChatGPT 从远程 Git 对本 R1 结果提交做独立复审**（该复审**其后已完成、结论 `APPROVED`**，R1 纠错通过）；**不得**把本轮草案写成已批准、已实现、已目测或已验收，也不得在复审与项目负责人批准前进入本轮代码实现。参考页 `/config/data-source` **仅作视觉对照且未被修改**，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_REVIEW`（R0 草案远程复审，现已完成、结论 `CHANGES_REQUIRED`），其余历史入口照实保留不改写。

**历史下一入口（第三轮实现起，覆盖以上历史入口；该入口其后已完成）**：`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_IMPLEMENTATION_REVIEW`（对象为实现任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-IMPLEMENTATION-001` 的结果提交）。该任务为**前端实现任务**（非纯文档）：按已批准基线实现 `/config/client` 的 `+N` 完整清单弹层与新增／编辑探针弹窗视觉调整，`adjustment3_implementation_status` 由 `NOT_STARTED` 变为 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`；范围**仅** `/config/client`（`+N` 清单两级信息与自然增高、移除 `.cc-full-list` 固定最大高度与内滚、弹窗放大至 900px 与数据源区调整、弹窗标签与底部主提交按钮对齐数据源管理、操作菜单“删除”不加粗）；覆盖 `CCFG-REQ-113~122`（10 条）、`CCFG-AC-105~117`（13 条）、`CCFG-DESIGN-054~060`（7 条）、`CCFG-UI-043~049`（7 条）；编号总数保持 需求 122 / 验收 117 / 设计 60 / UI 49，覆盖 122/122，**117 条验收全部 `NOT_RUN`**，`PENDING_USER_CONFIRMATION=0`；四类定义行相对 `cf0817f` **逐字节零变化**（含 R1 修订的 `CCFG-AC-113` 与 R0 定向标注的 `CCFG-REQ-016`/`CCFG-AC-011`/`CCFG-UI-009`/`CCFG-UI-024` 不回退）。**状态分层**：第三轮基线 `adjustment3_baseline_status=APPROVED` 与该时点实现状态 `adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW` 并存，本轮正式验收 `adjustment3_formal_acceptance_execution_status=NOT_RUN`；第一轮、第二轮已批准基线与三份实现事实 `IMPLEMENTED_PENDING_USER_ACCEPTANCE` 保持真实不改写。**该入口的下一步**（由 ChatGPT 从远程 Git 对本实现结果提交做独立代码复审）**其后已完成**——ChatGPT 从远程 Git 读取提交 `6cf7a554b457ed6c8edb827ef1971054df3354fb`（相对父提交 `cf0817f2` 恰一提交、九文件），结论为**代码实现与 R1 裁切修复通过**、**整次提交的文档复审 `CHANGES_REQUIRED`**（见本节末当前下一入口）；**不得**把本次实现写成已目测、已正式验收或已获项目负责人接受。参考页 `/config/data-source` **仅作视觉对照且未被修改**，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_APPROVAL_CLOSEOUT_REVIEW`（第三轮基线批准收口的远程复审，现已完成、结论 `APPROVED`）、`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_R1_REVIEW`、`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_REVIEW` 与其余历史入口照实保留不改写。

**历史下一入口（第三轮批准收口起，其入口其后已完成、ChatGPT 复审结论 `APPROVED`，由第三轮实现任务承接）**：`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_APPROVAL_CLOSEOUT_REVIEW`（对象为批准收口任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-BASELINE-APPROVAL-CLOSEOUT-001` 的结果提交）。本任务是**纯文档批准收口**：ChatGPT 从远程 Git 对第三轮 R1 纠错提交 `1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244` 的复审结论为 `APPROVED`，项目负责人于 2026-09-23 明确回复“批准”，第三轮调整基线统一收口为 `adjustment3_baseline_status=APPROVED`、`adjustment3_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment3_approval_date=2026-09-23`、`adjustment3_approved_reviewed_commit=1df0ef7b4e3717d9b980e9aa9cc1f8e4f036f244`；批准范围为 `/config/client` 的 `+N` 清单与新增／编辑弹窗视觉调整四项（`+N` 清单项两级内容与自然增高、弹窗放大至约 900px 与数据源区调整、配置项名称与主按钮对齐数据源管理、菜单“删除”不加粗）；覆盖 `CCFG-REQ-113~122`（10 条）、`CCFG-AC-105~117`（13 条）、`CCFG-DESIGN-054~060`（7 条）、`CCFG-UI-043~049`（7 条）；编号总数保持 需求 122 / 验收 117 / 设计 60 / UI 49，覆盖 122/122，**117 条验收全部 `NOT_RUN`**，`PENDING_USER_CONFIRMATION=0`；四类定义行相对 `1df0ef7` **逐字节零变化**（含 R1 修订的 `CCFG-AC-113` 与 R0 定向标注的 `CCFG-REQ-016`/`CCFG-AC-011`/`CCFG-UI-009`/`CCFG-UI-024` 不回退）。**状态分层**：第三轮基线 `adjustment3_baseline_status=APPROVED`（本轮实现 `adjustment3_implementation_status=NOT_STARTED`、本轮正式验收 `adjustment3_formal_acceptance_execution_status=NOT_RUN`）；第一轮、第二轮已批准基线与三份实现事实（`existing_feature_implementation_status`/`adjustment_implementation_status`/`adjustment2_implementation_status` 均为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`）保持真实不改写，不得把本轮基线批准写成第三轮已实现、已目测或已验收。**下一步须由 ChatGPT 从远程 Git 对本收口结果做独立复审**；只有该复审通过后才安排第三轮代码实现任务；**不得**在复审通过前进入本轮代码实现。参考页 `/config/data-source` **仅作视觉对照且未被修改**，本轮规则只作用于 `/config/client`，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_R1_REVIEW`（R1 纠错结果的远程复审，现已完成、结论 `APPROVED`，提交 SHA 由 ChatGPT 从远程 Git 取得，本文件不预造）、`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_BASELINE_REVIEW`（R0 草案复审，结论 `CHANGES_REQUIRED`）与其余历史入口照实保留不改写。

**历史下一入口（远程复审后的 README 文档纠错起，覆盖以上历史入口；该入口其后已完成——该未提交工作区差异已以普通提交 `cb9009a88a1fa4abb2b65a4ea850c5e881e46529`（父提交 `6cf7a554b457ed6c8edb827ef1971054df3354fb`）提交并推送到 `origin/develop`，本文件不预造其复审结论）**：`WORKTREE_DIFF_REVIEW_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_REMOTE_REVIEW_DOC_R1`（对象为文档纠错任务 `CLIENT-CONFIG-POPOVER-AND-DIALOG-VISUAL-REMOTE-REVIEW-DOC-R1` 的**未提交工作区差异**；该工作区差异已完成提交与推送，见上方括注）。本任务是**纯文档纠错**（仅 `README.md`）：ChatGPT 已从远程 Git 读取提交 `6cf7a554b457ed6c8edb827ef1971054df3354fb`（相对父提交 `cf0817f23c77190275b7f58c755c4e2c4619adc9` 恰一提交、九文件），**代码实现与 R1 裁切修复的远程复审通过**，**整次提交的文档复审为 `CHANGES_REQUIRED`**，原因仅为 ① §1.8 的“已批准条款／实现落点／结果”表把 `CCFG-REQ` 与 `CCFG-AC`／`CCFG-DESIGN`／`CCFG-UI` 的引用错位（已按 `REQUIREMENTS.md`/`ACCEPTANCE.md`/`DESIGN.md`/`UI.md` 的**已批准定义行**逐项修正，并把原先合并过宽的行拆分为 `CCFG-REQ-113~122` 逐条对应，原有实现事实与结果保留）；② §1.8 中“尚未提交、尚未推送”的表述已不能作为当前事实（已限定为 R1 修复当时的历史状态，并在同节与本节补充当前阶段）。**状态分层**：`adjustment3_baseline_status=APPROVED`、`adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`、`adjustment3_formal_acceptance_execution_status=NOT_RUN`、117 条验收全部 `NOT_RUN`、`PENDING_USER_CONFIRMATION=0` **保持不变**；四类定义行（122/117/60/49）相对 `cf0817f2` **逐字节零变化**；本次纠错**不**改写历史报告、**不**修改代码、测试或任何定义行。**该入口的下一步**为由 ChatGPT 对本次**未提交工作区差异**做复审；**不得**把本次纠错写成验收通过、页面已目测或已获最终接受，本任务亦**不**获提交或推送授权（旧的提交／推送授权已在 `6cf7a55` 用完）。参考页 `/config/data-source` **仅作视觉对照且未被修改**，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_IMPLEMENTATION_REVIEW`（第三轮实现（含 R1 裁切修复）的远程复审，现已完成：代码与裁切修复通过、整次提交文档复审 `CHANGES_REQUIRED`）及其余历史入口照实保留不改写。

**历史下一入口（第四轮新增／编辑弹窗校验与交互调整草案建立起，覆盖以上历史入口；该入口其后已完成、ChatGPT 从远程 Git 复审结论 `CHANGES_REQUIRED`，由 R1 定向纠错任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001-R1` 承接，现行下一入口见下方）**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_REVIEW`（对象为第四轮草案任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001` 的结果提交）。本任务是**纯文档草案建立**（未改代码、未执行验收、未启停服务、未访问数据库／ZooKeeper／Kafka）：按项目负责人**已明确确认的调整规则**，为 `/config/client` 的**新增／编辑探针弹窗**建立第四轮页面级调整草案，覆盖四项——① 探针 ID／描述／采集数据源的**字段级校验反馈**（对应控件红框 + 正下方红字、一次校验全部字段并定位第一个错误、修正即清除、取消页面顶端临时消息、可归属字段错误落字段下方）与**主提交按钮黑色可点击态**（表单不完整仍可点击并于点击后显示字段级错误、请求中防重复且加载态不跳色、不因未选数据源预先禁用）；② 未选数据源的**中性灰色说明**“至少选择 1 个数据源”（选中隐藏、全取消恢复、提交后仍未选转红色错误）与**字段反馈区稳定空间**；③ 弹窗相对**浏览器可见视口水平居中**（不以写死侧栏宽度偏移量修补）与配置项名称统一**右对齐**；④ **探针 ID 32 位**、**探针描述 256 字符**（Unicode 完整字符、输入与粘贴同限、输入提示更新、保留后端 UTF-8 `<=1024` 字节校验）、**自动生成保留前 256 字符**与**编辑历史记录仅回显前 256 字符草稿**。覆盖 `CCFG-REQ-123~136`（14 条）、`CCFG-AC-118~135`（18 条）、`CCFG-DESIGN-061~071`（11 条）、`CCFG-UI-050~059`（10 条）；本轮**定向修订** `CCFG-REQ-037`/`-039`/`-060`、`CCFG-DESIGN-059`、`CCFG-UI-013`/`-015`/`-017`（原文保留不改写）；编号总数为 需求 **136** / 验收 **135** / 设计 **71** / UI **59**，覆盖 **136/136** 与 **135/135**，**135 条验收全部 `NOT_RUN`**（本轮不执行验收），`PENDING_USER_CONFIRMATION=0`。**状态分层**：`adjustment4_baseline_status=DRAFT_PENDING_USER_REVIEW`、`adjustment4_approval_status=NOT_APPROVED`、`adjustment4_implementation_status=NOT_STARTED`、`adjustment4_formal_acceptance_execution_status=NOT_RUN`；第一轮、第二轮、第三轮已批准基线与三轮实现事实（`existing_feature_implementation_status`/`adjustment_implementation_status`/`adjustment2_implementation_status` 均为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`，`adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`）**保持真实不改写**。**下一步须由 ChatGPT 从远程 Git 对本草案结果提交做独立复审**；**不得**把本草案写成已批准、已实现、已目测或已验收，也不得在复审与项目负责人批准前进入本轮代码实现。参考页 `/config/data-source` **仅作视觉对照且未被修改**，本轮规则只作用于 `/config/client` 的新增／编辑弹窗，两套模板的模板级全局状态保持不变，**其他页面未被授权**；项目负责人认可的“新增／编辑表单弹窗模板”方向**须在本轮实现并经项目负责人目测认可后**另开任务提炼，本轮仅在文档登记入口。**历史入口**：`WORKTREE_DIFF_REVIEW_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_REMOTE_REVIEW_DOC_R1`（远程复审后的 README 文档纠错工作区差异，其后已完成提交与推送）、`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_IMPLEMENTATION_REVIEW` 与其余历史入口照实保留不改写。**该入口的下一步其后已完成**——ChatGPT 从远程 Git 读取第四轮草案提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880`，复审结论为 `CHANGES_REQUIRED`，仅涉及三处文档一致性问题（按钮旧规则与新规则冲突、`CCFG-AC-131` 不可能验收输入、`CCFG-REQ-136` 追踪映射不实），由 R1 定向纠错任务承接；本历史条目**不**宣布第四轮已批准、已实现或已验收。

**历史下一入口（第四轮弹窗校验与交互调整草案 R1 定向纠错起，覆盖以上历史入口；该入口其后已完成——ChatGPT 从远程 Git 对 R1 结果提交 `9daf03848d53008d3ac73e6a43c1368fa5d72750` 复审结论 `APPROVED`，项目负责人 2026-09-24 批准，现行下一入口见下方）**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_R1_REVIEW`（对象为 R1 纠错任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-001-R1` 的结果提交）。本任务是**纯文档 R1 纠错**（未改代码、未执行验收、未运行测试／构建／浏览器、未启停服务、未访问数据库／ZooKeeper／Kafka）：ChatGPT 从远程 Git 对 R0 草案提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 的复审结论为 `CHANGES_REQUIRED`，R1 仅修正三处一致性——① **按钮旧规则冲突**：`CCFG-REQ-120`/`CCFG-AC-115`/`CCFG-UI-047` 最小定向修订，区分“**表单未完成／校验未通过 → 仍可点击且呈黑色**”与“**提交请求处理中 → 暂不可重复提交、加载反馈清楚可辨、底色不蓝黑跳色**”，`CCFG-AC-115` 的“校验未通过预先禁用”场景标注为**由 `CCFG-AC-120` 取代**并给出**现行可执行验收步骤**，`CCFG-REQ-124`/`CCFG-AC-120`/`CCFG-DESIGN-062`/`CCFG-UI-051` 把“始终”限定为**非提交处理中**的常态，两条验收期望**同时成立、无冲突**；② **`CCFG-AC-131` 不可能输入**：256 个字符的 UTF-8 字节数**最多恰好 1024、不能超过 1024**，改写为三类互不重叠边界（256 汉字＝768 字节允许保存 / 恰好 256 个四字节码点＝恰好 1024 字节上限可接受 / 前端拦截第 257 字符且**绕过前端**直连后端提交 >1024 字节仍被拒绝），`VARCHAR2(1024 BYTE)` 契约与 Trim 仅判空、原文保存不变；③ **追踪映射不实**：`CCFG-REQ-136`/`CCFG-AC-135` 仅登记未来“新增／编辑表单弹窗模板”入口，属**文档治理／范围声明**、本轮**无新增 UI 元素**，从 `CCFG-UI-059` 与 `DESIGN.md` §12.1/§12.2 单元格**移除**无依据映射，改由 `CCFG-DESIGN-071` 承载。**本轮五组产品决定不重新设计**；除明列的定向行外，需求 136 / 验收 135 / 设计 71 / UI 59 条定义行相对 `51be9aa` **逐字节一致**，R0 已定向标注的旧定义行不回退；覆盖保持 **136/136** 与 **135/135**、**135 条验收全部 `NOT_RUN`**（本轮不执行验收）、`PENDING_USER_CONFIRMATION=0`；**不**回写 R0 报告。**状态分层**：`adjustment4_baseline_status=DRAFT_PENDING_USER_REVIEW`、`adjustment4_approval_status=NOT_APPROVED`、`adjustment4_implementation_status=NOT_STARTED`、`adjustment4_formal_acceptance_execution_status=NOT_RUN`；第一轮至第三轮已批准基线与三轮实现事实**保持真实不改写**。**下一步须由 ChatGPT 从远程 Git 对本 R1 结果提交做独立复审**；**不得**把本草案写成已批准、已实现、已目测或已验收，也不得在复审与项目负责人批准前进入本轮代码实现。参考页 `/config/data-source` **仅作视觉对照且未被修改**，本轮规则只作用于 `/config/client` 的新增／编辑弹窗，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_REVIEW`（R0 草案远程复审，现已完成、结论 `CHANGES_REQUIRED`）、`WORKTREE_DIFF_REVIEW_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_REMOTE_REVIEW_DOC_R1`（远程复审后的 README 文档纠错工作区差异，其后已完成提交与推送）、`CHATGPT_REMOTE_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_IMPLEMENTATION_REVIEW` 与其余历史入口照实保留不改写。

**历史下一入口（第四轮弹窗校验与交互调整基线批准收口起，覆盖以上历史入口；该入口对象为纯文档批准收口结果，其后第四轮基线已获批准并由独立实现任务承接，第四轮实现现状见下方当前下一入口）**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW`（对象为本批准收口任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-BASELINE-APPROVAL-CLOSEOUT-001` 的结果提交）。本任务是**纯文档批准收口**（未改代码、未执行验收、未运行测试／构建／浏览器、未启停服务、未访问数据库／ZooKeeper／Kafka）：R0 草案提交 `51be9aa66f0555da331b20b2ae5d4cc2f370f880` 的 ChatGPT 远程复审为 `CHANGES_REQUIRED`，R1 定向纠错提交 `9daf03848d53008d3ac73e6a43c1368fa5d72750` 的 ChatGPT 远程**基线文档复审**为 `APPROVED`，项目负责人随后于 2026-09-24 明确回复“批准”，第四轮调整基线统一收口为 `adjustment4_baseline_status=APPROVED`、`adjustment4_approval_status=APPROVED_BY_PROJECT_OWNER`、`adjustment4_approval_date=2026-09-24`、`adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750`；批准范围为 `/config/client` 新增／编辑探针弹窗的字段级校验错误呈现与主提交按钮黑色可点击态及提交中防重复、灰色／红色数据源提示与稳定布局、弹窗视口居中与配置项名称右对齐、探针 ID 32 位、描述 256 个 Unicode 码点（256 汉字可输入、按完整字符截断）、自动生成／历史编辑草稿截断、原有后端 UTF-8 `<=1024 BYTE` 防线及其验收定义，以及 R1 的按钮旧规则与新规则定向关系、`CCFG-AC-131` 可执行边界、后续模板入口追踪关系；覆盖 `CCFG-REQ-123~136`（14 条）、`CCFG-AC-118~135`（18 条）、`CCFG-DESIGN-061~071`（11 条）、`CCFG-UI-050~059`（10 条）；编号总数保持 需求 136 / 验收 135 / 设计 71 / UI 59，覆盖 136/136 与 135/135，**135 条验收全部 `NOT_RUN`**，`PENDING_USER_CONFIRMATION=0`；四类定义行相对 `9daf038` **逐字节零变化**，R0 的错误映射与不可能输入**不**回退。**状态分层**：`adjustment4_baseline_status=APPROVED`（`adjustment4_implementation_status=NOT_STARTED`、`adjustment4_formal_acceptance_execution_status=NOT_RUN`）；第一至第三轮已批准基线与三轮实现事实**保持真实不改写**。**下一步须由 ChatGPT 从远程 Git 对本收口结果提交做独立复审**；只有该复审完成后方可另行组织第四轮页面代码实现；**不得**在复审通过前进入本轮代码实现，也**不得**把本轮基线批准写成已实现、已目测或已验收。对“新增／编辑表单弹窗通用模板”仅保留后续入口——**须在本轮页面实现完成并经项目负责人实际目测认可后**另开任务提炼，本收口**不**创建、**不**批准、**不**推广任何模板。参考页 `/config/data-source` **仅作视觉对照且未被修改**，本轮规则只作用于 `/config/client` 的新增／编辑弹窗，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_R1_REVIEW`（R1 纠错结果的远程复审，现已完成、结论 `APPROVED`）、`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_REVIEW`（R0 草案远程复审，结论 `CHANGES_REQUIRED`）、`WORKTREE_DIFF_REVIEW_CLIENT_CONFIG_POPOVER_AND_DIALOG_VISUAL_REMOTE_REVIEW_DOC_R1` 与其余历史入口照实保留不改写。

**历史下一入口（第四轮新增／编辑弹窗校验与交互调整实现起，覆盖以上历史入口；该入口其后已完成——ChatGPT 从远程 Git 对实现提交 `9da96206cae51a891e326e586793e5e641082952` 复审结论 `CHANGES_REQUIRED`（仅主提交按钮加载态蓝黑跳色一项），已由 R1 定向纠偏任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001-R1` 承接，现行下一入口见下方）**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_IMPLEMENTATION_REVIEW`（对象为实现任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001` 的结果提交）。该任务为**前端实现任务**（非纯文档）：按第四轮已批准基线（`adjustment4_approved_reviewed_commit=9daf03848d53008d3ac73e6a43c1368fa5d72750`）实现 `/config/client` **新增／编辑探针弹窗**（两模式共用）的四项调整，`adjustment4_implementation_status` 由 `NOT_STARTED` 变为 `IMPLEMENTED_PENDING_CHATGPT_REVIEW`；范围**仅** `/config/client` 新增／编辑弹窗——① 探针 ID／描述／采集数据源三类业务校验改为字段级反馈（对应控件红框 + 正下方红字、一次校验全部字段并定位第一个错误、修正即清除或更新、取消同错误的顶端消息）、主提交按钮常态不预禁用且黑色可点击（点击后就地报错并拒绝写库）、提交处理中防重复且加载态由 Element Plus 呈现不被正常态规则覆盖、可归属字段的服务端错误码落字段下方而不可归属／网络错误保留全局提示；② 未选数据源的中性灰色“至少选择 1 个数据源”四态与 `.cc-field-feedback` 稳定预留空间（长文案完整换行不裁剪）；③ 弹窗相对浏览器可见视口水平居中（未写死侧栏宽度偏移量）与配置项名称统一右对齐；④ 探针 ID 上限 32 位、探针描述上限 256 个 Unicode 完整字符（输入与粘贴一致、输入提示更新、保留原文保存与后端 UTF-8 `<=1024 BYTE` 校验）、自动生成先拼接后保留前 256 字符、编辑历史记录仅回显前 256 字符草稿且不写库。覆盖 `CCFG-REQ-123~136`（14 条）、`CCFG-AC-118~135`（18 条）、`CCFG-DESIGN-061~071`（11 条）、`CCFG-UI-050~059`（10 条）；编号总数保持 需求 136 / 验收 135 / 设计 71 / UI 59，覆盖 136/136 与 135/135，**135 条验收全部 `NOT_RUN`**（本任务不执行正式验收），`PENDING_USER_CONFIRMATION=0`；四类定义行相对 `b35ea697` **逐字节零变化**，本轮定向修订的既有定义行不回退。**验证结果**：定向测试 `ClientConfigPage.spec.ts` 136/136 通过、全量前端测试 1116 通过、`npm run build` 成功、真实无头浏览器只读核对结论 `BROWSER_VERIFIED_HEADLESS`。**状态分层**：第四轮基线 `adjustment4_baseline_status=APPROVED` 与该时点实现状态 `adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW` 并存，本轮正式验收 `adjustment4_formal_acceptance_execution_status=NOT_RUN`；第一至第三轮已批准基线与三轮实现事实（`existing_feature_implementation_status`/`adjustment_implementation_status`/`adjustment2_implementation_status` 均为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`，`adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`）**保持真实不改写**。**下一步须由 ChatGPT 从远程 Git 对本实现结果提交做独立代码复审**；**不得**把本次实现写成已目测、已正式验收或已获项目负责人接受——项目负责人**后续仍须对实际页面做目测**。对“新增／编辑表单弹窗通用模板”仅保留后续入口：**须在本轮页面实现完成并经项目负责人实际目测认可后**另开任务提炼，本任务**不**创建、**不**批准、**不**推广任何模板；本轮**已知残余解释差异**（提交处理中按钮由 Element Plus 呈现为浅蓝加圆角，本实现依据保留条款 `CCFG-UI-047` 未覆盖该外观，详见 §1.10）**照实登记为待项目负责人目测确认项**，不擅自改判已批准基线。参考页 `/config/data-source` **仅作视觉对照且未被修改**，本轮规则只作用于 `/config/client` 的新增／编辑弹窗，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW`（第四轮基线批准收口的远程复审，现已完成、第四轮基线获批并由实现任务承接）、`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_R1_REVIEW`（R1 纠错结果远程复审，结论 `APPROVED`）、`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_REVIEW`（R0 草案远程复审，结论 `CHANGES_REQUIRED`）与其余历史入口照实保留不改写。

**当前下一入口（第四轮新增／编辑弹窗校验与交互调整实现 R1 定向纠偏，覆盖以上历史入口）**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_IMPLEMENTATION_R1_REVIEW`（对象为 R1 纠偏任务 `CLIENT-CONFIG-CREATE-EDIT-DIALOG-VALIDATION-IMPLEMENTATION-001-R1` 的结果提交）。该任务为**前端纠偏任务**（非纯文档）：ChatGPT 从远程 Git 对实现提交 `9da96206cae51a891e326e586793e5e641082952` 的复审结论为 `CHANGES_REQUIRED`，仅指出**主提交按钮加载态蓝黑跳色**一项——新增／编辑弹窗主提交按钮常态为黑，但提交请求处理中受 Element Plus `is-disabled is-loading` 影响变为浅蓝（实测 `rgb(160,207,255)`），违反已批准条款禁止的蓝黑跳色，且“只发生一次、没有闪烁”不构成符合条款的解释。R1 的**唯一功能目标**是修复该跳色：非提交处理中保持已实现的黑色可点击样式且表单未完成**不**预先禁用、字段级报错机制不变；提交请求处理中继续阻止重复提交并给出清楚可辨的加载反馈，但**底色与边框维持黑色系**（可采用略浅／略深的黑／深灰 + 明确加载图标 + 不可重复点击状态，且不得做成看似仍可点击的常态），文字、图标与背景保持可读；请求失败或结束后恢复常态黑色按钮，新增“创建”与编辑“保存”均适用，按钮文案与取消／自动生成／修改探针 ID 的既有样式与行为不变。落点**仅** `frontend/src/views/client-config/ClientConfigPage.vue` 与 `ClientConfigPage.spec.ts`：新增仅作用于 `.cc-dialog-submit.is-loading` 的定向深灰黑系（`#3f3f46`）背景／边框规则并覆盖 hover／focus／active，中和 Element Plus 加载态白色遮罩，沿用 scoped CSS 的 `[data-v-*]` 优先级稳压 EP `.el-button.is-disabled`（**不**新增全局覆盖、**不**用 `!important`），保留 `:loading`／`:disabled` 与 `submitDialog()` 防重复逻辑；**不**修改任何需求／验收／设计／UI 定义行，**未**放宽任何已批准条款。**验证结果**：定向测试 `ClientConfigPage.spec.ts` **140/140 通过**（含新增 4 条 R1 回归：常态黑可点击、挂起 `is-loading` 且防重复、结束后恢复，新增与编辑均覆盖，并断言加载态声明块的实际配色令牌与“无浅蓝令牌／无全局覆盖／无 `!important`”）、全量前端测试 57 文件 / **1120 用例通过**、`npm run build` 成功；真实无头浏览器（1440×900）在**新增**与**编辑**两条路径上分别核对三态——常态 `rgb(9,9,11)`／白字／6px／`cursor: pointer`，挂起 `rgb(63,63,70)`／白字／6px／`is-disabled is-loading`／加载图标／`cursor: not-allowed`／遮罩透明（**全程无蓝**），结束恢复 `rgb(9,9,11)`；挂起期间连点 3 次写入请求仍各 **1** 次。**状态分层**：第四轮基线 `adjustment4_baseline_status=APPROVED` 保持不变，`adjustment4_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW` **不变**（仍停在远程代码复审入口），本轮正式验收 `adjustment4_formal_acceptance_execution_status=NOT_RUN`；第一至第三轮已批准基线与三轮实现事实（`existing_feature_implementation_status`/`adjustment_implementation_status`/`adjustment2_implementation_status` 均为 `IMPLEMENTED_PENDING_USER_ACCEPTANCE`，`adjustment3_implementation_status=IMPLEMENTED_PENDING_CHATGPT_REVIEW`）**保持真实不改写**；四类定义行相对 `9da9620` **逐字节零变化**（136/135/71/59 条），**135 条验收全部 `NOT_RUN`**，`PENDING_USER_CONFIRMATION=0`。**下一步须由 ChatGPT 从远程 Git 对本 R1 结果提交做独立代码复审**；**远程代码复审通过仍不等于项目负责人目测或正式验收通过**——项目负责人**后续仍须对实际页面做目测**。对“新增／编辑表单弹窗通用模板”仍仅保留后续入口：**须在本轮页面实现完成并经项目负责人实际目测认可后**另开任务提炼，本任务**不**创建、**不**批准、**不**推广任何模板。参考页 `/config/data-source` **仅作视觉对照且未被修改**，本轮规则只作用于 `/config/client` 的新增／编辑弹窗，两套模板的模板级全局状态保持不变，**其他页面未被授权**。**历史入口**：`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_IMPLEMENTATION_REVIEW`（第四轮实现远程复审，现已完成、结论 `CHANGES_REQUIRED`、由本 R1 纠偏承接）、`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_APPROVAL_CLOSEOUT_REVIEW`、`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_R1_REVIEW`（结论 `APPROVED`）、`CHATGPT_REMOTE_CLIENT_CONFIG_CREATE_EDIT_DIALOG_VALIDATION_BASELINE_REVIEW`（结论 `CHANGES_REQUIRED`）与其余历史入口照实保留不改写。
