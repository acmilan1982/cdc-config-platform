# Feature 总索引（docs/features/）

> 文档状态：`DRAFT_PENDING_USER_REVIEW`
> 恢复任务：PROJECT-BASELINE-AND-DOCUMENTATION-RECOVERY-001
> 恢复日期：2026-08-27
> 恢复任务执行基线：6dc22ecd67b7268ae3ee4761f5412c1e7b50ce5c
> 恢复草案首次入库提交：a6f51f8a8ff984bc946a4e2ccaccbf56692722fe
> 本轮修订任务：PROJECT-BASELINE-DOCUMENTATION-REVIEW-FIX-001（结果提交见本轮实施报告）

## 用途

本文件是 **Feature 总索引**，不替代各 Feature 的需求。每个 Feature 在 `docs/features/<feature>/` 下可有 README、REQUIREMENTS、DESIGN、API、UI、DATABASE、ACCEPTANCE 等基线文档。

规则（PROJECT-BASELINE-AND-DOCUMENTATION-RECOVERY-001 §9）：

- 没有正式基线就写 `BASELINE_NOT_ESTABLISHED`；
- pages、modules、普通提示词、Agent 报告不能冒充 Feature 基线；
- 分散资料可列为恢复候选，但须独立复审后方可成为正式基线；
- 本索引不批准任何 Feature；具体 Feature 的缺失文档以后按 FEATURE_DEVELOPMENT_AND_ADJUSTMENT_PROCESS.md 通用流程单独建立。

## 状态口径

- **基线状态**：`APPROVED`（已批准）、`DRAFT`（草案/已入库未批准）、`BASELINE_NOT_ESTABLISHED`（未建立）、`LOCAL_CANDIDATE`（服务器本地候选，未进入 Git，待独立复审）。
- **代码状态**：已实现 / 部分实现 / 占位 / 未开始。
- Feature 状态编码（NOT_RUN / PASS / FAIL / BLOCKED / DEFERRED）见 DOMAIN_GLOSSARY.md。

清单以当前路由（frontend/src/router/index.ts，共 **15 条 Route Record**：1 条根路径重定向 + 14 条具名页面路由）、Git 已提交菜单（frontend/src/config/menu.ts，**2 组 10 项**，不含大屏入口）、前端页面（frontend/src/views/）、后端包与现有文档为准。

> 菜单 Git 事实与本地候选：Git 已提交菜单为 2 组 10 项（配置管理 4 + 运行监控 6，不含大屏入口）；大屏 standalone 路由 `/large-screen` 已提交；大屏菜单入口仅存在于服务器工作区未提交的 `menu.ts` 修改中（本地候选，未提交），不作为 Git 可复核事实。

---

## 配置管理

| Feature 标识 | 功能名称 | 代码状态 | 基线覆盖 | 基线状态 | 最新有效证据 | 当前缺口 | 下一入口 |
|---|---|---|---|---|---|---|---|
| data-source-management | 数据源管理 | 部分实现（后端 CRUD 完整，前端占位） | 无 | BASELINE_NOT_ESTABLISHED | backend `datasource` 包（CRUD+启停，7 个 API 端点） | 前端占位未对接；缺全部基线文档；docs/pages/data-source-management.md 为恢复候选 | 新建 |
| client-config | 客户端配置 | 占位 | 无 | BASELINE_NOT_ESTABLISHED | frontend `views/client-config/`（占位页） | 占位页；缺全部基线文档 | 新建 |
| data-subscription | 数据订阅 | 占位（`NOT_STARTED`） | REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE | 需求/验收：当前正式批准版本为“取消并发保护”需求与验收调整版本，状态 `APPROVED`（ChatGPT 对 R1 结果提交 `43a9097...` 正式复审 `APPROVED`；上一“含逗号数据源 ID 查询兼容”批准版本批准依据提交 `5d5b5f4...`；更早“英文句点 `.` 保留分隔符”批准版本批准依据提交 `bb8716c...` 作为历史事实保留；文档基线批准不代表功能已实现或验收已通过）；DESIGN/API/UI/DATABASE 设计基线已正式批准，状态 `APPROVED`，设计复审结论 `APPROVED`（批准版本 R4，R4-R1 完成状态元数据收口，ChatGPT 对 R4-R1 结果提交 `ba7fedd...` 正式复审结论 `APPROVED`），R3 已完成定向修订（删除旧并发设计：版本令牌/`DSUB-FP-V1`/黄金向量/行锁/并发字段比较/`40910`；编辑与删除改为普通主键读写；修正多源库空 token 判定；补充 nullable CSV 的 null-safe 契约；保留 R2 已通过设计），ChatGPT 对 R3 结果提交 `ac49544...` 正式复审结论 `CHANGES_REQUIRED`；R4 已按正式复审发现定向修正三个确定问题（DELETE 影响多行错误码统一为 `50041 DELETE_FAILED` 且不映射 `50040`；DELETE 接口删除前多源库异常后端防护统一为强制普通读取→存在性与异常判定→普通 DELETE，删除预览不替代 DELETE 自身防护；修正“NULL 被 split 成 `['']`”的 Java 语义错误为“对 null 调用 split 抛 `NullPointerException`”）；ChatGPT 对 R4 结果提交 `2d81f19...` 正式复审：三项业务修正均通过，但四文档顶部/页尾复审状态标记不一致（结论 `CHANGES_REQUIRED`）；R4-R1 已定向统一状态元数据（DESIGN/API/UI/DATABASE 顶部设计复审状态均更新为 `PENDING_R4_REVIEW`，页尾均改为等待 ChatGPT 对 R4-R1 结果提交正式复审，API/UI 业务设计在 R4 中零语义变化）；ChatGPT 对 R4-R1 结果提交 `ba7fedd...` 正式复审结论 `APPROVED`，设计基线批准收口完成（批准不代表功能已实现或验收已通过）；“取消并发保护”需求/验收调整已获得 ChatGPT 正式复审 `APPROVED`（R1 定向修订已修正 `DSUB-AC-048` 并发保护残留与原报告查询语义错误），正式批准收口完成 | docs/features/data-subscription/（Feature 文档目录标识 `data-subscription`；前端实现目录仍为 `views/data-subscribe/`，路由 `/config/subscribe`，与文档目录标识允许不同，均保持既有值不变）；正式设计基线 `DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md`、设计任务报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001.md`、点号调整报告 `reports/DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-001.md`、点号调整批准收口报告 `reports/DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`、设计 R1 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R1.md`、含逗号查询需求调整草案报告 `reports/DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-001.md`、含逗号查询需求调整批准收口报告 `reports/DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`、设计 R2 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R2.md`、取消并发保护需求调整草案报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001.md`、取消并发保护需求调整 R1 定向修订报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1.md`、取消并发保护需求调整批准收口报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`、设计 R3 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3.md`、设计 R4 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4.md`、设计 R4-R1 状态元数据收口报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4-R1.md`、设计批准收口报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-APPROVAL-001.md` | 功能尚未实现（前端仍占位）；业务/测试代码尚未建立；126 条验收尚未执行（`NOT_RUN`）；大屏修正仍延期 | 数据订阅实现阶段任务规划/实现基线建立 |
| server-config | 中心端配置 | 已实现并验收接受（`IMPLEMENTED_ACCEPTED`） | REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE、reports | APPROVED | docs/features/server-config/；正式验收最终收口报告 SERVER-CONFIG-FORMAL-ACCEPTANCE-CLOSEOUT-001、定向重验报告 SERVER-CONFIG-FORMAL-ACCEPTANCE-TARGETED-RETEST-001（提交 `b5aeec2...`）、缺陷修复提交 `70382a6...`；设计批准链（批准提交 `77a8c63...`、`b1c5349...`） | 无 | 未来如有新需求，按需求调整流程重新进入，不保留本轮待实现/待验收任务 |

## 运行监控

| Feature 标识 | 功能名称 | 代码状态 | 基线覆盖 | 基线状态 | 最新有效证据 | 当前缺口 | 下一入口 |
|---|---|---|---|---|---|---|---|
| zk-node-monitor | CDC 节点状态（ZK 客户端监控） | 已实现（用户验收通过） | README、REQUIREMENTS | DRAFT（已入库，未查到独立批准记录） | docs/features/zk-node-monitor/；docs/acceptance/zk-client-monitor-integration.md | 缺 DESIGN/API/UI/ACCEPTANCE/DATABASE；部分资料散落 api/acceptance/zookeeper | 调整/补全 |
| data-source-run-state | 数据源运行状态 | 占位 | 无 | BASELINE_NOT_ESTABLISHED | frontend `views/data-source-run-state/`（占位页） | 占位页；缺全部基线文档 | 新建 |
| topic-offset | Topic 偏移量 | 占位 | 无 | BASELINE_NOT_ESTABLISHED | frontend `views/topic-offset/`（占位页） | 占位页；缺全部基线文档 | 新建 |
| log-query | 日志查询 | 已实现（功能基线已批准，实现与开发验收已完成） | REQUIREMENTS、DESIGN、API、UI、ACCEPTANCE、reports | APPROVED | docs/features/log-query/；实现提交 17680b3 等；开发验收执行 7b3010e（前端 42 例、后端 logquery 专项 135 例通过） | 缺 README、DATABASE；用户视觉验收与最终收口待完成 | 接续（视觉验收→收口）；补 README/DATABASE |
| job-failure-monitor | 故障监控 | 主要页面与已列调整已实现并验收；仍存在已记录的状态映射差距 | README、REQUIREMENTS | 基线：现行业务需求已建立，多个调整链（ZK 状态融合/详情导航/故障历史/无分页等）已 APPROVED/收口（closeout 提交 e03c6df、bcc2320、2b54db3）；实现：主要页面与已列调整已实现验收，GAP-STATUS-001/002/003 仍开放 | docs/features/job-failure-monitor/；docs/acceptance/job-failure-acceptance.md | 统一的内部→对外状态映射层（对外 5 种状态）未实现（GAP-STATUS-001/002/003）；缺 DESIGN/API/UI/ACCEPTANCE/DATABASE 独立基线文档 | 针对开放 GAP 进入已有 Feature 调整/接续，不重新新建 Feature |
| large-screen | 数据同步统计大屏 | 已实现（视觉验收通过） | README、REQUIREMENTS（AS-IS-R3） | LOCAL_CANDIDATE（未入库，待独立复审） | docs/features/large-screen/（未提交）；Git 已提交 standalone 路由 /large-screen | 大屏基线文档未进入 Git；缺 DESIGN/API/UI/ACCEPTANCE/DATABASE 正式基线；大屏菜单入口仅存在于工作区未提交 menu.ts（本地候选），Git 已提交菜单不含大屏入口 | 复审（独立任务） |

## 应用外壳

| Feature 标识 | 功能名称 | 代码状态 | 基线覆盖 | 基线状态 | 最新有效证据 | 当前缺口 | 下一入口 |
|---|---|---|---|---|---|---|---|
| app-shell | 全局应用外壳 | 已实现（布局/菜单/路由/公共主题） | README、REQUIREMENTS | LOCAL_CANDIDATE（未入库，待独立复审） | docs/features/app-shell/（未提交） | 未进入 Git；批准证据未核验 | 复审（独立任务） |

## 本地候选说明

`docs/features/app-shell/`、`docs/features/large-screen/` 为服务器上未提交的 Feature 基线候选，按 PROJECT-BASELINE-AND-DOCUMENTATION-RECOVERY-001 §12 处理：列入本索引、标记"本地候选待独立复审"，默认不随本任务批量提交；只有被当前 Git、批准报告和实现充分证明且不引入新规则时，才可作为恢复文件提交。

## 下一步

各 Feature 的缺失文档（README/DATABASE/DESIGN/API/UI/ACCEPTANCE）按 FEATURE_DEVELOPMENT_AND_ADJUSTMENT_PROCESS.md 通用流程单独建立，本索引随后更新。

## 变更记录

| 日期 | 变更 | 依据 |
|---|---|---|
| 2026-08-27 | 仅同步 `server-config` 已批准需求/验收基线事实（基线覆盖、基线状态、最新有效证据、当前缺口、下一入口），不批准其他 Feature，不改变路由和菜单数量事实；本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | SERVER-CONFIG-FEATURE-BASELINE-APPROVAL-001（项目负责人批准驱动的 Feature 需求与验收基线收口；纯文档任务） |
| 2026-08-27 | 仅同步 `server-config` 已批准设计基线事实：基线覆盖更新为 REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE、reports；基线状态 APPROVED；最新有效证据更新为设计批准报告及完整设计链；当前缺口更新为未实现、65 条验收未执行、Feature README 待实现验收收口；下一入口更新为阶段5实现任务规划/前后端实现；不批准其他 Feature，不改变路由和菜单数量事实；本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | SERVER-CONFIG-DESIGN-BASELINE-APPROVAL-001（项目负责人批准驱动的阶段 4 设计基线收口；纯文档任务） |
| 2026-08-28 | 仅同步 `server-config` 验收前调整批准基线事实：基线状态保持 `APPROVED`；实现状态明确为 `IMPLEMENTED_ADJUSTMENT_PENDING`（旧批准版本已实现、两项已批准调整尚未实现）；最新有效证据增加验收前调整批准报告 SERVER-CONFIG-PRE-ACCEPTANCE-ADJUSTMENT-BASELINE-APPROVAL-001（批准提交 `b1c5349...`）；验收数量更新为 66 条全部 `NOT_RUN`；当前缺口更新为两项已批准调整代码尚未实现、66 条验收未执行；下一入口更新为两项已批准调整的小范围实现；不批准其他 Feature，不改变路由和菜单数量事实；本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | SERVER-CONFIG-PRE-ACCEPTANCE-ADJUSTMENT-BASELINE-APPROVAL-001（项目负责人批准驱动的验收前调整基线收口；纯文档任务） |
| 2026-08-29 | 仅同步 `server-config` 正式验收收口事实：代码状态更新为已实现并验收接受（`IMPLEMENTED_ACCEPTED`）；正式验收 66 条全部通过（`66 PASSED / 0 FAILED`）；最新有效证据更新为最终收口报告、定向重验报告及提交 `b5aeec2...`；当前缺口更新为无；下一入口更新为未来如有新需求按需求调整流程重新进入，不保留本轮待实现/待验收任务；不批准其他 Feature，不改变路由和菜单数量事实；本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | SERVER-CONFIG-FORMAL-ACCEPTANCE-CLOSEOUT-001（正式验收与功能基线最终收口；纯文档任务） |
| 2026-08-30 | 数据订阅 Feature 文档标识统一：Feature 标识由 `data-subscribe` 更新为 `data-subscription`，索引目录指向 `docs/features/data-subscription/`；基线覆盖更新为 REQUIREMENTS、ACCEPTANCE，基线状态更新为 DRAFT（`REQUIREMENTS.md`/`ACCEPTANCE.md` 为 `DRAFT_PENDING_USER_REVIEW` 草案，未批准、未实现）；注明前端实现目录 `views/data-subscribe/` 与文档目录标识允许不同；不改变前端代码目录、路由、菜单、代码包名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-REQUIREMENTS-BASELINE-001-R1（ChatGPT 正式复审 CHANGES_REQUIRED 定向修订；纯文档任务） |
| 2026-08-30 | 仅同步 `data-subscription` 需求与验收基线批准事实：基线状态更新为 `APPROVED`（仅文档基线获批，不代表功能已实现或验收已通过）；最新有效证据增加本批准任务及批准收口报告（依据 R1 结果提交 `b9fb1e9...` 的 ChatGPT 正式复审 `APPROVED`）；当前缺口更新为 DESIGN/API/UI/DATABASE 设计基线未建立、功能尚未实现（前端仍占位）、126 条验收未执行（`NOT_RUN`）；下一入口更新为设计阶段；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-REQUIREMENTS-BASELINE-APPROVAL-001（项目负责人批准驱动的需求与验收基线正式收口；纯文档任务） |
| 2026-08-30 | 仅同步 `data-subscription` 设计基线草案事实：基线覆盖更新为 REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE（后四者为设计基线草案，状态 `DRAFT_PENDING_USER_REVIEW`，未正式复审批准）；需求与验收基线仍为 `APPROVED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；最新有效证据增加设计任务报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001.md`；当前缺口更新为设计草案待正式复审批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为设计复审；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001（纯文档设计基线草案建立；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-30 | 仅同步 `data-subscription` 点号保留分隔符需求调整草案事实：需求/验收当前为点号调整草案（状态 `DRAFT_PENDING_USER_REVIEW`，待正式复审；上一正式批准版本提交 `d7560445...` 作为历史证据保留，需求与验收批准事实不因草案而失效）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，且设计复审为 `CHANGES_REQUIRED`，等待需求调整复审后再执行设计 R1；当前缺口更新为点号需求调整待复审、设计 R1 待执行、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为点号需求调整复审；最新有效证据增加点号调整报告 `reports/DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-001（项目负责人决策驱动的纯文档需求/验收定向调整草案） |
| 2026-08-30 | 仅同步 `data-subscription` 点号保留分隔符需求调整批准收口事实：需求/验收当前版本状态更新为 `APPROVED`（ChatGPT 对提交 `bb8716c...` 正式复审 `APPROVED`；本次批准只批准点号需求和对应验收标准，不批准设计、不实现功能、不执行验收）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，且设计复审仍为 `CHANGES_REQUIRED`，待设计 R1 定向修订后重新复审；当前缺口更新为设计 R1 待执行、设计待重新复审、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为设计 R1 定向修订；最新有效证据增加点号调整批准收口报告 `reports/DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-APPROVAL-001（项目负责人批准驱动的纯文档需求/验收调整批准收口） |
| 2026-08-30 | 仅同步 `data-subscription` 设计基线 R1 定向修订事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；R1 定向修订已完成（修正 ChatGPT 正式复审全部发现项：原子行锁并发、查询 OR/AND 分组与 token 精确匹配、结构化 `sourceTables`/`PRESERVE\|REPLACE` 唯一契约、删除预览令牌闭环、`@TableId IdType.INPUT` 依据修正、能力分层 Schema 过滤、真实章节交叉引用，并同步已批准点号规则与收紧项），待重新正式复审；需求/验收保持 `APPROVED`（107 条，点号批准版本 `bb8716c...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R1 草案待正式复审、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为设计 R1 正式复审；最新有效证据增加 R1 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R1.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R1（纯文档设计基线定向修订；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-30 | 仅同步 `data-subscription` 含逗号查询需求调整草案事实：需求/验收状态由 `APPROVED` 更新为 `DRAFT_PENDING_USER_REVIEW`（当前版本为“含逗号数据源 ID 查询兼容”调整草案，尚未获得 ChatGPT 正式复审批准；上一正式批准版本为点号保留分隔符批准版本，批准依据提交 `bb8716c...`，历史批准事实保留）；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为含逗号查询需求调整草案待正式复审、设计 R2 尚未执行、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为本需求调整草案正式复审；最新有效证据增加含逗号查询需求调整草案报告 `reports/DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-001（正式复审发现驱动的纯文档需求/验收定向调整草案） |
| 2026-08-30 | 仅同步 `data-subscription` 含逗号查询需求调整批准收口事实：需求/验收状态由 `DRAFT_PENDING_USER_REVIEW` 更新为 `APPROVED`（当前版本为“含逗号数据源 ID 查询兼容”调整批准版本，ChatGPT 对提交 `5d5b5f4...` 正式复审 `APPROVED`；上一“英文句点 `.` 保留分隔符”批准版本批准依据提交 `bb8716c...` 作为历史事实保留）；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R2 尚未执行、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R2` 定向修订；最新有效证据增加含逗号查询需求调整批准收口报告 `reports/DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001（项目负责人批准驱动的纯文档需求/验收调整批准收口） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线 R2 定向修订事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；R2 定向修订已完成（实现已批准三类查询语义在服务层 Java 字面量过滤中的落地、列表 API 响应结构调整为 items+queryWarnings、元数据 API 由路径参数改为 query 参数、Oracle 物化视图显式排除、DSUB-FP-V1 字节级确定性指纹并给出黄金测试向量；同步已批准含逗号查询规则与一致性收紧项），待重新正式复审；需求/验收保持 `APPROVED`（107 条，含逗号批准版本 `5d5b5f4...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R2 草案待正式复审、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为设计 R2 正式复审；最新有效证据增加 R2 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R2.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R2（纯文档设计基线 R2 定向修订；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-31 | 仅同步 `data-subscription` 取消并发保护需求调整草案事实：需求/验收状态由 `APPROVED` 更新为 `DRAFT_PENDING_USER_REVIEW`（当前版本为“取消并发保护”调整草案，尚未获得正式复审批准；上一正式批准版本为含逗号查询批准版本，批准依据提交 `5d5b5f4...`；更早“英文句点 `.` 保留分隔符”批准版本批准依据提交 `bb8716c...` 作为历史事实保留；产品负责人明确“数据订阅页面的新增、编辑、删除完全不处理并发操作”，本次仅定向调整需求 DSUB-REQ-097/098/099/103 与验收 DSUB-AC-107/108/109/110/114/117，删除版本令牌、内容指纹、行锁、并发字段比较、40910 等机制并明确为产品边界而非缺口）；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；R2 四项修订目标已通过正式复核，但产品负责人决策要求先完成本需求/验收调整复审，再执行设计 R3（删除并发设计：版本令牌/内容指纹/行锁等；修正多源库空 token 判定；补充 null-safe CSV 契约）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为取消并发保护需求调整草案待正式复审、设计 R3 待执行、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001` 正式复审；最新有效证据增加取消并发保护需求调整草案报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001（产品负责人决策驱动的纯文档需求/验收定向调整草案） |
| 2026-08-31 | 仅同步 `data-subscription` 取消并发保护需求调整草案 R1 事实：ChatGPT 对提交 `27a27e3...` 正式复审结论 `CHANGES_REQUIRED`，R1 定向修订已完成——修正 `DSUB-AC-048` 并发保护残留（结尾“并发保护规则执行”改为“不做并发保护”，该行编号/状态/关联需求/前置条件/查看/编辑行为不变，相对 `27a27e3...` 仅该一条验收业务行变化，相对 `026417e7...` 本轮“取消并发保护”调整累计承载验收业务行 7 条 `DSUB-AC-048/107/108/109/110/114/117`、其余 119 条不变），并修正原报告对设计 R2 三类查询语义的错误描述（改为三类数据源 ID 查询规则：普通不含逗号 ID 完整 token 字面精确匹配、仅含句点不含逗号 ID 仍完整 token 字面精确匹配、含逗号 ID 历史兼容可能匹配并经 `queryWarnings` 展示歧义警告）；需求/验收状态继续为 `DRAFT_PENDING_USER_REVIEW`，依据需求状态继续为 `DRAFT_PENDING_USER_REVIEW`；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为取消并发保护需求/验收调整草案 R1 待 ChatGPT 正式复审、设计 R3 尚未执行、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1` 正式复审；最新有效证据增加取消并发保护需求调整 R1 定向修订报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1（ChatGPT 复审 CHANGES_REQUIRED 驱动的纯文档定向修订） |
| 2026-08-31 | 仅同步 `data-subscription` 取消并发保护需求/验收调整批准收口事实：需求/验收状态由 `DRAFT_PENDING_USER_REVIEW` 更新为 `APPROVED`（当前正式批准版本为“取消并发保护”需求与验收调整版本，ChatGPT 对 R1 结果提交 `43a9097...` 正式复审 `APPROVED`；上一“含逗号数据源 ID 查询兼容”批准版本批准依据提交 `5d5b5f4...`、更早“英文句点 `.` 保留分隔符”批准版本批准依据提交 `bb8716c...` 作为历史事实保留；文档基线批准不代表功能已实现或验收已通过）；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R3 尚未执行（删除版本令牌/`DSUB-FP-V1`/黄金向量/行锁/并发字段比较/`40910`，编辑与删除改为普通主键更新/物理删除，删除预览只读不返回令牌，修正多源库异常空 token 归一化，补充 nullable CSV 的 null-safe 契约）、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3`；最新有效证据增加取消并发保护需求调整批准收口报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001（项目负责人批准驱动的纯文档需求/验收调整批准收口） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线 R3 定向修订事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审状态由 `CHANGES_REQUIRED` 更新为 `PENDING_R3_REVIEW`；R3 定向修订已完成（删除旧并发设计：版本令牌/`DSUB-FP-V1`/黄金向量/行锁/并发字段比较/`40910`；编辑打开与保存、删除预览与删除改为普通主键读写，无并发保证、最后一次成功写入生效，删除预览只读不返回令牌；修正多源库空 token 判定；补充 nullable CSV 的 null-safe 契约；保留 R2 已通过的三类数据源 ID 查询规则、元数据 API 改为 query 参数、物化视图显式排除），待 ChatGPT 正式复审；需求/验收保持 `APPROVED`（107 条，当前正式批准版本为“取消并发保护”需求与验收调整版本，批准依据提交 `43a9097...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R3 待 ChatGPT 正式复审、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3` 正式复审；最新有效证据增加设计 R3 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3（纯文档设计基线 R3 定向修订；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线 R4 定向修订事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审状态由 `PENDING_R3_REVIEW` 更新为 `PENDING_R4_REVIEW`；ChatGPT 对 R3 结果提交 `ac49544...` 正式复审结论 `CHANGES_REQUIRED`，R4 按正式复审发现定向修正三个确定问题（DELETE 影响多行错误码统一为 `50041 DELETE_FAILED` 且不映射 `50040`；DELETE 接口删除前多源库异常后端防护统一为强制普通读取→存在性与异常判定→普通 DELETE，删除预览不替代 DELETE 自身防护；修正“NULL 被 split 成 `['']`”的 Java 语义错误为“对 null 调用 split 抛 `NullPointerException`”）；需求/验收保持 `APPROVED`（107/126，取消并发保护批准版本 `43a9097...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R4 待 ChatGPT 正式复审、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4` 正式复审；最新有效证据增加设计 R4 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4（纯文档设计基线 R4 定向修订；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线 R4-R1 状态元数据收口事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审状态保持 `PENDING_R4_REVIEW`；ChatGPT 对 R4 结果提交 `2d81f19...` 正式复审：三项业务修正（DELETE 错误码 `50041`、DELETE 删除前多源库异常后端防护、Java null/split 语义）均通过，但四文档顶部/页尾复审状态标记不一致（结论 `CHANGES_REQUIRED`）；R4-R1 已定向统一状态元数据（DESIGN/DATABASE 页尾由“R3 定向修订版/等待 R3 复审”更新为 R4 状态并指向 R4-R1 正式复审；API/UI 顶部 `PENDING_R3_REVIEW` 更新为 `PENDING_R4_REVIEW` 且注明业务设计零语义变化、页尾同步指向 R4-R1 正式复审），业务语义零变化；需求/验收保持 `APPROVED`（107/126，取消并发保护批准版本 `43a9097...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R4-R1 待 ChatGPT 正式复审、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4-R1` 正式复审；最新有效证据增加设计 R4-R1 状态元数据收口报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4-R1.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4-R1（纯文档设计基线状态元数据定向收口；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线正式批准收口事实：DESIGN/API/UI/DATABASE 设计基线状态由 `DRAFT_PENDING_USER_REVIEW` 更新为 `APPROVED`，设计复审结论由 `PENDING_R4_REVIEW` 更新为 `APPROVED`（批准版本 R4，R4-R1 完成状态元数据收口，ChatGPT 对 R4-R1 结果提交 `ba7fedd...` 正式复审结论 `APPROVED`；批准不代表功能已实现或验收已通过）；需求/验收保持 `APPROVED`（107/126，取消并发保护批准版本 `43a9097...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；TBD-01/TBD-02 设计结论随四份设计基线正式批准闭环；当前缺口更新为功能尚未实现、业务/测试代码尚未建立、126 条验收未执行、大屏修正仍延期；下一入口更新为数据订阅实现阶段任务规划/实现基线建立；最新有效证据增加设计批准收口报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-APPROVAL-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-APPROVAL-001（项目负责人批准驱动的数据订阅设计基线正式批准收口；纯文档任务） |
