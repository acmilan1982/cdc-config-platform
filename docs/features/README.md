# Feature 总索引（docs/features/）

> 文档状态：`DRAFT_PENDING_USER_REVIEW`
> 恢复任务：PROJECT-BASELINE-AND-DOCUMENTATION-RECOVERY-001
> 恢复日期：2026-08-27
> 恢复任务执行基线：6dc22ecd67b7268ae3ee4761f5412c1e7b50ce5c
> 恢复草案首次入库提交：a6f51f8a8ff984bc946a4e2ccaccbf56692722fe
> 本轮修订任务：PROJECT-BASELINE-DOCUMENTATION-REVIEW-FIX-001（结果提交见本轮实施报告）

## 用途

本文件是 **Feature 总索引**，不替代各 Feature 的需求。每个 Feature 在 `docs/features/<feature>/` 下可有 README、REQUIREMENTS、DESIGN、API、UI、DATABASE、ACCEPTANCE 等基线文档。

规则（PROJECT-BASELINE-AND-DOCUMENTATION-RECOVERY-001 §9）：

- 没有正式基线就写 `BASELINE_NOT_ESTABLISHED`；
- pages、modules、普通提示词、Agent 报告不能冒充 Feature 基线；
- 分散资料可列为恢复候选，但须独立复审后方可成为正式基线；
- 本索引不批准任何 Feature；具体 Feature 的缺失文档以后按 FEATURE_DEVELOPMENT_AND_ADJUSTMENT_PROCESS.md 通用流程单独建立。

## 状态口径

- **基线状态**：`APPROVED`（已批准）、`DRAFT`（草案/已入库未批准）、`BASELINE_NOT_ESTABLISHED`（未建立）、`LOCAL_CANDIDATE`（服务器本地候选，未进入 Git，待独立复审）。
- **代码状态**：已实现 / 部分实现 / 占位 / 未开始。
- Feature 状态编码（NOT_RUN / PASS / FAIL / BLOCKED / DEFERRED）见 DOMAIN_GLOSSARY.md。

清单以当前路由（frontend/src/router/index.ts，共 **15 条 Route Record**：1 条根路径重定向 + 14 条具名页面路由）、Git 已提交菜单（frontend/src/config/menu.ts，**2 组 10 项**，不含大屏入口）、前端页面（frontend/src/views/）、后端包与现有文档为准。

> 菜单 Git 事实与本地候选：Git 已提交菜单为 2 组 10 项（配置管理 4 + 运行监控 6，不含大屏入口）；大屏 standalone 路由 `/large-screen` 已提交；大屏菜单入口仅存在于服务器工作区未提交的 `menu.ts` 修改中（本地候选，未提交），不作为 Git 可复核事实。

---

## 配置管理

| Feature 标识 | 功能名称 | 代码状态 | 基线覆盖 | 基线状态 | 最新有效证据 | 当前缺口 | 下一入口 |
|---|---|---|---|---|---|---|---|
| data-source-management | 数据源管理 | 部分实现（后端 CRUD 完整，前端占位） | 无 | BASELINE_NOT_ESTABLISHED | backend `datasource` 包（CRUD+启停，7 个 API 端点） | 前端占位未对接；缺全部基线文档；docs/pages/data-source-management.md 为恢复候选 | 新建 |
| client-config | 探针端管理（目标用户可见名称；当前占位仍显示“客户端配置”） | 占位（功能未实现，`NOT_STARTED`） | README、REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE | `REQUIREMENTS.md`/`ACCEPTANCE.md`=`APPROVED`（2026-09-03 曾获项目负责人批准、旧口径状态 `APPROVED`；因并发口径调整于 2026-09-04 转为本轮待复审草案：取消旧需求的并发“最多一个成功”强承诺（该强承诺是旧需求/验收目标；`LOCK TABLE ... WAIT 5` 是后续未批准设计草案为实现该强承诺提出的方案，本轮已过时），改为“DML 前重新读取 + 尽力写前检查 + 已接受极端并发边界”；首版调整结果经 ChatGPT 正式复审 `CHANGES_REQUIRED`（R1-01~R1-04），R1 定向修订完成后，ChatGPT 对 R1 结果提交 `f2a4d7d...` 正式复审 `APPROVED`，项目负责人于 2026-09-04 明确回复“批准”，需求/验收经并发口径调整批准收口（`CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001`）重新收口为 `APPROVED`；2026-09-03 批准保留为历史且不自动批准本轮调整；批准的是基线文档，不代表功能已实现或验收已通过；`DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md` 曾为设计草案（2026-09-03 建立，`DRAFT_PENDING_USER_REVIEW`、`PENDING_USER_CONFIRMATION=0`，其中表锁/锁等待方案从未获项目负责人批准、原 `LOCK TABLE ... WAIT 5` 设计未获批），初版正式设计复审结论 `CHANGES_REQUIRED`（R1-01~R1-09）、2026-09-04 已完成 R1 定向修订；2026-09-04 设计并发口径定向调整任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001` 已按已重新批准的需求并发口径从四份设计草案中移除已过时的 `LOCK TABLE ... WAIT 5` 表锁方案并调整设计（标记 `DESIGN_CONCURRENCY_ADJUSTED_PENDING_FORMAL_REVIEW`）；调整结果经 ChatGPT 对提交 `ba7c5e9...` 正式复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）结论 `APPROVED`、项目负责人于 2026-09-04 明确回复“批准”，四份设计文档经批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001` 收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`，整体标记为 `APPROVED_READY_FOR_IMPLEMENTATION`（批准的是设计基线，不代表代码已实现、已测试或验收已执行通过；实现状态仍 `NOT_STARTED`，76 条验收仍全部 `NOT_RUN`） | docs/features/client-config/（README/REQUIREMENTS/ACCEPTANCE/DESIGN/API/UI/DATABASE，需求 `CCFG-REQ-001~090`、验收 `CCFG-AC-001~076` 全部 `NOT_RUN`，设计编号 `CCFG-DESIGN-001~037`/`CCFG-API-001~020`/`CCFG-UI-001~026`/`CCFG-DB-001~022`）；需求与验收草案报告 `reports/CLIENT-CONFIG-REQUIREMENTS-BASELINE-001.md`、R1 定向修订报告 `reports/CLIENT-CONFIG-REQUIREMENTS-BASELINE-001-R1.md`、批准收口报告 `reports/CLIENT-CONFIG-REQUIREMENTS-BASELINE-APPROVAL-001.md`（批准依据提交 `9b31893...`，首版草案提交 `abf2f40...` 为历史依据）、设计草案建立报告 `reports/CLIENT-CONFIG-DESIGN-BASELINE-001.md`、R1 定向修订报告 `reports/CLIENT-CONFIG-DESIGN-BASELINE-001-R1.md`、并发口径调整草案报告 `reports/CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001.md`、并发口径调整 R1 定向修订报告 `reports/CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1.md`、并发口径调整批准收口报告 `reports/CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`、设计并发口径调整执行报告 `reports/CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001.md`、设计并发口径调整批准收口执行报告 `reports/CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001.md`；既有路由 /config/client 与前端 `views/client-config/` 目录名保持既有值不变 | 本轮并发口径调整草案（`CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001`，需求/验收状态 `DRAFT_PENDING_USER_REVIEW`）首版结果经 ChatGPT 正式复审 `CHANGES_REQUIRED`，R1 定向修订完成（`CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1`），ChatGPT 对 R1 结果提交 `f2a4d7d...` 正式复审 `APPROVED`、项目负责人于 2026-09-04 明确回复“批准”，需求/验收已重新收口为 `APPROVED`（批准收口任务 `CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001`）；功能未实现（实现状态 `NOT_STARTED`：页面/菜单更名“探针端管理”、探针 CRUD 均未实施）；76 条验收未执行（全部 `NOT_RUN`）；`DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md` 已于 2026-09-04 批准收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`、整体标记 `APPROVED_READY_FOR_IMPLEMENTATION`，不再存在“设计待复审/待批准”缺口；`CLIENT_DESC` 真实语义已确认为 `VARCHAR2(1024 BYTE)`，256 与 1024 已批准数据库基线长度差异及旧表述“仅只读/人工维护”“编辑时探针 ID 不可改”等影响项见 REQUIREMENTS §9，继续作为后续独立数据库基线同步事项保留 | 四份设计文档（`DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md`）已于 2026-09-04 经 ChatGPT 对提交 `ba7c5e9...` 正式设计复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）结论 `APPROVED`、项目负责人明确回复“批准”后，由批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001` 收口为 `APPROVED`、`PENDING_USER_CONFIRMATION=0`、整体标记 `APPROVED_READY_FOR_IMPLEMENTATION`；下一入口为 `CLIENT_CONFIG_IMPLEMENTATION`（以已批准需求 90、验收 76（全部 `NOT_RUN`）、设计 37/20/26/22 为唯一业务基线进入实现阶段，本入口不直接实现代码）；实现状态保持 `NOT_STARTED`、76 条验收保持全部 `NOT_RUN` |
| data-subscription | 数据订阅 | 已实现并验收接受（`IMPLEMENTED_ACCEPTED`）：正式验收已执行并经 ChatGPT 对结果提交 `ae66d90...` 正式复审批准（`APPROVED`），126 条全部 `PASS`（0 FAIL / 0 BLOCKED / 0 NOT_RUN），R1 真实浏览器补验与 R1-R1 元数据修订后 126 条全部保持 PASS | REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE | 正式验收前 UI 交互基线对齐批准收口完成：REQUIREMENTS/ACCEPTANCE/DESIGN/UI 文档状态由 `DRAFT_PENDING_USER_REVIEW` 更新为 `APPROVED`（ChatGPT 对 R1 结果提交 `26094c6...` 正式复审结论 `APPROVED`：正式验收前 UI 交互基线业务调整正确，R1 已消除 `ACCEPTANCE.md` 两处过期“尚未实现/NOT_STARTED”当前态冲突，107 条需求业务行相对 R1 基准零变化，126 条验收业务行相对 R1 基准零变化且全部 `NOT_RUN`，实现状态保持 `IMPLEMENTED_REVIEW_APPROVED_PENDING_FORMAL_ACCEPTANCE`；本次批准的是正式验收前 UI 交互基线文档标准，不代表正式验收执行或通过，不代表 `IMPLEMENTED_ACCEPTED`），API/DATABASE 保持 `APPROVED`（零变化）；历史批准链不变——需求/验收：当前正式批准版本为“取消并发保护”需求与验收调整版本，状态 `APPROVED`（ChatGPT 对 R1 结果提交 `43a9097...` 正式复审 `APPROVED`；上一“含逗号数据源 ID 查询兼容”批准版本批准依据提交 `5d5b5f4...`；更早“英文句点 `.` 保留分隔符”批准版本批准依据提交 `bb8716c...` 作为历史事实保留；文档基线批准不代表功能已实现或验收已通过）；DESIGN/API/UI/DATABASE 设计基线已正式批准，状态 `APPROVED`，设计复审结论 `APPROVED`（批准版本 R4，R4-R1 完成状态元数据收口，ChatGPT 对 R4-R1 结果提交 `ba7fedd...` 正式复审结论 `APPROVED`），R3 已完成定向修订（删除旧并发设计：版本令牌/`DSUB-FP-V1`/黄金向量/行锁/并发字段比较/`40910`；编辑与删除改为普通主键读写；修正多源库空 token 判定；补充 nullable CSV 的 null-safe 契约；保留 R2 已通过设计），ChatGPT 对 R3 结果提交 `ac49544...` 正式复审结论 `CHANGES_REQUIRED`；R4 已按正式复审发现定向修正三个确定问题（DELETE 影响多行错误码统一为 `50041 DELETE_FAILED` 且不映射 `50040`；DELETE 接口删除前多源库异常后端防护统一为强制普通读取→存在性与异常判定→普通 DELETE，删除预览不替代 DELETE 自身防护；修正“NULL 被 split 成 `['']`”的 Java 语义错误为“对 null 调用 split 抛 `NullPointerException`”）；ChatGPT 对 R4 结果提交 `2d81f19...` 正式复审：三项业务修正均通过，但四文档顶部/页尾复审状态标记不一致（结论 `CHANGES_REQUIRED`）；R4-R1 已定向统一状态元数据（DESIGN/API/UI/DATABASE 顶部设计复审状态均更新为 `PENDING_R4_REVIEW`，页尾均改为等待 ChatGPT 对 R4-R1 结果提交正式复审，API/UI 业务设计在 R4 中零语义变化）；ChatGPT 对 R4-R1 结果提交 `ba7fedd...` 正式复审结论 `APPROVED`，设计基线批准收口完成（批准不代表功能已实现或验收已通过）；“取消并发保护”需求/验收调整已获得 ChatGPT 正式复审 `APPROVED`（R1 定向修订已修正 `DSUB-AC-048` 并发保护残留与原报告查询语义错误），正式批准收口完成；R1 状态元数据定向修订：ChatGPT 对批准收口提交 `dc97ffd...` 正式复核结论 `CHANGES_REQUIRED`（UI 交互业务内容、107 条需求与 126 条验收业务行、批准边界与提交范围全部通过；唯一问题是 DESIGN/UI 依赖基线仍标为草案、当前批准版本说明停留在 R4、四份文档实现状态说明仍把当前版本称为草案），R1 已定向修正这些状态元数据，业务语义零变化，REQUIREMENTS/ACCEPTANCE/DESIGN/UI/API/DATABASE 均保持 `APPROVED`，实现状态更新为 `IMPLEMENTED_FORMAL_ACCEPTANCE_EXECUTED_PENDING_REVIEW`，126 条正式验收已执行（全部 `PASS`）；其后 ChatGPT 对正式验收 R1-R1 结果提交 `ae66d90...` 正式复审 `APPROVED`，任务 `DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-CLOSEOUT-001` 完成最终接受收口：REQUIREMENTS/ACCEPTANCE/DESIGN/API/UI/DATABASE 文档状态均保持 `APPROVED`，实现状态收口为 `IMPLEMENTED_ACCEPTED`、正式验收状态 `ACCEPTED`、验收执行状态 `PASS`（126 条全部 PASS / 0 FAIL / 0 BLOCKED / 0 NOT_RUN），本 Feature 不再有待验收复审入口 | docs/features/data-subscription/（Feature 文档目录标识 `data-subscription`；前端实现目录仍为 `views/data-subscribe/`，路由 `/config/subscribe`，与文档目录标识允许不同，均保持既有值不变）；正式设计基线 `DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md`、设计任务报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001.md`、点号调整报告 `reports/DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-001.md`、点号调整批准收口报告 `reports/DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`、设计 R1 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R1.md`、含逗号查询需求调整草案报告 `reports/DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-001.md`、含逗号查询需求调整批准收口报告 `reports/DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`、设计 R2 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R2.md`、取消并发保护需求调整草案报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001.md`、取消并发保护需求调整 R1 定向修订报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1.md`、取消并发保护需求调整批准收口报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`、设计 R3 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3.md`、设计 R4 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4.md`、设计 R4-R1 状态元数据收口报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4-R1.md`、设计批准收口报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-APPROVAL-001.md`、前端实现报告 `reports/DATA-SUBSCRIPTION-FRONTEND-IMPLEMENTATION-001.md`、正式验收前 UI 交互基线对齐草案报告 `reports/DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001.md`、正式验收前 UI 交互基线对齐草案 R1 状态文字定向修订报告 `reports/DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001-R1.md`、正式验收前 UI 交互基线对齐批准收口报告 `reports/DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-APPROVAL-001.md`、正式验收前 UI 交互基线对齐批准收口 R1 状态元数据定向修订报告 `reports/DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-APPROVAL-001-R1.md`、正式验收执行报告 `reports/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001.md`、正式验收证据目录 `evidence/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001/`、正式验收 R1 真实浏览器补验与证据修订报告 `reports/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001-R1.md`、R1 证据目录 `evidence/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001-R1/`、正式验收 R1 状态与证据元数据定向修订报告 `reports/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001-R1-R1.md`、正式验收接受收口报告 `reports/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-CLOSEOUT-001.md` | 正式验收已执行（126 条全部 PASS），R1 真实浏览器补验与证据修订已执行（54 个浏览器场景；126 条全部保持 PASS；R1 覆盖矩阵 `coverage-matrix-r1.md` 取代原矩阵证据标注并纠正不准确 `BR` 声明）；R1-R1 已回应 ChatGPT 对 R1 结果提交正式复审 `CHANGES_REQUIRED`，定向修正三处状态/证据元数据矛盾（ACCEPTANCE 顶部残留当前态 `NOT_RUN`、S4-7a/S4-7b 保留字符汇总文字与明细 JSON 相反、S5-9 场景漏列 `DSUB-AC-126`），126 条全部保持 `PASS`；ChatGPT 已对正式验收 R1-R1 结果提交 `ae66d90...` 正式复审 `APPROVED`，任务 `DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-CLOSEOUT-001` 完成最终接受收口：实现状态 `IMPLEMENTED_ACCEPTED`、正式验收状态 `ACCEPTED`、验收执行状态 `PASS`，126 条全部 `PASS`（0 `FAIL`/0 `BLOCKED`/0 `NOT_RUN`），本 Feature 当前不存在“等待实现复审/等待正式验收复审”缺口；大屏 `DATA_SOURCE_TABLE` 换行符解析修正尚未执行、尚未验证、尚未完成（`READY_FOR_SEPARATE_TASK`，已具备另立独立任务前置条件，本次未执行） | 大屏 `DATA_SOURCE_TABLE` 英文逗号分隔解析修正任务的需求核验与任务规划；后续本 Feature 功能变更应重新走需求、设计、实现与验收变更流程 |
| server-config | 中心端配置 | 已实现并验收接受（`IMPLEMENTED_ACCEPTED`） | REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE、reports | APPROVED | docs/features/server-config/；正式验收最终收口报告 SERVER-CONFIG-FORMAL-ACCEPTANCE-CLOSEOUT-001、定向重验报告 SERVER-CONFIG-FORMAL-ACCEPTANCE-TARGETED-RETEST-001（提交 `b5aeec2...`）、缺陷修复提交 `70382a6...`；设计批准链（批准提交 `77a8c63...`、`b1c5349...`） | 无 | 未来如有新需求，按需求调整流程重新进入，不保留本轮待实现/待验收任务 |

## 运行监控

| Feature 标识 | 功能名称 | 代码状态 | 基线覆盖 | 基线状态 | 最新有效证据 | 当前缺口 | 下一入口 |
|---|---|---|---|---|---|---|---|
| zk-node-monitor | CDC 节点状态（ZK 客户端监控） | 已实现（用户验收通过） | README、REQUIREMENTS | DRAFT（已入库，未查到独立批准记录） | docs/features/zk-node-monitor/；docs/acceptance/zk-client-monitor-integration.md | 缺 DESIGN/API/UI/ACCEPTANCE/DATABASE；部分资料散落 api/acceptance/zookeeper | 调整/补全 |
| data-source-snapshot-status | 源库快照状态（用户可见名称；既有路由 `/monitor/data-source-state`、前端目录 `views/data-source-run-state/` 保持既有值不变，命名映射见 Feature README §6） | 既有实现已完成（只读全栈）并保留；第一轮 UI 调整已实现（实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-001` 及其 R1 `…-001-R1`，2026-09-07，按批准内容基准提交 `5757237...` 落地，R1 修复 Tooltip 单行优先/防旧坐标，实现 R1 提交 `5933ec2...` 经 ChatGPT 独立代码与证据复审 `APPROVED`，第一轮实现状态曾为 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`，历史）；2026-09-08 项目负责人对第一轮 UI 调整实现 R1 对应预览页人工检查结论 `human_visual_review_status=CHANGES_REQUIRED`，据此建立**第二轮验收前 UI 调整草案** `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002`（纯文档）及其 R1 极小定向修订 `...-002-R1`，曾于 2026-09-08 收口为 `APPROVED`（正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002`，批准内容基准提交 `5da9b17...`，作为历史批准事实保留）；批准收口后 ChatGPT 在准备第二轮实现任务时发现批准内容误记正常源库行 Tooltip 为完整 `DATA_SOURCE_ORG`、与项目负责人真实需求冲突并暂停实现，项目负责人再次确认悬停 Tooltip 只显示完整原始 `DATA_SOURCE_ID`，现经 **R2 极小纠正** `...-002-R2`、**R3 记录纠正** `...-002-R3`，并由 ChatGPT 对本 R3 结果提交 `cf40b5d...` 独立正式复审 `APPROVED`、项目负责人明确回复“批准”后重新批准收口（重新批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002-R1`，批准内容基准提交 `cf40b5d1e5ef03712011edb5e10c20070b582273`，2026-09-08）；第二轮 UI 调整已由实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-002` 于 2026-09-08 落地（`implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`）；未自我接受，`IMPLEMENTED_ACCEPTED` 未执行 | README、REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE、reports | （2026-09-08 时点）第二轮 UI 调整版本经 R2 极小纠正 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002-R2`、R3 记录纠正 `...-002-R3` 后，由 ChatGPT 对本 R3 结果提交 `cf40b5d1e5ef03712011edb5e10c20070b582273` 独立正式复审 `APPROVED`、项目负责人明确回复“批准”重新批准收口为 `APPROVED`（当前正式重新批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002-R1`，批准内容基准提交 `cf40b5d1e5ef03712011edb5e10c20070b582273`，2026-09-08；第二轮版本曾于 2026-09-08 收口为 `APPROVED`：`DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002`，ChatGPT 对 R1 结果提交 `5da9b17c1a720f89482eeda1436ad633145fe9fa` 独立正式复审 `APPROVED` 且项目负责人明确回复“批准”，批准日期 2026-09-08，仅作为 R2 纠正前的历史批准事实保留、不得作为当前批准内容或未来实现业务基准；因批准内容误记正常源库行 Tooltip 为完整 `DATA_SOURCE_ORG`、与项目负责人真实需求冲突，R2 纠正该唯一业务语义（正常行主内容仍显示 ORG、悬停 Tooltip 只显示完整原始 `DATA_SOURCE_ID`）、R3 记录纠正审计记录；REQUIREMENTS/ACCEPTANCE/DESIGN/UI 第二轮版本为 `APPROVED`、当时实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`（2026-09-12 前历史状态；当前实现状态为 `IMPLEMENTED_ADJUSTMENT_PENDING_FORMAL_ACCEPTANCE`；第二轮 UI 调整已由实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-002` 于 2026-09-08 落地，`browser_verification_status=DEV_SELF_TEST_DONE`）、`pending_user_review=NO`、`pending_user_confirmation_count=0`）；历史批准基线保留为历史：需求与验收基线已批准（正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-APPROVAL-001`，批准内容基准提交 `4234af73db2190098f3dcd219319a4281fdabafd`）、设计基线已批准（正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-APPROVAL-001`，批准内容基准提交 `61117a62f44d39f7c548ebcb650891abf91b9b8c`，2026-09-06）、第一轮 UI 调整批准版本（正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-001`，批准内容基准提交 `575723711ca39d7761df308c1c99b1e6e957cf70`，2026-09-07，71 条/80 条）；批准的是本轮第二轮 UI 调整需求/验收/设计基线，不代表功能已实现、验收已执行或通过，也不等于 `IMPLEMENTED_ACCEPTED`；`API.md`/`DATABASE.md` 保持已批准且第二轮整文件零差异。2026-09-10 隔离视觉原型 R2～R7 设计固化正式批准收口（`DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-DESIGN-FREEZE-APPROVAL-001`）：R2～R7 固化新增需求 `DSS-REQ-076~083`（累计 83）与验收 `DSS-AC-087~095`（累计 95，全部 `NOT_RUN`）、DESIGN §25、UI §19 由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`（ChatGPT 对批准内容基准提交 `e67b2ecc3897c3e83597126e259ee4c19349a66a` 最终复审 `APPROVED`、项目负责人 2026-09-10 明确回复“批准”），`API.md`/`DATABASE.md` 顶层组合状态由 `APPROVED_BASELINE_WITH_DRAFT_PROTOTYPE_EXTENSION_PENDING_USER_REVIEW` 收口为 `APPROVED`（需求 83、验收 95，文件业务内容零差异）；`requirements_status`/`acceptance_status`/`design_status` 当前均为 `APPROVED`（83/95，追踪 83/83、95/95），`pending_user_review=NO`、`pending_user_confirmation_count=0`，文档总体状态 `PROTOTYPE_DESIGN_APPROVED_READY_FOR_FORMAL_5173_IMPLEMENTATION`，通用查询列表页 UI 基线 `NOT_CREATED_BY_DESIGN` | docs/features/data-source-snapshot-status/（README/REQUIREMENTS/ACCEPTANCE/DESIGN/API/UI/DATABASE；需求 `DSS-REQ-001~087` 共 87 条（001~065 批准基线既有行＋066~071 第一轮已批准＋072~075 第二轮新增并已批准＋076~083 隔离视觉原型 R2～R7 设计固化已批准＋084~086 查询控件交互调整已批准＋087 查询下拉弹层固定宽度已批准）、验收 `DSS-AC-001~107` 共 107 条全部 `NOT_RUN`（含第二轮新增 081~086、原型固化 087~095、查询控件调整 096~103、查询下拉弹层固定宽度 104~107 并已批准，见 §4.19/§4.20/§4.21/§4.22）、设计落点完整（本轮第二轮 UI 调整落点见 DESIGN §22/§23、UI §16/§17；查询下拉弹层固定宽度落点见 DESIGN §27、UI §21））；数据库只读复核报告 `docs/database/reports/DATA-SOURCE-SNAPSHOT-STATUS-DATABASE-VERIFICATION-001.md`（提交 `72b305a...`）；需求/验收各阶段报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-001.md`/-R1/-R2/-R3/-APPROVAL-001.md 与设计各阶段报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-001.md`/-R1/-APPROVAL-001.md；实现执行报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-IMPLEMENTATION-001.md`（2026-09-06）；浏览器证据目录 `evidence/DATA-SOURCE-SNAPSHOT-STATUS-IMPLEMENTATION-001/`；第一轮 UI 调整草案执行报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-001.md`（2026-09-07）、R1 极小定向修订执行报告 `...-001-R1.md`（2026-09-07）、批准收口执行报告 `...-APPROVAL-001.md`（2026-09-07）、第一轮 UI 调整实现执行报告 `...-IMPLEMENTATION-001.md`（2026-09-07）与证据目录 `evidence/DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-001/`、实现 R1 执行报告 `...-IMPLEMENTATION-001-R1.md`（2026-09-07）与证据目录 `evidence/DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-001-R1/`；第二轮 UI 调整草案执行报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002.md`（2026-09-08）、R1 定向修订执行报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002-R1.md`（2026-09-08）；第二轮 UI 调整草案批准收口执行报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002.md`（2026-09-08，批准收口）、R2 极小纠正执行报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002-R2.md`（2026-09-08，R2）；隔离视觉原型 R2～R7 设计固化正式批准收口报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-DESIGN-FREEZE-APPROVAL-001.md`（2026-09-10，批准收口；需求累计 83、验收累计 95 全部 `NOT_RUN`）；查询控件交互调整基线批准收口报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-APPROVAL-001.md`（2026-09-10，批准收口；需求累计 86、验收累计 103 全部 `NOT_RUN`） | 107 条正式验收未执行（`DSS-AC-001~107` 全部 `NOT_RUN`，formal_acceptance_execution_status=NOT_RUN、formal_acceptance_not_run_count=107）；人工页面验收未执行（human_visual_acceptance_status=NOT_RUN）；`human_visual_review_status=CHANGES_REQUIRED`（项目负责人对第一轮 UI 调整实现 R1 对应预览页人工页面检查结论，驱动第二轮草案，历史）；本轮第二轮 UI 调整版本已重新批准收口，实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-002` 已于 2026-09-08 落地（实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`，待 ChatGPT 独立复审与人工页面查看）——此前曾于 2026-09-08 收口为 `APPROVED` 的 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002`（ChatGPT 对 R1 结果提交 `5da9b17...` 独立正式复审 `APPROVED` 且项目负责人明确回复“批准”）仅作为 R2 纠正前的历史批准事实保留；批准内容误记正常源库行 Tooltip 为完整 `DATA_SOURCE_ORG`、与负责人真实需求冲突，经 **R2 极小纠正** `...-002-R2`（项目负责人再次确认悬停 Tooltip 只显示完整原始 `DATA_SOURCE_ID`）、**R3 记录纠正** `...-002-R3` 后，ChatGPT 对本 R3 结果提交 `cf40b5d...` 独立正式复审 `APPROVED`、项目负责人明确回复“批准”，当前重新批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002-R1`、批准内容基准提交 `cf40b5d1e5ef03712011edb5e10c20070b582273`、2026-09-08（重新批准时实现状态为 `IMPLEMENTED_ADJUSTMENT_PENDING`，随后由上述实现任务落地）；正式验收与人工页面验收仍未执行（107 条 `DSS-AC-001~107` 全部 `NOT_RUN`、`formal_acceptance_not_run_count=107`）；R2 只纠正“正常源库行 Tooltip 只显示完整原始 `DATA_SOURCE_ID`”唯一业务语义，其余四类展示调整及 `API.md`/`DATABASE.md`/源码/测试/config/evidence 零差异；第一轮 UI 调整批准并实现版本保留为历史（第一轮 UI 调整草案 `...-BASELINE-001` 及 R1 极小定向修订 `...-001-R1` 已批准收口并已实现，历史）；隔离视觉原型 R2～R7 设计固化文档已于 2026-09-10 经 `DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-DESIGN-FREEZE-APPROVAL-001` 批准收口为 `APPROVED`（需求 83、验收 95、DESIGN §25、UI §19、追踪 83/83 与 95/95；`API.md`/`DATABASE.md` 顶层组合状态收口为 `APPROVED`）；R2～R7 已由独立正式实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-TO-FORMAL-IMPLEMENTATION-001`（2026-09-10，隔离 worktree `/agent/dss-formal-impl-001`）应用到 `5173`（`formal_5173_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`）；本轮（2026-09-10）查询控件交互调整基线 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-APPROVAL-001` 已批准收口为 `APPROVED`（批准内容基准提交 `cf9f9eb0240f275cd50eb37546e6d6256892a9f4`），当前实现状态为 `IMPLEMENTED_ADJUSTMENT_PENDING_FORMAL_ACCEPTANCE`（本轮调整已由实现任务于 2026-09-11 落地，经 2026-09-12 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-IMPLEMENTATION-REVIEW-CLOSEOUT-001` 复审收口：代码复审 `APPROVED`、项目负责人人工视觉/交互检查 `APPROVED_BY_PROJECT_OWNER`，`pending_user_review=NO`、`pending_user_confirmation_count=0`），107 条正式验收仍全部 `NOT_RUN` | ChatGPT 已对本 R3 结果提交 `cf40b5d1e5ef03712011edb5e10c20070b582273` 独立正式复审结论 `APPROVED`、项目负责人明确回复“批准”，当前 R2/R3 纠正版已重新批准收口（重新批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002-R1`，批准内容基准提交 `cf40b5d1e5ef03712011edb5e10c20070b582273`，2026-09-08；第二轮批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002` 曾于 2026-09-08 收口为 `APPROVED`，仅作为 R2 纠正前的历史批准事实保留；R2 变更记录对 `ACCEPTANCE.md` 的错误零差异声明与未来实现锚点已由 R3 记录纠正 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002-R3`（2026-09-08，纯文档，不改任何业务行）纠正，见下方变更记录）；**（2026-09-08 时点下一入口为 ChatGPT 对第二轮 UI 调整实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-002` 结果提交（实现提交、代码、自动化测试、构建与真实浏览器证据，见实现报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-002.md` 与证据目录）独立复审，该历史入口已处理完毕，当前统一下一入口见本行末）**，复审通过后由项目负责人对预览页人工查看；本轮四类展示调整已按重新批准内容基准提交 `cf40b5d1e5ef03712011edb5e10c20070b582273` 落地（实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`；需求 `DSS-REQ-001~075` 共 75 条、验收 `DSS-AC-001~086` 共 86 条全部 `NOT_RUN`；未采用 R2 纠正前的历史批准内容基准 `5da9b17c1a720f89482eeda1436ad633145fe9fa` 作为业务依据）；正式验收（`DSS-AC-001~086` 共 86 条）另立独立正式验收任务执行；本任务不执行正式验收、不把开发自测写成 PASS；此外（2026-09-10）隔离视觉原型 R2～R7 设计固化任务 `DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-DESIGN-FREEZE-001`（纯文档）把项目负责人已认可（`APPROVED_BY_PROJECT_OWNER`）的 `5174` 隔离视觉原型 R2～R7 最终视觉方案固化入 `REQUIREMENTS.md` §21.4/§21.5（新增 `DSS-REQ-076~083`，需求累计 83）、`ACCEPTANCE.md` §4.20（新增 `DSS-AC-087~095`，全部 `NOT_RUN`，验收累计 95）、`DESIGN.md` §25（含 §14.2 覆盖 83/83、§14.3 覆盖 95/95）、`UI.md` §19 与 Feature README——固化内容本身曾为 `DRAFT_PENDING_USER_REVIEW`（待用户复审、未批准），2026-09-10 已由 `DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-DESIGN-FREEZE-APPROVAL-001` 批准收口为 `APPROVED`（ChatGPT 对批准内容基准提交 `e67b2ecc3897c3e83597126e259ee4c19349a66a` 最终复审 `APPROVED`、项目负责人明确回复“批准”；需求 83、验收 95 全部 `NOT_RUN`、追踪 83/83 与 95/95，`API.md`/`DATABASE.md` 顶层组合状态亦收口为 `APPROVED`、业务内容零差异），文档总体状态 `PROTOTYPE_DESIGN_APPROVED_READY_FOR_FORMAL_5173_IMPLEMENTATION`；`5173` 正式实现 `PENDING_FORMAL_IMPLEMENTATION_ON_5173`、正式验收 `NOT_RUN`，`5174` 原型证据仅为设计验证证据不得写成正式验收 `PASS`，通用查询列表页 UI 基线 `NOT_CREATED_BY_DESIGN`；该独立正式实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-TO-FORMAL-IMPLEMENTATION-001` 已于 2026-09-10 完成（在全新隔离 worktree `/agent/dss-formal-impl-001`、基准提交 `c568df1f7b0be14b7decdf59f2ef9116ff0bf403`，把 R2～R7 已批准固化设计落到 `5173` 正式前端白名单 12 个 `frontend/src/views/data-source-run-state/` 文件，严格以批准内容基准提交 `e67b2ecc3897c3e83597126e259ee4c19349a66a` 为业务基准）：`formal_5173_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`，通过定向/Feature/前端全量测试、类型检查、生产构建与真实后端四档视口浏览器开发自测（console error 0、页面写请求 0、其他路由无 `dss-*` 泄漏；后端链路全 `SELECT`、无手工 DML/DDL、本 Feature 业务表未写入）；`formal_acceptance_status=NOT_RUN`、`human_visual_acceptance_status=NOT_RUN`、`acceptance_not_run_count=95`（95 条 `DSS-AC-001~095` 全部 `NOT_RUN`）、`pending_user_review=NO`、`pending_user_confirmation_count=0`、`query_list_global_baseline_status=NOT_CREATED_BY_DESIGN`，文档总体状态（2026-09-10 时点）更新为 `PROTOTYPE_DESIGN_APPROVED_AND_IMPLEMENTED_ON_5173_PENDING_REVIEW`（2026-09-12 前历史状态；当前文档总体状态为 `PROTOTYPE_DESIGN_APPROVED_AND_IMPLEMENTED_ON_5173_PENDING_FORMAL_ACCEPTANCE`）；`API.md`/`DATABASE.md` 仅同步顶部实现状态元数据与简短实现记录，业务正文与后端零差异。**（2026-09-10 时点最新下一入口为 ChatGPT 从远程 Git 对本次正式实现（实现提交、12 文件差异、测试/构建与浏览器证据，见实现报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-TO-FORMAL-IMPLEMENTATION-001.md`）独立复审，复审通过后由项目负责人人工目测 `5173`，该历史入口已处理完毕，当前统一下一入口见本行末）**（`CHATGPT_FORMAL_IMPLEMENTATION_REVIEW_FROM_GIT_THEN_PROJECT_OWNER_VISUAL_REVIEW`）；正式验收须另立任务，本实现任务不执行、不自我接受；**（2026-09-10）查询控件交互调整基线批准收口** `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-001`（纯文档）：ChatGPT 已从 Git 对 `5173` R2～R7 正式实现代码独立复审 `APPROVED`（`formal_5173_code_review_status=APPROVED`）、项目负责人 `5173` 人工页面检查 `CHANGES_REQUIRED`（`project_owner_visual_review_status=CHANGES_REQUIRED`）并确认三类查询控件交互问题，据此新增需求 `DSS-REQ-084~086`（纳入批准基线，累计 86）与验收 `DSS-AC-096~103`（累计 103 全部 `NOT_RUN`、`acceptance_not_run_count=103`），落点 REQUIREMENTS §21.6/§21.7、ACCEPTANCE §4.21/§5、DESIGN §26、UI §20，追踪 86/86 与 103/103；`API.md`/`DATABASE.md` 业务契约与既有历史报告整文件零差异；（2026-09-10 时点状态）`query_control_interaction_adjustment_status=APPROVED`、`query_control_interaction_adjustment_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`（2026-09-12 前历史状态；当前为 `IMPLEMENTED_ADJUSTMENT_PENDING_FORMAL_ACCEPTANCE`）、`formal_5173_code_review_status=APPROVED`、`project_owner_visual_review_status=CHANGES_REQUIRED`（2026-09-12 前历史状态；当前为 `APPROVED_BY_PROJECT_OWNER`）、`formal_acceptance_status=NOT_RUN`、`human_visual_acceptance_status=NOT_RUN`、`pending_user_review=NO`、`pending_user_confirmation_count=0`（本轮查询控件交互调整基线已批准、尚未实现；保留既有 `formal_5173_code_review_status=APPROVED` 与 R2～R7 已批准视觉内容、已进 `5173` 的实现事实，不回退）；**（2026-09-12）查询控件交互调整实现复审收口** `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-IMPLEMENTATION-REVIEW-CLOSEOUT-001`（纯文档）：ChatGPT 已从远程 Git 复审 R1 修正实现提交 `edba7c891884d0f000a7c9edc196b2bffb95e0b0` 结论 `APPROVED`（R2 提交 `2a9a271690bfdc68b16772268e84928a33abdeda` 仅为测试计数文档纠正 `100→102`、无代码变化），项目负责人对含后续 popper 固定宽度修正的最终 `5173` 页面人工检查回复“人工检查了，没有问题”，当前 `query_control_interaction_adjustment_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_FORMAL_ACCEPTANCE`、`query_control_interaction_adjustment_code_review_status=APPROVED`、`query_control_human_visual_interaction_review_status=APPROVED_BY_PROJECT_OWNER`、`project_owner_visual_review_status=APPROVED_BY_PROJECT_OWNER`（2026-09-12 前历史状态为 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`/`PENDING_CHATGPT_REVIEW`/`CHANGES_REQUIRED`）**；**（2026-09-11）查询下拉固定宽度基线草案** `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-001`（纯文档草案）：项目负责人已批准隔离视觉原型 `5174`（`DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-PROTOTYPE-001`）三档 popper 固定宽度（探针端/client **480px**、源库/source **400px**、快照状态/status **240px**，受 `min(目标宽度, calc(100vw - 16px))` 视口安全上界约束），据此新增需求 `DSS-REQ-087`（累计 87）与验收 `DSS-AC-104~107`（累计 107 全部 `NOT_RUN`、`acceptance_not_run_count=107`），落点 REQUIREMENTS §21.8、ACCEPTANCE §4.22、DESIGN §27、UI §21，追踪 87/87 与 107/107；`API.md`/`DATABASE.md` 仅同步组合计数（需求 87、验收 107）与分层状态/下一入口，业务契约/正文逐字节不变；该草案随后已于 2026-09-11 经 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-APPROVAL-001` 批准收口为 `APPROVED`（ChatGPT 对批准内容基准提交 `f74dd725682b745f1b8ac6a1b9358eff10aaddc2` 最终复审 `APPROVED`、项目负责人明确回复“批准”），该批准收口当时 `popper_width_document_status=APPROVED`、`prototype_width_decision_status=APPROVED_BY_PROJECT_OWNER`、`popper_width_formal_5173_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`（2026-09-12 前历史状态）、`popper_width_formal_code_review_status=PENDING_CHATGPT_REVIEW`（历史）、`formal_acceptance_status=NOT_RUN`、`human_formal_5173_review_status=NOT_RUN_FOR_THIS_ADJUSTMENT`、`pending_user_review=NO`、`pending_user_confirmation_count=0`；**（2026-09-11）查询下拉固定宽度正式实现任务** `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001` **已完成**：在全新隔离 worktree `/agent/dss-popper-width-impl-001`（基准提交 `485be09db758e4ba6f543fcc88a399cc5a46d384`＝`origin/develop`；主工作区与既有 `5174` prototype worktree 既有修改零触碰）中，按批准内容基准提交 `f74dd725682b745f1b8ac6a1b9358eff10aaddc2` 把三档查询下拉弹层固定宽度规则（探针端 480 / 源库 400 / 快照状态 240，受 `min(目标宽度, calc(100vw - 16px))` 视口安全上界约束）落到 `5173` 正式前端，仅改 Feature 私有 `frontend/src/views/data-source-run-state/components/DataSourceSnapshotQueryBar.vue` 与其 `.spec.ts`（把固定的外层选择器由裸 class 收敛为 `.el-popper.dss-*-popper`，同时锁定 `width`/`min-width`/`max-width`；不使用 `!important`、不新增全局 Element Plus 覆写、不引入 `window.resize`/`ResizeObserver`/轮询等脚本尺寸监听）；开发验证状态 `DEV_SELF_TEST_DONE`（定向/Feature/前端全量测试、类型检查、生产构建、`git diff --check`、四档真实浏览器视口与状态几何矩阵、窄视口、交互与请求状态机回归、console error 0、页面写请求 0、其他路由无 `dss-*` 泄漏）；`API.md`/`DATABASE.md` 仅同步分层状态、下一入口与简短实现记录，业务契约与业务正文逐字节不变，后端零差异；正式验收与人工正式 `5173` 复审仍 `NOT_RUN`；已按已批准 R2 支持边界完成实现复审与人工视觉/交互复审收口（2026-09-12，`DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-REVIEW-CLOSEOUT-001`：ChatGPT 从远程 Git 对实现提交 `e3c239230bbe854f0280a05f8151d30174fea110` 复审 `APPROVED`、项目负责人回复“人工检查了，没有问题”，`popper_width_formal_5173_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_FORMAL_ACCEPTANCE`、`popper_width_formal_code_review_status=APPROVED`、`human_visual_interaction_review_status=APPROVED_BY_PROJECT_OWNER`；2026-09-12 前历史状态为 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`/`CHANGES_REQUIRED`/`NOT_PASSED`；R2 支持视口边界修正已于 2026-09-12 经 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-R2-APPROVAL-001` 批准收口为 `popper_width_r2_document_status=APPROVED`，原先超窄视口冲突据此合法解除），**当前统一下一入口为 `DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001`**（另立独立任务，按当前已批准需求、设计、API、UI、DATABASE 与 `DSS-AC-001~107` 执行正式验收；正式验收 107 条全部 `NOT_RUN`、`formal_acceptance_status=NOT_RUN`，`5174` 原型浏览器/测试/构建自检仅为设计验证证据，不得写成正式验收 `PASS`，代码复审与人工视觉检查通过只表示实现可进入独立正式验收阶段，不得写成 `IMPLEMENTED_ACCEPTED`/`FORMALLY_ACCEPTED`/`COMPLETED`）** |
| topic-offset | 数据同步进度 | 占位（功能未实现，`NOT_STARTED`） | README、REQUIREMENTS、ACCEPTANCE | DRAFT（草案待审批，已入库未批准） | docs/features/topic-offset/（README/REQUIREMENTS/ACCEPTANCE）；既有路由 /monitor/topic-offset 保持既有值不变 | 功能未实现（前端 `views/topic-offset/` 为占位页）；缺 DESIGN/API/UI/DATABASE；`CDC_TOPIC_OFFSET` 物理表结构待后续单独只读复核 | 用户审阅并批准或退回需求与验收草案 |
| log-query | 日志查询 | 已实现（功能基线已批准，实现与开发验收已完成） | REQUIREMENTS、DESIGN、API、UI、ACCEPTANCE、reports | APPROVED | docs/features/log-query/；实现提交 17680b3 等；开发验收执行 7b3010e（前端 42 例、后端 logquery 专项 135 例通过） | 缺 README、DATABASE；用户视觉验收与最终收口待完成 | 接续（视觉验收→收口）；补 README/DATABASE |
| job-failure-monitor | 故障监控 | 主要页面与已列调整已实现并验收；仍存在已记录的状态映射差距 | README、REQUIREMENTS | 基线：现行业务需求已建立，多个调整链（ZK 状态融合/详情导航/故障历史/无分页等）已 APPROVED/收口（closeout 提交 e03c6df、bcc2320、2b54db3）；实现：主要页面与已列调整已实现验收，GAP-STATUS-001/002/003 仍开放 | docs/features/job-failure-monitor/；docs/acceptance/job-failure-acceptance.md | 统一的内部→对外状态映射层（对外 5 种状态）未实现（GAP-STATUS-001/002/003）；缺 DESIGN/API/UI/ACCEPTANCE/DATABASE 独立基线文档 | 针对开放 GAP 进入已有 Feature 调整/接续，不重新新建 Feature |
| large-screen | 数据同步统计大屏 | 已实现（视觉验收通过） | README、REQUIREMENTS（AS-IS-R3） | LOCAL_CANDIDATE（未入库，待独立复审） | docs/features/large-screen/（未提交）；Git 已提交 standalone 路由 /large-screen | 大屏基线文档未进入 Git；缺 DESIGN/API/UI/ACCEPTANCE/DATABASE 正式基线；大屏菜单入口仅存在于工作区未提交 menu.ts（本地候选），Git 已提交菜单不含大屏入口 | 复审（独立任务） |

## 应用外壳

| Feature 标识 | 功能名称 | 代码状态 | 基线覆盖 | 基线状态 | 最新有效证据 | 当前缺口 | 下一入口 |
|---|---|---|---|---|---|---|---|
| app-shell | 全局应用外壳 | 已实现（布局/菜单/路由/公共主题） | README、REQUIREMENTS | LOCAL_CANDIDATE（未入库，待独立复审） | docs/features/app-shell/（未提交） | 未进入 Git；批准证据未核验 | 复审（独立任务） |

## 本地候选说明

`docs/features/app-shell/`、`docs/features/large-screen/` 为服务器上未提交的 Feature 基线候选，按 PROJECT-BASELINE-AND-DOCUMENTATION-RECOVERY-001 §12 处理：列入本索引、标记"本地候选待独立复审"，默认不随本任务批量提交；只有被当前 Git、批准报告和实现充分证明且不引入新规则时，才可作为恢复文件提交。

## 下一步

各 Feature 的缺失文档（README/DATABASE/DESIGN/API/UI/ACCEPTANCE）按 FEATURE_DEVELOPMENT_AND_ADJUSTMENT_PROCESS.md 通用流程单独建立，本索引随后更新。

## 变更记录

| 日期 | 变更 | 依据 |
|---|---|---|
| 2026-08-27 | 仅同步 `server-config` 已批准需求/验收基线事实（基线覆盖、基线状态、最新有效证据、当前缺口、下一入口），不批准其他 Feature，不改变路由和菜单数量事实；本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | SERVER-CONFIG-FEATURE-BASELINE-APPROVAL-001（项目负责人批准驱动的 Feature 需求与验收基线收口；纯文档任务） |
| 2026-08-27 | 仅同步 `server-config` 已批准设计基线事实：基线覆盖更新为 REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE、reports；基线状态 APPROVED；最新有效证据更新为设计批准报告及完整设计链；当前缺口更新为未实现、65 条验收未执行、Feature README 待实现验收收口；下一入口更新为阶段5实现任务规划/前后端实现；不批准其他 Feature，不改变路由和菜单数量事实；本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | SERVER-CONFIG-DESIGN-BASELINE-APPROVAL-001（项目负责人批准驱动的阶段 4 设计基线收口；纯文档任务） |
| 2026-08-28 | 仅同步 `server-config` 验收前调整批准基线事实：基线状态保持 `APPROVED`；实现状态明确为 `IMPLEMENTED_ADJUSTMENT_PENDING`（旧批准版本已实现、两项已批准调整尚未实现）；最新有效证据增加验收前调整批准报告 SERVER-CONFIG-PRE-ACCEPTANCE-ADJUSTMENT-BASELINE-APPROVAL-001（批准提交 `b1c5349...`）；验收数量更新为 66 条全部 `NOT_RUN`；当前缺口更新为两项已批准调整代码尚未实现、66 条验收未执行；下一入口更新为两项已批准调整的小范围实现；不批准其他 Feature，不改变路由和菜单数量事实；本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | SERVER-CONFIG-PRE-ACCEPTANCE-ADJUSTMENT-BASELINE-APPROVAL-001（项目负责人批准驱动的验收前调整基线收口；纯文档任务） |
| 2026-08-29 | 仅同步 `server-config` 正式验收收口事实：代码状态更新为已实现并验收接受（`IMPLEMENTED_ACCEPTED`）；正式验收 66 条全部通过（`66 PASSED / 0 FAILED`）；最新有效证据更新为最终收口报告、定向重验报告及提交 `b5aeec2...`；当前缺口更新为无；下一入口更新为未来如有新需求按需求调整流程重新进入，不保留本轮待实现/待验收任务；不批准其他 Feature，不改变路由和菜单数量事实；本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | SERVER-CONFIG-FORMAL-ACCEPTANCE-CLOSEOUT-001（正式验收与功能基线最终收口；纯文档任务） |
| 2026-08-30 | 数据订阅 Feature 文档标识统一：Feature 标识由 `data-subscribe` 更新为 `data-subscription`，索引目录指向 `docs/features/data-subscription/`；基线覆盖更新为 REQUIREMENTS、ACCEPTANCE，基线状态更新为 DRAFT（`REQUIREMENTS.md`/`ACCEPTANCE.md` 为 `DRAFT_PENDING_USER_REVIEW` 草案，未批准、未实现）；注明前端实现目录 `views/data-subscribe/` 与文档目录标识允许不同；不改变前端代码目录、路由、菜单、代码包名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-REQUIREMENTS-BASELINE-001-R1（ChatGPT 正式复审 CHANGES_REQUIRED 定向修订；纯文档任务） |
| 2026-08-30 | 仅同步 `data-subscription` 需求与验收基线批准事实：基线状态更新为 `APPROVED`（仅文档基线获批，不代表功能已实现或验收已通过）；最新有效证据增加本批准任务及批准收口报告（依据 R1 结果提交 `b9fb1e9...` 的 ChatGPT 正式复审 `APPROVED`）；当前缺口更新为 DESIGN/API/UI/DATABASE 设计基线未建立、功能尚未实现（前端仍占位）、126 条验收未执行（`NOT_RUN`）；下一入口更新为设计阶段；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-REQUIREMENTS-BASELINE-APPROVAL-001（项目负责人批准驱动的需求与验收基线正式收口；纯文档任务） |
| 2026-08-30 | 仅同步 `data-subscription` 设计基线草案事实：基线覆盖更新为 REQUIREMENTS、ACCEPTANCE、DESIGN、API、UI、DATABASE（后四者为设计基线草案，状态 `DRAFT_PENDING_USER_REVIEW`，未正式复审批准）；需求与验收基线仍为 `APPROVED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；最新有效证据增加设计任务报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001.md`；当前缺口更新为设计草案待正式复审批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为设计复审；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001（纯文档设计基线草案建立；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-30 | 仅同步 `data-subscription` 点号保留分隔符需求调整草案事实：需求/验收当前为点号调整草案（状态 `DRAFT_PENDING_USER_REVIEW`，待正式复审；上一正式批准版本提交 `d7560445...` 作为历史证据保留，需求与验收批准事实不因草案而失效）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，且设计复审为 `CHANGES_REQUIRED`，等待需求调整复审后再执行设计 R1；当前缺口更新为点号需求调整待复审、设计 R1 待执行、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为点号需求调整复审；最新有效证据增加点号调整报告 `reports/DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-001（项目负责人决策驱动的纯文档需求/验收定向调整草案） |
| 2026-08-30 | 仅同步 `data-subscription` 点号保留分隔符需求调整批准收口事实：需求/验收当前版本状态更新为 `APPROVED`（ChatGPT 对提交 `bb8716c...` 正式复审 `APPROVED`；本次批准只批准点号需求和对应验收标准，不批准设计、不实现功能、不执行验收）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，且设计复审仍为 `CHANGES_REQUIRED`，待设计 R1 定向修订后重新复审；当前缺口更新为设计 R1 待执行、设计待重新复审、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为设计 R1 定向修订；最新有效证据增加点号调整批准收口报告 `reports/DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DOT-DELIMITER-REQUIREMENTS-ADJUSTMENT-APPROVAL-001（项目负责人批准驱动的纯文档需求/验收调整批准收口） |
| 2026-08-30 | 仅同步 `data-subscription` 设计基线 R1 定向修订事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；R1 定向修订已完成（修正 ChatGPT 正式复审全部发现项：原子行锁并发、查询 OR/AND 分组与 token 精确匹配、结构化 `sourceTables`/`PRESERVE\|REPLACE` 唯一契约、删除预览令牌闭环、`@TableId IdType.INPUT` 依据修正、能力分层 Schema 过滤、真实章节交叉引用，并同步已批准点号规则与收紧项），待重新正式复审；需求/验收保持 `APPROVED`（107 条，点号批准版本 `bb8716c...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R1 草案待正式复审、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为设计 R1 正式复审；最新有效证据增加 R1 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R1.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R1（纯文档设计基线定向修订；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-30 | 仅同步 `data-subscription` 含逗号查询需求调整草案事实：需求/验收状态由 `APPROVED` 更新为 `DRAFT_PENDING_USER_REVIEW`（当前版本为“含逗号数据源 ID 查询兼容”调整草案，尚未获得 ChatGPT 正式复审批准；上一正式批准版本为点号保留分隔符批准版本，批准依据提交 `bb8716c...`，历史批准事实保留）；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为含逗号查询需求调整草案待正式复审、设计 R2 尚未执行、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为本需求调整草案正式复审；最新有效证据增加含逗号查询需求调整草案报告 `reports/DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-001（正式复审发现驱动的纯文档需求/验收定向调整草案） |
| 2026-08-30 | 仅同步 `data-subscription` 含逗号查询需求调整批准收口事实：需求/验收状态由 `DRAFT_PENDING_USER_REVIEW` 更新为 `APPROVED`（当前版本为“含逗号数据源 ID 查询兼容”调整批准版本，ChatGPT 对提交 `5d5b5f4...` 正式复审 `APPROVED`；上一“英文句点 `.` 保留分隔符”批准版本批准依据提交 `bb8716c...` 作为历史事实保留）；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R2 尚未执行、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R2` 定向修订；最新有效证据增加含逗号查询需求调整批准收口报告 `reports/DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-COMMA-ID-QUERY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001（项目负责人批准驱动的纯文档需求/验收调整批准收口） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线 R2 定向修订事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；R2 定向修订已完成（实现已批准三类查询语义在服务层 Java 字面量过滤中的落地、列表 API 响应结构调整为 items+queryWarnings、元数据 API 由路径参数改为 query 参数、Oracle 物化视图显式排除、DSUB-FP-V1 字节级确定性指纹并给出黄金测试向量；同步已批准含逗号查询规则与一致性收紧项），待重新正式复审；需求/验收保持 `APPROVED`（107 条，含逗号批准版本 `5d5b5f4...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R2 草案待正式复审、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为设计 R2 正式复审；最新有效证据增加 R2 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R2.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R2（纯文档设计基线 R2 定向修订；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-31 | 仅同步 `data-subscription` 取消并发保护需求调整草案事实：需求/验收状态由 `APPROVED` 更新为 `DRAFT_PENDING_USER_REVIEW`（当前版本为“取消并发保护”调整草案，尚未获得正式复审批准；上一正式批准版本为含逗号查询批准版本，批准依据提交 `5d5b5f4...`；更早“英文句点 `.` 保留分隔符”批准版本批准依据提交 `bb8716c...` 作为历史事实保留；产品负责人明确“数据订阅页面的新增、编辑、删除完全不处理并发操作”，本次仅定向调整需求 DSUB-REQ-097/098/099/103 与验收 DSUB-AC-107/108/109/110/114/117，删除版本令牌、内容指纹、行锁、并发字段比较、40910 等机制并明确为产品边界而非缺口）；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；R2 四项修订目标已通过正式复核，但产品负责人决策要求先完成本需求/验收调整复审，再执行设计 R3（删除并发设计：版本令牌/内容指纹/行锁等；修正多源库空 token 判定；补充 null-safe CSV 契约）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为取消并发保护需求调整草案待正式复审、设计 R3 待执行、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001` 正式复审；最新有效证据增加取消并发保护需求调整草案报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001（产品负责人决策驱动的纯文档需求/验收定向调整草案） |
| 2026-08-31 | 仅同步 `data-subscription` 取消并发保护需求调整草案 R1 事实：ChatGPT 对提交 `27a27e3...` 正式复审结论 `CHANGES_REQUIRED`，R1 定向修订已完成——修正 `DSUB-AC-048` 并发保护残留（结尾“并发保护规则执行”改为“不做并发保护”，该行编号/状态/关联需求/前置条件/查看/编辑行为不变，相对 `27a27e3...` 仅该一条验收业务行变化，相对 `026417e7...` 本轮“取消并发保护”调整累计承载验收业务行 7 条 `DSUB-AC-048/107/108/109/110/114/117`、其余 119 条不变），并修正原报告对设计 R2 三类查询语义的错误描述（改为三类数据源 ID 查询规则：普通不含逗号 ID 完整 token 字面精确匹配、仅含句点不含逗号 ID 仍完整 token 字面精确匹配、含逗号 ID 历史兼容可能匹配并经 `queryWarnings` 展示歧义警告）；需求/验收状态继续为 `DRAFT_PENDING_USER_REVIEW`，依据需求状态继续为 `DRAFT_PENDING_USER_REVIEW`；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为取消并发保护需求/验收调整草案 R1 待 ChatGPT 正式复审、设计 R3 尚未执行、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1` 正式复审；最新有效证据增加取消并发保护需求调整 R1 定向修订报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1（ChatGPT 复审 CHANGES_REQUIRED 驱动的纯文档定向修订） |
| 2026-08-31 | 仅同步 `data-subscription` 取消并发保护需求/验收调整批准收口事实：需求/验收状态由 `DRAFT_PENDING_USER_REVIEW` 更新为 `APPROVED`（当前正式批准版本为“取消并发保护”需求与验收调整版本，ChatGPT 对 R1 结果提交 `43a9097...` 正式复审 `APPROVED`；上一“含逗号数据源 ID 查询兼容”批准版本批准依据提交 `5d5b5f4...`、更早“英文句点 `.` 保留分隔符”批准版本批准依据提交 `bb8716c...` 作为历史事实保留；文档基线批准不代表功能已实现或验收已通过）；DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审仍为 `CHANGES_REQUIRED`；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R3 尚未执行（删除版本令牌/`DSUB-FP-V1`/黄金向量/行锁/并发字段比较/`40910`，编辑与删除改为普通主键更新/物理删除，删除预览只读不返回令牌，修正多源库异常空 token 归一化，补充 nullable CSV 的 null-safe 契约）、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3`；最新有效证据增加取消并发保护需求调整批准收口报告 `reports/DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-NO-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001（项目负责人批准驱动的纯文档需求/验收调整批准收口） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线 R3 定向修订事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审状态由 `CHANGES_REQUIRED` 更新为 `PENDING_R3_REVIEW`；R3 定向修订已完成（删除旧并发设计：版本令牌/`DSUB-FP-V1`/黄金向量/行锁/并发字段比较/`40910`；编辑打开与保存、删除预览与删除改为普通主键读写，无并发保证、最后一次成功写入生效，删除预览只读不返回令牌；修正多源库空 token 判定；补充 nullable CSV 的 null-safe 契约；保留 R2 已通过的三类数据源 ID 查询规则、元数据 API 改为 query 参数、物化视图显式排除），待 ChatGPT 正式复审；需求/验收保持 `APPROVED`（107 条，当前正式批准版本为“取消并发保护”需求与验收调整版本，批准依据提交 `43a9097...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R3 待 ChatGPT 正式复审、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3` 正式复审；最新有效证据增加设计 R3 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R3（纯文档设计基线 R3 定向修订；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线 R4 定向修订事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审状态由 `PENDING_R3_REVIEW` 更新为 `PENDING_R4_REVIEW`；ChatGPT 对 R3 结果提交 `ac49544...` 正式复审结论 `CHANGES_REQUIRED`，R4 按正式复审发现定向修正三个确定问题（DELETE 影响多行错误码统一为 `50041 DELETE_FAILED` 且不映射 `50040`；DELETE 接口删除前多源库异常后端防护统一为强制普通读取→存在性与异常判定→普通 DELETE，删除预览不替代 DELETE 自身防护；修正“NULL 被 split 成 `['']`”的 Java 语义错误为“对 null 调用 split 抛 `NullPointerException`”）；需求/验收保持 `APPROVED`（107/126，取消并发保护批准版本 `43a9097...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R4 待 ChatGPT 正式复审、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4` 正式复审；最新有效证据增加设计 R4 定向修订报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4（纯文档设计基线 R4 定向修订；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线 R4-R1 状态元数据收口事实：DESIGN/API/UI/DATABASE 仍为 `DRAFT_PENDING_USER_REVIEW` 设计草案，设计复审状态保持 `PENDING_R4_REVIEW`；ChatGPT 对 R4 结果提交 `2d81f19...` 正式复审：三项业务修正（DELETE 错误码 `50041`、DELETE 删除前多源库异常后端防护、Java null/split 语义）均通过，但四文档顶部/页尾复审状态标记不一致（结论 `CHANGES_REQUIRED`）；R4-R1 已定向统一状态元数据（DESIGN/DATABASE 页尾由“R3 定向修订版/等待 R3 复审”更新为 R4 状态并指向 R4-R1 正式复审；API/UI 顶部 `PENDING_R3_REVIEW` 更新为 `PENDING_R4_REVIEW` 且注明业务设计零语义变化、页尾同步指向 R4-R1 正式复审），业务语义零变化；需求/验收保持 `APPROVED`（107/126，取消并发保护批准版本 `43a9097...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为设计 R4-R1 待 ChatGPT 正式复审、设计尚未批准、功能尚未实现（前端仍占位）、126 条验收未执行；下一入口更新为 `DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4-R1` 正式复审；最新有效证据增加设计 R4-R1 状态元数据收口报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4-R1.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-001-R4-R1（纯文档设计基线状态元数据定向收口；设计草案未批准、功能未实现、验收未执行） |
| 2026-08-31 | 仅同步 `data-subscription` 设计基线正式批准收口事实：DESIGN/API/UI/DATABASE 设计基线状态由 `DRAFT_PENDING_USER_REVIEW` 更新为 `APPROVED`，设计复审结论由 `PENDING_R4_REVIEW` 更新为 `APPROVED`（批准版本 R4，R4-R1 完成状态元数据收口，ChatGPT 对 R4-R1 结果提交 `ba7fedd...` 正式复审结论 `APPROVED`；批准不代表功能已实现或验收已通过）；需求/验收保持 `APPROVED`（107/126，取消并发保护批准版本 `43a9097...`）；代码状态仍为“占位”（`NOT_STARTED`）；126 条验收仍全部 `NOT_RUN`；TBD-01/TBD-02 设计结论随四份设计基线正式批准闭环；当前缺口更新为功能尚未实现、业务/测试代码尚未建立、126 条验收未执行、大屏修正仍延期；下一入口更新为数据订阅实现阶段任务规划/实现基线建立；最新有效证据增加设计批准收口报告 `reports/DATA-SUBSCRIPTION-DESIGN-BASELINE-APPROVAL-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-DESIGN-BASELINE-APPROVAL-001（项目负责人批准驱动的数据订阅设计基线正式批准收口；纯文档任务） |
| 2026-08-31 | 仅同步 `data-subscription` 前端实现事实：代码状态由“占位”（`NOT_STARTED`）更新为“部分实现（后端实现完成；前端 `IMPLEMENTED_PENDING_REVIEW`）”；前端已完整实现（列表/详情/新增/编辑/删除弹窗、近全屏可移动弹窗、左 Schema 右表格高容量源表选择器、源库四级搜索、PRESERVE/REPLACE 编辑保存、删除预览等，严格对接已批准设计与 10 项 API，不包含任何并发令牌/指纹/快照版本字段、不分页/不自动刷新/无表头排序、无最右侧已选源表面板），数据订阅前端定向测试 91 用例、前端全量 `npm test` 326 用例（23 文件）、`npm run build` 全部 PASS，后端数据订阅模块 106 用例回归 PASS；后端 `mvn clean package` 存在 4 个既有无关失败（`OracleDateMappingTest` 1 + `JobFailureServiceTest` 3，均为共享开发库数据漂移导致的 `@SpringBootTest` 集成断言失败，本任务零后端改动、不负责修复，如实记录）；真实浏览器只读视觉联调 PASS（列表/详情/新增/编辑回显/删除预览，无 console error；截图仅存 `/tmp` 未提交）；需求/验收与 DESIGN/API/UI/DATABASE 基线保持 `APPROVED`；126 条验收仍全部 `NOT_RUN`；当前缺口更新为前端待 ChatGPT 正式代码与视觉复审、126 条验收未执行、大屏修正仍延期；下一入口更新为 ChatGPT 对前端实现结果提交进行正式代码与视觉复审；最新有效证据增加前端实现报告 `reports/DATA-SUBSCRIPTION-FRONTEND-IMPLEMENTATION-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-FRONTEND-IMPLEMENTATION-001（数据订阅前端完整实现并验证；纯前端+报告任务，未执行正式验收） |
| 2026-09-01 | 仅同步 `data-subscription` 正式验收前 UI 交互基线对齐草案事实：代码状态更新为“已实现并经 ChatGPT 正式复审通过（后端实现及真实数据库集成验证、前端 R3 代码与视觉、R3-R1 报告元数据收口均已获正式批准；待正式验收）”；需求/验收/设计/UI 状态由 `APPROVED` 更新为 `DRAFT_PENDING_USER_REVIEW`（正式验收前 UI 交互基线对齐草案：仅把已确认、已实现、已复审通过的最终交互事实——弹窗 `1280px`/`82vh` 及视口约束、目标库卡片白色主体四态、Shift 连选——同步为待正式复审的文档草案，上一正式批准版本保留为历史事实，待 ChatGPT 正式复审）；API/DATABASE 保持 `APPROVED`（零变化）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为后端与前端实现均已完成并经 ChatGPT 正式复审通过（待正式验收，非 `IMPLEMENTED_ACCEPTED`）、本 UI 交互基线对齐草案待 ChatGPT 正式复审与批准收口后执行正式验收、126 条验收未执行、大屏修正仍延期（`DEFERRED_AFTER_DATA_SUBSCRIPTION_FEATURE_ACCEPTANCE`）；下一入口更新为 ChatGPT 对本 UI 交互基线对齐草案（`DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001`）正式复审，复审通过后先批准收口，再执行 126 条正式验收；最新有效证据增加正式验收前 UI 交互基线对齐草案报告 `reports/DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001（正式验收前纯文档 UI 交互基线对齐草案；待 ChatGPT 正式复审，不代表正式验收通过） |
| 2026-09-01 | 仅同步 `data-subscription` 正式验收前 UI 交互基线对齐草案 R1 状态文字定向修订事实：ChatGPT 对结果提交 `fd58688...` 正式复审结论 `CHANGES_REQUIRED`——UI 交互业务调整全部通过，唯一问题是 `ACCEPTANCE.md` 两处把“尚未实现/NOT_STARTED”写成当前事实的过期文字（§1 状态说明与 §4 表前）与已正确记录的实现状态 `IMPLEMENTED_REVIEW_APPROVED_PENDING_FORMAL_ACCEPTANCE` 冲突；R1 已定向修正该状态冲突（§1 改写为当前事实并限定历史 `NOT_STARTED` 为当时状态、§4 改为“本 Feature 已实现并完成实现复审，但尚未执行正式验收”），业务规则零变化；需求/验收/设计/UI 状态继续为 `DRAFT_PENDING_USER_REVIEW`；API/DATABASE 保持 `APPROVED`（零变化）；126 条验收仍全部 `NOT_RUN`；当前缺口更新为本 R1 状态文字定向修订待 ChatGPT 正式复审、批准收口后执行正式验收、126 条验收未执行、大屏修正仍延期（`DEFERRED_AFTER_DATA_SUBSCRIPTION_FEATURE_ACCEPTANCE`）；下一入口更新为 ChatGPT 对 R1 结果提交（`DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001-R1`）正式复审，复审通过后先执行基线批准收口，再执行 126 条正式验收；最新有效证据增加正式验收前 UI 交互基线对齐草案 R1 状态文字定向修订报告 `reports/DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001-R1.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-001-R1（ChatGPT 复审 CHANGES_REQUIRED 驱动的纯文档状态文字定向修订；待 ChatGPT 正式复审，不代表正式验收通过） |
| 2026-09-01 | 仅同步 `data-subscription` 正式验收前 UI 交互基线批准收口事实：ChatGPT 对 R1 结果提交 `26094c6...` 正式复审结论 `APPROVED`，REQUIREMENTS/ACCEPTANCE/DESIGN/UI 文档状态由 `DRAFT_PENDING_USER_REVIEW` 更新为 `APPROVED`（正式验收前 UI 交互基线对齐草案及 R1 状态文字修订获正式批准收口；R1 已消除 `ACCEPTANCE.md` 两处过期“尚未实现/NOT_STARTED”当前态冲突；107 条需求与 126 条验收业务行逐字保持，126 条验收仍全部 `NOT_RUN`；本次批准的是 UI 交互基线文档标准，不代表正式验收执行或通过，不代表 `IMPLEMENTED_ACCEPTED`）；API/DATABASE 保持 `APPROVED`（零变化）；实现状态保持 `IMPLEMENTED_REVIEW_APPROVED_PENDING_FORMAL_ACCEPTANCE`；当前缺口更新为 126 条正式验收尚未执行、大屏修正仍延期（`DEFERRED_AFTER_DATA_SUBSCRIPTION_FEATURE_ACCEPTANCE`）；下一入口更新为数据订阅 126 条正式验收任务规划与执行；最新有效证据增加正式验收前 UI 交互基线对齐批准收口报告 `reports/DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-APPROVAL-001.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-APPROVAL-001（ChatGPT 复审 APPROVED 驱动的纯文档批准收口；不代表正式验收通过） |
| 2026-09-01 | 仅同步 `data-subscription` 正式验收前 UI 交互基线批准收口 R1 状态元数据定向修订事实：ChatGPT 对批准收口结果提交 `dc97ffd...` 正式复核结论 `CHANGES_REQUIRED`（正式验收前 UI 交互业务内容、107 条需求与 126 条验收业务行、批准边界和提交范围全部通过；唯一问题是 DESIGN/UI 依赖基线仍标为草案、当前批准版本说明停留在 R4、四份文档实现状态说明仍把当前版本称为草案）；R1 已定向修正这些状态元数据（DESIGN/UI 依据的已批准需求/验收基线更新为当前 REQUIREMENTS/ACCEPTANCE `APPROVED` 并注明 UI 对齐内容正式复审依据 `26094c6...`，`8331fbb...` 不再作为当前依赖基线；当前批准设计/界面基线明确已纳入 UI 交互对齐内容，R4/R4-R1 仅作为历史批准基础保留；四份文档实现状态说明不再把当前版本称为草案），业务语义零变化；REQUIREMENTS/ACCEPTANCE/DESIGN/UI/API/DATABASE 均保持 `APPROVED`；实现状态保持 `IMPLEMENTED_REVIEW_APPROVED_PENDING_FORMAL_ACCEPTANCE`；126 条验收仍全部 `NOT_RUN`；当前仍不是正式验收通过或 `IMPLEMENTED_ACCEPTED`；当前缺口更新为 R1 状态元数据定向修订待 ChatGPT 正式复审、126 条正式验收未执行、大屏修正仍延期（`DEFERRED_AFTER_DATA_SUBSCRIPTION_FEATURE_ACCEPTANCE`）；下一入口更新为 ChatGPT 对 R1 结果提交正式复审，R1 复审通过后才允许开始数据订阅 126 条正式验收任务规划与执行；最新有效证据增加批准收口 R1 状态元数据定向修订报告 `reports/DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-APPROVAL-001-R1.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-PRE-ACCEPTANCE-UI-BASELINE-ALIGNMENT-APPROVAL-001-R1（ChatGPT 正式复核 CHANGES_REQUIRED 驱动的纯文档状态元数据定向修订；待 ChatGPT 正式复审，不代表正式验收通过） |
| 2026-09-02 | 仅同步 `data-subscription` 正式验收执行事实：代码状态更新为“已实现并经 ChatGPT 正式复审通过，正式验收已执行（126 条全部 PASS）”；实现状态更新为 `IMPLEMENTED_FORMAL_ACCEPTANCE_EXECUTED_PENDING_REVIEW`（正式验收已执行，待 ChatGPT 正式复审；不是 `IMPLEMENTED_ACCEPTED`）；126 条正式验收已执行（全部 `PASS`），正式验收结果待 ChatGPT 正式复审；REQUIREMENTS/ACCEPTANCE/DESIGN/API/UI/DATABASE 均保持 `APPROVED`（业务行零变化）；当前缺口更新为正式验收结果待 ChatGPT 正式复审（正式复审通过前非 `IMPLEMENTED_ACCEPTED`）、大屏修正仍延期（`DEFERRED_AFTER_DATA_SUBSCRIPTION_FEATURE_ACCEPTANCE`）；下一入口更新为 ChatGPT 对正式验收结果提交进行正式复审，正式复审通过后执行最终验收接受收口并更新为 `IMPLEMENTED_ACCEPTED`；最新有效证据增加正式验收执行报告 `reports/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001.md` 与证据目录 `evidence/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001/`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001（数据订阅正式验收执行；纯文档+证据任务，验收业务语义零变化） |
| 2026-09-02 | 仅同步 `data-subscription` 正式验收 R1-R1 状态与证据元数据定向修订事实：ChatGPT 对 R1 结果提交 `f76239b...` 正式复审结论 `CHANGES_REQUIRED`（R1 真实浏览器补验 54 场景、干净 worktree 定向 124/全量 359、构建、数据库恢复与 126 条证据映射均通过复审，仅三处状态/证据元数据矛盾需定向修正）；R1-R1 已定向修正 ACCEPTANCE 顶部与 §1 残留当前态 `NOT_RUN`（限定为历史阶段）、S4-7a/S4-7b 保留字符汇总文字（按 `s4b-shift240.json reservedSelected=[]`/`s4b-anchor240.json reservedFinal=[]` 修正为“保留字符未被选中/已跳过”）、S5-9 场景机读与索引追加 `DSUB-AC-126`；126 条验收业务行逐字零变化并全部保持 `PASS`，浏览器场景数量保持 54；纯文档与既有证据元数据修订，未运行测试/构建/浏览器/服务或数据库操作；REQUIREMENTS/ACCEPTANCE/DESIGN/API/UI/DATABASE 均保持 `APPROVED`；实现状态保持 `IMPLEMENTED_FORMAL_ACCEPTANCE_EXECUTED_PENDING_REVIEW`，验收执行状态保持 `EXECUTED_PENDING_REVIEW`；当前缺口更新为 R1-R1 元数据修订结果待 ChatGPT 正式复审（正式复审通过前非 `IMPLEMENTED_ACCEPTED`）、大屏修正仍延期（`DEFERRED_AFTER_DATA_SUBSCRIPTION_FEATURE_ACCEPTANCE`）；下一入口更新为 ChatGPT 对 R1-R1 结果提交正式复审，正式复审通过后执行最终验收接受收口并更新为 `IMPLEMENTED_ACCEPTED`；最新有效证据增加 R1-R1 报告 `reports/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001-R1-R1.md`；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-001-R1-R1（数据订阅正式验收 R1 状态与证据元数据定向修订；纯文档+证据元数据任务，验收业务语义零变化） |
| 2026-09-02 | 仅同步 `data-subscription` 正式验收接受收口事实：ChatGPT 对正式验收初始结果 `49eb778...`、R1 实质证据补验结果 `f76239b...`、R1-R1 元数据定向修订结果 `ae66d90...` 进行正式复审，最终结论 `APPROVED`：126 条正式验收用例全部 `PASS`（0 `FAIL`/0 `BLOCKED`/0 `NOT_RUN`）、126 条验收业务行逐字零变化、R1 的 54 真实浏览器场景/干净 worktree 前端测试与构建/真实 HTTP/源 Oracle 元数据/数据库恢复证据继续有效、R1-R1 三项元数据修订正确、需求/设计/API/UI/数据库及前后端业务与测试代码无未经批准变化；代码状态更新为已实现并验收接受（`IMPLEMENTED_ACCEPTED`）；REQUIREMENTS/ACCEPTANCE/DESIGN/API/UI/DATABASE 文档状态均保持 `APPROVED`，正式验收状态更新为 `ACCEPTED`，验收执行状态更新为 `PASS`（126 条全部 `PASS`），本 Feature 不再存在“等待实现复审/等待正式验收复审”缺口；当前缺口更新为无待复审缺口、大屏 `DATA_SOURCE_TABLE` 换行符解析修正尚未执行（`READY_FOR_SEPARATE_TASK`，已具备另立独立任务前置条件，本次未执行）；下一入口更新为大屏 `DATA_SOURCE_TABLE` 英文逗号分隔解析修正任务的需求核验与任务规划，后续本 Feature 功能变更应重新走需求/设计/实现/验收流程；最新有效证据增加正式验收接受收口报告 `reports/DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-CLOSEOUT-001.md`；本任务为纯文档收口，未修改业务语义/代码/测试/证据，未运行测试/构建/浏览器/服务，未访问数据库、未执行 DDL/DML，未操作 ZooKeeper/Kafka/sync-client；前端目录仍为 `views/data-subscribe/`、路由仍为 `/config/subscribe`，均不重命名；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SUBSCRIPTION-FORMAL-ACCEPTANCE-CLOSEOUT-001（数据订阅正式验收接受收口；纯文档任务，仅收口状态元数据与执行汇总） |
| 2026-09-03 | 仅同步 `client-config`（探针端管理）需求与验收草案建立事实：为当前只有占位页、`BASELINE_NOT_ESTABLISHED` 的 `client-config` 建立第一版需求与验收草案（纯文档）；基线覆盖更新为 README、REQUIREMENTS、ACCEPTANCE；基线状态更新为 DRAFT（`REQUIREMENTS.md`/`ACCEPTANCE.md` 为 `DRAFT_PENDING_USER_REVIEW` 草案，未批准、未实现）；功能名称列为目标用户可见名“探针端管理”（当前占位仍显示“客户端配置”）；代码状态仍为“占位”（功能未实现，`NOT_STARTED`）；验收 `CCFG-AC-001~076` 共 76 例全部 `NOT_RUN`；最新有效证据增加 docs/features/client-config/ 三份文档；当前缺口更新为需求与验收草案待正式复审与批准、功能未实现（更名与探针 CRUD 未实施）、缺 DESIGN/API/UI/DATABASE；既有路由 `/config/client` 与前端 `views/client-config/` 目录名保持既有值不变；基线影响项（旧表述“客户端配置”“管理平台仅只读/人工维护”“编辑时探针 ID 不可改”、`CLIENT_DESC` 256 与 1024 数据库基线长度差异、既有 ≤20 条记录事实与“不新增数量上限”不冲突）记录于 client-config REQUIREMENTS §9；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | CLIENT-CONFIG-REQUIREMENTS-BASELINE-001（全新 Feature 需求与验收基线建立；纯文档任务，草案未批准、功能未实现、验收未执行） |
| 2026-09-03 | 仅同步 `client-config`（探针端管理）需求与验收基线批准收口事实：基线状态由 DRAFT 更新为 `APPROVED`（ChatGPT 对 R1 结果提交 `9b31893...` 正式复审结论 `APPROVED`，项目负责人于 2026-09-03 明确回复“批准”；本次批准的是需求基线与验收标准本身，不代表功能已实现或验收已通过）；代码状态仍为“占位”（功能未实现，`NOT_STARTED`）；验收 `CCFG-AC-001~076` 共 76 例仍全部 `NOT_RUN`；最新有效证据增加需求/验收草案报告 `reports/CLIENT-CONFIG-REQUIREMENTS-BASELINE-001.md`、R1 定向修订报告 `reports/CLIENT-CONFIG-REQUIREMENTS-BASELINE-001-R1.md`、批准收口报告 `reports/CLIENT-CONFIG-REQUIREMENTS-BASELINE-APPROVAL-001.md`（首版草案提交 `abf2f40...` 为历史依据）；当前缺口更新为功能未实现（更名与探针 CRUD 未实施）、76 条验收未执行、缺 DESIGN/API/UI/DATABASE、`CLIENT_DESC` 256 与 1024 已批准数据库基线长度差异待后续独立同步；下一入口更新为设计基线（`CLIENT_CONFIG_DESIGN_BASELINE`）；既有路由 `/config/client` 与前端 `views/client-config/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | CLIENT-CONFIG-REQUIREMENTS-BASELINE-APPROVAL-001（项目负责人批准驱动的需求与验收基线批准收口；纯文档任务，未实现、未执行验收） |
| 2026-09-03 | 仅同步 `client-config`（探针端管理）设计基线草案建立事实：新增 `DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md` 四份设计草案，状态 `DRAFT_PENDING_USER_REVIEW`（未正式复审批准，`PENDING_USER_CONFIRMATION=0`）；需求/验收保持 `APPROVED`（90/76，批准依据提交 `9b31893...`）；实现状态仍为占位（`NOT_STARTED`）；76 条验收仍全部 `NOT_RUN`；当前缺口更新为四份设计草案待 ChatGPT 正式设计复审与批准、功能未实现（更名与探针 CRUD 未实施）、76 条验收未执行；下一入口更新为 `CHATGPT_FORMAL_DESIGN_REVIEW`；最新有效证据增加设计草案建立报告 `reports/CLIENT-CONFIG-DESIGN-BASELINE-001.md`；既有路由 `/config/client` 与前端 `views/client-config/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | CLIENT-CONFIG-DESIGN-BASELINE-001（阶段 4 设计基线草案建立；纯文档任务，设计草案未批准、功能未实现、验收未执行） |
| 2026-09-04 | 仅同步 `client-config`（探针端管理）设计基线 R1 定向修订事实：ChatGPT 对初版设计草案提交 `21f4729...` 正式设计复审结论 `CHANGES_REQUIRED`（R1-01~R1-09），R1 已定向修订完成（纯文档）；API 设计编号按定义行重排为 `CCFG-API-001~020`（20 条定义行，连续唯一，修正初版 35 个定义行/仅 14 个不同编号的重复定义）；四文档编号范围同步为 `CCFG-DESIGN-001~037`/`CCFG-API-001~020`/`CCFG-UI-001~026`/`CCFG-DB-001~022`；统一“数据源数组原存储顺序返回 + 前端非持久化前三项投影”单一契约；固定 `CLIENT_DESC` 原文保存、Trim 仅判空、按原文计 UTF-8 字节；关键词 LIKE 补 `\` 字面量转义与 `ESCAPE '\'`；删除未批准的数据源 ID“其他非法字符”限制；补齐 `CATEGORY_MISMATCH`/`TYPE_MISMATCH`、含逗号歧义（`COMMA_PROTOCOL_AMBIGUOUS`/`rawDataSourceIds`/`possibleCommaDataSourceIds`）与历史 NULL/空白 `CLIENT_DESC` 契约；§12 追踪矩阵改为全称编号，需求设计覆盖 90/90、验收设计覆盖 76/76；需求/验收保持 `APPROVED`（90/76，零差异）；实现状态保持 `NOT_STARTED`；76 条验收仍全部 `NOT_RUN`；四文档状态保持 `DRAFT_PENDING_USER_REVIEW`、`PENDING_USER_CONFIRMATION=0`，待 ChatGPT 正式 R1 复审；`git fetch origin develop` 已按要求实际执行并核验本地/`origin/develop`/`git ls-remote` 一致；下一入口更新为 `CHATGPT_FORMAL_DESIGN_R1_REVIEW`；最新有效证据增加 R1 定向修订报告 `reports/CLIENT-CONFIG-DESIGN-BASELINE-001-R1.md`；既有路由 `/config/client` 与前端 `views/client-config/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | CLIENT-CONFIG-DESIGN-BASELINE-001-R1（正式设计复审 `CHANGES_REQUIRED` 驱动的定向修订；纯文档任务，设计草案未批准、功能未实现、验收未执行） |
| 2026-09-04 | 仅同步 `client-config`（探针端管理）并发口径定向调整草案事实：按项目负责人明确决定，需求/验收并发口径由旧需求的并发强保证“数据源唯一分配、并发最多一笔成功”（旧需求/验收只把该并发强保证作为目标承诺，未批准任何具体表锁语句；`LOCK TABLE CDC_CLIENT_MULTIPLE IN EXCLUSIVE MODE WAIT 5` 是后续未批准设计草案为实现该强保证提出的方案，本轮已过时）调整为“新增/编辑/启用写入前重新读取 + 尽力写前检查 + 已接受极端并发下两笔先后都成功边界；运行侧 `sync-client`/`sync-server` 使用配置时的重复检查为最终防线但不属本 Feature 范围”；基线状态由旧口径 `APPROVED` 更新为 `DRAFT_PENDING_USER_REVIEW`（本轮并发口径调整草案，2026-09-03 批准保留为历史且不自动批准本轮调整）；受影响需求 `CCFG-REQ-038/068/071/072/074/077`、验收 `CCFG-AC-030/056/058/059/061/064`，需求 90/验收 76 编号与数量不变、76 条验收执行状态保持全部 `NOT_RUN`；实现状态保持 `NOT_STARTED`；四份设计文档本轮零改动，仍含已过时 `LOCK TABLE ... WAIT 5` 表锁方案，标记 `STALE_LOCK_DESIGN_PENDING_REQUIREMENTS_APPROVAL`，暂不可批准、不可用于实现，待需求调整重新批准后定向修订；最新有效证据增加并发口径调整草案报告 `reports/CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001.md`；下一入口更新为 `CHATGPT_FORMAL_REQUIREMENTS_ADJUSTMENT_REVIEW`；既有路由 `/config/client` 与前端 `views/client-config/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001（项目负责人决策驱动的需求/验收并发口径定向调整；纯文档任务，不修改设计文档、不实现、不执行验收） |
| 2026-09-04 | 仅同步 `client-config`（探针端管理）并发口径调整 R1 定向修订草案事实：ChatGPT 对并发口径调整首版结果（提交 `6071d7a...`）正式复审结论 `CHANGES_REQUIRED`（R1-01~R1-04），R1 已定向修订完成（纯文档）：修正状态残留与批准链归属（2026-09-03 批准仅覆盖旧需求/验收的并发强保证目标，`LOCK TABLE ... WAIT 5` 为后续未批准设计草案，未写成已批准内容）；探针 ID 唯一性限定为普通顺序新增/编辑与当次写前检查（`CCFG-REQ-038`），验收 `CCFG-AC-030` 并发子场景改用不受既有 `probe-001` 前置污染的新 ID 对 `race-001`/`RACE-001`、`CCFG-AC-058` 改为可执行的针对另一探针的顺序新增/编辑场景；需求 90/验收 76 编号与数量不变、仅业务行 `CCFG-REQ-038`/`CCFG-AC-030`/`CCFG-AC-058` 变化、76 条验收执行状态保持全部 `NOT_RUN`；实现状态保持 `NOT_STARTED`；四份设计文档本轮零改动；基线状态保持 `DRAFT_PENDING_USER_REVIEW`（2026-09-03 批准保留为历史且不自动批准本轮调整，待 ChatGPT 对 R1 结果正式复审与项目负责人重新批准）；最新有效证据增加 R1 定向修订报告 `reports/CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1.md`；下一入口更新为 `CHATGPT_FORMAL_REQUIREMENTS_ADJUSTMENT_R1_REVIEW`；既有路由 `/config/client` 与前端 `views/client-config/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-001-R1（ChatGPT 正式复审 `CHANGES_REQUIRED` 驱动的 R1 定向修订；纯文档任务，不修改设计文档、不实现、不执行验收） |
| 2026-09-04 | 仅同步 `client-config`（探针端管理）并发口径调整批准收口事实：ChatGPT 对并发口径调整 R1 结果提交 `f2a4d7d...` 正式复审结论 `APPROVED`（确认 R1-01~R1-04 全部正确完成、旧批准未批准任何具体 `LOCK TABLE` 语句、`CCFG-REQ-038` 区分普通操作/当次检查与极端并发例外、`CCFG-AC-030` 用独立 `race-001`/`RACE-001`、`CCFG-AC-058` 为可执行的单条记录顺序场景、需求 90/验收 76 连续唯一且全部 `NOT_RUN`、覆盖 90/90、业务行差异仅 `CCFG-REQ-038`/`CCFG-AC-030`/`CCFG-AC-058`、四份设计文档零修改、实现 `NOT_STARTED`），项目负责人于 2026-09-04 明确回复“批准”；`REQUIREMENTS.md`/`ACCEPTANCE.md` 基线状态由本轮待复审草案更新为 `APPROVED`（2026-09-03 批准保留为历史，不自动批准本轮调整；本轮依据独立复审与项目负责人明确回复）；需求 90/验收 76 编号与数量不变、业务行相对 R1 起点零差异、76 条验收执行状态保持全部 `NOT_RUN`；实现状态保持 `NOT_STARTED`；四份设计文档本轮零改动，仍为 `DRAFT_PENDING_USER_REVIEW` 草案、含已过时表锁方案，需求已重新批准，标记 `STALE_LOCK_DESIGN_PENDING_DESIGN_REVISION`，待独立设计并发口径调整任务定向修订；最新有效证据增加批准收口报告 `reports/CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001.md`；下一入口更新为 `CLIENT_CONFIG_DESIGN_CONCURRENCY_ADJUSTMENT`；既有路由 `/config/client` 与前端 `views/client-config/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001（项目负责人批准驱动的需求与验收批准收口；纯文档任务，不修改设计文档、不实现、不执行验收） |
| 2026-09-04 | 仅同步 `client-config`（探针端管理）设计并发口径调整事实：按已重新批准的需求/验收并发口径（`CLIENT-CONFIG-CONCURRENCY-REQUIREMENTS-ADJUSTMENT-APPROVAL-001`）定向调整四份设计文档（纯文档任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001`）：移除设计中已过时的 `LOCK TABLE ... WAIT 5` 表级互斥锁设计、`ORA-30006→50050 LOCK_WAIT_TIMEOUT` 专用锁等待错误及并发“最多一个成功”强承诺表述，统一为新/编辑/启用“开启普通短事务 + DML 前全量重读 + 当次尽力写前检查 + 发现冲突不执行 DML 返回业务冲突 + 未发现冲突立即 DML、接受极端并发竞态边界、无显式锁/无 `SELECT ... FOR UPDATE`/无 JVM/分布式锁/无 DDL 串行化”口径；保留顺序冲突业务码 `40940/40941/40942`、单请求原子性、row-count==1、失败回滚、历史异常展示及运行侧 `sync-client`/`sync-server` 最终防线（后者不属本 Feature 范围）；四份设计文档仍为 `DRAFT_PENDING_USER_REVIEW` 草案，状态标记由 `STALE_LOCK_DESIGN_PENDING_DESIGN_REVISION` 更新为 `DESIGN_CONCURRENCY_ADJUSTED_PENDING_FORMAL_REVIEW`，`PENDING_USER_CONFIRMATION=0`，待正式设计 R1 复审；需求与验收文档保持 `APPROVED`（90/76，零差异、全部 `NOT_RUN`），设计编号与覆盖不变（37/20/26/22，需求设计覆盖 90/90、验收设计覆盖 76/76）；实现状态保持 `NOT_STARTED`；最新有效证据增加执行报告 `reports/CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001.md`；下一入口更新为 `CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`；既有路由 `/config/client` 与前端 `views/client-config/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-001（正式需求并发口径重新批准驱动的设计并发口径定向调整；纯文档任务，设计草案未批准、功能未实现、验收未执行） |
| 2026-09-04 | 仅同步 `client-config`（探针端管理）设计并发口径调整批准收口事实：ChatGPT 对设计并发口径调整结果（提交 `ba7c5e9...`）正式复审（`CHATGPT_FORMAL_DESIGN_CONCURRENCY_ADJUSTMENT_REVIEW`）结论 `APPROVED`（确认四份设计文档已按已重新批准的需求并发口径完成调整：删除表锁/`WAIT 5`/`ORA-30006→50050`/并发“最多一个成功”表述，新增/编辑/启用统一为普通短事务 + DML 前全量重读 + 当次写前检查，接受极端并发双成功边界；37/20/26/22 条设计定义连续唯一、105 个设计引用可解析、需求设计覆盖 90/90、验收设计覆盖 76/76、相对批准提交业务行零差异；复审时四份设计文档为 `DRAFT_PENDING_USER_REVIEW`、实现 `NOT_STARTED`、76 条验收全部 `NOT_RUN`），项目负责人于 2026-09-04 明确回复“批准”；`DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md` 文档状态由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`（批准收口任务 `CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001`），整体标记由 `DESIGN_CONCURRENCY_ADJUSTED_PENDING_FORMAL_REVIEW` 收口为 `APPROVED_READY_FOR_IMPLEMENTATION`、`PENDING_USER_CONFIRMATION=0`，可用于后续实现；需求/验收保持 `APPROVED`（90/76、blob 零差异、全部 `NOT_RUN`），设计编号 37/20/26/22 与覆盖不变；批准的是设计基线，不代表代码已实现、已测试或验收已执行通过；实现状态保持 `NOT_STARTED`；76 条验收仍全部 `NOT_RUN`；最新有效证据增加批准收口执行报告 `reports/CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001.md`；下一入口更新为 `CLIENT_CONFIG_IMPLEMENTATION`；既有路由 `/config/client` 与前端 `views/client-config/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | CLIENT-CONFIG-DESIGN-CONCURRENCY-ADJUSTMENT-APPROVAL-001（设计并发口径调整结果批准收口；纯文档任务，未实现、未执行验收） |
| 2026-09-05 | 仅同步 `data-source-snapshot-status`（源库快照状态）需求与验收草案建立事实：为运行监控下原 `data-source-run-state`/“数据源运行状态”占位 Feature（此前 `BASELINE_NOT_ESTABLISHED`）建立第一版需求与验收草案并同步 Feature 命名（用户可见名“源库快照状态”、Feature 目录 `data-source-snapshot-status`；既有路由 `/monitor/data-source-state` 与前端目录 `views/data-source-run-state/` 保持既有值不变，命名映射见 Feature README §6）；Feature 标识由 `data-source-run-state` 更新为 `data-source-snapshot-status`；基线覆盖更新为 README、REQUIREMENTS、ACCEPTANCE；基线状态更新为 DRAFT（`REQUIREMENTS.md`/`ACCEPTANCE.md` 为 `DRAFT_PENDING_USER_REVIEW` 草案，未批准、未实现；requirements_status/acceptance_status=`DRAFT_PENDING_USER_REVIEW`、实现状态 `NOT_STARTED`、验收执行状态 `NOT_RUN`、设计状态 `NOT_STARTED`）；功能名称列为用户可见名“源库快照状态”；代码状态仍为“占位”（功能未实现，`NOT_STARTED`）；验收 `DSS-AC-001~067` 共 67 例全部 `NOT_RUN`；最新有效证据增加 docs/features/data-source-snapshot-status/ 三份文档与需求验收草案报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-001.md`；当前缺口更新为需求与验收草案待用户审阅/ChatGPT 正式复审与批准、功能未实现（前端 `views/data-source-run-state/DataSourceRunStatePage.vue` 仍为占位页）、缺 DESIGN/API/UI/DATABASE；全部数据库事实引用已提交数据库只读复核报告 `docs/database/reports/DATA-SOURCE-SNAPSHOT-STATUS-DATABASE-VERIFICATION-001.md`（提交 `72b305a...`）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-001（全新 Feature 需求与验收基线建立；纯文档任务，草案未批准、功能未实现、验收未执行） |
| 2026-09-05 | 仅同步 `data-source-snapshot-status`（源库快照状态）需求与验收草案 R1 定向修订事实：ChatGPT 正式复审初版草案（提交 `91eb220...`）结论 `CHANGES_REQUIRED`，R1 已定向修订完成（纯文档）：修正 R1-01（统一“只读访问 RUN_STATE＋只读关联 `CDC_CLIENT_MULTIPLE`、`CDC_DATA_SOURCE`”边界，消除“不得访问本表之外数据”的自相矛盾）、R1-02（`DSS-AC-065` 限定测试数据 DML 仅可操作 `CDC_DATA_SOURCE_RUN_STATE`，其余场景按既有只读关联配置或后端自动化/前端 Mock 组件测试）、R1-03（建立“界面选择条件/已应用查询条件”双状态与查询区“重置不查询”，`DSS-AC-024` 完整验证重置五行为）；吸收项目负责人已确认的 8 项交互方案与刷新工具栏稳定宽度（并入 `DSS-REQ-022/023/024/025/029/035/036/038/045/050/051/054/055/061` 等）；原 `DSS-PROP-001~008` 草案建议全部决策吸收、`pending_user_confirmation_count=0`；验收计数更新为 `DSS-AC-001~068` 共 68 条全部 `NOT_RUN`（R1 新增 `DSS-AC-068`）；基线状态保持 DRAFT（`REQUIREMENTS.md`/`ACCEPTANCE.md` 为 `DRAFT_PENDING_USER_REVIEW` R1 修订草案，未批准、未实现）；代码状态仍为“占位”（`NOT_STARTED`）；最新有效证据增加 R1 定向修订报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-001-R1.md`；当前缺口更新为需求/验收 R1 修订草案待 ChatGPT 对 R1 结果正式复审与项目负责人批准、功能未实现、缺 DESIGN/API/UI/DATABASE；下一入口更新为 ChatGPT 对 R1 结果正式复审；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-001-R1（ChatGPT 正式复审 `CHANGES_REQUIRED` 驱动的纯文档定向修订；草案未批准、功能未实现、验收未执行） |
| 2026-09-05 | 仅同步 `data-source-snapshot-status`（源库快照状态）需求与验收草案 R2 最小定向修订事实：ChatGPT 对 R1 结果正式复审（提交 `0476c40...` 对应的 R1 结果）结论 `CHANGES_REQUIRED`，R2 已最小定向修订完成（纯文档），只消除两个剩余歧义，不新增/删除/重编号任何需求与验收条目：R2-01（新条件查询失败不替换“已应用查询条件”、保留上一次成功结果与条件、界面保留新选择、后续自动/立即刷新仍用旧已应用条件、仅成功含成功返回 0 条才替换、首次失败仍保持初始三项“全部”，落实于 `DSS-REQ-023/025/059/060/061` 与 `DSS-AC-021/024/056/057/058`）、R2-02（页面可见时每次实际请求结束无论成功失败都重启完整 60 秒周期、刷新失败约 60 秒后按已应用条件自动重试、被抑制触发不视为实际请求不单独重置计时、不可见停止且不保留剩余秒数复用、恢复可见刷新后重启，落实于 `DSS-REQ-050/051/053/054` 与 `DSS-AC-048/050/051/068`）；需求仍为 `DSS-REQ-001~065` 共 65 条、验收仍为 `DSS-AC-001~068` 共 68 条全部 `NOT_RUN`、`pending_user_confirmation_count=0`；基线状态保持 DRAFT（`REQUIREMENTS.md`/`ACCEPTANCE.md` 为 `DRAFT_PENDING_USER_REVIEW` R2 修订草案，未批准、未实现、未执行验收）；代码状态仍为“占位”（`NOT_STARTED`）；最新有效证据增加 R2 最小定向修订报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-001-R2.md`；当前缺口更新为需求/验收 R2 修订草案待 ChatGPT 对 R2 结果正式复审与项目负责人批准、功能未实现、缺 DESIGN/API/UI/DATABASE；下一入口更新为 ChatGPT 对 R2 结果正式复审；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-001-R2（ChatGPT 对 R1 结果正式复审 `CHANGES_REQUIRED` 驱动的纯文档最小定向修订；草案未批准、功能未实现、验收未执行） |
| 2026-09-05 | 仅同步 `data-source-snapshot-status`（源库快照状态）需求与验收草案 R3 极小定向修订事实：ChatGPT 对 R2 结果正式复审（提交 `5c58af6...` 对应的 R2 结果）结论 `CHANGES_REQUIRED`，R3 已极小定向修订完成（纯文档），只修正验收草案 `DSS-AC-024` 一处验收矛盾，不新增/删除/重编号任何需求与验收条目：按旧“已应用条件”触发的自动/立即刷新成功返回时必须把“最近成功刷新时间”更新为本次成功刷新完成时间并按本次成功结果更新表格（不得写作“成功刷新也不更新时间”），同时明确成功刷新不得替换“已应用查询条件”——只有用户点击“查询”且查询成功（含成功返回 0 条空结果）才允许用该次请求快照替换；需求仍为 `DSS-REQ-001~065` 共 65 条业务行相对授权基线逐字节不变、除 `DSS-AC-024` 外其余 67 条验收业务行相对授权基线逐字节不变、验收仍为 `DSS-AC-001~068` 共 68 条全部 `NOT_RUN`、`pending_user_confirmation_count=0`；基线状态保持 DRAFT（`REQUIREMENTS.md`/`ACCEPTANCE.md` 为 `DRAFT_PENDING_USER_REVIEW` R3 极小定向修订草案，未批准、未实现、未执行验收）；代码状态仍为“占位”（`NOT_STARTED`）；最新有效证据增加 R3 极小定向修订报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-001-R3.md`；当前缺口更新为需求/验收 R3 极小定向修订草案待 ChatGPT 对 R3 结果正式复审与项目负责人批准、功能未实现、缺 DESIGN/API/UI/DATABASE；下一入口更新为 ChatGPT 对 R3 结果正式复审；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-001-R3（ChatGPT 对 R2 结果正式复审 `CHANGES_REQUIRED` 驱动的纯文档极小定向修订；草案未批准、功能未实现、验收未执行） |
| 2026-09-05 | 仅同步 `data-source-snapshot-status`（源库快照状态）需求与验收基线批准收口事实：基线状态由 DRAFT 更新为 `APPROVED`（ChatGPT 对 R3 结果提交 `4234af73db2190098f3dcd219319a4281fdabafd` 正式复审结论 `APPROVED`，项目负责人随后明确回复“批准”，批准日期 2026-09-05；正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-APPROVAL-001`，批准内容基准提交 `4234af7...`；批准的是需求与验收标准基线，不代表功能已实现、验收已执行或通过）；需求仍为 `DSS-REQ-001~065` 共 65 条业务行相对批准内容基准逐字节零差异、验收仍为 `DSS-AC-001~068` 共 68 条全部 `NOT_RUN` 业务行相对批准内容基准逐字节零差异、需求—验收追踪矩阵零差异、`pending_user_confirmation_count=0`；代码状态仍为“占位”（`NOT_STARTED`）、验收执行状态仍为 `NOT_RUN`、设计状态仍为 `NOT_STARTED`（DESIGN/API/UI/DATABASE 尚未建立）；最新有效证据增加批准收口报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-APPROVAL-001.md`；当前缺口更新为设计文档尚未建立、功能尚未实现、68 条验收尚未执行；下一入口更新为设计基线建立；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-APPROVAL-001（项目负责人批准驱动的需求与验收基线批准收口；纯文档任务，未设计、未实现、未执行验收） |
| 2026-09-05 | 仅同步 `data-source-snapshot-status`（源库快照状态）设计基线草案建立事实：新增 `DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md` 四份设计草案（任务 `DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-001`，纯文档），把已批准需求与验收转换为可复审/可实现/可测试设计草案，design_status=`DRAFT_PENDING_USER_REVIEW`（未批准，待 ChatGPT 对四份设计草案正式复审与项目负责人批准；设计文档已建立不等于设计已批准）；需求/验收保持 `APPROVED`（65 需求 `DSS-REQ-001~065`/68 验收 `DSS-AC-001~068` 全部 `NOT_RUN`，正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-REQUIREMENTS-BASELINE-APPROVAL-001`，相对批准内容基准逐字节零差异）；仅唯一只读接口 `GET /api/monitor/data-source-run-state/list`（1 个 GET 端点，响应内嵌 `records+candidates`）；设计矩阵覆盖 65/65 需求与 68/68 验收、无悬空引用；`PENDING_USER_CONFIRMATION=0`；实现状态保持占位（`NOT_STARTED`）；68 条验收执行状态保持全部 `NOT_RUN`；最新有效证据增加设计草案建立报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-001.md`；当前缺口更新为四份设计草案（`DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md`）已建立但为 `DRAFT_PENDING_USER_REVIEW`、待 ChatGPT 对设计草案正式复审与项目负责人批准、功能尚未实现（前端 `views/data-source-run-state/DataSourceRunStatePage.vue` 仍为占位页）、68 条验收尚未执行；下一入口更新为 ChatGPT 对四份设计草案（DESIGN/API/UI/DATABASE）进行正式复审，复审通过并经项目负责人批准后再进入实现；既有路由 `/monitor/data-source-state` 与前端 `views/data-source-run-state/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-001（纯文档设计草案建立；设计未批准、功能未实现、验收未执行） |
| 2026-09-06 | 仅同步 `data-source-snapshot-status`（源库快照状态）设计基线 R1 极小定向修订事实：ChatGPT 对设计草案初版结果正式复审结论 `CHANGES_REQUIRED`（R1-01~R1-04），R1 已按 `DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-001-R1` 完成极小定向修订（纯文档，2026-09-06，基准提交 `31aa9f5...`）；R1-01（页面实例生命周期：路由进入即创建页面/composable 实例局部状态并初始化为三项“全部”后立即自动查询，离开路由销毁实例、不跨路由保留，浏览器页签隐藏/恢复针对同一存活实例采用实例内状态与恢复刷新）、R1-02（统一在途抑制：任意时刻最多一个实际在途请求，忙碌期间“查询/立即刷新”按钮禁用、自动刷新被抑制、被抑制触发不算实际请求、不更新最近成功/不重置计时，仅保留请求实例令牌 seq 用于防卸载迟写与防旧响应覆盖，删除“最新用户意图槽位/排队补发”语义）、R1-03（恢复可见刷新唯一例外：空闲即按当前已应用条件立即恢复刷新并随后重启完整 60 秒，忙碌则置一次性 `pendingVisibilityRefresh=true`、在途结束后读取当时最新已应用条件执行一次恢复刷新再重启 60 秒、不可见/卸载清除标志且不再补发，不作为通用排队机制）、R1-04（元数据纠正：Feature README §9 当前态与设计草案建立报告 §6 的 `pending_user_review=NO` 修正为 `YES`、`pending_user_confirmation_count=0` 不变，四份设计文档仍 `DRAFT_PENDING_USER_REVIEW`、不批准设计）；修改文件仅 DESIGN.md/UI.md/Feature README/设计草案建立报告/本索引/新增 R1 执行报告，REQUIREMENTS/ACCEPTANCE/API/DATABASE 整文件零差异；需求/验收保持 `APPROVED`（65 需求 `DSS-REQ-001~065`/68 验收 `DSS-AC-001~068` 全部 `NOT_RUN`，相对批准内容基准零差异）；实现状态保持占位（`NOT_STARTED`）；68 条验收执行状态保持全部 `NOT_RUN`；最新有效证据增加设计基线 R1 极小定向修订报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-001-R1.md`（2026-09-06）；当前缺口更新为四份设计草案完成 R1 极小定向修订但仍为 `DRAFT_PENDING_USER_REVIEW`、`pending_user_review=YES`、待 ChatGPT 对 R1 结果提交正式复审与项目负责人批准、功能尚未实现（前端 `views/data-source-run-state/DataSourceRunStatePage.vue` 仍为占位页）、68 条验收尚未执行；下一入口更新为 ChatGPT 对设计 R1 结果提交正式复审，复审通过并经项目负责人批准设计后再进入实现；既有路由 `/monitor/data-source-state` 与前端 `views/data-source-run-state/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-001-R1（ChatGPT 正式设计复审 `CHANGES_REQUIRED` 驱动的纯文档极小定向修订；设计未批准、功能未实现、验收未执行） |
| 2026-09-06 | 仅同步 `data-source-snapshot-status`（源库快照状态）设计基线批准收口事实：ChatGPT 对设计 R1 结果提交（`61117a62f44d39f7c548ebcb650891abf91b9b8c`）正式复审结论 `APPROVED`，项目负责人随后明确回复“批准”；四份设计文档（`DESIGN.md`/`API.md`/`UI.md`/`DATABASE.md`）状态由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`、`pending_user_review` 由 `YES` 收口为 `NO`（`design_status=APPROVED`、`implementation_status=NOT_STARTED`、`acceptance_execution_status=NOT_RUN`、`pending_user_confirmation_count=0`），正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-APPROVAL-001`，批准内容基准提交 `61117a62f44d39f7c548ebcb650891abf91b9b8c`，批准日期 2026-09-06；相对批准内容基准，四份设计接口/参数/响应字段/SQL/状态映射/排序/候选范围/时间与 null/行键/错误码/组件职责/生命周期/请求快照/忙碌抑制/可见性补发/计时器/测试设计与追踪矩阵业务内容零差异，需求与验收基线（65 需求 `DSS-REQ-001~065`/68 验收 `DSS-AC-001~068` 全部 `NOT_RUN`）整文件零差异；批准的是设计基线，不代表功能已实现、验收已执行或通过，也不等于 `IMPLEMENTED_ACCEPTED`；实现状态保持占位（`NOT_STARTED`）；68 条验收执行状态保持全部 `NOT_RUN`；最新有效证据增加设计基线批准收口报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-APPROVAL-001.md`（2026-09-06）；当前缺口更新为功能尚未实现（前端 `views/data-source-run-state/DataSourceRunStatePage.vue` 仍为占位页）、68 条验收尚未执行（设计缺口已关闭）；下一入口更新为另立实现任务，按已批准 DESIGN/API/UI/DATABASE 四份设计基线开始编码；既有路由 `/monitor/data-source-state` 与前端 `views/data-source-run-state/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-DESIGN-BASELINE-APPROVAL-001（ChatGPT 对设计 R1 结果正式复审 `APPROVED` 且项目负责人批准驱动的设计基线批准收口；纯文档任务，设计已批准、功能未实现、验收未执行） |
| 2026-09-06 | 仅同步 `data-source-snapshot-status`（源库快照状态）实现任务事实：按已批准 DESIGN/API/UI/DATABASE 四份设计基线执行只读全栈实现（任务 `DATA-SOURCE-SNAPSHOT-STATUS-IMPLEMENTATION-001`，2026-09-06，基准提交 `98fe66e...`，实现后 `implementation_status=IMPLEMENTED_PENDING_REVIEW`、`formal_acceptance_execution_status=NOT_RUN`、`human_visual_acceptance_status=NOT_RUN`、`acceptance_not_run_count=68`）：后端新增唯一只读接口 `GET /api/monitor/data-source-run-state/list`（同维 OR/跨维 AND/空值归一化为全部，响应 `records+candidates`、无分页）及 `monitor/datasourcerunstate/**` 主代码与测试，后端专项 27 例测试通过、`mvn clean test` 通过、`mvn clean package` 成功；前端把 `views/data-source-run-state/DataSourceRunStatePage.vue` 占位页替换为只读查询页（查询区/结果表/刷新/状态标签/候选下拉，忙碌抑制与 60 秒可见自动刷新等按已批准 UI 基线），新增 `api/dataSourceSnapshot.ts`/`types/dataSourceSnapshot.ts`/组件/组合式/工具及其 spec，`config/menu.ts` 仅本页面标题、`router/index.ts` 仅本页面标题元数据（path/name 不变），前端专项 62 例测试通过、全量 663 例通过、`npm run build` 成功；提交范围仅含上述授权文件，工作区其余人工未提交修改保持原样；数据库全程只读、无 DML/DDL；浏览器证据 `evidence/DATA-SOURCE-SNAPSHOT-STATUS-IMPLEMENTATION-001/`（真实运行流 3 张 + 首载失败 Mock 1 张，服务已启动待人工页面验收）；需求/验收/设计基线保持 `APPROVED`、相对批准内容基准零差异（需求 `DSS-REQ-001~065` 共 65 条、验收 `DSS-AC-001~068` 共 68 条全部 `NOT_RUN`、追踪矩阵零差异）；未自我接受，`IMPLEMENTED_ACCEPTED` 收口未执行；正式验收未执行；最新有效证据增加实现执行报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-IMPLEMENTATION-001.md`；当前缺口更新为 68 条正式验收未执行、人工页面验收未执行、待 ChatGPT 代码复审；下一入口更新为 ChatGPT 代码复审与项目负责人人工页面验收，正式验收另立任务执行；既有路由 `/monitor/data-source-state` 与前端 `views/data-source-run-state/` 目录名保持既有值不变；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-IMPLEMENTATION-001（全栈只读实现；实现状态 `IMPLEMENTED_PENDING_REVIEW`，未自我接受、未执行正式验收，待 ChatGPT 代码复审与项目负责人人工页面验收） |
| 2026-09-06 | 仅同步 `data-source-snapshot-status`（源库快照状态）实现 R1 修复与验证事实：按前次代码复审 `CHANGES_REQUIRED`，R1-01 在公共 `frontend/src/services/http.ts` 增加请求级 `skipGlobalErrorPopup` 开关（默认其它页面错误弹窗行为不变、未删公共拦截器、未全局关闭弹窗），`fetchSnapshotStatusList` 开启该开关由本页统一脱敏/收敛失败反馈，真实拦截链测试（HTTP500/超时断网/业务码非 200/默认行为不变/60 秒恢复）6 例通过，前端全量 46 文件 669/669、`vue-tsc`＋`vite build` 成功；R1-02 按负责人本会话授权对所指生产库（经只读证据核验为本项目内网库 `DB_NAME=prod`/`snoopy-linux`/Schema=CDC）三表 `CDC_DATA_SOURCE_RUN_STATE`/`CDC_CLIENT_MULTIPLE`/`CDC_DATA_SOURCE` 执行测试 INSERT＋COMMIT 并保留：新增 RUN_STATE+29、停用探针+5、停用源+5（前缀 `dssr1-0906-`、`FG_ACTIVE='0'`、描述含“快照页测试，请勿启用”、连接字段为不可实际连接的合成值且绝不连接、不 UPDATE/DELETE 已有行、无 TRUNCATE/DDL/其它表/触发器写入），授权例外仅限本任务，产品只读规则不变；R1-03 补齐 1440×900/1920×1080 浏览器证据（七列/状态标签/三时间/三多选/成功 0 条/重置不请求/真实 HTTP500 保留与恢复/60 秒自动刷新/隐藏暂停/恢复可见/工具栏稳定宽度/边缘 Tooltip，错误仅在 CDP Fetch 层注入）与后端全量测试澄清（`cd backend && mvn clean test` 默认 profile、无筛选/排除/跳过，退出码 1：Tests 1022/Failures 3/Errors 17/Skipped 0，非通过全部集中在既存 `monitor.jobfailure` 的 ZooKeeper 不可达与日期映射环境性失败，未用 skipTests/testFailureIgnore 掩盖）；`REQUIREMENTS.md` 仅新增 §20.1 任务级授权例外与 §24 变更记录，65 条 REQ/68 条 AC 业务行与追踪矩阵零差异，`ACCEPTANCE.md` 与四份设计整文件零差异；实现状态保持 `IMPLEMENTED_PENDING_REVIEW`、未自我接受、未执行正式验收，待 ChatGPT 代码复审与项目负责人人工页面验收 | DATA-SOURCE-SNAPSHOT-STATUS-IMPLEMENTATION-001-R1（实现修复与证据补齐；实现状态 `IMPLEMENTED_PENDING_REVIEW`，未自我接受、未执行正式验收，待 ChatGPT 代码复审与项目负责人人工页面验收） |
| 2026-09-07 | 仅同步 `data-source-snapshot-status`（源库快照状态）验收前 UI 调整草案事实：ChatGPT 对实现 R1 提交 `37825272...` 正式复审 `CHANGES_REQUIRED`（表格长文本可读性、浏览器证据不足），项目负责人提出更完整 UI 调整；本轮在已批准需求/验收/设计基线之上建立**纯文档调整草案** `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-001`（未实现/未批准/未验收，任务仅改 7 份文档/报告，不改 API.md/DATABASE.md/源码/测试/config/evidence）：页面三块分区（页头语义区＋独立查询卡＋独立结果卡）、结果卡头左摘要（`共 N 条`＋仅未知>0 时 light-orange `其中 N 条未知状态`）右不可拆散刷新逻辑组（右对齐、窄屏整组换行）、七列固定列宽（源库 280 宽于探针端 170、三时间各 165 且 `YYYY-MM-DD HH:mm:ss`）、探针端列只显 `CLIENT_ID` 与源库列只显 `ORG`（悬停 Tooltip、缺失/空 `ORG` 回退 raw `DATA_SOURCE_ID`）、页面级单实例受控 Tooltip、busy 视觉隔离；需求新增 `DSS-REQ-066~071`（共 `DSS-REQ-001~071` 71 条）、验收新增 `DSS-AC-069~080`（共 `DSS-AC-001~080` 80 条全部 `NOT_RUN`），就地修订 `DSS-REQ-028/029/050` 与 `DSS-AC-026/027/068`；本轮 `REQUIREMENTS.md`/`ACCEPTANCE.md`/`DESIGN.md`/`UI.md` 当前调整版本为 `DRAFT_ADJUSTMENT_PENDING_USER_REVIEW`、`implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING`、正式/人工验收 `NOT_RUN`、`pending_user_review=YES`、`pending_user_confirmation_count=0`；`API.md`/`DATABASE.md` 保持 `APPROVED`（整文件零差异），已批准基线保留为历史、批准旧基线不自动批准本轮调整；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-001（验收前 UI 调整草案建立；纯文档任务，未实现/未批准/未验收，实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING`，待 ChatGPT 对本调整草案正式复审与项目负责人审阅） |
| 2026-09-07 | 仅同步 `data-source-snapshot-status`（源库快照状态）验收前 UI 调整草案 R1 极小定向修订事实：ChatGPT 对本调整草案（`DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-001`）正式复审 `CHANGES_REQUIRED`、项目负责人已确认六类请求视觉映射，R1 已完成**纯文档极小定向修订** `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-001-R1`（未实现/未批准/未验收，仅改白名单 7 份文档/报告，不改 API.md/DATABASE.md/源码/测试/config/evidence）：只澄清唯一歧义——`auto`（60 秒自动刷新）与 `restore`（恢复可见刷新）在途**只激活刷新状态圆点（变蓝并呈刷新动态），不让“立即刷新”按钮显示 loading**；“立即刷新”按钮 loading 仅由 `manual` 触发、“查询”按钮 loading 仅由 `query` 触发、初次加载表格 loading（`initial`）与错误区“重新加载”按钮 loading（`retry`）独立；落点仅 `REQUIREMENTS.md` `DSS-REQ-071`（§21）、`ACCEPTANCE.md` `DSS-AC-072/078/079`（§4.18）、`DESIGN.md` §19.3/§19.6、`UI.md` §13.3/§13.6（并消除旧 §6 冲突表述）；`DSS-REQ-001~070` 与 `DSS-AC-001~071/073~077/080` 业务行逐字节零差异、需求仍 `DSS-REQ-001~071` 共 71 条、验收仍 `DSS-AC-001~080` 共 80 条全部 `NOT_RUN`；本轮 `REQUIREMENTS.md`/`ACCEPTANCE.md`/`DESIGN.md`/`UI.md` 当前调整版本仍为 `DRAFT_ADJUSTMENT_PENDING_USER_REVIEW`、`implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING`、正式/人工验收 `NOT_RUN`、`pending_user_review=YES`、`pending_user_confirmation_count=0`；`API.md`/`DATABASE.md` 保持 `APPROVED`（整文件零差异）；下一入口更新为 ChatGPT 对 R1 结果提交正式复审（不是直接实现）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-001-R1（验收前 UI 调整草案 R1 极小定向修订；纯文档任务，未实现/未批准/未验收，实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING`，待 ChatGPT 对 R1 结果正式复审与项目负责人审阅） |
| 2026-09-07 | 仅同步 `data-source-snapshot-status`（源库快照状态）验收前 UI 调整草案批准收口事实：ChatGPT 对 R1 结果提交 `575723711ca39d7761df308c1c99b1e6e957cf70` 独立正式复审 `APPROVED`、项目负责人明确回复“批准”，纯文档批准收口任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-001`（2026-09-07，仅改白名单 7 份文档/报告，不改 API.md/DATABASE.md/源码/测试/config/evidence）记录批准并收口状态；`REQUIREMENTS.md`/`ACCEPTANCE.md`/`DESIGN.md`/`UI.md` 当前 UI 调整版本由 `DRAFT_ADJUSTMENT_PENDING_USER_REVIEW` 收口为 `APPROVED`（正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-001`，批准内容基准提交 `5757237...`）；业务内容相对批准内容基准零差异（需求 `DSS-REQ-001~071` 共 71 条、验收 `DSS-AC-001~080` 共 80 条全部 `NOT_RUN`、追踪 71/71 与 80/80、六类请求视觉映射保持不变、`auto`/`restore` 只激活刷新状态圆点且按钮外观稳定）、`API.md`/`DATABASE.md` 整文件零差异；批准的是本轮 UI 调整需求/验收/设计基线，不代表本轮调整已实现、正式验收或人工验收已执行或通过，不等于 `IMPLEMENTED_ACCEPTED`；实现状态保持 `IMPLEMENTED_ADJUSTMENT_PENDING`（本轮调整未实现）、`pending_user_review` 由 `YES` 收口为 `NO`、`pending_user_confirmation_count=0`、80 条验收全部 `NOT_RUN`、正式/人工验收 `NOT_RUN`；下一入口更新为另立 UI 调整实现任务基于已批准提交 `5757237...` 落地（本批准收口任务不直接实现）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-001（验收前 UI 调整草案批准收口；纯文档任务，记录已发生的正式批准并收口状态，未实现/未验收，实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING`） |
| 2026-09-08 | 仅同步 `data-source-snapshot-status`（源库快照状态）第二轮验收前 UI 调整草案批准收口事实：ChatGPT 对 R1 结果提交 `5da9b17c1a720f89482eeda1436ad633145fe9fa` 独立正式复审 `APPROVED`、项目负责人明确回复“批准”，纯文档批准收口任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002`（2026-09-08，仅改白名单 7 份文档/报告，不改 API.md/DATABASE.md/源码/测试/config/evidence）记录批准并收口状态；`REQUIREMENTS.md`/`ACCEPTANCE.md`/`DESIGN.md`/`UI.md` 当前第二轮版本由 `DRAFT_ADJUSTMENT_PENDING_USER_REVIEW` 收口为 `APPROVED`（正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002`，批准内容基准提交 `5da9b17c1a720f89482eeda1436ad633145fe9fa`，批准日期 2026-09-08）；业务内容相对批准内容基准零差异（需求 `DSS-REQ-001~075` 共 75 条、验收 `DSS-AC-001~086` 共 86 条全部 `NOT_RUN`、追踪 75/75 与 86/86、四类展示调整与 R1 修订后的关联引用状态模型/可访问性口径/历史说明/预计实现文件职责保持不变）、`API.md`/`DATABASE.md` 整文件零差异；批准的是本轮第二轮 UI 调整需求/验收/设计基线，不代表本轮调整已实现、正式验收或人工验收已执行或通过，不等于 `IMPLEMENTED_ACCEPTED`；实现状态保持 `IMPLEMENTED_ADJUSTMENT_PENDING`（本轮第二轮调整尚未实现）、`pending_user_review` 由 `YES` 收口为 `NO`、`pending_user_confirmation_count=0`、86 条验收全部 `NOT_RUN`、正式/人工验收 `NOT_RUN`；下一入口更新为另立第二轮 UI 调整实现任务基于已批准内容基准提交 `5da9b17c1a720f89482eeda1436ad633145fe9fa` 落地（本批准收口任务不直接实现）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002（第二轮验收前 UI 调整草案批准收口；纯文档任务，记录已发生的正式批准并收口状态，未实现/未验收，实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING`） |
| 2026-09-08 | 仅同步 `data-source-snapshot-status`（源库快照状态）第二轮草案 R2 极小纠正事实：第二轮草案及其 R1 曾于 2026-09-08 批准收口为 `APPROVED`（正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002`，批准内容基准提交 `5da9b17c1a720f89482eeda1436ad633145fe9fa`，作为历史批准事实保留）；批准收口后 ChatGPT 在准备第二轮实现任务时发现批准内容把“正常源库行 Tooltip 显示完整 `DATA_SOURCE_ORG`”误记为现行规则、与项目负责人真实需求（源库列表格主内容显示 ORG、悬停显示完整原始 `DATA_SOURCE_ID`）冲突并暂停实现，项目负责人再次确认源库列悬停 Tooltip 只显示完整原始 `DATA_SOURCE_ID`；纯文档任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002-R2`（2026-09-08）只原位纠正该唯一业务语义并同步四份核心文档＋Feature README＋本总索引，`REQUIREMENTS.md`/`ACCEPTANCE.md`/`DESIGN.md`/`UI.md` 当前第二轮版本由 `APPROVED`（正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002`，作为历史批准事实保留）重新进入复审 `DRAFT_ADJUSTMENT_PENDING_USER_REVIEW`（requirements/acceptance/design/ui_status 均为此值）、`implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING`（第二轮调整尚未实现）、正式验收与人工页面验收 `NOT_RUN`、`human_visual_review_status=CHANGES_REQUIRED`（第一轮历史）、86 条验收全部 `NOT_RUN`、`pending_user_review=YES`、`pending_user_confirmation_count=0`；`API.md`/`DATABASE.md`/源码/测试/config/evidence 整文件零差异；下一入口为 ChatGPT 对 R2 结果独立正式复审（不是直接实现）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002-R2（第二轮草案 R2 极小纠正；纯文档任务，未重新批准、第二轮调整未实现、未执行正式验收，待 ChatGPT 对 R2 结果独立正式复审与项目负责人重新批准） |
| 2026-09-08 | 仅同步 `data-source-snapshot-status`（源库快照状态）第二轮草案 R3 记录纠正事实：ChatGPT 对 R2 结果提交独立正式复审结论 `CHANGES_REQUIRED`（确认 R2 核心业务纠正与零差异证明正确，仅两类文档审计记录错误需纠正）；纯文档任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002-R3`（2026-09-08，只改本 Feature 白名单既有文档/报告＋新增 R3 报告，不改 `ACCEPTANCE.md`/`DESIGN.md`/`UI.md`/`API.md`/`DATABASE.md`/源码/测试/config/evidence）只纠正两类记录错误：① R3-01 原位纠正 `REQUIREMENTS.md` R2 变更记录“`ACCEPTANCE.md` 整文件零差异”的错误声明——R2 实际原位纠正 `DSS-AC-027/075/076/077/083/086` 六条验收业务行中的源库 Tooltip 内容，其余 80 条验收业务行逐字节零差异、86 条验收仍全部 `NOT_RUN`、需求—验收追踪矩阵零差异；② R3-02 把 Feature README 与 R2 报告现行“下一入口”中作为未来第二轮实现依据的旧批准内容基准 `5da9b17c...` 改为“重新批准并完成批准收口后记录的、包含 R2 Tooltip 纠正的最新批准内容基准提交”（`5da9b17c...` 仅作为 R2 纠正前的历史批准内容基准保留，禁止作为未来第二轮实现业务基准）。业务零变化：需求 75 条、验收 86 条全部 `NOT_RUN`、四份核心文档当前第二轮版本仍为 `DRAFT_ADJUSTMENT_PENDING_USER_REVIEW`（requirements/acceptance/design/ui_status 均为此值）、`implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING`（第二轮调整尚未实现）、正式验收与人工页面验收 `NOT_RUN`、`human_visual_review_status=CHANGES_REQUIRED`（第一轮历史）、`pending_user_review=YES`、`pending_user_confirmation_count=0`；`ACCEPTANCE.md`/`DESIGN.md`/`UI.md`/`API.md`/`DATABASE.md`/源码/测试/config/evidence 整文件零差异；下一入口为 ChatGPT 对 R3 结果独立正式复审（不是直接实现）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-002-R3（第二轮草案 R3 记录纠正；纯文档任务，只纠正 R2 变更记录错误零差异声明与未来实现锚点，未重新批准、第二轮调整未实现、未执行正式验收，待 ChatGPT 对 R3 结果独立正式复审与项目负责人重新批准） |
| 2026-09-08 | 仅同步 `data-source-snapshot-status`（源库快照状态）第二轮 UI 调整版本 R2/R3 纠正版重新批准收口事实：ChatGPT 对本 R3 结果提交 `cf40b5d1e5ef03712011edb5e10c20070b582273` 独立正式复审 `APPROVED`、项目负责人随后明确回复“批准”，纯文档重新批准收口任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002-R1`（2026-09-08，仅改白名单 7 份文档/报告，不改 API.md/DATABASE.md/源码/测试/config/evidence，也不改第二轮初版/R1/批准收口/R2/R3 报告）记录重新批准并收口状态；`REQUIREMENTS.md`/`ACCEPTANCE.md`/`DESIGN.md`/`UI.md` 当前第二轮版本由 `DRAFT_ADJUSTMENT_PENDING_USER_REVIEW` 重新批准收口为 `APPROVED`（重新批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002-R1`，批准内容基准提交 `cf40b5d1e5ef03712011edb5e10c20070b582273`，批准日期 2026-09-08；原批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002`/内容提交 `5da9b17c1a720f89482eeda1436ad633145fe9fa` 仅作为 R2 纠正前的历史批准事实保留，不得作为当前批准内容或未来实现业务基准）；业务内容相对批准内容基准零差异（需求 `DSS-REQ-001~075` 共 75 条、验收 `DSS-AC-001~086` 共 86 条全部 `NOT_RUN`、追踪矩阵零差异、DESIGN/UI 业务内容与追踪矩阵零差异）、`API.md`/`DATABASE.md`/第二轮初版报告 `...-002.md`/R1 报告 `...-002-R1.md`/批准收口报告 `...-APPROVAL-002.md`/R2 报告 `...-002-R2.md`/R3 报告 `...-002-R3.md` 整文件零差异；重新批准的是本轮第二轮 UI 调整需求/验收/设计基线，不代表本轮调整已实现、正式验收或人工验收已执行或通过，不等于 `IMPLEMENTED_ACCEPTED`；实现状态保持 `IMPLEMENTED_ADJUSTMENT_PENDING`（本轮第二轮调整尚未实现）、`pending_user_review` 由 `YES` 收口为 `NO`、`pending_user_confirmation_count=0`、`human_visual_review_status=CHANGES_REQUIRED`（第一轮页面人工检查历史）、86 条验收全部 `NOT_RUN`、正式/人工验收 `NOT_RUN`；下一入口更新为另立第二轮 UI 调整实现任务、严格以重新批准内容提交 `cf40b5d1e5ef03712011edb5e10c20070b582273` 为业务基准（本重新批准收口任务不直接实现）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002-R1（第二轮 UI 调整版本重新批准收口；纯文档任务，记录已发生的重新批准并收口状态，未实现/未验收，实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING`） |
| 2026-09-08 | 仅同步 `data-source-snapshot-status`（源库快照状态）第二轮 UI 调整实现事实：实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-002`（2026-09-08，任务起始提交 `79bba9575655b588be6e16c7e97f49b5d89cb37d`＝重新批准收口提交）已按重新批准内容基准提交 `cf40b5d1e5ef03712011edb5e10c20070b582273`（重新批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-BASELINE-APPROVAL-002-R1`）在 `frontend/src/views/data-source-run-state/` 前端展示层落地四类展示调整（① 表格铺满结果卡片：取消固定 `width:1145px`，五固定列（序号 70/快照状态 130/三时间各 165）＋探针端/源库两弹性列（`min-width` 170/280、按 170:280 吸收全部剩余、`width:100%; min-width:1145px`、正文窄于 1145px 时容器横向滚动）；② 探针端列展示简化：主内容仅原始 `CLIENT_ID`、仅 INACTIVE 显示红色普通文字“停用”、删除黄色图标、Tooltip 仅完整非空 `CLIENT_DESC`；③ 源库列展示简化：ORG 非空显 ORG/ORG 空或配置缺失回退原始 ID、Tooltip 恒为完整原始 `DATA_SOURCE_ID`、删除黄色图标与异常说明、无红字“停用”；④ 探针端查询下拉截断与宽度：ID/描述各按 Unicode code point 截断至 20＋`...`、完整 value 与请求参数严格分离、控件宽 240/300/200px、`dss-client-popper`/`dss-source-popper` 命名空间与面板宽度上限）；不改接口/SQL/表结构/只读边界，`API.md`/`DATABASE.md`/后端源码/测试/config/evidence 整文件零差异、`useDataSourceSnapshot.ts` 整文件零差异；开发验证：定向/Feature/前端全量测试与 `npm run build`（含 `vue-tsc --noEmit`）通过（原始日志见证据 `frontend/`），真实浏览器开发验证证据见 `evidence/DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-002/browser/`（表格铺满实测宽度、窄屏横向滚动、探针端/源库列状态与 Tooltip 内容、下拉截断与完整值请求、Tooltip 单实例、manual 刷新单请求、重置零请求、console/网络汇总）；实现状态更新为 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`，正式验收与人工页面验收仍 `NOT_RUN`、`human_visual_review_status=CHANGES_REQUIRED`（第一轮页面人工检查历史）、86 条 `DSS-AC-001~086` 全部 `NOT_RUN`（`acceptance_not_run_count=86`）；实现完成不等于代码复审通过、不等于正式验收或人工验收已执行或通过、不等于 `IMPLEMENTED_ACCEPTED`；下一入口为 ChatGPT 对本实现提交、代码、测试、构建与浏览器证据独立正式复审，复审通过后由项目负责人人工页面查看，正式验收另立独立任务执行；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-UI-ADJUSTMENT-IMPLEMENTATION-002（第二轮 UI 调整实现；前端只读展示层，实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`，未执行正式验收/未自我接受，待 ChatGPT 独立复审与项目负责人人工页面查看） |
| 2026-09-10 | 仅同步 `data-source-snapshot-status`（源库快照状态）隔离视觉原型 R2～R7 正式落到 `5173` 的实现事实：实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-TO-FORMAL-IMPLEMENTATION-001`（2026-09-10）在**全新隔离 worktree**（`/agent/dss-formal-impl-001`，自 `origin/develop` 派生的 detached HEAD，基准提交 `c568df1f7b0be14b7decdf59f2ef9116ff0bf403`，主工作区与 `5174` prototype worktree 既有修改零触碰）中，把已批准（批准内容基准提交 `e67b2ecc3897c3e83597126e259ee4c19349a66a`）的 R2～R7 视觉方案移植到 `5173` 正式前端，白名单 12 个文件（`frontend/src/views/data-source-run-state/` 下 `DataSourceRunStatePage.vue/.spec.ts`、`components/DataSourceSnapshotQueryBar|Toolbar|Table|StatusTag.vue/.spec.ts`、`composables/useDataSourceSnapshot.ts/.spec.ts`）；落地要点：`.dss-page` 透明画布＋独立查询卡片（`rgb(244,244,245)`）＋独立结果卡片（白底弱阴影）、查询标签 14px/600 `#3F3F46` 无背景无边框、下拉 240/300/200×32、黑主按钮/浅灰次按钮、结果摘要 `共 N 条` 16px/700 与 `其中 N 条未知状态` 暖黄胶囊、不可拆散刷新组（16px 倒计时环 `#E4E4E7`/`#2563EB`＋`N 秒后自动刷新`＋`最近成功刷新：HH:mm:ss`＋白底细边框 `立即刷新` 固定 110px）、七列列宽模型 `70/170/285/140/170/170/170=1175px`（`width:100%; min-width:1175px`、三时间列恒等宽、19 字时间戳不省略）、行高 49px、探针端 600 字重 `CLIENT_ID`＋`停用` 徽章、源库主内容 `ORG` 缺失回退原始 `DATA_SOURCE_ID` 且 Tooltip 恒为完整原始 `DATA_SOURCE_ID`、三状态 Tag 带符号、未知行底色 `rgba(254,243,199,.42)`（hover `.66`）；开发验证：定向 6 文件 125 用例、Feature 目录 13 文件 173 用例、前端全量 50 文件 762 用例全部通过，`vue-tsc` 类型检查＋`vite build` 成功（仅既有 chunk 体积告警），`git diff --check` 干净；**真实后端**（本机 `8080` 只读 `GET /api/monitor/data-source-run-state/list`，30 条真实数据）驱动 1280×800/1700×920/1920×1080/2560×1440 四档视口与前两档查询区/结果头部/表格/首行/未知行特写截图，交互状态机（选择零请求、重置零请求、手动刷新 +1 GET、自动刷新 +1 GET、倒计时走秒零额外 GET、hidden 冻结/恢复、失败保留旧数据与旧已应用条件、Tooltip 单实例且源库 Tooltip 严格为原始 ID、busy 期间并发点击不排队不补发）全部符合既有设计，浏览器 console error 0、页面写请求 0、`/config/data-source` 无 `dss-*` 泄漏；数据库全程只读（后端日志 55 条语句全为 `SELECT`），未手工执行任何 DML/DDL、未改动本 Feature 业务表（既有大屏统计调度器按正常逻辑触发一轮但 `stopReason=all_caught_up`、0 批 0 行，统计表与基线逐表一致）；同步白名单文档：Feature `README.md`、`REQUIREMENTS.md`、`ACCEPTANCE.md`、`DESIGN.md`、`UI.md` 顶部实现状态元数据更新为 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`（文档总体状态 `PROTOTYPE_DESIGN_APPROVED_AND_IMPLEMENTED_ON_5173_PENDING_REVIEW`），`API.md`/`DATABASE.md` **仅**同步顶部实现状态元数据与简短实现记录（`formal_5173_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`），**业务正文零变化**；状态：`formal_acceptance_status=NOT_RUN`、`human_visual_acceptance_status=NOT_RUN`、`acceptance_not_run_count=95`（95 条 `DSS-AC-001~095` 全部 `NOT_RUN`）、`pending_user_review=NO`、`pending_user_confirmation_count=0`、`query_list_global_baseline_status=NOT_CREATED_BY_DESIGN`；实现完成不等于代码复审通过、不等于正式验收或人工视觉验收已执行或通过、不等于 `IMPLEMENTED_ACCEPTED`；`API.md`/`DATABASE.md` 业务正文、后端源码、接口契约与数据库契约零差异（`api_contract_change_status=NONE`、`database_contract_change_status=NONE`）；下一入口为 ChatGPT 从远程 Git 对本次正式实现独立复审，复审通过后由项目负责人人工目测 `5173`，正式验收另立任务执行；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-PROTOTYPE-TO-FORMAL-IMPLEMENTATION-001（R2～R7 视觉方案落到 `5173` 正式实现；前端只读展示层，`formal_5173_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`，未执行正式验收/未自我接受，待 ChatGPT 从 Git 独立复审与项目负责人人工页面查看） |
| 2026-09-10 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询控件交互调整草案事实：ChatGPT 已从 Git 对 `5173` R2～R7 正式实现代码独立复审 `APPROVED`（`formal_5173_code_review_status=APPROVED`），项目负责人 `5173` 人工页面检查结论 `CHANGES_REQUIRED`（`project_owner_visual_review_status=CHANGES_REQUIRED`）并确认三类查询控件交互问题（① 查询区下拉宽度随所选内容变化；② 四个展示字段 `CLIENT_ID`/`CLIENT_DESC`/`DATA_SOURCE_ORG`/`DATA_SOURCE_ID` 缺少统一字段级截断；③ `CLIENT_DESC` 截断后需 Tooltip 可读回全文）；据此建立纯文档草案 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-001`：新增草案需求 `DSS-REQ-084~086`（累计 86 条）与草案验收 `DSS-AC-096~103`（累计 103 条全部 `NOT_RUN`、`acceptance_not_run_count=103`），追踪矩阵 86/86 与 103/103，落点 REQUIREMENTS §21.6/§21.7、ACCEPTANCE §4.21/§5、DESIGN §26、UI §20；`API.md`/`DATABASE.md` 及既有历史报告整文件零差异、无源码/测试/config/evidence 变更。状态更新：`query_control_interaction_adjustment_status=DRAFT_PENDING_USER_REVIEW`、`query_control_interaction_adjustment_implementation_status=PENDING_APPROVAL_AND_IMPLEMENTATION`、`formal_acceptance_status=NOT_RUN`、`pending_user_review=YES`、`pending_user_confirmation_count=0`；保留 `formal_5173_code_review_status=APPROVED` 与 R2～R7 已批准视觉内容、已进 `5173` 的实现事实，不回退为“未实现”；草案未批准、未实现、正式验收未执行。下一入口更新为 `CHATGPT_BASELINE_REVIEW_FROM_GIT_THEN_PROJECT_OWNER_APPROVAL`（批准前不得开始实现）。不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-001（项目负责人确认问题驱动的纯文档查询控件交互调整草案；报告见 `docs/features/data-source-snapshot-status/reports/DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-001.md`） |
| 2026-09-10 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询控件交互调整基线批准收口事实：ChatGPT 已从远程 Git 对 R3 结果提交 `cf9f9eb0240f275cd50eb37546e6d6256892a9f4` 最终复审 `APPROVED`（`business_draft_content_status=APPROVED`、`r3_document_correction_status=APPROVED`、需求 86、验收 103、正式验收 `NOT_RUN`），项目负责人于 2026-09-10 明确回复“批准”；纯文档批准收口任务 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-APPROVAL-001`（仅改白名单 8 份既有文档＋新增 1 份批准报告，不改 `frontend/**`、`backend/**`、SQL、config、测试、图片、runtime evidence 及任何既有报告）记录批准并收口状态，正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-APPROVAL-001`、批准内容基准提交 `cf9f9eb0240f275cd50eb37546e6d6256892a9f4`、批准日期 2026-09-10；批准范围为一套不可拆分查询控件交互方案（① 三个查询下拉框外部几何固定：探针端 240px、源库 300px、快照状态 200px，选择/取消/清空/多选/重置/打开/Tooltip 均不得改变外部宽度或推动后续控件；② `CLIENT_ID`/`CLIENT_DESC`/`DATA_SOURCE_ORG`/`DATA_SOURCE_ID` 按最多 20 个 Unicode code point 显示、超过显示前 20 加 `...`，仅影响显示、完整 value/选择值/请求参数/已应用条件不变；③ 原始 `CLIENT_DESC` 超过 20 code point 时探针候选项与可见选中项悬停 Tooltip 显示完整原始 `CLIENT_DESC`、不超过或为空不显示，安全宽度/换行/单实例/不污染既有表格 Tooltip 按批准原文）；新增需求 `DSS-REQ-084~086` 纳入批准基线（累计 `DSS-REQ-001~086` 共 86 条，追踪 86/86）、新增验收 `DSS-AC-096~103` 纳入批准基线（累计 `DSS-AC-001~103` 共 103 条全部 `NOT_RUN`、`acceptance_not_run_count=103`、追踪 103/103）、落点 REQUIREMENTS §21.6/§21.7、ACCEPTANCE §4.21/§5、DESIGN §26、UI §20；业务内容相对批准内容基准零差异（86 条需求业务行、103 条验收业务行、DESIGN §14.2/§14.3、DESIGN §26、UI §20、API §9 映射表及业务契约、DATABASE 业务正文逐字节一致）、`API.md`/`DATABASE.md` 业务契约零变化（新增规则为纯前端查询控件展示与交互规则，不新增 API 路径/参数/响应字段/DTO/VO/错误码，不改三表投影/SQL/字段/主键/索引/约束/关联/排序/只读边界）；状态统一收口为 `query_control_interaction_adjustment_status=APPROVED`、`query_control_interaction_adjustment_implementation_status=PENDING_FORMAL_IMPLEMENTATION_ON_5173`、`formal_5173_code_review_status=APPROVED`、`project_owner_visual_review_status=CHANGES_REQUIRED`、`formal_acceptance_status=NOT_RUN`、`human_visual_acceptance_status=NOT_RUN`、`pending_user_review=NO`、`pending_user_confirmation_count=0`；本轮调整尚未实现于 `5173`、正式验收与正式人工视觉验收未执行，保留上一正式实现代码复审 `APPROVED` 与项目负责人问题检查 `CHANGES_REQUIRED`，不把 103 条验收写成 `PASS`；R0→R1→R2→R3→批准收口提交链与批准报告见 `reports/DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-APPROVAL-001.md`；下一入口统一为独立实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-IMPLEMENTATION-001`（本批准收口任务不直接实现，也不得替代后续实现任务）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-APPROVAL-001（项目负责人批准驱动的纯文档查询控件交互调整基线批准收口；新增批准报告见 `docs/features/data-source-snapshot-status/reports/DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-BASELINE-APPROVAL-001.md`） |
| 2026-09-11 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询控件交互调整实现事实：实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-IMPLEMENTATION-001`（2026-09-11）在**全新隔离 worktree**（`/agent/dss-query-ctl-impl-001`，detached HEAD，任务基准提交 `548e16147675cdc6013a4d166bf60d7b1b8bda36`＝`origin/develop`；主工作区与 `5174` prototype worktree 既有修改零触碰）中，严格以批准内容基准提交 `cf9f9eb0240f275cd50eb37546e6d6256892a9f4` 为业务基准，把已批准查询控件交互调整落到 `5173` 正式前端，白名单 4 个文件（`frontend/src/views/data-source-run-state/` 下 `utils/format.ts`、`components/DataSourceSnapshotQueryBar.vue` 及各自 `.spec.ts`）：① 三下拉固定外部几何（探针端 240px/源库 300px/快照状态 200px，根节点与 `.el-select__wrapper` 同宽，`width`/`min-width`/`max-width`/`flex-basis` 显式锁定＋内层 `min-width: 0`，下游字段组与查询/重置按钮 `x` 位移恒 0、查询栏高度 delta 0、无横向溢出，无全局 Element Plus 覆盖、无 JS 尺寸监听）；② 四字段 `CLIENT_ID`/`CLIENT_DESC`/`DATA_SOURCE_ORG`/`DATA_SOURCE_ID` 按 Unicode 码点统一截断（`<=20` 原文、`>20` 前 20＋`...`，CJK 1 码点、代理对不拆、空描述仅 ID、空 ORG 回退原始 ID，显示与完整 `value`/请求语义分离）；③ `CLIENT_DESC` 原始码点 `>20` 时探针候选项与可见选中项悬停 Tooltip 显示完整原始值（`min(480px, calc(100vw - 16px))`、单实例、移出即隐、不改几何、不污染表格 Tooltip）；开发验证：定向 2 文件 72 用例、Feature 13 文件 211 用例、前端全量 50 文件 800 用例全部通过，`vue-tsc` 类型检查＋`vite build` 成功，`git diff --check` 干净；真实浏览器四档视口（1280×800/1700×920/1920×1080/2560×1440）16 状态几何矩阵、合成与真实后端截断/Tooltip 验证、请求状态机回归（初始 1 GET、选择/取消/清空/重置/Tooltip 0、查询 +1 且完整原始参数、单飞行、倒计时 0、自动刷新 +1、hidden 冻结/恢复、失败保留）、console error 0、页面写请求 0、`/config/data-source` 无 `dss-*` 泄漏，证据见 `docs/features/data-source-snapshot-status/evidence/DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-IMPLEMENTATION-001/`；数据库全程只读、无手工 DML/DDL、未改本 Feature 业务表（既有大屏统计调度器按既有配置触发一轮 `stopReason=all_caught_up`、0 批 0 行、全文日志无 INSERT/UPDATE/MERGE/DELETE），未访问 ZooKeeper/Kafka；同步白名单文档：Feature `README.md`、`REQUIREMENTS.md`、`ACCEPTANCE.md`、`DESIGN.md`、`UI.md`、`API.md`、`DATABASE.md` 仅更新当前实现状态元数据与导航/记录（业务正文/契约/追踪矩阵逐字节零变化，需求 86、验收 103 全部 `NOT_RUN`），并新增实现报告与证据目录；状态更新为 `query_control_interaction_adjustment_status=APPROVED`、`query_control_interaction_adjustment_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`query_control_interaction_adjustment_code_review_status=PENDING_CHATGPT_REVIEW`、`formal_5173_code_review_status=APPROVED`、`project_owner_visual_review_status=CHANGES_REQUIRED`、`formal_acceptance_status=NOT_RUN`、`human_visual_acceptance_status=NOT_RUN`、`acceptance_not_run_count=103`、`pending_user_review=NO`、`pending_user_confirmation_count=0`；实现完成不等于代码复审通过、不等于正式验收或人工视觉验收已执行或通过、不等于 `IMPLEMENTED_ACCEPTED`；下一入口更新为 `CHATGPT_QUERY_CONTROL_INTERACTION_IMPLEMENTATION_REVIEW_FROM_GIT_THEN_PROJECT_OWNER_VISUAL_REVIEW`；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-IMPLEMENTATION-001（查询控件交互调整正式实现；前端只读展示层，实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、代码复审 `PENDING_CHATGPT_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`，未执行正式验收/未自我接受，待 ChatGPT 从 Git 独立复审与项目负责人人工页面查看） |

| 2026-09-11 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询控件交互调整实现 R1 修正事实：实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-IMPLEMENTATION-001` 的结果提交 `a47988820c797ff60bd7244b2d0f899bd8fc3be5` 经 ChatGPT 独立代码复审结论 `CHANGES_REQUIRED`；R1 修正任务 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-IMPLEMENTATION-001-R1`（2026-09-11）在同一隔离 worktree（`/agent/dss-query-ctl-impl-001-r1`，detached HEAD，基准提交 `a47988820c797ff60bd7244b2d0f899bd8fc3be5`；主工作区与 `5174` prototype worktree 既有修改零触碰）完成五类复审问题修正：① `CLIENT_DESC` Tooltip 改为按**原始完整 `CLIENT_ID`** 稳定身份定位（删除以截断/组合显示文字反查探针的 `matchClientByLabel(textContent)` 实现，改用 Feature 私有 `data-dss-client-id`：候选行经 `el-option` 属性透传、可见已选项经 Element Plus `label` slot 提供的原始 option value；构造可见标签在 trim＋20 码点截断后逐字相同、原始 `CLIENT_ID` 与完整 `CLIENT_DESC` 均不同的两颗探针，分别验证候选项悬停、选中后已选项悬停、快速交替悬停均映射正确且任意时刻至多一个 Tooltip；不使用全局 Element Plus DOM 猜测或跨 Feature 全局覆盖）；② 四字段 `CLIENT_ID`/`CLIENT_DESC`/`DATA_SOURCE_ORG`/`DATA_SOURCE_ID` 统一为“先 `null` 安全归一 → `trim()` → 按 Unicode 码点计数 → `<= 20` 原文 / `> 20` 前 20 码点＋ASCII `...`”，以单一纯函数 `displayField` 表达，组合标签对每个组成字段分别处理（空描述不产生空括号、空 ORG 回退处理后的 ID）；③ Tooltip 内容为 trim 后完整 `CLIENT_DESC`（不再次截断、不留首尾无意义空白），判定基于 trim 后码点 `> 20`，候选与可见已选项均覆盖，安全宽度/单实例/移出销毁/不污染表格 Tooltip 约束保持；④ 补交 **Git 可独立复核**的原始测试与构建日志——上一提交证据索引所列 `vitest-targeted.log`/`vitest-feature.log`/`vitest-full.log`/`build.log` 因 `.gitignore` 的 `*.log` 规则实际未进入远程 Git，本次改以不被忽略的 `.txt` 重跑并提交，证据索引文件与 `git ls-tree -r --name-only HEAD` 逐一对齐；⑤ 消解 `1280` 响应式文档冲突——保留既有动作组换行、修正 UI/DESIGN 中“`1280` 不换行”的当前表述，业务行/编号/契约不变。开发验证：定向 2 文件 102 用例、Feature 目录 13 文件 241 用例、前端全量与 `vue-tsc --noEmit`＋`vite build`、`git diff --check` 全部通过（原始输出以 `.txt` 提交）；真实浏览器四档视口（1280×800/1700×920/1920×1080/2560×1440）几何、截断标签碰撞、Tooltip、请求与 console/网络回归见 R1 证据目录 `evidence/DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-IMPLEMENTATION-001-R1/`；数据库只读、无手工 DML/DDL、未改本 Feature 业务表，未访问 ZooKeeper/Kafka。状态：`query_control_interaction_adjustment_status=APPROVED`、`query_control_interaction_adjustment_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`query_control_interaction_adjustment_code_review_status=PENDING_CHATGPT_REVIEW`、`formal_acceptance_status=NOT_RUN`、`human_visual_acceptance_status=NOT_RUN`、`acceptance_not_run_count=103`、需求 86、验收 103、`pending_user_review=NO`、`pending_user_confirmation_count=0`；`API.md`/`DATABASE.md` 业务契约/正文逐字节不变、后端零差异。**下一入口为 `CHATGPT_R1_IMPLEMENTATION_REVIEW_FROM_GIT_THEN_PROJECT_OWNER_VISUAL_INTERACTION_REVIEW`**——ChatGPT 从远程 Git 复审 R1 结果，随后由项目负责人进行视觉/交互复审。实现完成不等于代码复审通过、不等于正式验收或人工视觉验收已执行或通过，不等于 `IMPLEMENTED_ACCEPTED`；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-ADJUSTMENT-IMPLEMENTATION-001-R1（查询控件交互调整实现 R1 修正；前端只读展示层，实现状态 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、代码复审 `PENDING_CHATGPT_REVIEW`、`browser_verification_status=DEV_SELF_TEST_DONE`，未执行正式验收/未自我接受，待 ChatGPT 从 Git 独立复审 R1 与项目负责人视觉/交互复审） |
| 2026-09-11 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询下拉固定宽度基线草案事实：项目负责人已完成对隔离视觉原型 `5174`（任务 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-PROTOTYPE-001`，在独立 worktree 中仅以 `frontend/src/views/data-source-run-state/components/DataSourceSnapshotQueryBar.vue` 与 Feature 私有样式实现）的检查，并明确批准三档 popper 固定宽度（探针端/client **480px**、源库/source **400px**、快照状态/status **240px**，受 `min(目标宽度, calc(100vw - 16px))` 视口安全上界约束；`prototype_width_decision_status=APPROVED_BY_PROJECT_OWNER`，口径“我检查过了，没有问题”）；根因诊断为 Element Plus `.el-select-dropdown__item.is-selected { font-weight: 700 }` 与定宽 flex 组合导致外层 `.el-popper` 随选中项内容撑宽。据此建立**纯文档草案** `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-001`（仅改白名单 `docs/features/data-source-snapshot-status/{README,REQUIREMENTS,ACCEPTANCE,DESIGN,UI,API,DATABASE}.md`、`docs/features/README.md` 与新增 1 份报告，不改 `frontend/**`、`backend/**`、SQL、config、测试、证据及原型 worktree）：新增草案需求 `DSS-REQ-087`（REQUIREMENTS §21.8，扩展 `DSS-REQ-084` 的“固定外部几何”范围到 Teleport 外层 popper，不改写 `DSS-REQ-084` 本体；累计 `DSS-REQ-001~087` 共 87 条）与草案验收 `DSS-AC-104~107`（ACCEPTANCE §4.22，累计 `DSS-AC-001~107` 共 107 条全部 `NOT_RUN`、`acceptance_not_run_count=107`），新增 DESIGN §27、UI §21，追踪 87/87 与 107/107；既有 `DSS-REQ-001~086` 与 `DSS-AC-001~103` 业务行零变化；`API.md`/`DATABASE.md` 在本 baseline 草案任务中仅同步 §1 组合计数（需求 87、验收 107）、分层状态、下一入口及简短说明，API 业务契约与 §9 映射表逐字节不变、DATABASE 查询设计业务正文逐字节不变。规则要点：固定对象为 Feature 私有 `popper-class` 对应的外层 `.el-popper` 可见边界（非内层 dropdown、非触发控件）；三档宽度同时锁定 `width`/`min-width`/`max-width`；内层 dropdown 自适应外层、不两层同锁；选中项 `font-weight:700` 保留但不得改变 popper 宽度；选择/取消/清空/重置/多选/折叠/展开闭合/Tooltip 显隐/滚动条等状态不得改变宽度；小视口以 `calc(100vw - 16px)` 为安全上界、无横向滚动/溢出；触发控件保持 240/300/200×32px；四字段 trim＋20 码点截断/Tooltip/完整值/查询语义与 API/DB/后端/请求状态机不变。状态：`content_status=DRAFT_PENDING_CHATGPT_REVIEW_AND_PROJECT_OWNER_APPROVAL`、`prototype_width_decision_status=APPROVED_BY_PROJECT_OWNER`、`popper_width_document_status=DRAFT_PENDING_CHATGPT_REVIEW_AND_PROJECT_OWNER_APPROVAL`、`popper_width_formal_5173_implementation_status=PENDING_FORMAL_IMPLEMENTATION_ON_5173`、`popper_width_formal_code_review_status=NOT_RUN`、`formal_acceptance_status=NOT_RUN`、`human_formal_5173_review_status=NOT_RUN_FOR_THIS_ADJUSTMENT`、`pending_user_review=YES`、`pending_user_confirmation_count=0`。草案未批准、未在 `5173` 实现、正式验收未执行；`5174` 原型浏览器/测试/构建自检仅为设计验证证据，不得写成正式验收 `PASS`，不得把草案写成 `APPROVED`。下一入口更新为 `CHATGPT_POPPER_WIDTH_BASELINE_R1_REVIEW_FROM_GIT_THEN_PROJECT_OWNER_DOCUMENT_APPROVAL`（ChatGPT 从远程 Git 复审草案，通过后由项目负责人决定是否批准；批准前不得开始 `5173` 正式实现）。不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-001（项目负责人已批准隔离原型宽度驱动的纯文档查询下拉固定宽度基线草案；报告见 `docs/features/data-source-snapshot-status/reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-001.md`） |
| 2026-09-11 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询下拉固定宽度基线批准收口事实：ChatGPT 已完成对批准内容基准提交 `f74dd725682b745f1b8ac6a1b9358eff10aaddc2`（`...-BASELINE-001-R1` 结果）的远程 Git 独立复审结论 `APPROVED`（`chatgpt_final_review_status=APPROVED`），项目负责人随后明确回复“批准”（`approval_date=2026-09-11`）；纯文档批准收口任务 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-APPROVAL-001`（仅改白名单 9 份文档/报告，不改 `frontend/**`、`backend/**`、SQL、config、测试、图片、runtime 日志及任何既有报告）记录批准并收口状态，正式批准版本 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-APPROVAL-001`、批准内容基准提交 `f74dd725682b745f1b8ac6a1b9358eff10aaddc2`、批准日期 2026-09-11；批准范围为一套不可拆分的查询下拉（`.el-popper` 外层）固定宽度方案（探针端/client **480px**、源库/source **400px**、快照状态/status **240px**，受 `min(目标宽度, calc(100vw - 16px))` 视口安全上界约束；固定对象为 Feature 私有 `popper-class` 对应的外层 `.el-popper` 可见边界，内层 `.el-select-dropdown` 自适应外层、不两层同锁；选中项 `font-weight:700` 保留但不得改变 popper 宽度；选择/取消/清空/重置/多选/折叠/展开闭合/Tooltip 显隐/滚动条等状态不得改变宽度；触发控件保持 240/300/200×32px；四字段 trim＋20 个 Unicode code point 截断、`CLIENT_DESC` 超 20 code point 时 Tooltip 显示完整原始值、完整 value/选择值/请求参数/已应用条件不变；不改 API 路径/参数/响应字段/DTO/VO/错误码，不改三表投影/SQL/字段/主键/索引/约束/关联/排序/只读边界）；新增需求 `DSS-REQ-087` 纳入批准基线（累计 `DSS-REQ-001~087` 共 87 条，追踪 87/87）、新增验收 `DSS-AC-104~107` 纳入批准基线（累计 `DSS-AC-001~107` 共 107 条全部 `NOT_RUN`、`formal_acceptance_not_run_count=107`、追踪 107/107）、落点 REQUIREMENTS §21.8、ACCEPTANCE §4.22、DESIGN §27、UI §21；业务内容相对批准内容基准零差异（87 条需求业务行、107 条验收业务行、DESIGN §14.2/§14.3、DESIGN §27、UI §21、API §9 映射表及业务契约、DATABASE 业务正文逐字节一致）、`API.md`/`DATABASE.md` 业务契约零变化；状态统一收口为 `prototype_width_decision_status=APPROVED_BY_PROJECT_OWNER`、`popper_width_document_status=APPROVED`、`requirements_status=APPROVED`、`acceptance_status=APPROVED`、`design_status=APPROVED`、`popper_width_formal_5173_implementation_status=PENDING_FORMAL_IMPLEMENTATION_ON_5173`、`popper_width_formal_code_review_status=NOT_RUN`、`formal_acceptance_status=NOT_RUN`、`human_formal_5173_review_status=NOT_RUN_FOR_THIS_ADJUSTMENT`、`pending_user_review=NO`、`pending_user_confirmation_count=0`；本轮固定宽度规则尚未在 `5173` 实现、正式验收与正式人工 `5173` 验收未执行，`5174` 原型浏览器/测试/构建自检仅为设计验证证据、不得写成正式验收 `PASS`，不得把批准写成 `IMPLEMENTED`/`IMPLEMENTED_ACCEPTED`/`FORMALLY_ACCEPTED`/`ACCEPTANCE_PASSED`/`COMPLETED`；保留 R0 草案 `...-BASELINE-001` 与 R1 修订 `...-BASELINE-001-R1` 历史记录；下一入口统一为独立实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001`（本批准收口任务不直接实现，也不得替代后续实现任务，不得开始 `5173` 实现或正式验收）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-APPROVAL-001（项目负责人批准驱动的纯文档查询下拉固定宽度基线批准收口；批准报告见 `docs/features/data-source-snapshot-status/reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-APPROVAL-001.md`） |
| 2026-09-11 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询下拉固定宽度基线批准收口 R1 复审修正事实：ChatGPT 对批准收口结果提交 `5649a8a8040b67bec0dc23b10596281cd5706bb3` 从远程 Git 独立复审结论 `CHANGES_REQUIRED`，确认提交链、文件范围、快进推送、`DSS-REQ-001~087`（87 行）、`DSS-AC-001~107`（107 行全部 `NOT_RUN`）、DESIGN §27/UI §21 业务规则、API 契约、DATABASE 查询设计业务正文、`5173` 未实现事实与正式验收未执行事实均无问题，唯一缺陷是四份核心文档 §1 当前元数据“本轮（查询下拉固定宽度基线）任务编号”一行仍以现行时态写“纯文档草案、只建立文档草案、不批准草案”，与现行 `popper_width_document_status=APPROVED` 歧义冲突；R1 极小修正任务 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-APPROVAL-001-R1`（纯文档，仅改 `docs/features/data-source-snapshot-status/{README,REQUIREMENTS,ACCEPTANCE,DESIGN,UI,API,DATABASE}.md`、`docs/features/README.md` 与新增 1 份 R1 报告，不改既有报告/R0/R1 草案报告/证据目录，不改 `frontend/**`、`backend/**`、SQL、config、测试、runtime 日志）把该行改为历史限定表述（原基线任务为纯文档草案建立任务、当时只建立草案不批准不实现不验收，该草案现已批准收口为 `APPROVED`；本行描述原任务历史性质、不代表当前仍为草案或待批准；`5173` 正式实现与正式验收仍未执行），并把“当前下一入口”统一为 `CHATGPT_POPPER_WIDTH_BASELINE_APPROVAL_R1_REVIEW_FROM_GIT_THEN_FORMAL_IMPLEMENTATION`；零变化证明：`DSS-REQ-001~087` 87 条连续唯一逐字节不变、`DSS-AC-001~107` 107 条连续唯一逐字节不变且全部 `NOT_RUN`、DESIGN §14.2/§14.3 与 §27.2~§27.6、UI §21.2~§21.6、API §9 映射表与接口业务契约、DATABASE 查询设计业务正文逐字节不变、`frontend/`/`backend/`/SQL/config/测试/证据零差异；不撤销项目负责人批准、不重新批准业务内容；状态维持 `prototype_width_decision_status=APPROVED_BY_PROJECT_OWNER`、`popper_width_document_status=APPROVED`、`requirements_status=APPROVED`、`acceptance_status=APPROVED`、`design_status=APPROVED`、`popper_width_formal_5173_implementation_status=PENDING_FORMAL_IMPLEMENTATION_ON_5173`、`popper_width_formal_code_review_status=NOT_RUN`、`formal_acceptance_status=NOT_RUN`、`human_formal_5173_review_status=NOT_RUN_FOR_THIS_ADJUSTMENT`、`pending_user_review=NO`、`pending_user_confirmation_count=0`；下一入口为 `CHATGPT_POPPER_WIDTH_BASELINE_APPROVAL_R1_REVIEW_FROM_GIT_THEN_FORMAL_IMPLEMENTATION`（ChatGPT 从远程 Git 复审本 R1 结果，复审通过后的下一实际任务仍为独立实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001`，不得写成已开始/已完成/已验收）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-APPROVAL-001-R1（查询下拉固定宽度基线批准收口 R1 复审修正；纯文档极小修正，不撤销批准、不重新批准业务内容，未实现/未验收；报告见 `docs/features/data-source-snapshot-status/reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-APPROVAL-001-R1.md`） |
| 2026-09-12 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询下拉固定宽度基线 **R2 支持边界修正**事实：ChatGPT 从远程 Git 对固定宽度实现提交的独立代码复审结论为 `popper_width_formal_code_review_status=CHANGES_REQUIRED`（2026-09-11），并确认 `240px` 视口证据——外层 `.el-popper` 为 224px 时 Element Plus 内层 `.el-select-dropdown` 内联 `min-width` 仍为探针端 238px / 源库 298px / 快照状态 222px，原基线“任意视口下内外层均无溢出且不使用 `!important`、不引入 JS 尺寸监听、不侵入 Element Plus 内部”的组合在极端窄视口下不可同时满足。纯文档任务 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-001-R2`（2026-09-12，仅改白名单 `docs/features/data-source-snapshot-status/{README,REQUIREMENTS,ACCEPTANCE,DESIGN,UI,API,DATABASE}.md`、`docs/features/README.md`、实现报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001.md`（仅追加“ChatGPT 复审纠正记录”章节）、证据目录 `evidence/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001/README.md`（仅追加纠正说明）与证据 `backend/feature-get-window.txt`、`backend/scheduler-window.txt`（仅机械清理行尾空白）及新增 1 份 R2 报告，不改 `frontend/**`、`backend/**`、SQL、config、测试代码、图片、浏览器 JSON/截图矩阵、其他 Feature 文档、既有批准报告及任何其他 worktree）把固定宽度基线**正式支持视口下限修正为 `viewport width >= 1280px`**（正式验收视口至少覆盖 `1280×800`/`1700×920`/`1920×1080`/`2560×1440`），`<1280px` 仅保留防御性收缩（外层表达式 `min(目标宽度, calc(100vw - 16px))` 不变）、**不再要求** `<496px`/`<416px`/`<256px` 必须正式通过、**不把** EP 内层内联 `min-width` 溢出或裁切计为正式验收失败，窄视口观察一律标注“非正式支持范围内的防御性观察”且不得写成正式 `PASS`；**这是项目负责人基于实现证据划定的支持边界修正，不是把已发生的窄视口溢出伪装成原基线已经通过**。修订范围严格限于 `DSS-REQ-087` 与 `DSS-AC-104~107` 业务行（删除/替换 `<496/<416/<256px` 正式通过要求；“无横向滚动/越界/溢出”仅在正式支持范围内成立），`DSS-REQ-001~086` 与 `DSS-AC-001~103` 业务行逐字节不变，需求 87、验收 107（全部 `NOT_RUN`）、追踪 87/87 与 107/107；480/400/240 外层宽度、触发控件 240/300/200×32px、四字段 trim＋20 码点截断、`CLIENT_DESC` Tooltip、`API.md` 契约与 §9 映射表、`DATABASE.md` 查询设计业务正文均冻结不变；另机械清理 `evidence/.../backend/feature-get-window.txt`（2/5/8/11/14/17 行）与 `backend/scheduler-window.txt`（20/47/50/53 行）行尾空白并核验内容除行尾空白外逐字节一致，纠正原实现报告“最终提交 `git diff --check` CLEAN”与实际不符的记录。状态：`popper_width_r2_document_status=DRAFT_PENDING_CHATGPT_REVIEW_AND_PROJECT_OWNER_APPROVAL`、`popper_width_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`popper_width_implementation_code_review_status=CHANGES_REQUIRED`、`formal_acceptance_status=NOT_RUN`、`human_visual_interaction_review_status=NOT_PASSED`、`supported_viewport_min_width_px=1280`、`sub_1280_formal_support_status=NOT_FORMALLY_SUPPORTED_DEFENSIVE_ONLY`、`pending_user_review=YES`、`pending_user_confirmation_count=0`；R2 文档未获批、不得写成 `APPROVED`/`IMPLEMENTED_ACCEPTED`/`FORMALLY_ACCEPTED`/`PASS`/`COMPLETED`，不把任何 `DSS-AC-*` 改为 `PASS`。下一入口统一为 `CHATGPT_POPPER_WIDTH_BASELINE_R2_REVIEW_FROM_GIT_THEN_PROJECT_OWNER_DOCUMENT_APPROVAL`（ChatGPT 从远程 Git 复审 R2 文档，复审通过后由项目负责人明确决定是否批准 R2 文档）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | data-source-snapshot-status 的 `README.md`、`REQUIREMENTS.md`、`ACCEPTANCE.md`、`DESIGN.md`、`UI.md`、`API.md`、`DATABASE.md`、实现报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001.md`（追加纠正章节）、证据索引 `evidence/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001/README.md`（追加纠正说明）、证据日志 `backend/feature-get-window.txt`、`backend/scheduler-window.txt`（清理行尾空白）与新增 R2 报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-001-R2.md` | DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-001-R2（查询下拉固定宽度基线 R2 支持边界修正；纯文档事实一致性修订，把正式支持视口下限修正为 `1280px`、`<1280px` 仅防御性观察，不撤销批准、不重新批准业务内容、不实现、不执行正式验收；实现代码复审 `CHANGES_REQUIRED`、R2 文档 `DRAFT_PENDING_CHATGPT_REVIEW_AND_PROJECT_OWNER_APPROVAL`、107 条验收全部 `NOT_RUN`） |
| 2026-09-12 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询下拉固定宽度基线 **R2 支持边界修正文档批准收口**事实：ChatGPT 已从远程 Git 对批准内容基准提交 `0666cd96f1f27784f6d77404bd8d4820dbd96013`（R2 支持边界修正结果）完成独立复审结论 `chatgpt_r2_final_review_status=APPROVED`，项目负责人明确回复“批准”（`project_owner_r2_approval_status=APPROVED`，批准日期 2026-09-12）；纯文档批准收口任务 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-R2-APPROVAL-001`（仅改白名单 8 份既有文档 `docs/features/README.md`、`docs/features/data-source-snapshot-status/{README,REQUIREMENTS,ACCEPTANCE,DESIGN,UI,API,DATABASE}.md` 与新增 1 份批准报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-R2-APPROVAL-001.md`，不改 `frontend/**`、`backend/**`、SQL/YAML/XML/config/测试代码、证据/截图/日志/浏览器矩阵、既有报告、提示词、其他 Feature 文档）记录批准并把 R2 文档由 `DRAFT_PENDING_CHATGPT_REVIEW_AND_PROJECT_OWNER_APPROVAL` 收口为 `popper_width_r2_document_status=APPROVED`；批准范围为 R2 支持边界修正结论（正式支持视口下限 `supported_viewport_min_width_px=1280`、`sub_1280_formal_support_status=NOT_FORMALLY_SUPPORTED_DEFENSIVE_ONLY`、`ultra_narrow_formal_acceptance_requirement_status=WITHDRAWN_BY_APPROVED_R2`），批准内容基准提交 `0666cd96f1f27784f6d77404bd8d4820dbd96013`；需求 87（`DSS-REQ-001~087`）与验收 107（`DSS-AC-001~107` 全部 `NOT_RUN`、`acceptance_not_run_count=107`）业务行相对批准内容基准逐字节不变（含 `DSS-REQ-087`、`DSS-AC-104~107` 零变化）、追踪 87/87 与 107/107；`API.md` 接口路径/参数/响应模型/错误码/DTO/VO 与 §9 映射表逐字节不变、`DATABASE.md` 三表投影/SQL/字段/主键/索引/约束/关联/排序/只读边界逐字节不变；`frontend/`/`backend/`/SQL/配置/测试/证据零改动。状态：`popper_width_r2_document_status=APPROVED`、`chatgpt_r2_final_review_status=APPROVED`、`project_owner_r2_approval_status=APPROVED`、`popper_width_formal_5173_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`popper_width_formal_code_review_status=CHANGES_REQUIRED`、`formal_acceptance_status=NOT_RUN`、`human_visual_interaction_review_status=NOT_PASSED`、`pending_user_review=NO`、`pending_user_confirmation_count=0`；**批准 R2 文档不代表实现已通过代码复审、不代表正式验收或人工视觉/交互复审已执行或通过，不等于 `IMPLEMENTED_ACCEPTED`，不把任何 `DSS-AC-*` 改为 `PASS`**；下一入口统一为 `CHATGPT_POPPER_WIDTH_IMPLEMENTATION_REREVIEW_AGAINST_APPROVED_R2_THEN_PROJECT_OWNER_VISUAL_INTERACTION_REVIEW`（ChatGPT 按已批准 R2 支持边界从远程 Git 重新复审现有实现，随后由项目负责人做 `5173` 视觉/交互复审；正式验收仍为后续独立任务）；不批准其他 Feature，本索引自身状态仍为 `DRAFT_PENDING_USER_REVIEW` | data-source-snapshot-status 的 `README.md`、`REQUIREMENTS.md`、`ACCEPTANCE.md`、`DESIGN.md`、`UI.md`、`API.md`、`DATABASE.md` 与新增批准报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-R2-APPROVAL-001.md` | DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-BASELINE-R2-APPROVAL-001（查询下拉固定宽度基线 R2 支持边界修正文档批准收口；ChatGPT 远程 Git 复审 `APPROVED`＋项目负责人“批准”，R2 文档由 `DRAFT_PENDING_CHATGPT_REVIEW_AND_PROJECT_OWNER_APPROVAL` 收口为 `APPROVED`；实现仍 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、代码复审仍 `CHANGES_REQUIRED`、107 条验收全部 `NOT_RUN`；纯文档，未实现/未验收） |
| 2026-09-11 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询下拉固定宽度正式实现事实：实现任务 `DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001`（2026-09-11，全新隔离 worktree `/agent/dss-popper-width-impl-001`，任务基准提交 `485be09db758e4ba6f543fcc88a399cc5a46d384`＝`origin/develop`，严格以批准内容基准提交 `f74dd725682b745f1b8ac6a1b9358eff10aaddc2` 为业务基准）已按 `DSS-REQ-087`/`DSS-AC-104~107` 把三档查询下拉弹层固定宽度规则（探针端 480 / 源库 400 / 快照状态 240，受 `min(目标宽度, calc(100vw - 16px))` 视口安全上界约束）落到 `5173` 正式前端，仅改 Feature 私有 `frontend/src/views/data-source-run-state/components/DataSourceSnapshotQueryBar.vue` 与其 `.spec.ts`（外层选择器由裸 class 收敛为 `.el-popper.dss-*-popper`，同时锁定 `width`/`min-width`/`max-width`；不使用 `!important`、不新增全局 Element Plus 覆写、不引入 `window.resize`/`ResizeObserver`/轮询等脚本尺寸监听）；开发验证：定向 1 文件 85 用例、Feature 13 文件 245 用例、前端全量 50 文件 834 用例全部通过，`vue-tsc --noEmit`＋`vite build` 成功，`git diff --check` 干净，四档真实浏览器视口（1280×800/1700×920/1920×1080/2560×1440）与状态下外层宽度恒定（相邻状态 delta 0px）、窄视口安全上界、交互与请求状态机回归（选择/取消/清空/重置/Tooltip 0 请求、查询 +1、单飞行、失败保留）、console error 0、页面写请求 0、其他路由无 `dss-*` 泄漏；未连接数据库、未访问 ZooKeeper/Kafka、后端零差异。状态：`popper_width_document_status=APPROVED`、`prototype_width_decision_status=APPROVED_BY_PROJECT_OWNER`、`popper_width_formal_5173_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`、`popper_width_formal_code_review_status=PENDING_CHATGPT_REVIEW`、`formal_acceptance_status=NOT_RUN`、`human_formal_5173_review_status=NOT_RUN_FOR_THIS_ADJUSTMENT`、需求 87、验收 107（全部 `NOT_RUN`、`acceptance_not_run_count=107`）、`pending_user_review=NO`、`pending_user_confirmation_count=0`。**下一入口为 `CHATGPT_POPPER_WIDTH_FORMAL_IMPLEMENTATION_REVIEW_FROM_GIT_THEN_PROJECT_OWNER_VISUAL_INTERACTION_REVIEW`**（ChatGPT 从远程 Git 独立复审本实现结果（实现提交、2 个白名单前端文件差异、Git 内原始测试/构建日志与真实浏览器证据），复审通过后由项目负责人做视觉/交互复审；实现完成不等于代码复审通过、不等于正式验收或人工正式 `5173` 验收已执行或通过，不等于 `IMPLEMENTED_ACCEPTED`；本任务不执行正式验收、不自我接受、不创建通用查询列表页 UI 基线；报告见 `docs/features/data-source-snapshot-status/reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001.md`） | data-source-snapshot-status 的 `README.md`、`REQUIREMENTS.md`、`ACCEPTANCE.md`、`DESIGN.md`、`UI.md`、`API.md`、`DATABASE.md` 与新增实现报告/证据目录（`reports/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001.md`、`evidence/DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001/`） | DATA-SOURCE-SNAPSHOT-STATUS-SELECT-POPPER-FIXED-WIDTH-IMPLEMENTATION-001（查询下拉固定宽度正式实现；`5173` 正式前端已实现 `DSS-REQ-087` 固定宽度规则、状态 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW`，开发自测 `DEV_SELF_TEST_DONE`，待 ChatGPT 代码复审与项目负责人视觉/交互复审，未验收；107 条验收全部 `NOT_RUN`） |
| 2026-09-12 | 仅同步 `data-source-snapshot-status`（源库快照状态）查询控件交互调整实现复审收口事实：纯文档复审结论收口任务 `DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-IMPLEMENTATION-REVIEW-CLOSEOUT-001`（只改白名单 8 份既有文档 `docs/features/README.md`、`docs/features/data-source-snapshot-status/{README,REQUIREMENTS,ACCEPTANCE,DESIGN,UI,API,DATABASE}.md` 与新增 1 份收口报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-IMPLEMENTATION-REVIEW-CLOSEOUT-001.md`，不改 `frontend/**`、`backend/**`、SQL/YAML/XML/config/测试代码、证据/截图/日志、既有报告、提示词、其他 Feature 文档）把 ChatGPT 已从远程 Git 对 R1 修正实现提交 `edba7c891884d0f000a7c9edc196b2bffb95e0b0` 的独立代码复审结论 `query_control_interaction_adjustment_code_review_status=APPROVED`（R2 提交 `2a9a271690bfdc68b16772268e84928a33abdeda` 仅修正测试计数 `100→102`、无代码变化）与项目负责人对含后续 popper 固定宽度修正的最终 `5173` 页面人工视觉/交互检查结论“人工检查了，没有问题”（`query_control_human_visual_interaction_review_status=APPROVED_BY_PROJECT_OWNER`、`project_owner_visual_review_status=APPROVED_BY_PROJECT_OWNER`）同步入正式文档；实现状态由 `IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW` 收口为 `query_control_interaction_adjustment_implementation_status=IMPLEMENTED_ADJUSTMENT_PENDING_FORMAL_ACCEPTANCE`；需求 87（`DSS-REQ-001~087`）与验收 107（`DSS-AC-001~107` 全部 `NOT_RUN`、`formal_acceptance_not_run_count=107`）业务行逐字节不变、追踪 87/87 与 107/107；`DESIGN.md` §14.2 `DSS-REQ-084~086` 与 §14.3 `DSS-AC-096~103` 当前状态说明由 `PENDING_CHATGPT_REVIEW` 收口为代码复审 `APPROVED`、实现 `IMPLEMENTED_ADJUSTMENT_PENDING_FORMAL_ACCEPTANCE`（8 条仍全部 `NOT_RUN`），不动任何追踪映射表行；`API.md` 接口契约与 §9 映射表、`DATABASE.md` 业务正文逐字节不变；`frontend/`/`backend/`/SQL/配置/测试/证据/既有报告零改动；原有 `PENDING_CHATGPT_REVIEW`/`CHANGES_REQUIRED`/`IMPLEMENTED_ADJUSTMENT_PENDING_REVIEW` 仅以带日期/旧任务编号/历史限定保留，未做无差别全局替换；正式验收、正式人工验收仍 `NOT_RUN`，本任务不执行正式验收；下一入口统一为 `DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001` | data-source-snapshot-status 的 `README.md`、`REQUIREMENTS.md`、`ACCEPTANCE.md`、`DESIGN.md`、`UI.md`、`API.md`、`DATABASE.md` 与新增收口报告 `reports/DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-IMPLEMENTATION-REVIEW-CLOSEOUT-001.md` | DATA-SOURCE-SNAPSHOT-STATUS-QUERY-CONTROL-INTERACTION-IMPLEMENTATION-REVIEW-CLOSEOUT-001（查询控件交互调整实现复审收口；ChatGPT 远程 Git 复审 R1 `APPROVED`＋项目负责人“人工检查了，没有问题”，实现收口为 `IMPLEMENTED_ADJUSTMENT_PENDING_FORMAL_ACCEPTANCE`；107 条验收全部 `NOT_RUN`；纯文档，未实现/未验收） |
| 2026-09-12 | 仅同步 `data-source-snapshot-status`（源库快照状态）**正式验收执行**事实：正式验收执行任务 `DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001`（在全新隔离 worktree `/agent/dss-formal-acceptance-001`、detached HEAD、起点提交 `af88aa847c7cf6a197237a46f59130744d13f082` 中执行）已按已批准需求/设计/UI/API/DATABASE 对 `DSS-AC-001~107` 全部 **107** 条执行业务验收。**结果：`PASS 106 / FAIL 0 / BLOCKED 1 / NOT_RUN 0 = 107`**；唯一 `BLOCKED` 为 `DSS-AC-065`——受控测试数据需人工 `INSERT/UPDATE/DELETE/MERGE`，本任务提示词 §6.2 明确不授权、且无充分替代证据，其展示子行为由 `DSS-AC-006/033/037/038/039/041/042` 覆盖；无 `FAIL`、无 `NOT_RUN`。证据类型：`SC` 静态只读审阅、`BT` 后端定向测试、`FT` 前端定向/Feature/全量测试、`DB` 真实开发库只读前后快照对照、`HTTP` 真实后端 `GET /api/monitor/data-source-run-state/list` 22 条用例、`BR` 真实 Chromium（Playwright-core + CDP，`--no-sandbox`，deviceScaleFactor 1）四档正式视口页面测量、`BI` 仅替换列表响应字节（文件内显式标注“非真实后端数据”，未被当作真实后端证据）、`BUILD`、`SVC`；项目负责人实现阶段人工检查（`PRIOR-HUMAN`）与 `5174` 隔离原型证据**均未被用作任何用例的判定依据**。只读边界：三张业务表（`CDC_DATA_SOURCE_RUN_STATE`、`CDC_CLIENT_MULTIPLE`、`CDC_DATA_SOURCE`）执行前后只读快照一致、运行时日志 SQL 分类审计 `write_statement_count=0`（三表 `SELECT=23 WRITE=0`），未执行任何人工 DML/DDL/DCL；调度器行为与本 Feature 三表零写入分别审计；未访问/操作 ZooKeeper/Kafka/sync-client（均记 `NONE`），未连接 `5174`、未杀未知进程、未遗留重复进程。业务零变化：`REQUIREMENTS.md` 需求 `DSS-REQ-001~087` 共 87 条业务行逐字节不变；`ACCEPTANCE.md` 仅更新 107 行“状态”列（业务列逐字节不变）、§1/§2/§3/§4/§6 当前状态描述与 §7 变更记录，并按 §5.2 最小修正三处历史叙述残留（§3 过期草案标签、§4 执行前前言、§6 轮次计数）；DESIGN §14.2/§14.3 追踪 87/87 与 107/107 业务映射不变；`API.md` 接口契约与 §9 映射表、`DATABASE.md` 三表投影/SQL/字段/主键/索引/约束/关联/排序/只读边界逐字节不变；`frontend/**`、`backend/**`、测试代码、SQL、config 零改动。状态：`formal_acceptance_status=PARTIALLY_EXECUTED_BLOCKED`、`acceptance_execution_status=BLOCKED`、`formal_acceptance_executed_count=106`、`formal_acceptance_blocked_count=1`、`formal_acceptance_not_run_count=0`（2026-09-12 前历史值为 `NOT_RUN`/107）；`human_visual_acceptance_status` 仍为 `NOT_RUN`、`project_owner_visual_review_status` 仍为 `APPROVED_BY_PROJECT_OWNER`。**正式验收已执行不等于最终接受**：未设 `IMPLEMENTED_ACCEPTED`/`FORMALLY_ACCEPTED`、未宣告 Feature 完成、未代替 ChatGPT Git 复核或项目负责人验收决定、未修复任何 `FAIL`、未补做 `BLOCKED`；下一入口统一为 `CHATGPT_FORMAL_ACCEPTANCE_REVIEW_FROM_GIT_THEN_TARGETED_COMPLETION_TASK`（ChatGPT 从远程 Git 复核本次正式验收，随后另立定向补全任务处理 `DSS-AC-065`） | `docs/features/data-source-snapshot-status/ACCEPTANCE.md`（107 行“状态”列 + §1/§2/§3/§4/§6/§7）、`docs/features/data-source-snapshot-status/{README,REQUIREMENTS,DESIGN,UI,API,DATABASE}.md`（仅同步当前正式验收执行状态、结果计数、下一入口并追加变更记录）、`docs/features/README.md`（本行）、新增报告与证据目录 `reports/DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001.md`、`evidence/DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001/` | DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001（源库快照状态正式验收执行任务；107 条 `DSS-AC-001~107` 业务验收结果 `PASS 106 / FAIL 0 / BLOCKED 1 / NOT_RUN 0`，唯一 `BLOCKED` 为 `DSS-AC-065`（受控测试数据 DML 授权未获批准）；真实库只读、三表零写入；`frontend/`/`backend/`/测试代码零改动；正式验收已执行但**不等于**最终接受，待 ChatGPT 从 Git 复核与后续定向补全） |
| 2026-09-12 | 仅同步 `data-source-snapshot-status`（源库快照状态）**R1 定向补验**事实：定向补验任务 `DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001-R1`（在全新隔离 worktree `/agent/dss-formal-acceptance-001-r1`、detached HEAD、起点提交 `3190b3d2450478bef34b24578312c76c3973cb08`＝R0 交付提交中执行）完成 R0 唯一 `BLOCKED` 用例 `DSS-AC-065` 的定向补验，并纠正 R0 报告两项事实（`git diff --check` 非零仅来自保留的 R0 原始逐字日志行尾空白；ZooKeeper 分层事实）。项目负责人 2026-09-12 明确总体授权、并在完整展示阶段 A SQL 后明确回复“批准执行上述 SQL”；补验仅在开发库 `CDC` 唯一业务表 `CDC_DATA_SOURCE_RUN_STATE` 插入 7 条带独立前缀 `dss-fa065-r1-` 的临时行（`COMMIT`，覆盖正常关联已完成/未知状态/孤立探针端/孤立源库/既有停用探针/既有停用源库/非 SOURCE 类别七类场景），未对 `CDC_CLIENT_MULTIPLE`/`CDC_DATA_SOURCE` 或其它表写入、未执行 `UPDATE`/`MERGE`/DDL/`TRUNCATE`/匿名 PL/SQL/存储过程、未创建备份表、未访问或写入任何 ZooKeeper 节点；真实 DB/真实后端 `GET /api/monitor/data-source-run-state/list`（12 条用例）/真实 Chromium 页面（26 条断言全部 `PASS`，含 7 条临时行渲染、排序、真实查询控件过滤与零写请求）三方补验与 §9.1 必要回归通过后，按 §8.3 以完整复合主键 `DELETE` 全部 7 条并 `COMMIT`，三表与任务前**逐字节一致**、任务前缀残留 `0`。**结果：`PASS 107 / FAIL 0 / BLOCKED 0 / NOT_RUN 0 = 107`**。业务零变化：`REQUIREMENTS.md` 需求 `DSS-REQ-001~087` 共 87 条业务行逐字节不变；`ACCEPTANCE.md` 仅更新 `DSS-AC-065` 状态列与当前状态/计数/下一入口/变更记录，其余 106 条业务行逐字节不变；DESIGN §14.2/§14.3 追踪 87/87 与 107/107 业务映射不变；`API.md` 接口契约与 §9 映射表、`DATABASE.md` 三表投影/SQL/字段/主键/索引/约束/关联/排序/只读边界逐字节不变；`frontend/**`、`backend/**`、测试代码、SQL、config 零改动。状态：`formal_acceptance_status=EXECUTED_PENDING_CHATGPT_REVIEW`、`acceptance_execution_status=PASS`、`formal_acceptance_pass_count=107`、`formal_acceptance_blocked_count=0`、`formal_acceptance_not_run_count=0`（2026-09-12 前历史值为 `NOT_RUN`/107）；`human_visual_acceptance_status` 仍为 `NOT_RUN`。**正式验收已执行不等于最终接受**：未设 `IMPLEMENTED_ACCEPTED`/`FORMALLY_ACCEPTED`、未宣告 Feature 完成、未代替 ChatGPT Git 复核或项目负责人验收决定；下一入口统一为 `CHATGPT_FORMAL_ACCEPTANCE_R1_REVIEW_FROM_GIT_THEN_PROJECT_OWNER_ACCEPTANCE_DECISION`（ChatGPT 从远程 Git 复核 R1 正式验收，随后由项目负责人作正式人工验收决定） | `docs/features/data-source-snapshot-status/ACCEPTANCE.md`（`DSS-AC-065` 状态列 + 当前状态/计数/下一入口/变更记录）、`docs/features/data-source-snapshot-status/{README,REQUIREMENTS,DESIGN,UI,API,DATABASE}.md`（仅同步 R1 正式验收结果、计数、当前状态、下一入口并追加变更记录）、`docs/features/README.md`（本行）、新增报告与证据目录 `reports/DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001-R1.md`、`evidence/DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001-R1/` | DATA-SOURCE-SNAPSHOT-STATUS-FORMAL-ACCEPTANCE-001-R1（源库快照状态 R1 定向补验任务；`DSS-AC-065` 由 `BLOCKED` 转 `PASS`，最终 `PASS 107 / FAIL 0 / BLOCKED 0 / NOT_RUN 0`；经项目负责人明确批准的 7 条临时行已逐字节恢复、任务前缀残留 0；真实库/真实后端/真实浏览器三方补验；`frontend/`/`backend/`/测试代码零改动；正式验收已执行但**不等于**最终接受，待 ChatGPT 从 Git 复核与项目负责人人工验收决定） |
