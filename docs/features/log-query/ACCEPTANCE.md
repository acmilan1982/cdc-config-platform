# 日志查询详细验收设计（ACCEPTANCE）

## 1. 文档元数据与状态

| 项目 | 值 |
|---|---|
| 正式功能名称 | 日志查询 |
| 正式功能标识 | `log-query` |
| 正式菜单 | 日志查询（单菜单，双 Tab） |
| 目标路由 | `/monitor/log-query` |
| 目标文档 | `docs/features/log-query/ACCEPTANCE.md` |
| 文档状态 | `APPROVED`（`LOG-QUERY-BASELINE-ADJUSTMENT-001` 及 `-R1` 完整调整内容已经用户复审批准收口；批准不代表任何验收用例已经执行或通过；所有普通用例仍为 `NOT_RUN`，延期用例仍为 `DEFERRED_UNTIL_PHYSICAL_DESIGN`；最终物理数据库设计与生产等价性能验收仍为延期项；菜单始终显示、始终可点击，生产环境在全部阻断项通过并获批前 `CDC_LOG_QUERY_ENABLED` 必须保持 `false`） |
| 实现状态 | `IN_PROGRESS`（后端既有四接口 `IMPLEMENTED_ACCEPTED`；前端初版 `IMPLEMENTED_PENDING_REVIEW_WITH_REQUIRED_CHANGES`；本次功能开放状态接口及相应交互修订尚未实现；本文件是验收设计，不是已执行的验收报告） |
| 创建日期 | 2026-08-20 |
| 创建任务 | `LOG-QUERY-UI-ACCEPTANCE-DESIGN-001` |
| 关联任务 | `LOG-QUERY-UI-ACCEPTANCE-DESIGN-001`（初版）、`LOG-QUERY-UI-ACCEPTANCE-DESIGN-001-R1`（定向修订）、`LOG-QUERY-UI-ACCEPTANCE-DESIGN-001-R1.1`（微型一致性修订）、`LOG-QUERY-BASELINE-ADJUSTMENT-001`（功能开放控制与交互基线定向修订）、`LOG-QUERY-BASELINE-ADJUSTMENT-APPROVAL-001`（正式批准收口） |
| 依据需求 | `docs/features/log-query/REQUIREMENTS.md`（`APPROVED / IN_PROGRESS`） |
| 关联契约 | `docs/features/log-query/API.md`（`APPROVED / IN_PROGRESS`） |
| 关联设计 | `docs/features/log-query/DESIGN.md`（`APPROVED / IN_PROGRESS`） |
| 关联 UI | `docs/features/log-query/UI.md`（`APPROVED / IN_PROGRESS`） |
| 最新批准日期 | `2026-08-21` |
| 最新批准任务 | `LOG-QUERY-BASELINE-ADJUSTMENT-APPROVAL-001` |
| 最新批准范围 | `LOG-QUERY-BASELINE-ADJUSTMENT-001 + LOG-QUERY-BASELINE-ADJUSTMENT-001-R1` 完整调整后的当前日志查询基线 |

修订记录（以下各条目描述对应任务完成当时的状态，均为历史记录，不代表当前文档状态）：

- `LOG-QUERY-UI-ACCEPTANCE-DESIGN-001`：初版，创建 UI 与 ACCEPTANCE 两份设计文档（均为 `DRAFT_PENDING_USER_REVIEW / NOT_STARTED`）。本文件为日志查询详细验收设计，待用户人工复审；通过本文件复审不等于代码实现通过，本文件不构成任何已执行验收结论。
- `LOG-QUERY-UI-ACCEPTANCE-DESIGN-001-R1`：基于对提交 `ce973d1f0ed5eb561a31430b3220a957f37b01b7` 的人工复审结论定向修订：① 分页条严格不显示任何页码/页次，删除待确认项 `LQ-UI-P-01`（`LQ-AC-059 / 060` 同步收口）；② 修正三页游标用例 off-by-one（`LQ-AC-065 / 066`，第 3 页栈严格 `[null,C1,C2]`，无 `C3`）；③ 纵向滚动统一为"仅表格数据区一条纵向滚动条"（`LQ-AC-149`）；④ 明确当前阶段菜单隐藏、路由直访（`LQ-AC-001 / 155 / 170`）（历史规则，其"当前阶段菜单隐藏"现行口径已被 `LOG-QUERY-BASELINE-ADJUSTMENT-001` 废止）；⑤ 物理设计相关用例 `LQ-AC-156~158` 状态统一为 `DEFERRED_UNTIL_PHYSICAL_DESIGN`；⑥ 首次查询失败标志语义收口为 `initialQueryAttempted`，新增 `LQ-AC-020`。文档当时仍为 `DRAFT_PENDING_USER_REVIEW / NOT_STARTED`，待用户人工复审。
- `LOG-QUERY-UI-ACCEPTANCE-DESIGN-001-R1.1`：拆分"当前物理解耦检查"与"未来物理方案落地验收"两类用例职责：`LQ-AC-156~158` 恢复为代码开发阶段可执行的物理解耦边界检查（不绑定最终 RANGE 粒度、子分区、索引或生产 DDL），初始状态改回 `NOT_RUN`，不再放入 §8 B 层；新增 `LQ-AC-171`（最终物理设计完成并批准）、`LQ-AC-172`（生产 DDL 在完全离线窗口执行并验证）、`LQ-AC-173`（菜单开放前阻断条件全部通过）（历史规则，其"菜单开放"命名已被 `LOG-QUERY-BASELINE-ADJUSTMENT-001` 调整为"生产启用，`CDC_LOG_QUERY_ENABLED=true`"），初始状态 `DEFERRED_UNTIL_PHYSICAL_DESIGN`；§8 A/B 分层同步，B 层延期用例精确为 `LQ-AC-164 / 165 / 171 / 172 / 173` 共 5 个，`LQ-AC-155` 保持 `NOT_RUN`。文档当时仍为 `DRAFT_PENDING_USER_REVIEW / NOT_STARTED`，待用户人工复审。
- `LOG-QUERY-UI-ACCEPTANCE-APPROVAL-001`（2026-08-20）：用户已最终人工复审并批准初版、R1、R1.1 形成的完整详细验收设计链，执行正式批准收口。本文档状态由 `DRAFT_PENDING_USER_REVIEW` 转为 `APPROVED`，实现状态仍为 `NOT_STARTED`。当前文档成为日志查询功能的正式验收基线；允许据此开发测试、联调与执行开发阶段验收；文档批准不等于任何验收用例已经执行或通过，所有普通用例仍为 `NOT_RUN`，延期用例仍为 `DEFERRED_UNTIL_PHYSICAL_DESIGN`（精确集合 `LQ-AC-164 / 165 / 171 / 172 / 173`）；菜单开放阻断链继续有效，最终物理设计、生产 DDL 与生产等价性能验收完成并批准前，日志查询菜单必须保持隐藏（本条目为历史记录，其"菜单必须保持隐藏"的现行口径已被 `LOG-QUERY-BASELINE-ADJUSTMENT-001` 废止，生产启用阻断对象改为将 `CDC_LOG_QUERY_ENABLED` 置为 `true`）。
- `LOG-QUERY-BASELINE-ADJUSTMENT-001`（2026-08-21）：依据用户对日志查询功能开放方式的最新确认及 ChatGPT 前端实现复审要求定向修订：① 菜单改为始终显示、始终可点击、路由 `/monitor/log-query` 允许直访，废止"验证完成前菜单隐藏"现行规则，功能开放改由后端 `cdc.log-query.enabled`（默认 `false`，`CDC_LOG_QUERY_ENABLED`）唯一权威控制；② 新增只读状态接口 `GET /api/log-query/status`（不读数据库，返回 `data.enabled`），新增功能未开放页与状态检测失败页及手动"重新检测"（无自动轮询/刷新/重试）（其中手动"重新检测"按钮为历史方案，已由 `LOG-QUERY-BASELINE-ADJUSTMENT-001-R1` 废止）；③ 明确原四接口不受开关保护，直接调用仍按原契约工作；④ 修订"全部"双向即时互斥、再次点击当前菜单完整重新初始化、重置清除校验错误、数据源降级展示四种情况；⑤ 授权前端测试框架引入并同步验收覆盖。本文档状态由 `APPROVED` 调整为 `DRAFT_PENDING_USER_REVIEW`，实现状态为 `IN_PROGRESS`（后端既有四接口 `IMPLEMENTED_ACCEPTED`；前端初版 `IMPLEMENTED_PENDING_REVIEW`；本次状态接口及相应交互修订尚未实现）。本条目描述修订动作，不代表本次调整已经用户复审批准。
- `LOG-QUERY-BASELINE-ADJUSTMENT-001-R1`（2026-08-21）：依据用户复审确认做三项微型一致性修订：① 取消状态失败页"重新检测"按钮及全部现行规则，状态失败页固定文案为标题"功能状态获取失败"、说明"暂时无法获取日志查询功能状态，请刷新页面或稍后重新进入。"，无"重新检测"按钮，不自动重试/轮询/刷新，用户只能通过刷新页面、离开后重新进入、再次点击当前"日志查询"菜单重新检测状态（`LQ-AC-178 / 179` 同步改写）；② 列表查询触发事件严格为五类（首次进入且 `enabled=true` 后默认查询、某 Tab 首次打开、点击查询、上一页/下一页、重新进入且 `enabled=true` 后默认查询），同步 `LQ-AC-135`；③ 页面事实修正：真实日志查询页面初版已实现（前端提交 `17680b34` 已将原占位页替换），`LQ-AC-001` 同步明确不再显示占位内容。本文档状态保持 `DRAFT_PENDING_USER_REVIEW`，实现状态保持 `IN_PROGRESS`，所有用例状态不变；本条目描述修订动作，不代表本次调整已经用户复审批准。
- `LOG-QUERY-BASELINE-ADJUSTMENT-APPROVAL-001`（2026-08-21）：用户已批准 `LOG-QUERY-BASELINE-ADJUSTMENT-001` 与 `LOG-QUERY-BASELINE-ADJUSTMENT-001-R1` 形成的完整调整内容，执行正式批准收口。本文档状态由 `DRAFT_PENDING_USER_REVIEW` 转为 `APPROVED`，成为日志查询功能当前正式验收基线；允许进入本次调整相关的后端、前端、自动化测试、开发库联调和验收执行；批准不代表新增状态接口和前端调整已经实现，不代表任何验收用例已经执行或通过；所有普通用例仍为 `NOT_RUN`，延期用例仍为 `DEFERRED_UNTIL_PHYSICAL_DESIGN`（精确集合 `LQ-AC-164 / 165 / 171 / 172 / 173`）；最终物理设计、生产 DDL 与生产等价性能验收继续延期；菜单始终显示，但生产阻断条件完成前 `CDC_LOG_QUERY_ENABLED` 必须保持 `false`；原四接口不判断开关的边界继续有效。
- `LOG-QUERY-BASELINE-ADJUSTMENT-APPROVAL-001-R1`（2026-08-21）：批准后现行状态标识与声明措辞微型一致性补正：统一现行前端实现状态为 `IMPLEMENTED_PENDING_REVIEW_WITH_REQUIRED_CHANGES`（历史修订记录中的 `IMPLEMENTED_PENDING_REVIEW` 保留为当时状态）。本补正仅为批准后的状态与措辞一致性修订，不改变批准范围、业务语义、实现事实或验收状态，不改变任何验收用例状态；文档状态保持 `APPROVED / IN_PROGRESS`，最新批准任务仍为 `LOG-QUERY-BASELINE-ADJUSTMENT-APPROVAL-001`。
- `LOG-QUERY-CURSOR-CORRECT-TAB-ADJUSTMENT-001`（2026-08-25）：依据用户对游标分页与正确日志 Tab 的视觉复审，同步修订验收基线：① 正确日志首次切换不再自动查询，改为"第一次切换只初始化并展示缺省条件（当前自然日、源库/目标库“全部”、表名空），状态为 `NOT_QUERIED`，显示引导文案，等待用户点击“查询”后才发起首次列表查询"；② 列表查询触发事件由五类收口为四类（正确日志不自动查询，见 `LQ-AC-135`）；③ 受此规则变化影响、此前依赖旧行为得出的既有 PASS 用例（`LQ-AC-004 / 016 / 020 / 135 / 177`）不得继续冒充有效 PASS，按本文档状态体系改为 `NOT_RUN` 并改写预期结果，待按新规则重新验收；`LQ-AC-037`（原 `NOT_RUN`）预期结果同步改写；新增 `LQ-AC-004A~004D` 覆盖正确日志未查询引导态、手动查询触发单次请求、查询后切换回状态保留、重置保留列表/已生效条件、重新进入后旧响应不覆盖新状态；④ 开发环境游标密钥配置调整（`cdc.log-query.cursor-secret` 固定开发密钥，值不写入本文档正文，见对应报告），生产配置不携带该密钥，不新增验收用例。本条目描述修订动作，不代表上述用例已经按新规则重新执行；本次未执行的用例保持 `NOT_RUN`。本文档状态保持 `APPROVED`，实现状态保持 `IN_PROGRESS`。
- `LOG-QUERY-CURSOR-CORRECT-TAB-ADJUSTMENT-001-R1`（2026-08-25）：依据对提交 `ab7bf528e4c68b1f7a71b1d5f66861aac4fe801c` 的 ChatGPT 代码复审定向修订验收基线：① 前端修复正确日志引导只应在正确日志且 `NOT_QUERIED` 时显示（错误日志初始化候选加载期间不得显示属于正确日志的引导，对应 `LQ-AC-004 / 004A` 语义）；② 明确正确日志首次手动点击”查询”真正发起请求后 `initialQueryAttempted` 必须为 `true`（无论成功、业务失败、网络失败或超时），`LQ-AC-004` 预期同步落实；③ 复审判定 `LQ-AC-119 / 120 / 125` 三个既有 `PASS` 用例证据不足（分别为：既有证据仅覆盖初始化锁定而非普通用户查询加载态、仅证明静态设置 `timeout: 30000` 而未实际模拟超过 30 秒、证据为后端超时路径而非前端超时页面提示），按本文档状态体系恢复为 `NOT_RUN`，待后续真实组件/接口模拟或人工视觉验收后重判，不得伪造 `PASS`。本次不改变 5 个 `DEFERRED_UNTIL_PHYSICAL_DESIGN` 用例（`LQ-AC-164 / 165 / 171 / 172 / 173`）与任何 `BLOCKED` 用例。本条目描述修订动作，不代表上述用例已经重新执行；未执行的用例保持 `NOT_RUN`。本文档状态保持 `APPROVED`，实现状态保持 `IN_PROGRESS`。

本验收设计状态为 `APPROVED`，`LOG-QUERY-BASELINE-ADJUSTMENT-001` 与 `-R1` 完整调整内容已经用户批准收口，本文档是日志查询功能当前正式验收基线；批准设计不等于验收通过，用例尚未执行。**重要声明**：本文档定义「应该被如何验收」的验收基线，所有用例初始状态均为 `NOT_RUN`（未执行）或 `DEFERRED_UNTIL_PHYSICAL_DESIGN`（受最终物理设计延期约束），**不伪造任何执行结果**。最终物理数据库设计（一级 RANGE 分区粒度、子分区、索引形态、生产 DDL、生产等价性能验收）仍为延期项（`LQ-AC-171 / 172 / 173` 验收）；菜单始终显示、始终可点击（`LQ-AC-001`），生产启用阻断对象为在全部阻断项通过并获批前将 `CDC_LOG_QUERY_ENABLED` 置为 `true`（`LQ-AC-173`、`LQ-DB-13~14`）；`LQ-AC-156~158` 仅为代码开发阶段可执行的"物理解耦边界检查"，不代表最终物理设计已完成。

## 2. 验收设计目的、适用范围与非目标

### 2.1 目的

- 本文档把已批准 `REQUIREMENTS.md`、`API.md`、`DESIGN.md` 与本次 `UI.md` 的可验收规则转化为可直接指导开发自测、联调、接口测试与人工验收的详细用例，供用户复审与后续执行。
- 每个用例明确前置条件、操作步骤、预期结果、证据类型与建议执行层级，验收结论可复核、可追溯。
- 验收用例只覆盖上游设计允许的行为；上游明确禁止的行为（见 §2.3）不设用例或只设"不得出现"的反向校验用例。

### 2.2 适用范围

- 覆盖 §6.2 列出的 31 个验收领域；
- 性能验收按 §8 明确分层：A（代码开发阶段可执行）/ B（生产启用前必须执行）。

### 2.3 非目标与禁止行为

以下行为不提供正向用例，且对应反向校验用例（如无特殊标注）必须确认其不发生（来源 `LQ-NONGOAL-01~20`、`LQ-LOAD-01 / 33`、`LQ-PAGE-04 / 05 / 06`、`LQ-DETAIL-22 / 27`、`LQ-API-10`、`LQ-DB-14`）：

1. 自动刷新、轮询、WebSocket、自动重试；
2. 导出、删除、修改、重试、忽略、提醒等写操作入口；
3. 总数、页码跳转、任意 `pageSize`、传统 OFFSET 分页；
4. 用户点击表头排序；
5. 模糊表名查询、自动大小写转换、表名自动补全或缓存候选；
6. 无时间范围查询、清空时间范围后查询；
7. 列表预取完整 `LOG_DETAIL`、`RAW_MESSAGE` 或 `RESULT_DETAIL`；
8. 抽屉替代已确认的弹窗；
9. 未经批准的新查询条件或统计卡片；
10. `RESULT_DETAIL` 展示；
11. 查询状态跨页面持久化（LocalStorage / SessionStorage）；
12. 在全部生产阻断项（`LQ-AC-171 / 172 / 164 / 165`）通过并获得人工批准前，将 `CDC_LOG_QUERY_ENABLED` 配置为 `true`（`LQ-DB-14`）。

## 3. 验收结果状态模型

| 状态 | 含义 | 说明 |
|---|---|---|
| `NOT_RUN` | 尚未执行 | 所有新建用例的初始状态；执行前不得改为 PASS/FAIL |
| `PASS` | 已执行且符合预期 | 必须附证据（截图 / 接口记录 / 构建日志 / 测试报告 / 性能报告） |
| `FAIL` | 已执行且不符合预期 | 必须记录失败点、复现步骤与证据 |
| `BLOCKED` | 受环境或依赖阻塞 | 必须记录阻塞原因与恢复条件 |
| `DEFERRED_UNTIL_PHYSICAL_DESIGN` | 必须在最终物理设计完成后执行 | 仅用于依赖最终分区/索引/生产 DDL/生产等价性能环境的用例（见 §8 性能分层 B） |

规则：

- 本文档所有用例初始状态为 `NOT_RUN`；物理设计相关用例初始状态为 `DEFERRED_UNTIL_PHYSICAL_DESIGN`。
- 不允许把"验收设计完成"写成"功能验收通过"；不允许把开发库验证结果外推为生产性能结论。
- 执行状态记录必须标注执行人、执行日期与证据位置；本文档不预留伪造状态的空间。

## 4. 证据类型与建议执行层级约定

### 4.1 证据类型

| 证据类型 | 说明 |
|---|---|
| 人工 UI 截图 | 页面交互、布局、弹窗、空值、错误态、无障碍等视觉/交互证据 |
| 接口证据 | HTTP 请求/响应记录（URL、方法、请求体、响应体、耗时） |
| 后端单测 | JUnit + MockMvc / Service 单测通过记录 |
| 前端单测 | 前端组件/逻辑单测通过记录（若实现阶段引入测试框架） |
| 接口自动化测试 | 接口级自动化用例执行记录 |
| 集成测试 | 前后端联调与数据库联调记录 |
| SQL / 执行计划证据 | 逻辑 SQL 形态检查、可获得的执行计划、扫描量或索引命中记录 |
| 构建日志 | `mvn clean test/package`、`npm run build` 输出 |
| 性能验证报告 | 生产等价规模与并发下的响应时间、扫描量、执行计划 |

### 4.2 建议执行层级

`前端单测` / `后端单测` / `接口测试` / `集成测试` / `人工 UI` / `性能验证`，用例按性质选择一项或多项；执行时以对应层级的最小充分组合为准。

## 5. 验收领域范围

本文档覆盖以下 31 个验收领域（编号即 §6.2 顺序）：

1. 页面入口、双 Tab 顺序、默认 Tab 与首次查询；
2. 两 Tab 独立状态及重新进入菜单的状态失效；
3. 查询条件、默认值、必填校验、精确匹配、多选与"全部"；
4. 当前自然日的计算时点、跨午夜不自动推进；
5. 最大 7 天排他边界公式，包括恰好 7 天合法和多 1 秒拒绝；
6. 表单条件与已生效条件分离；
7. 重置不查询、不清列表、不改游标；
8. 固定排序、固定 100 条、101 条探测、无总数、无跳页；
9. 请求游标栈的首页、下一页、连续翻页到达第 3 页、两次上一页、失败原子性；
10. 新增/晚到日志下不承诺跨请求快照一致性；
11. `CDC_LOG_ID` 超过 JavaScript 安全整数时仍以字符串无损传输；
12. 数据源候选、名称映射、停用/缺失/空值降级、禁止 N+1；
13. 列表 12 列、列顺序、横向滚动、固定列、单行省略和 Tooltip；
14. 四个时间字段全部显示且格式一致；
15. 日志摘要与完整详情隔离；
16. 原始消息按需加载且列表不预取；
17. 合法 JSON 的"原文/格式化"切换、复制必须复制原始内容；
18. 非 JSON、空内容、超长文本、超大 CLOB 的展示；
19. 纯文本安全展示，禁止把日志内容作为 HTML 执行；
20. 查询加载、3 秒慢提示、动态秒数、25/30 秒超时协调；
21. 失败、超时、游标失效、旧响应和跨 Tab 响应隔离；
22. 空数据与字段 NULL 的 `--` 规则；
23. 不自动刷新、不自动重试；
24. 权限与只读边界、非法 `logType`、SQL 注入防护；
25. 列表、详情、原始消息三条 SQL 路径的大字段隔离；
26. 页面支持的桌面分辨率、页面不出现纵向滚动条、仅表格数据区滚动；
27. 无障碍与键盘交互；
28. 菜单始终显示、当前物理解耦边界检查与物理设计延期、正式启用前置条件；
29. 开发库功能验收与生产等价性能验收的明确分层；
30. 回归范围与构建、测试、截图、接口证据要求；
31. 功能开放、状态接口与页面状态流程（新增，`LQ-AC-174~182`）。

> 执行说明：左侧"日志查询"菜单始终显示、始终可点击，可通过路由 `/monitor/log-query` 直接访问（领域 28）。功能是否开放由后端配置 `cdc.log-query.enabled`（`CDC_LOG_QUERY_ENABLED`，默认 `false`）唯一权威控制，只决定前端页面是否进入查询流程，不是认证、鉴权或接口安全控制；`enabled=false` 时前端显示功能未开放页，不加载数据源候选、不查询任何日志，但直接调用原四接口仍按原契约工作。涉及"进入菜单"的用例在开发阶段验证到"菜单显示且可点击 + 路由可访问 + 状态接口按配置返回"即可；不得以 `enabled=false` 时调用原四接口被拒绝作为预期。

## 6. 详细验收用例

> 每个用例包含：前置条件、操作步骤、预期结果、证据类型、建议执行层级、执行状态。编号从 `LQ-AC-001` 起，唯一且不悬空。

> **开发阶段验收执行记录（`LOG-QUERY-DEVELOPMENT-ACCEPTANCE-EXECUTION-001`，2026-08-25）**：本任务在授权基线提交 `7b3010e8fa2b408204929b5df2d2dc54717996c0` 上重新执行了可复核的开发阶段验收（前端 Vitest 7 文件 42 例、后端日志查询专项 135 例、前后端构建、只读开发库联调、SQL 静态形态检查、代码审查），将各用例的字段由"初始状态"更新为"执行状态"：121 例 `PASS`、8 例 `BLOCKED`（含阻塞说明与解除条件）、46 例 `NOT_RUN`（需用户视觉验收或未执行）、5 例保持 `DEFERRED_UNTIL_PHYSICAL_DESIGN`（`LQ-AC-164 / 165 / 171 / 172 / 173`）。全部 PASS/FAIL/BLOCKED/NOT_RUN 的证据映射、前后状态数量对比与总数守恒核对见报告 `docs/features/log-query/reports/LOG-QUERY-DEVELOPMENT-ACCEPTANCE-EXECUTION-001.md`。未伪造任何执行结果；需人工视觉判断的用例保持 `NOT_RUN`。**后续说明**：`LOG-QUERY-CURSOR-CORRECT-TAB-ADJUSTMENT-001`（2026-08-25）将正确日志首次切换改为"只初始化缺省条件不自动查询"，据此按规则把其中 5 例既有 `PASS`（`LQ-AC-004 / 016 / 020 / 135 / 177`）改为 `NOT_RUN` 并改写预期，新增 `LQ-AC-004A~004D`；上述 121 例 `PASS` 为执行任务当时的记录，不代表受本次规则调整影响的用例当前仍有效。**R1 后续说明**：`LOG-QUERY-CURSOR-CORRECT-TAB-ADJUSTMENT-001-R1`（2026-08-25）复审判定 `LQ-AC-119 / 120 / 125` 三个既有 `PASS` 证据不足，恢复为 `NOT_RUN`；当前有效状态为 113 例 `PASS`、58 例 `NOT_RUN`、8 例 `BLOCKED`、5 例 `DEFERRED_UNTIL_PHYSICAL_DESIGN`（`LQ-AC-164 / 165 / 171 / 172 / 173`），合计 184 例。上述开发阶段 121 例 `PASS` 与各历史任务记录均为当时记录，不代表当前有效状态。

### 6.1 领域 1：页面入口、双 Tab 顺序、默认 Tab 与首次查询

#### LQ-AC-001 单菜单与路由
- 前置条件：功能代码已实现并可在开发库运行；页面可通过路由访问。
- 操作步骤：1. 登录平台，检查左侧菜单，确认"运行监控"分组下始终显示"日志查询"入口且可点击；2. 直接访问 `/monitor/log-query`；3.（可选）通过静态配置/代码审查确认只存在一个"日志查询"菜单定义，未新增第二个"日志观察"菜单。
- 预期结果：左侧"日志查询"菜单始终显示、始终可点击，不通过隐藏菜单控制功能开放；直接访问 `/monitor/log-query` 能打开真实日志查询页面初版（原占位页已被前端提交 `17680b34` 替换，不再显示占位内容）；静态配置/代码层面只存在一个"日志查询"菜单定义；未删除未来菜单定义，也未新增第二个日志菜单。
- 证据类型：人工 UI 截图、代码审查/静态配置核对
- 建议执行层级：人工 UI、前端单测
- 执行状态：PASS

#### LQ-AC-002 双 Tab 顺序与默认 Tab
- 前置条件：同上。
- 操作步骤：进入页面，观察 Tab 区。
- 预期结果：两个 Tab，顺序为"错误日志"在前、"正确日志"在后；默认打开"错误日志"。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-003 首次进入自动查询错误日志第一页
- 前置条件：同上。
- 操作步骤：1. 重新进入页面；2. 观察请求网络记录与列表。
- 预期结果：首次进入即发起一次 `POST /api/log-query/logs/search`（`logType=error`，默认当前自然日），请求成功后列表展示第一页；请求失败则显示错误态（见领域 21）。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：PASS

#### LQ-AC-004 正确日志第一次切换只初始化缺省条件，点击"查询"才首次查询
- 前置条件：同上。
- 操作步骤：1. 进入页面，等待错误日志首查完成；2. 观察是否出现 `logType=correct` 请求；3. 首次切换到"正确日志"；4. 观察缺省条件、列表区与请求网络记录；5. 点击"查询"。
- 预期结果：首次进入后未出现 `logType=correct` 请求；首次切换到"正确日志"时只填充缺省条件（当前自然日 `00:00:00`–`23:59:59`、源库/目标库"全部"、表名空）并展示引导文案"正确日志数据量较大，请设置查询条件后点击"查询""，不发起任何列表请求、无加载遮罩/旋转图标/等待秒数/慢查询提示、不显示"暂无数据"，查询状态为 `NOT_QUERIED`；点击"查询"后才发起首次 `logType=correct` 列表请求，其 `initialQueryAttempted` 在首次查询触发后置为 `true`，无论成功、失败或超时。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-004A 正确日志未查询引导态不混淆空数据与加载态
- 前置条件：同上。
- 操作步骤：1. 首次切换到"正确日志"；2. 观察列表区文案、加载遮罩、等待秒数与慢查询提示。
- 预期结果：未查询状态显示引导文案"正确日志数据量较大，请设置查询条件后点击"查询""（弱提示"默认查询时间为当天。缩小时间范围或指定数据源、表名可提高查询速度。"可选），不显示"暂无数据"（与"查询成功但无数据"的 `SUCCESS_EMPTY` 区分），不显示加载遮罩、旋转图标、等待秒数与"查询耗时较长"慢查询提示；分页按钮不可用。
- 证据类型：人工 UI 截图、前端单测
- 建议执行层级：人工 UI、前端单测
- 执行状态：NOT_RUN

#### LQ-AC-004B 正确日志手动查询触发单次请求且成功后进入有数据状态
- 前置条件：同上。
- 操作步骤：1. 首次切换到"正确日志"；2. 点击"查询"；3. 观察网络请求与列表。
- 预期结果：点击"查询"恰好触发一次 `POST /api/log-query/logs/search`（`logType=correct`，携带当前表单条件）；请求期间显示加载遮罩；成功且有数据时状态为 `SUCCESS_WITH_DATA`，成功但无数据时状态为 `SUCCESS_EMPTY` 并显示"当前查询条件下暂无日志"；失败时状态为 `FAILED` 并显示错误态；未查询状态下不产生任何请求。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-004C 正确日志查询后切换回保留状态且不重新查询
- 前置条件：正确日志已通过点击"查询"完成首次查询。
- 操作步骤：1. 在正确日志查询并翻页；2. 切换到错误日志再切回正确日志；3. 观察网络请求与状态。
- 预期结果：切回正确日志时恢复其表单、已生效条件、列表、错误与游标历史，不重新发起查询；已查询过的正确日志不再自动触发首次查询；错误日志状态不被覆盖。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-004D 正确日志重置保留列表与已生效条件，重新进入恢复未查询态
- 前置条件：正确日志已查询且有数据。
- 操作步骤：1. 正确日志点击"重置"，观察请求与列表；2. 重新进入页面，切换到正确日志。
- 预期结果：重置只把表单恢复为缺省（源库/目标库"全部"、表名清空、时间=点击重置时所在自然日），不发起查询、不清空列表、不改已生效条件与游标历史，`validationError` 被清除；重新进入且 `enabled=true` 后正确日志恢复到"缺省条件已填充但未查询"的 `NOT_QUERIED` 状态并显示引导文案，旧页面的在途响应不得覆盖该新状态。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-005 Tab 与数据源表映射
- 前置条件：开发库 `CDC_LOG_ERROR`、`CDC_LOG_CORRECT` 有数据。
- 操作步骤：分别在两个 Tab 查询并核对网络请求 `logType`。
- 预期结果：错误日志 Tab 请求 `logType=error`；正确日志 Tab 请求 `logType=correct`；服务端映射到 `CDC_LOG_ERROR` / `CDC_LOG_CORRECT`，返回对应表数据。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-006 Tab 标题不显示总数
- 前置条件：进入页面。
- 操作步骤：观察 Tab 标题与页面任何区域。
- 预期结果：Tab 标题仅显示"错误日志""正确日志"，页面不展示全量总数、不展示总记录数。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-007 无写操作入口
- 前置条件：进入页面。
- 操作步骤：检查列表行、表格工具栏、弹窗与页面全部可操作控件。
- 预期结果：不存在新增、修改、删除、导出、重试、忽略、提醒等任何写操作入口；页面只提供查询、翻页、查看详情/原始消息、复制。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-008 页面标题与说明
- 前置条件：进入页面。
- 操作步骤：观察页面顶部。
- 预期结果：页面标题显示"日志查询"，标题下有一行简短说明（只读检索同步日志、错误日志/正确日志双 Tab、按固定排序游标分页）。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

### 6.2 领域 2：两 Tab 独立状态及重新进入菜单的状态失效

#### LQ-AC-011 两 Tab 状态完全独立
- 前置条件：进入页面。
- 操作步骤：1. 错误日志查询并翻页；2. 切换正确日志查询并翻页；3. 来回切换观察各 Tab 表单、列表、分页、游标。
- 预期结果：两个 Tab 的表单条件、已生效条件、列表、请求游标栈、`hasNext`/`nextCursor`、加载状态、错误状态、首次查询触发标志 `initialQueryAttempted` 各自独立，互不覆盖；来回切换不发起新查询，直接恢复各自状态。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、接口测试
- 执行状态：PASS

#### LQ-AC-012 切换 Tab 不销毁不重建
- 前置条件：两个 Tab 均已至少查询过一次。
- 操作步骤：1. 错误日志翻到第 2 页；2. 切换正确日志再切回错误日志。
- 预期结果：错误日志仍显示第 2 页、原已生效条件与原游标栈；切换过程无新查询请求（已触发过首次查询、无论成败的 Tab 切回不再次自动触发首次查询）。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、接口测试
- 执行状态：PASS

#### LQ-AC-013 一个 Tab 的操作不影响另一个 Tab
- 前置条件：两个 Tab 均已有列表。
- 操作步骤：在错误日志点击查询/重置/翻页，观察正确日志状态；反之亦然。
- 预期结果：正确日志的表单、列表、游标、加载状态完全不变；错误日志响应不得写入正确日志状态。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-014 后台查询 Tab 的独立加载标识
- 前置条件：通过造数使列表查询耗时超过约 1 秒（或接口测试层验证逻辑）。
- 操作步骤：1. 在错误日志发起慢查询；2. 切换正确日志。
- 预期结果：错误日志 Tab 标题旁显示小型加载标识，正确日志可正常操作；两 Tab 加载状态相互独立。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-015 重新进入清除状态恢复默认
- 前置条件：两个 Tab 均已做过修改表单、查询、翻页操作。
- 操作步骤：分别以三种方式重新进入：浏览器刷新；离开后从其他菜单返回；再次点击左侧"日志查询"菜单（即使当前已在该路由）。
- 预期结果：三种方式均先调用状态接口 `GET /api/log-query/status` 并作废在途的列表、详情、原始消息与状态请求；`enabled=true` 时清除两个 Tab 全部临时状态与游标，恢复默认表单（时间=重新进入时所在自然日），默认打开错误日志并自动查询第一页；`enabled=false` 时显示功能未开放页（不加载数据源候选、不查询任何日志）。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-016 重新进入后正确日志恢复"缺省条件已填充但未查询"，等待用户点击"查询"
- 前置条件：进入页面完成错误日志首查。
- 操作步骤：重新进入页面后观察网络请求与正确日志状态。
- 预期结果：重新进入且 `enabled=true` 时仅错误日志自动首查；正确日志不发起任何查询，恢复到"缺省条件已填充但未查询"的 `NOT_QUERIED` 状态并显示引导文案，直到用户点击"查询"才发起首次列表查询。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：NOT_RUN

#### LQ-AC-017 不持久化查询状态
- 前置条件：进入页面并修改表单、翻页。
- 操作步骤：1. 检查浏览器 LocalStorage / SessionStorage；2. 刷新后观察。
- 预期结果：查询状态不写入任何浏览器持久化存储；刷新后状态清空并恢复默认。
- 证据类型：人工 UI（浏览器存储面板截图）
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-018 重新进入后旧响应被丢弃
- 前置条件：接口测试环境；能制造"旧请求慢返回"场景。
- 操作步骤：1. 发起慢查询 A 后立即重新进入页面；2. 慢查询 A 的响应在重新进入后返回。
- 预期结果：旧请求尽量被取消；无法取消时通过页面代次/请求令牌失效；旧响应返回不得覆盖新页面的表单、列表、游标、加载与错误状态。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-019 跨 Tab 响应隔离
- 前置条件：能制造"错误日志请求慢、正确日志请求快"场景。
- 操作步骤：1. 在错误日志发起慢查询；2. 切换正确日志并查询成功；3. 错误日志慢响应随后返回。
- 预期结果：错误日志响应只写入错误日志状态，正确日志列表/游标不被污染；反之同理。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-020 首次查询失败后切回不自动重试
- 前置条件：可制造首次查询失败/超时（错误日志首次进入的默认查询，或正确日志首次点击"查询"）。
- 操作步骤：1. 进入页面使错误日志默认查询失败，或切到正确日志点击"查询"使其首次查询失败；2. 切换到另一 Tab 再切回该 Tab；3. 观察网络请求与页面；4. 再次点击"查询"。
- 预期结果：首次查询无论成功、失败或超时，该 Tab 的 `initialQueryAttempted` 均置为 `true`；切回已触发过首次查询的 Tab 不再次自动触发首次查询；正确日志在未点击"查询"前不自动查询；只有用户点击"查询"才主动重试，重试请求携带当前表单条件。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：NOT_RUN

### 6.3 领域 3：查询条件、默认值、必填校验、精确匹配、多选与"全部"

#### LQ-AC-021 查询项顺序固定
- 前置条件：进入页面。
- 操作步骤：观察查询区控件顺序。
- 预期结果：顺序固定为：源库、源表名、目标库、目标表名、同步到目标库时间范围、查询、重置；字段标签置于控件上方；控件为 `size=small`。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-022 源库/目标库多选默认"全部"
- 前置条件：进入页面。
- 操作步骤：观察源库、目标库下拉初始状态。
- 预期结果：两个下拉框初始均为"全部"状态。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-023 候选来源、显示与提交值
- 前置条件：`GET /api/log-query/data-source-options` 可访问。
- 操作步骤：1. 打开源库下拉；2. 核对候选与请求/响应。
- 预期结果：候选来自接口 `sourceList`/`targetList`；页面显示 `org`，提交 `id`；候选加载失败时下拉框禁用并给出可重试入口，不影响已生效列表与详情。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：PASS

#### LQ-AC-024 候选排序
- 前置条件：候选接口返回多条。
- 操作步骤：打开下拉，观察候选顺序。
- 预期结果：候选按 `DATA_SOURCE_ORG` 排序；`org` 为 NULL/空串的按"未定义名称"稳定显示并参与排序。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-025 下拉过滤
- 前置条件：候选数量 ≥2 且名称可区分。
- 操作步骤：在下拉输入框输入名称片段。
- 预期结果：候选按名称过滤显示；过滤不改变提交值语义。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-026 名称重复时辅助显示 ID
- 前置条件：候选存在相同 `org` 的多个 ID（或用构造数据）。
- 操作步骤：展开下拉观察重复名称项。
- 预期结果：重复名称的选项辅助显示 `DATA_SOURCE_ID`，可区分、可正确提交对应 ID。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-027 "全部"与具体值双向即时互斥
- 前置条件：下拉已打开。
- 操作步骤：1. 在"全部"状态下选择某个具体值；2. 在具体值状态下选择"全部"；3. 反复切换并核对表单状态。
- 预期结果：选择任一具体值时立即取消"全部"；选择"全部"时立即清空全部具体值；清空全部具体值后自动恢复"全部"；"全部"只是前端表单状态，不作为数据源 ID 传输。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-028 多选去重与折叠展示、"全部"不提交、SQL 无全部 ID 展开
- 前置条件：下拉有多个候选。
- 操作步骤：1. 重复选择同一 ID；2. 选择多个具体值；3. 处于"全部"时点击查询并核对请求体与后端 SQL；4. 选择具体数据源时点击查询并核对 SQL。
- 预期结果：选中值去重；多个具体值以标签折叠展示；"全部"状态时请求体不携带 `sourceDataSourceIds`/`targetDataSourceIds`（或为 `[]`），不提交"全部"魔法字符串；后端在"全部"状态下不生成对应字段 `IN (...)` 谓词，禁止把候选全部 ID 拼接成 `IN (...)` 模拟"全部"；仅选择具体数据源时使用绑定参数 `IN` 查询；选择具体数据源时日志表数据源字段为 NULL 的记录自然不匹配（不做特殊兼容）是可接受行为。
- 证据类型：接口证据、人工 UI 截图、SQL / 执行计划证据
- 建议执行层级：接口测试、人工 UI、集成测试
- 执行状态：PASS

#### LQ-AC-029 表名输入框与占位文字
- 前置条件：进入页面。
- 操作步骤：观察源表名/目标表名控件。
- 预期结果：为普通文本框，可单独使用不要求先选库；占位文字为"请输入完整表名，区分大小写"。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-030 表名去空格、精确匹配、区分大小写
- 前置条件：开发库存在大小写不同的同名表构造数据。
- 操作步骤：1. 输入含首尾空格的表名查询；2. 分别输入大小写不同表名查询。
- 预期结果：只去除首尾空白；大小写敏感；以完整表名精确匹配（`=`），不做模糊匹配、不转换大小写；不同大小写返回不同结果。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-031 表名超 64 字符校验
- 前置条件：进入页面。
- 操作步骤：1. 输入超过 64 字符（去除首尾空白后）的表名并点击查询；2. 直接向接口提交超长表名。
- 预期结果：前端阻止查询并在控件下方显示校验错误；后端独立返回 `TABLE_NAME_INVALID`（40017），不因前端已校验而省略。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：前端单测、接口测试
- 执行状态：PASS

#### LQ-AC-032 时间控件精确到秒与默认值
- 前置条件：进入页面。
- 操作步骤：观察"同步到目标库时间范围"控件。
- 预期结果：为精确到秒的时间范围选择器，格式 `yyyy-MM-dd HH:mm:ss`，不含毫秒与时间戳/时区后缀；默认值为当前自然日 `00:00:00`–`23:59:59`（含端点）。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-033 时间必填与成对校验
- 前置条件：进入页面。
- 操作步骤：1. 清空时间范围点击查询；2. 只保留开始或结束一端点击查询。
- 预期结果：任一端为空或整体为空时不得发起查询，时间控件附近显示明确提示。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-034 时间顺序校验
- 前置条件：进入页面。
- 操作步骤：1. 选择开始晚于结束并点击查询；2. 直接向接口提交开始晚于结束。
- 预期结果：前端阻止查询并提示顺序错误；后端独立返回 `TIME_ORDER_INVALID`（40011）。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：前端单测、接口测试
- 执行状态：PASS

#### LQ-AC-035 候选加载失败只影响下拉框
- 前置条件：可制造候选接口失败场景（接口测试层）。
- 操作步骤：1. 候选接口失败；2. 观察下拉框与已生效列表；3. 点击重试入口。
- 预期结果：下拉框禁用并显示可重试入口；已生效列表、详情/原始消息不受影响；重试成功后可恢复下拉。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试
- 执行状态：PASS

### 6.4 领域 4：当前自然日计算时点、跨午夜不自动推进

#### LQ-AC-036 首次进入按进入时自然日
- 前置条件：进入页面。
- 操作步骤：记录进入时日期，检查错误日志默认时间。
- 预期结果：错误日志默认时间按进入时所在自然日生成 `00:00:00`–`23:59:59`。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-037 正确日志第一次切换按切换时自然日填充缺省时间且不查询
- 前置条件：进入页面后等待一段时间或跨日场景可构造。
- 操作步骤：第一次切换到正确日志，检查其默认时间与是否发起请求。
- 预期结果：正确日志默认时间按第一次切换时所在自然日生成 `00:00:00`–`23:59:59`，仅填充缺省条件不发起查询，状态为 `NOT_QUERIED` 并显示引导文案。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-038 重置按点击重置时自然日
- 前置条件：进入页面。
- 操作步骤：点击当前 Tab"重置"，检查时间值。
- 预期结果：时间恢复为点击重置时所在自然日 `00:00:00`–`23:59:59`。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-039 跨午夜不自动推进
- 前置条件：可构造跨自然日午夜停留场景（或用系统时间控制方式验证逻辑）。
- 操作步骤：页面打开后跨过午夜，不刷新、不重置。
- 预期结果：已打开页面时间范围不自动切换为新自然日；不自动刷新、不自动重查。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-040 重置后等待再查询不暗中重算
- 前置条件：进入页面。
- 操作步骤：1. 点击重置得到某自然日时间；2. 等待一段时间；3. 点击查询并核对请求体。
- 预期结果：请求体携带表单中已显示的开始、结束时间；不得在查询时暗中重新计算自然日。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

### 6.5 领域 5：最大 7 天排他边界公式

#### LQ-AC-041 默认当天合法
- 前置条件：进入页面。
- 操作步骤：默认时间直接点击查询。
- 预期结果：请求成功发起（`endExclusive` 为所选结束秒 + 1 秒，即次自然日 `00:00:00`；跨度校验通过）。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-042 完整 7 个自然日合法（边界值）
- 前置条件：进入页面。
- 操作步骤：1. 选择开始 `2026-08-01 00:00:00`、结束 `2026-08-07 23:59:59`；2. 点击查询；3. 接口层直接提交同一范围。
- 预期结果：`endExclusive`=`2026-08-08 00:00:00`，`endExclusive - startTime = 7×24 小时`，为合法边界值，查询成功（前端与后端均不拒绝）。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-043 多 1 秒拒绝（前端）
- 前置条件：进入页面。
- 操作步骤：选择开始 `2026-08-01 00:00:00`、结束 `2026-08-07 23:59:59` 后再把结束延后 1 秒（或选择使 `endExclusive - startTime > 7×24h` 的范围）并点击查询。
- 预期结果：前端阻止查询并提示"时间跨度超过 7 天，请缩小查询范围"。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-044 多 1 秒拒绝（后端）与半开区间语义
- 前置条件：接口测试环境。
- 操作步骤：1. 提交 `endExclusive - startTime > 7×24h` 的请求；2. 提交缺时间、顺序错误、跨度超限等请求；3. 核对 SQL 边界语义。
- 预期结果：后端独立返回 `TIME_RANGE_REQUIRED`（40010）/ `TIME_ORDER_INVALID`（40011）/ `TIME_SPAN_EXCEEDED`（40012）；列表 SQL 使用 `TARGET_TIME >= :startTime AND TARGET_TIME < :endExclusive` 半开区间，`endTime` 为包含端点、`endExclusive = endTime + 1 秒`。
- 证据类型：接口证据、SQL / 执行计划证据
- 建议执行层级：接口测试、集成测试
- 执行状态：PASS

#### LQ-AC-045 页面不提交 endExclusive
- 前置条件：进入页面。
- 操作步骤：查询并核对请求体字段。
- 预期结果：请求体只包含 `startTime`、`endTime`（包含端点 `23:59:59`），不含 `endExclusive`；后端 Service 统一转换。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

### 6.6 领域 6：表单条件与已生效条件分离

#### LQ-AC-046 编辑表单不立即查询、不改变已生效条件
- 前置条件：已有一次成功查询。
- 操作步骤：1. 修改任意表单值；2. 观察是否发起请求与已生效条件。
- 预期结果：表单值变化不立即查询；已生效条件（`appliedCriteria`）不变。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-047 查询成功才原子替换
- 前置条件：已有一次成功查询。
- 操作步骤：修改表单后点击查询（成功）。
- 预期结果：请求成功后原子替换已生效条件、列表与请求游标栈（栈重置为 `[null]`）。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-048 查询失败保留旧列表旧已生效旧游标
- 前置条件：已有一次成功查询；可制造查询失败。
- 操作步骤：修改表单后触发查询失败。
- 预期结果：旧列表、旧已生效条件、旧游标栈全部保留；失败条件不得标记为已生效。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-049 失败条件不得标记为已生效
- 前置条件：同上。
- 操作步骤：查询失败后点击上一页/下一页。
- 预期结果：分页仍使用旧已生效条件（非失败的表单条件）。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-050 编辑后未查询前分页用旧已生效条件
- 前置条件：已有一次成功查询并翻到第 2 页。
- 操作步骤：1. 修改表单但未查询；2. 点击上一页/下一页并核对请求条件。
- 预期结果：分页请求携带原已生效条件，不读取尚未生效的表单条件。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

### 6.7 领域 7：重置不查询、不清列表、不改游标

#### LQ-AC-051 重置只改当前 Tab 表单
- 前置条件：当前 Tab 已修改表单；存在已显示的校验错误（如表名超长、时间缺失/顺序错误）。
- 操作步骤：点击"重置"。
- 预期结果：源库恢复"全部"、源表名清空、目标库恢复"全部"、目标表名清空、时间恢复为点击重置时所在自然日；同时清除当前 Tab 已显示的 `validationError` 和逐字段校验错误；保持当前 Tab。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-052 重置不发起查询
- 前置条件：同上。
- 操作步骤：点击"重置"并核对网络请求。
- 预期结果：无任何列表查询请求发出。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-053 重置不清空列表
- 前置条件：当前 Tab 已有列表。
- 操作步骤：点击"重置"。
- 预期结果：当前列表保持不变，不清空。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-054 重置不改分页位置、不清游标、不改已生效
- 前置条件：当前 Tab 已翻到第 2 页。
- 操作步骤：点击"重置"后观察分页与翻页行为。
- 预期结果：分页位置不变、游标栈不变、已生效条件不变；点击上一页仍回到第 1 页。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、接口测试
- 执行状态：PASS

### 6.8 领域 8：固定排序、固定 100 条、101 条探测、无总数、无跳页

#### LQ-AC-055 固定排序与游标辅助
- 前置条件：接口测试环境。
- 操作步骤：1. 首查并核对 SQL/执行计划排序；2. 构造同一 `TARGET_TIME` 多行数据翻页。
- 预期结果：排序固定为 `TARGET_TIME DESC, CDC_LOG_ID DESC`；同一秒内通过 `CDC_LOG_ID` 保证稳定顺序；游标边界同时包含 `TARGET_TIME` 与 `CDC_LOG_ID`。
- 证据类型：接口证据、SQL / 执行计划证据
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-056 表头不可点击排序
- 前置条件：进入页面。
- 操作步骤：点击各列表头。
- 预期结果：表头不可点击、无排序箭头/排序状态，不发起按列排序请求。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-057 每页固定 100 条且无 pageSize 控件
- 前置条件：接口测试环境。
- 操作步骤：1. 查询得到超过 100 条的结果集；2. 检查页面是否有页大小控件。
- 预期结果：第一页固定返回 100 条；页面无 `pageSize` 选择控件；请求体不含 `pageSize`。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-058 不显示总记录数
- 前置条件：进入页面。
- 操作步骤：观察列表与分页区域。
- 预期结果：页面任何位置不显示"共 N 条"等总数信息；响应不包含 `total`。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、接口测试
- 执行状态：PASS

#### LQ-AC-059 分页条仅显示上一页/下一页
- 前置条件：进入页面并已有列表。
- 操作步骤：观察分页条。
- 预期结果：分页条仅显示"上一页 / 下一页"两个按钮，不显示任何页码、当前页次或数字页位置标签，无页码跳转、无数字输入、无总数、无 `pageSize` 控件。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-060 无页码跳转
- 前置条件：进入页面。
- 操作步骤：检查分页条与页面控件。
- 预期结果：不存在页码列表、数字输入、跳页按钮、"第 N 页共 M 页"导航或任何当前页次标签；除上一页/下一页外无其他翻页方式。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-061 恰好 100 条 hasNext=false
- 前置条件：接口测试环境；构造结果集恰为 100 条。
- 操作步骤：首查该结果集并核对响应。
- 预期结果：`hasNext=false`，`nextCursor` 字段省略；"下一页"按钮禁用。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-062 101 条探测 hasNext=true
- 前置条件：接口测试环境；构造结果集为 101 条。
- 操作步骤：首查该结果集并核对响应。
- 预期结果：返回前 100 条、`hasNext=true`、`nextCursor` 为第 100 条记录的 `TARGET_TIME` 与 `CDC_LOG_ID` 边界；不执行 `COUNT(*)`。
- 证据类型：接口证据、SQL / 执行计划证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

### 6.9 领域 9：请求游标栈

#### LQ-AC-063 首页游标栈为 [null]
- 前置条件：进入页面完成首查。
- 操作步骤：观察首查请求与前端状态（前端单测或接口证据）。
- 预期结果：首查请求 `cursor=null`；成功后 `requestCursorStack=[null]`；"上一页"按钮禁用。
- 证据类型：接口证据、前端单测
- 建议执行层级：接口测试、前端单测
- 执行状态：PASS

#### LQ-AC-064 第 1 页进第 2 页使用 nextCursor
- 前置条件：第一页 `hasNext=true`。
- 操作步骤：点击"下一页"，核对请求 `cursor`。
- 预期结果：请求 `cursor`=第一页响应保存的 `nextCursor`（记为 `C1`）；成功后 `requestCursorStack=[null, C1]`。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-065 连续两次下一页到达第 3 页
- 前置条件：构造 250 条以上结果集。
- 操作步骤：从第 1 页连续点击"下一页"两次，到达第 3 页。
- 预期结果：请求游标依次为 `C1`、`C2`（第 1 页进第 2 页使用第 1 页响应保存的 `C1`，第 2 页进第 3 页使用第 2 页响应保存的 `C2`）；栈依次为 `[null,C1]`、`[null,C1,C2]`；不出现 `C3`；每次返回 100 条且不重不漏（按固定排序与边界谓词）。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-066 第 3 页返回第 2 页
- 前置条件：已到第 3 页，当前 `requestCursorStack=[null,C1,C2]`。
- 操作步骤：点击"上一页"一次，核对请求 `cursor`。
- 预期结果：上一页先计算目标栈并弹出栈顶 `C2`，请求游标为新栈顶 `C1`；成功后栈替换为 `[null,C1]`；列表为第 2 页内容。不得出现"弹出 C3"。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-067 第 2 页返回第 1 页
- 前置条件：已到第 2 页。
- 操作步骤：点击"上一页"一次，核对请求 `cursor`。
- 预期结果：请求游标为弹出 `C1` 后的新栈顶 `null`；成功后栈替换为 `[null]`；列表为第 1 页内容；"上一页"按钮禁用。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-068 下一页失败不压栈
- 前置条件：已到第 1 页；可制造下一页失败。
- 操作步骤：点击"下一页"并触发失败。
- 预期结果：失败后不压栈、`requestCursorStack` 不变、停留当前页；失败前不永久压栈。
- 证据类型：接口证据、前端单测
- 建议执行层级：接口测试、前端单测
- 执行状态：PASS

#### LQ-AC-069 上一页失败不弹栈
- 前置条件：已到第 2 页；可制造上一页失败。
- 操作步骤：点击"上一页"并触发失败。
- 预期结果：失败后不弹栈、当前页与原栈保持不变；成功前不得永久弹栈。
- 证据类型：接口证据、前端单测
- 建议执行层级：接口测试、前端单测
- 执行状态：PASS

#### LQ-AC-070 查询失败保留旧栈
- 前置条件：已翻到第 2 页；制造查询失败。
- 操作步骤：修改表单点击查询并失败。
- 预期结果：`requestCursorStack` 保留 `[null,C1]`，旧列表与旧已生效条件保留。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

### 6.10 领域 10：新增/晚到日志与快照一致性

#### LQ-AC-071 翻页不重不漏
- 前置条件：接口测试环境；固定结果集。
- 操作步骤：连续翻页并汇总所有记录。
- 预期结果：在无新增/晚到数据且相同边界谓词下，翻页覆盖结果集且不重复、不遗漏（`LQ-PAGE-25` 语义以固定排序与边界谓词为准）。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-072 晚到数据可能在后续页出现
- 前置条件：接口测试环境；可构造晚到数据。
- 操作步骤：1. 翻页过程中插入一条 `TARGET_TIME` 晚到且排序位置靠前的数据；2. 继续翻页观察。
- 预期结果：晚到且排序位置落入后续页的数据可能在后续页出现；已显示列表不被新插入数据自动改写；用户需重新点击查询获取最新（`LQ-API-59`、`LQ-DESIGN-101 / 102`）。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：BLOCKED
- 阻塞说明：阻塞：需构造晚到数据（写操作），只读联调禁止造数；解除条件：提供可写测试环境或现成晚到数据后重查。

#### LQ-AC-073 不承诺跨请求快照一致性
- 前置条件：接口测试环境。
- 操作步骤：1. 两次以相同条件与相同目标页游标查询；2. 期间插入/修改数据后再次查询。
- 预期结果：重复发送目标页游标按相同固定排序与边界谓词重新查询，不承诺与首次访问该页完全一致；不得在任何 UI/接口文案中承诺跨请求快照一致性。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：BLOCKED
- 阻塞说明：阻塞：需两次查询之间插入/修改数据以验证快照语义，只读联调禁止；解除条件：可写测试环境。

### 6.11 领域 11：`CDC_LOG_ID` 字符串无损传输

#### LQ-AC-074 列表记录 ID 为字符串
- 前置条件：接口测试环境。
- 操作步骤：首查并核对响应 `items[].cdcLogId`。
- 预期结果：`cdcLogId` 为十进制字符串；前端不转为 Number 后回传；以 `cdcLogId`（字符串）作为列表行唯一 key。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-075 游标载荷与路径参数为字符串
- 前置条件：接口测试环境。
- 操作步骤：1. 翻页核对请求 `cursor`；2. 触发详情/原始消息请求核对 URL 路径参数。
- 预期结果：游标中 `CDC_LOG_ID` 为十进制字符串；`/logs/{logType}/{cdcLogId}/detail` 与 `/raw-message` 的路径参数 `cdcLogId` 为十进制字符串。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-076 超过 MAX_SAFE_INTEGER 仍无损
- 前置条件：开发库存在 `CDC_LOG_ID` 超过 JavaScript `Number.MAX_SAFE_INTEGER`（如 `7755033852453421056`）的记录或通过接口测试构造。
- 操作步骤：1. 列表查询返回该 ID；2. 打开其详情/原始消息；3. 翻页经过该 ID。
- 预期结果：该 ID 全程以字符串传输与回传，无精度丢失；前端不转 Number；后端以不丢精度数值类型（建议 BigDecimal）绑定，不使用 VARCHAR 到 NUMBER 隐式转换。
- 证据类型：接口证据、后端单测、接口自动化测试
- 建议执行层级：后端单测、接口测试
- 执行状态：PASS

### 6.12 领域 12：数据源候选、名称映射、降级、禁止 N+1

#### LQ-AC-077 每次列表请求恰好读取一次数据源全表、无 N+1
- 前置条件：接口测试环境；可观测 SQL 或日志。
- 操作步骤：1. 依次执行首查、点击查询、下一页、上一页；2. 核对数据源全表读取次数。
- 预期结果：每个列表请求恰好读取一次 `CDC_DATA_SOURCE` 全表（四列），在请求内内存映射；无按行查询数据源（无 N+1）；无分页前大表 JOIN。
- 证据类型：SQL / 执行计划证据、接口自动化测试
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-078 候选接口一次返回 source+target
- 前置条件：接口测试环境。
- 操作步骤：调用 `GET /api/log-query/data-source-options`。
- 预期结果：一次请求返回 `sourceList` 与 `targetList`；候选只含启用（`FG_ACTIVE='1'`）且类别匹配（SOURCE/TARGET）的数据源；每次请求恰好读取一次全表。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-079 名称映射四态降级显示
- 前置条件：开发库构造四类数据源字段日志：名称与 ID 均缺失 / 名称缺失但 ID 存在 / 名称存在且 ID 存在 / 数据源记录存在但 `DATA_SOURCE_ORG` 为空。
- 操作步骤：查询列表并核对源库/目标库列。
- 预期结果：名称与 ID 均缺失 → 显示 `--`；名称缺失但 ID 存在 → 单元格只显示 ID；名称存在且 ID 存在 → 单元格显示名称；记录存在但 `DATA_SOURCE_ORG` 为空 → 显示"未定义名称"；名称映射和降级只影响展示，不改变查询条件。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：集成测试、人工 UI
- 执行状态：PASS

#### LQ-AC-080 名称与 ID 均缺失显示 `--`
- 前置条件：开发库存在 `SOURCE_DATA_SOURCE_ID` / `TARGET_DATA_SOURCE_ID` 为 NULL 且无名称的记录。
- 操作步骤：查询并核对对应列与悬停。
- 预期结果：名称与 ID 均缺失时页面显示 `--` 且不显示 Tooltip；不回退为字符串 `"null"`、空串或"未定义名称"。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：集成测试、人工 UI
- 执行状态：PASS

#### LQ-AC-081 悬停显示完整名称与 ID
- 前置条件：源库/目标库列被截断；构造名称缺失但 ID 存在 / 名称存在且 ID 存在 / 记录存在但 `DATA_SOURCE_ORG` 为空三类记录。
- 操作步骤：悬停源库/目标库单元格。
- 预期结果：名称缺失但 ID 存在 → Tooltip 只显示 `数据源 ID：{ID}`；名称存在且 ID 存在 → Tooltip 显示完整名称和 `数据源 ID：{ID}`；记录存在但 `DATA_SOURCE_ORG` 为空 → Tooltip 显示"未定义名称"和 ID；名称与 ID 均缺失 → 不显示 Tooltip；不得出现 Tooltip 显示值与单元格降级值矛盾，不得出现 `ID（ID）`。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-082 停用源历史展示与候选校验分离
- 前置条件：存在历史日志引用已停用/类别变化的数据源。
- 操作步骤：1. 查询含停用源的历史日志；2. 尝试把停用源作为新候选选中。
- 预期结果：历史日志名称按降级规则正常展示（不受 `FG_ACTIVE` 限制）；停用源不在下拉候选中、不可作为新查询条件选中；若强行提交非候选 ID，后端返回 `DATA_SOURCE_IDS_INVALID`（40013）。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：集成测试、接口测试
- 执行状态：PASS

#### LQ-AC-083 名称映射不跨请求缓存
- 前置条件：可观测 SQL 或日志。
- 操作步骤：连续执行多个列表请求。
- 预期结果：每个请求独立重新读取全表并构建映射，不依赖上一次请求的映射结果、不跨请求缓存。
- 证据类型：SQL / 执行计划证据
- 建议执行层级：集成测试
- 执行状态：PASS

### 6.13 领域 13：列表 12 列、列顺序、横向滚动、固定列、单行省略和 Tooltip

#### LQ-AC-084 12 列顺序与字段来源
- 前置条件：进入页面并已有列表。
- 操作步骤：核对表头顺序与各列取值来源。
- 预期结果：顺序固定为：源库（`sourceDataSourceName`）、源表名、目标库（`targetDataSourceName`）、目标表名、指令类型（`instructionType` 原样显示）、日志摘要（`logSummary`）、偏移量（`offset` 字符串右对齐）、采集时间（`sourceTime`）、进入链路时间（`kafkaEnqueueTime`）、同步到目标表时间（`targetTime`）、日志落盘时间（`insertTime`）、操作（日志详情、原始消息）。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、接口测试
- 执行状态：PASS

#### LQ-AC-085 固定列策略
- 前置条件：列表产生横向滚动。
- 操作步骤：缩小窗口宽度触发横向滚动，观察固定列。
- 预期结果：源库、源表名固定左侧；操作列固定右侧；中间列随横向滚动。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-086 中间列横向滚动
- 前置条件：同上。
- 操作步骤：拖动横向滚动条。
- 预期结果：中间列可横向滚动，固定列保持不动；表头与表体对齐（Element Plus 固定列约束）。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-087 表头固定
- 前置条件：列表数据多到触发纵向滚动。
- 操作步骤：滚动列表数据区。
- 预期结果：表头固定，不随数据区滚动；数据区纵向滚动。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-088 单行省略
- 前置条件：单元格内容超出列宽。
- 操作步骤：观察各文本列。
- 预期结果：普通文本列单行省略显示；日志摘要在固定宽度单行省略；偏移量完整显示（字符串）。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-089 数据源名/表名悬停完整信息
- 前置条件：源库/目标库、源表名/目标表名被截断。
- 操作步骤：悬停对应单元格。
- 预期结果：源库/目标库 Tooltip 显示完整名称与 ID；源表名/目标表名 Tooltip 显示完整表名。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-090 日志摘要不通过 Tooltip 展示完整异常
- 前置条件：`logSummary` 较长。
- 操作步骤：悬停日志摘要列。
- 预期结果：日志摘要单行省略且不通过 Tooltip 展示完整异常；完整异常只能通过"日志详情"弹窗查看。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-091 列表行唯一 key 为字符串 ID
- 前置条件：前端单测或代码审查。
- 操作步骤：核对表格行 key 实现。
- 预期结果：行 key 使用 `cdcLogId`（字符串），不以索引或数值化 ID 作为 key。
- 证据类型：前端单测
- 建议执行层级：前端单测
- 执行状态：PASS

### 6.14 领域 14：四个时间字段全部显示且格式一致

#### LQ-AC-092 四时间列全部显示、不隐藏不合并
- 前置条件：进入页面并已有列表。
- 操作步骤：观察时间相关列。
- 预期结果：采集时间、进入链路时间、同步到目标表时间、日志落盘时间四列全部显示，不隐藏、不合并成一个含糊字段。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-093 时间格式一致且无毫秒时区
- 前置条件：进入页面。
- 操作步骤：观察四时间列与详情弹窗时间字段。
- 预期结果：时间格式统一为 `yyyy-MM-dd HH:mm:ss`，精确到秒，无毫秒、无时区后缀；不做 UTC 转换。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-094 时间空值显示 `--`
- 前置条件：存在某时间字段为 NULL 的记录。
- 操作步骤：查询并核对对应时间列。
- 预期结果：NULL 时间字段显示 `--`（`targetTime` 按需求视为非空恒返回，其余时间列按可空性处理）。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

### 6.15 领域 15：日志摘要与完整详情隔离

#### LQ-AC-095 列表只返回受限摘要 ≤300 字符
- 前置条件：接口测试环境；构造 `LOG_DETAIL` 超过 300 字符。
- 操作步骤：首查并核对 `logSummary` 长度与响应。
- 预期结果：`logSummary` 最大 300 字符（Unicode 码点），由服务端 `SUBSTR(LOG_DETAIL, 1, 300)` 截取；列表响应不含完整 `LOG_DETAIL`。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-096 无 LOG_DETAIL 时摘要 `--` 且按钮禁用
- 前置条件：存在 `LOG_DETAIL` 为 NULL 的记录。
- 操作步骤：查询并核对日志摘要列与操作列。
- 预期结果：日志摘要列显示 `--`；"日志详情"按钮禁用并提示"暂无日志详情"。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：BLOCKED
- 阻塞说明：阻塞：开发库无 LOG_DETAIL 为 NULL 的日志记录，无法只读验证；解除条件：存在该类记录或可写环境构造数据。

#### LQ-AC-097 hasLogDetail/hasRawMessage 存在性标记
- 前置条件：接口测试环境。
- 操作步骤：核对响应 `hasLogDetail`、`hasRawMessage`。
- 预期结果：布尔标记由服务端以 LOB 长度/空值轻量判定计算（`LENGTH(列)>0` 或等价），返回是否存在但不读取完整内容；按钮可用性与此对应。
- 证据类型：接口证据、SQL / 执行计划证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-098 详情弹窗按需加载且名称复用
- 前置条件：点击某行"日志详情"。
- 操作步骤：1. 点击"日志详情"；2. 核对请求与弹窗字段。
- 预期结果：请求 `GET /api/log-query/logs/{logType}/{cdcLogId}/detail` 按需加载；弹窗展示 CDC 日志 ID、源库名称与 ID、源表名、目标库名称与 ID、目标表名、指令类型、结果码、Kafka 偏移量、四个时间字段、完整 `LOG_DETAIL`；源库/目标库名称复用列表行已返回名称，弹窗接口不重新读取数据源表、不返回名称；详情弹窗的源库/目标库降级展示与列表四态规则一致（名称缺失但 ID 存在时只显示一次 ID，名称与 ID 均缺失时显示 `--`，不得出现 `ID（ID）`）。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：PASS

#### LQ-AC-099 摘要截取边界（服务端单测）
- 前置条件：后端单测环境。
- 操作步骤：构造 `LOG_DETAIL` 长度 299/300/301 字符的用例。
- 预期结果：截取后摘要分别为 299/300/300 字符；含多字节字符时按字符（非字节）语义截取；截取窗口内换行、空格、缩进保留。
- 证据类型：后端单测
- 建议执行层级：后端单测
- 执行状态：PASS

### 6.16 领域 16：原始消息按需加载且列表不预取

#### LQ-AC-100 列表不预取 RAW_MESSAGE
- 前置条件：接口测试环境。
- 操作步骤：首查并核对列表 SQL/响应。
- 预期结果：列表响应与 SQL 不读取 `RAW_MESSAGE`；只有点击"原始消息"才请求。
- 证据类型：接口证据、SQL / 执行计划证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-101 无原始消息时按钮禁用
- 前置条件：存在 `RAW_MESSAGE` 为 NULL 的记录。
- 操作步骤：查询并核对操作列。
- 预期结果："原始消息"按钮禁用并提示"暂无原始消息"。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-102 原始消息按需加载
- 前置条件：某行 `hasRawMessage=true`。
- 操作步骤：点击"原始消息"，核对请求与弹窗。
- 预期结果：请求 `GET /api/log-query/logs/{logType}/{cdcLogId}/raw-message`；弹窗显示原始文本；`RAW_MESSAGE` 不随日志详情自动加载。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：PASS

#### LQ-AC-103 详情不随带加载 RAW_MESSAGE
- 前置条件：接口测试环境。
- 操作步骤：打开日志详情，核对详情接口响应字段。
- 预期结果：详情接口不读取、不返回 `RAW_MESSAGE`；详情弹窗不展示原始消息。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

### 6.17 领域 17：JSON 原文/格式化切换、复制原始内容

#### LQ-AC-104 合法 JSON 提供原文/格式化切换
- 前置条件：构造 `RAW_MESSAGE` 为合法 JSON 的记录。
- 操作步骤：打开其原始消息弹窗。
- 预期结果：弹窗提供"原文 / 格式化"视图切换（强制需求）；默认显示数据库原始文本。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：BLOCKED
- 阻塞说明：阻塞：开发库无 RAW_MESSAGE 为合法 JSON 的记录，无法只读验证切换；解除条件：存在该类记录或可写环境构造数据。

#### LQ-AC-105 非 JSON 不提供切换
- 前置条件：构造 `RAW_MESSAGE` 为非 JSON（如多行异常文本）的记录。
- 操作步骤：打开其原始消息弹窗。
- 预期结果：保持原始文本展示，不提供原文/格式化切换。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：BLOCKED
- 阻塞说明：阻塞：开发库无 RAW_MESSAGE 为非 JSON 文本的记录，无法只读验证；解除条件：存在该类记录或可写环境构造数据。

#### LQ-AC-106 切换不修改不保存
- 前置条件：打开合法 JSON 原始消息弹窗。
- 操作步骤：1. 切换到格式化视图；2. 关闭再重新打开。
- 预期结果：切换只影响当前弹窗展示，不修改、不保存数据库内容；重新打开仍默认显示数据库原始文本。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：BLOCKED
- 阻塞说明：阻塞：依赖 LQ-AC-104 的合法 JSON 原始消息数据，当前不可得；解除条件：同 LQ-AC-104。

#### LQ-AC-107 复制始终复制原始内容
- 前置条件：打开合法 JSON 原始消息弹窗并切到格式化视图。
- 操作步骤：点击"复制原文"，粘贴到文本编辑器。
- 预期结果：无论当前显示视图为原文或格式化，复制结果都是数据库原始内容（原文视图内容）。
- 证据类型：人工 UI
- 建议执行层级：人工 UI
- 执行状态：BLOCKED
- 阻塞说明：阻塞：依赖 LQ-AC-104 的合法 JSON 原始消息数据，当前不可得；解除条件：同 LQ-AC-104。

#### LQ-AC-108 弹窗关闭清理内容
- 前置条件：打开原始消息或详情弹窗。
- 操作步骤：1. 关闭弹窗；2. 检查前端持有内容。
- 预期结果：关闭后前端清理持有内容（实现采用 `destroy-on-close` 并清空本地状态），不残留。
- 证据类型：前端单测、人工 UI
- 建议执行层级：前端单测
- 执行状态：PASS

### 6.18 领域 18：非 JSON、空内容、超长文本、超大 CLOB 的展示

#### LQ-AC-109 空内容提示明确
- 前置条件：构造 `RAW_MESSAGE` 为空字符串的记录。
- 操作步骤：打开其原始消息弹窗。
- 预期结果：显示明确提示"暂无原始消息"；空内容与记录不存在互不混淆（记录不存在返回 `LOG_RECORD_NOT_FOUND`，空内容返回成功且 `rawMessage=""`）。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、接口测试
- 执行状态：PASS

#### LQ-AC-110 超长文本滚动承载不截断
- 前置条件：构造超长 `RAW_MESSAGE` / `LOG_DETAIL`。
- 操作步骤：打开对应弹窗。
- 预期结果：内容完整展示，内容区域支持滚动与换行，不截断内容。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-111 换行、空格、缩进保留
- 前置条件：构造含多行、缩进、异常堆栈的详情与原始消息。
- 操作步骤：打开弹窗观察展示。
- 预期结果：换行、空格、缩进、异常堆栈原样保留；异常文本使用等宽字体。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-112 原始消息 NULL/空串/JSON/非 JSON/超大语义
- 前置条件：接口测试环境；构造五类记录。
- 操作步骤：分别请求其原始消息接口。
- 预期结果：NULL 或空串 → 成功且 `rawMessage=""`；合法 JSON → 原样返回不格式化；非 JSON → 原样返回；超大文本 → 原样返回；API 一律不修改、不格式化、不保存。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

### 6.19 领域 19：纯文本安全展示

#### LQ-AC-113 日志内容不作为 HTML 执行
- 前置条件：构造含 `<script>`、`<img onerror>` 等标签的 `LOG_DETAIL` / `RAW_MESSAGE`。
- 操作步骤：打开详情与原始消息弹窗。
- 预期结果：内容以纯文本展示，标签与脚本不被执行；代码审查确认未使用 `v-html` 渲染日志内容。
- 证据类型：人工 UI 截图、后端单测/代码审查
- 建议执行层级：人工 UI、前端单测
- 执行状态：PASS

#### LQ-AC-114 原样展示不脱敏
- 前置条件：构造含换行、缩进、堆栈的详情。
- 操作步骤：打开详情弹窗。
- 预期结果：`LOG_DETAIL`、`RAW_MESSAGE` 原样展示，不脱敏、不转换。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-115 不允许修改回写日志内容
- 前置条件：进入页面。
- 操作步骤：检查页面控件与接口调用。
- 预期结果：页面无任何修改日志内容的控件；不调用任何写接口回写日志内容。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、接口测试
- 执行状态：PASS

### 6.20 领域 20：查询加载、3 秒慢提示、动态秒数、25/30 秒超时协调

#### LQ-AC-116 查询开始立即显示加载
- 前置条件：可制造耗时查询。
- 操作步骤：点击查询，观察表格区。
- 预期结果：请求开始立即显示半透明加载遮罩、旋转图标与文案"正在查询错误日志/正确日志，请稍候"；保留原列表不先清空。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-117 动态显示已等待秒数
- 前置条件：查询耗时超过数秒。
- 操作步骤：观察加载区域文案。
- 预期结果：动态显示已等待秒数（纯文本更新）。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-118 超过 3 秒显示慢查询提示
- 前置条件：查询耗时超过 3 秒。
- 操作步骤：观察加载区域文案。
- 预期结果：超过 3 秒后追加"查询耗时较长，请耐心等待"提示。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-119 查询按钮加载状态与控件禁用
- 前置条件：查询进行中。
- 操作步骤：观察查询按钮与当前 Tab 控件。
- 预期结果：查询按钮显示加载状态；当前 Tab 的查询控件、查询、重置、分页按钮暂时禁用，防止重复提交；仍允许切换到另一 Tab。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN
- R1 状态说明：`LOG-QUERY-CURSOR-CORRECT-TAB-ADJUSTMENT-001-R1` 复审判定既有证据不足以证明普通用户查询 `loading=true` 期间的真实控件状态（既有证据主要覆盖初始化锁定），需后续真实组件/接口模拟或人工视觉验收后重判，不得伪造 `PASS`。

#### LQ-AC-120 前端请求 30 秒超时
- 前置条件：可制造超过 30 秒未返回的请求（接口测试层模拟）。
- 操作步骤：发起查询并观察。
- 预期结果：日志查询相关请求显式设置 30 秒超时（请求级覆盖全局 10 秒默认，不影响其他功能）；30 秒后结束本次加载并显示可操作错误；不允许加载图标无限旋转。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：NOT_RUN
- R1 状态说明：`LOG-QUERY-CURSOR-CORRECT-TAB-ADJUSTMENT-001-R1` 复审判定既有证据只证明请求代码静态设置 `timeout: 30000`，未实际模拟超过 30 秒未返回并验证前端结束加载、进入可操作错误态，需后续接口模拟或人工视觉验收后重判，不得伪造 `PASS`。

#### LQ-AC-121 后端语句 25 秒超时映射 QUERY_TIMEOUT
- 前置条件：集成测试环境；可制造数据库语句超时。
- 操作步骤：触发数据库语句超过 25 秒。
- 预期结果：后端数据库语句具备超时约束（25 秒，低于前端 30 秒），超时后 Service 捕获并映射 `QUERY_TIMEOUT`（50020）；保证浏览器停止等待前数据库已中止。
- 证据类型：接口证据、SQL / 执行计划证据
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-122 弹窗独立加载态
- 前置条件：点击详情/原始消息且请求未返回。
- 操作步骤：观察弹窗。
- 预期结果：弹窗内显示独立加载效果（遮罩/旋转图标），不遮挡整个列表。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

### 6.21 领域 21：失败、超时、游标失效、旧响应和跨 Tab 响应隔离

#### LQ-AC-123 失败保留旧列表旧已生效旧游标
- 前置条件：已有成功列表；制造查询失败。
- 操作步骤：触发查询失败。
- 预期结果：旧列表、旧已生效条件、旧游标栈保留；显示明确错误，不显示为空数据。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：接口测试、人工 UI
- 执行状态：PASS

#### LQ-AC-124 查询失败与空数据为不同状态
- 前置条件：分别构造查询失败与查询成功无数据。
- 操作步骤：1. 触发查询失败观察；2. 触发查询成功无数据观察。
- 预期结果：失败显示错误信息并保留旧数据；空数据显示"当前查询条件下暂无日志"且分页按钮禁用；两种状态文案与样式可区分。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-125 超时提示可操作
- 前置条件：制造前端超时。
- 操作步骤：观察超时后页面。
- 预期结果：显示明确超时/错误提示并提示用户缩小查询范围或增加条件；保留旧状态；不自动重试。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN
- R1 状态说明：`LOG-QUERY-CURSOR-CORRECT-TAB-ADJUSTMENT-001-R1` 复审判定既有证据是后端 25 秒语句超时/50020 路径或静态错误映射，不是"制造前端 30 秒超时并观察页面提示"，需后续前端超时模拟或人工视觉验收后重判，不得伪造 `PASS`。

#### LQ-AC-126 游标失效 CURSOR_INVALID
- 前置条件：接口测试环境；可构造失效游标（篡改/密钥轮换/版本不兼容）。
- 操作步骤：携带失效游标请求翻页。
- 预期结果：返回 `CURSOR_INVALID`（40015）；页面显示"查询条件已变化或游标已失效，请重新查询第一页"并提供重新查询入口。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：PASS

#### LQ-AC-127 游标条件失配 CURSOR_CONDITION_MISMATCH
- 前置条件：接口测试环境；构造与当前请求条件不一致的游标。
- 操作步骤：以不同条件携带旧游标请求翻页。
- 预期结果：返回 `CURSOR_CONDITION_MISMATCH`（40016）；页面提示重新查询。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-128 网络错误提示
- 前置条件：可制造网络错误/HTTP 层超时。
- 操作步骤：触发网络错误。
- 预期结果：显示"网络请求失败"或明确超时提示；接口异常只影响当前请求所在 Tab 或弹窗，不污染其他 Tab 与列表旧数据。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

#### LQ-AC-129 旧响应不覆盖新页面
- 前置条件：可制造慢请求。
- 操作步骤：1. 发起慢查询后立即重新查询或重新进入；2. 旧响应随后返回。
- 预期结果：旧响应（含超时/失败/成功）不得覆盖新查询或新页面状态。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-130 弹窗响应过期不重开
- 前置条件：可制造详情/原始消息慢请求。
- 操作步骤：1. 打开详情/原始消息弹窗；2. 关闭弹窗并离开页面；3. 响应随后返回。
- 预期结果：过期响应不得重新打开已关闭弹窗或写入新页面实例。
- 证据类型：接口证据、前端单测
- 建议执行层级：接口测试、前端单测
- 执行状态：PASS

### 6.22 领域 22：空数据与字段 NULL 的 `--` 规则

#### LQ-AC-131 空数据态
- 前置条件：查询成功但无数据。
- 操作步骤：观察列表区与分页。
- 预期结果：列表区显示"当前查询条件下暂无日志"；分页按钮禁用；无总数显示。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-132 字段空值统一 `--`
- 前置条件：存在各字段为 NULL 的记录。
- 操作步骤：查询并核对各空值列。
- 预期结果：普通空值统一显示 `--`（源表名、目标表名、指令类型、偏移量、时间、摘要等）。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-133 缺失字段等同 null 渲染 `--`
- 前置条件：接口按 `non_null` 省略 null 字段。
- 操作步骤：核对前端对缺失字段的渲染。
- 预期结果：前端把缺失字段等同于 null，渲染 `--`；布尔 `hasLogDetail`/`hasRawMessage`/`hasNext` 恒为真值型不受省略影响。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、接口测试
- 执行状态：NOT_RUN

#### LQ-AC-134 数据源名称降级展示
- 前置条件：构造原始 ID NULL / 名称空 / 名称有值 / 未找到记录四类日志。
- 操作步骤：查询并核对源库/目标库列、悬停 Tooltip 与详情弹窗的展示。
- 预期结果：名称与 ID 均缺失 → `--` 且无 Tooltip；名称缺失但 ID 存在 → 单元格只显示 ID、Tooltip 只显示 `数据源 ID：{ID}`、详情只显示一次 ID；名称存在且 ID 存在 → 单元格显示名称、Tooltip 显示完整名称和 `数据源 ID：{ID}`、详情展示名称与 ID；记录存在但 `DATA_SOURCE_ORG` 为空 → 显示"未定义名称"、Tooltip 同时显示 ID；列表、Tooltip 与详情三处降级展示一致，不矛盾、不重复 ID，不出现 `ID（ID）`。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS

### 6.23 领域 23：不自动刷新、不自动重试

#### LQ-AC-135 仅四类事件触发查询、无自动刷新/轮询/WebSocket
- 前置条件：进入页面后保持停留。
- 操作步骤：1. 停留数分钟观察网络请求；2. 代码审查确认无轮询/WebSocket 逻辑。
- 预期结果：列表查询触发事件严格为以下四类：页面首次进入且状态检测返回 `enabled=true` 后的错误日志默认查询；用户点击"查询"；用户点击"上一页"或"下一页"；页面重新进入且状态检测返回 `enabled=true` 后的错误日志默认查询。正确日志不自动查询，其首次列表查询必须由用户点击"查询"触发。除上述事件外，不产生任何查询请求；无自动刷新、无轮询、无 WebSocket。
- 证据类型：接口证据、前端单测/代码审查
- 建议执行层级：接口测试、前端单测
- 执行状态：NOT_RUN

#### LQ-AC-136 不自动重试
- 前置条件：制造一次失败查询。
- 操作步骤：观察失败后请求。
- 预期结果：失败或超时后不自动重试慢查询或失败查询。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-137 不持久化查询状态
- 前置条件：进入页面并操作。
- 操作步骤：检查浏览器存储与刷新行为。
- 预期结果：不使用 LocalStorage、SessionStorage 或其他持久化方式保存查询状态；刷新后状态清空（见 LQ-AC-015/017）。
- 证据类型：人工 UI（浏览器存储面板截图）
- 建议执行层级：人工 UI
- 执行状态：PASS

### 6.24 领域 24：权限与只读边界、非法 `logType`、SQL 注入防护

#### LQ-AC-138 只读接口边界
- 前置条件：接口测试环境。
- 操作步骤：核对日志查询全部接口方法。
- 预期结果：列表为 `POST /api/log-query/logs/search`（只读查询）、数据源候选/详情/原始消息为 GET；无任何写操作接口。
- 证据类型：接口证据
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-139 非法 logType 拒绝
- 前置条件：接口测试环境。
- 操作步骤：以 `logType` 为非 `error`/`correct` 的值请求列表、详情、原始消息。
- 预期结果：服务端返回 `LOG_TYPE_INVALID`（40014）；`logType` 大小写敏感，白名单仅 `error`/`correct`。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-140 SQL 注入防护
- 前置条件：接口测试环境。
- 操作步骤：1. 在表名、数据源 ID、时间、游标字段提交注入载荷；2. 核对响应与数据库状态。
- 预期结果：表名条件以绑定参数精确匹配，不使用 `UPPER`/`LOWER`/`LIKE`/通配符；`IN` 展开与时间全部绑定参数；非法输入在到达 SQL 前被拒绝；数据库状态无任何改变。
- 证据类型：接口证据、后端单测、接口自动化测试
- 建议执行层级：后端单测、接口测试
- 执行状态：PASS

#### LQ-AC-141 `${tableName}` 只来自封闭枚举
- 前置条件：后端单测/代码审查。
- 操作步骤：审查 Mapper XML 与 Service 的固定表选择逻辑。
- 预期结果：`${tableName}` 只能由 `LogTypeEnum` 产生（`CDC_LOG_ERROR`/`CDC_LOG_CORRECT`）；用户输入永远无法到达 `${}`；无客户端字符串触达表名。
- 证据类型：后端单测
- 建议执行层级：后端单测
- 执行状态：PASS

#### LQ-AC-142 非法 cdcLogId 与 40410 区分
- 前置条件：接口测试环境。
- 操作步骤：1. 提交非十进制/越界 `cdcLogId`；2. 提交格式合法但不存在的 `cdcLogId`。
- 预期结果：非法格式/越界返回 HTTP 400（参数格式错误）；格式合法但记录不存在返回 HTTP 200 + `LOG_RECORD_NOT_FOUND`（40410）；两者明确区分。
- 证据类型：接口证据、接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

### 6.25 领域 25：列表、详情、原始消息三条 SQL 路径的大字段隔离

#### LQ-AC-143 列表路径不读完整大字段
- 前置条件：SQL / 执行计划证据环境。
- 操作步骤：审查列表 SQL 与执行计划。
- 预期结果：列表只读取轻字段 + `SUBSTR(LOG_DETAIL,1,300)` 摘要 + LOB 存在性标记；不读完整 `LOG_DETAIL`、不读 `RAW_MESSAGE`、不读 `RESULT_DETAIL`；`FETCH FIRST 101 ROWS ONLY`。
- 证据类型：SQL / 执行计划证据
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-144 详情路径读完整 LOG_DETAIL 但不读 RESULT_DETAIL/RAW_MESSAGE
- 前置条件：同上。
- 操作步骤：审查详情 SQL。
- 预期结果：详情读取 `CDC_LOG_ID`、`SOURCE_DATA_SOURCE_ID`、`SOURCE_TABLE_NAME`、`TARGET_DATA_SOURCE_ID`、`TARGET_TABLE_NAME`、`INSTRUCTION_TYPE`、`RESULT_CODE`、`OFFSET`、`SOURCE_TIME`、`KAFKA_ENQUEUE_TIME`、`TARGET_TIME`、`INSERT_TIME`、完整 `LOG_DETAIL`；不读 `RESULT_DETAIL`、不读 `RAW_MESSAGE`。
- 证据类型：SQL / 执行计划证据
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-145 原始消息路径只读 RAW_MESSAGE
- 前置条件：同上。
- 操作步骤：审查原始消息 SQL。
- 预期结果：只读 `CDC_LOG_ID` 与 `RAW_MESSAGE`，不顺带读取完整日志详情或其他大字段。
- 证据类型：SQL / 执行计划证据
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-146 数据源读取与连接池边界
- 前置条件：集成测试环境。
- 操作步骤：1. 观察列表请求执行的数据源全表读取次数；2. 核对连接池/超时配置。
- 预期结果：每个列表/候选请求恰好一次读取数据源全表；无 N+1、无分页前大表 JOIN；Hikari `connection-timeout` 与池配置符合 API 设计（连接获取超时非语句执行超时）。
- 证据类型：SQL / 执行计划证据、接口证据
- 建议执行层级：集成测试
- 执行状态：PASS

### 6.26 领域 26：页面支持的桌面分辨率、页面不出现纵向滚动条、仅表格数据区滚动

#### LQ-AC-147 最小支持宽度 1366×768
- 前置条件：浏览器可切换分辨率。
- 操作步骤：在 1366×768 分辨率下进入页面并操作。
- 预期结果：页面可用；查询区按响应式布局自然换行；列表横向滚动可用；无功能遮挡。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-148 主基准 1920×1080
- 前置条件：浏览器可切换分辨率。
- 操作步骤：在 1920×1080 分辨率下进入页面。
- 预期结果：查询区宽屏优先单行；列表、分页布局完整；页面标题/Tab/查询区/分页固定，列表占剩余高度。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-149 仅表格数据区一条纵向滚动条
- 前置条件：列表数据多到触发纵向滚动。
- 操作步骤：在支持分辨率范围内检查浏览器页面滚动条与表格内部滚动条。
- 预期结果：页面整体不出现纵向滚动条；表格数据超过可见行数时仅表格数据区出现纵向滚动条；表头、查询区、分页条保持可见；浏览器页面滚动条与表格内部滚动条不会同时出现。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-150 查询区响应式布局
- 前置条件：同上。
- 操作步骤：在宽屏与窄屏之间切换窗口宽度。
- 预期结果：宽屏优先单行；宽度不足时按控件最小宽度自然换行，不机械固定为"第二行只剩时间、查询、重置"的失衡布局；按钮组与时间控件保持视觉关联。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

### 6.27 领域 27：无障碍与键盘交互

#### LQ-AC-151 键盘 Tab 可达焦点
- 前置条件：进入页面。
- 操作步骤：使用 Tab 键遍历页面控件。
- 预期结果：所有可交互控件（Tab、下拉、输入框、按钮、表格行按钮）可通过键盘到达并获得可见焦点；Element Plus 内置键盘/焦点能力未被移除或覆盖。
- 证据类型：人工 UI
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-152 颜色不作为唯一状态表达
- 前置条件：进入页面并制造加载/禁用/错误/空数据状态。
- 操作步骤：逐一观察各状态。
- 预期结果：加载（遮罩+图标+文案）、禁用（置灰+Tooltip/文案）、错误（图标+文案）、空数据（文案）均同时提供非颜色信息。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-153 弹窗焦点管理
- 前置条件：打开详情/原始消息弹窗。
- 操作步骤：1. 打开弹窗；2. 关闭弹窗。
- 预期结果：弹窗打开时焦点移入弹窗，关闭时焦点回到触发按钮；弹窗标题、内容以可读文本呈现。
- 证据类型：人工 UI
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-154 慢查询秒数纯文本
- 前置条件：查询耗时数秒。
- 操作步骤：观察慢提示。
- 预期结果：已等待秒数为纯文本动态更新，供所有用户可见，不依赖声音或闪烁。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

### 6.28 领域 28：菜单始终显示、物理设计延期和正式启用前置条件

#### LQ-AC-155 菜单始终显示且为单一入口
- 前置条件：功能代码已实现并可在开发库运行。
- 操作步骤：登录平台检查左侧菜单并反复点击"日志查询"。
- 预期结果：左侧菜单始终显示"日志查询"入口、始终可点击，且为唯一入口；不通过隐藏菜单控制功能开放；生产环境是否进入查询流程由 `CDC_LOG_QUERY_ENABLED` 决定，在最终分区、索引与生产等价性能验证完成并批准前生产环境该配置必须保持 `false`。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：PASS
- 说明：本用例是持续约束，贯穿整个开发与验收阶段；功能是否开放不通过菜单可见性表达，而由后端配置 `cdc.log-query.enabled`（默认 `false`）唯一权威控制；不得删除未来菜单定义（名称、分组、图标、目标路由），不得新增第二个日志菜单，不得修改其他菜单或路由语义。

#### LQ-AC-156 代码与当前设计不绑定最终 RANGE 粒度
- 前置条件：代码开发阶段；代码审查与设计核对。
- 操作步骤：核对当前业务代码与当前设计文档是否硬编码具体一级 RANGE 分区粒度或引用具体分区名称；核对 API、DTO 与业务查询语义是否依赖特定物理分区形态。
- 预期结果：`TARGET_TIME` 作为第一层 `RANGE` 分区键是已批准边界；当前代码不硬编码按月、按天、按小时或不等长边界；当前代码不引用具体分区名称；后续物理方案调整不需要改变 API、DTO 和业务查询语义。
- 证据类型：后端单测/代码审查、文档核对
- 建议执行层级：后端单测
- 执行状态：PASS

#### LQ-AC-157 当前实现未提前绑定子分区、索引或生产 DDL
- 前置条件：代码开发阶段；同上。
- 操作步骤：核对当前业务代码、Mapper 与功能文档是否依赖最终子分区方案、硬编码最终索引名称/数量/列序/本地或全局属性，或已执行生产 DDL。
- 预期结果：当前代码不依赖最终子分区方案；当前代码不硬编码最终索引名称、数量、列序、本地/全局属性；当前功能文档不把候选索引或候选分区写成已批准方案；当前任务未执行生产 DDL。
- 证据类型：后端单测/代码审查、文档核对
- 建议执行层级：后端单测
- 执行状态：PASS

#### LQ-AC-158 物理调整保持完全离线能力边界
- 前置条件：代码开发阶段；系统与实施方案设计核对。
- 操作步骤：核对当前系统与实施方案是否具备在完全离线窗口内调整物理结构的能力（本用例只验证能力边界，不实际执行 DDL）。
- 预期结果：可以停止 `sync-log`，从而关闭日志表写入；可以同期关闭日志查询读取；当前业务代码不依赖在线 DDL；实际生产 DDL 的审批与执行由新增的延期用例验收（`LQ-AC-172`）。
- 证据类型：代码审查/文档核对
- 建议执行层级：集成测试
- 执行状态：BLOCKED
- 阻塞说明：阻塞：需在受控环境停止 sync-log 与日志查询读取以验证离线能力边界，只读开发验收无法执行；解除条件：生产启用前具备运维访问的受控窗口。

#### LQ-AC-171 最终物理设计完成并批准
- 前置条件：约 40 个源数据源首次全量完成，取得两张日志表各自真实记录量、`TARGET_TIME` 分布、字段选择性和主要访问路径证据。
- 操作步骤：审阅最终物理设计文档及批准记录。
- 预期结果：两张表分别明确并经用户/项目审批确认：一级 RANGE 分区粒度和边界、是否使用子分区及具体方案、最终索引名称/数量/列序/本地或全局形态、实施与回退方案；不得用开发库分布代替生产事实。
- 证据类型：最终物理设计文档、分布分析报告、批准记录
- 建议执行层级：性能验证/人工审批
- 执行状态：DEFERRED_UNTIL_PHYSICAL_DESIGN

#### LQ-AC-172 生产 DDL 在完全离线窗口执行并验证
- 前置条件：`LQ-AC-171` 已 PASS；可完全停止日志读写；生产 DDL 与回退方案已批准。
- 操作步骤：停止 `sync-log` 和日志查询读取，确认无并发读写，执行已批准 DDL，完成结构、对象、数据完整性与回退能力检查。
- 预期结果：DDL 只按批准方案执行；执行期间日志表无并发读写；执行后分区、子分区、索引与批准设计一致，数据完整，日志写入和查询具备恢复条件；未擅自执行未批准 DDL。
- 证据类型：变更审批、停写停读证据、DDL 执行日志、对象核对与数据完整性报告
- 建议执行层级：生产变更验证
- 执行状态：DEFERRED_UNTIL_PHYSICAL_DESIGN

#### LQ-AC-173 生产启用（将 CDC_LOG_QUERY_ENABLED 置为 true）前阻断条件全部通过
- 前置条件：`LQ-AC-171`、`LQ-AC-172`、`LQ-AC-164`、`LQ-AC-165` 均已 PASS，且不存在未关闭的阻断性性能或正确性问题。
- 操作步骤：核对上述用例证据、批准记录及生产启用审批。
- 预期结果：只有全部阻断条件通过并获得明确批准后，才允许在相应环境将 `CDC_LOG_QUERY_ENABLED` 配置为 `true`；任一用例未通过、BLOCKED 或仍为延期状态时，生产环境配置必须保持 `false`。菜单始终显示，不受此阻断影响。
- 证据类型：验收汇总、性能报告、批准记录
- 建议执行层级：人工审批/发布验收
- 执行状态：DEFERRED_UNTIL_PHYSICAL_DESIGN

### 6.29 领域 29：开发库功能验收与生产等价性能验收的明确分层

#### LQ-AC-159 SQL 形态检查：必带 TARGET_TIME、无 OFFSET、无 COUNT、无大表 JOIN
- 前置条件：代码开发阶段。
- 操作步骤：审查列表 SQL 形态。
- 预期结果：所有列表查询必带 `TARGET_TIME` 半开区间谓词；无 `OFFSET`、无 `COUNT(*)`、无日志大表与数据源表大范围 JOIN、无无上限结果集；`FETCH FIRST 101 ROWS ONLY`。
- 证据类型：SQL / 执行计划证据
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-160 固定 101 条读取上限
- 前置条件：代码开发阶段。
- 操作步骤：审查列表 SQL 与翻页逻辑。
- 预期结果：每次列表请求读取上限固定为 101 条（100+1 探测），取 101 则 `hasNext=true`，只返回 100 条。
- 证据类型：SQL / 执行计划证据
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-161 列表不预取完整大字段
- 前置条件：代码开发阶段。
- 操作步骤：审查列表 SQL。
- 预期结果：列表不预取完整 `LOG_DETAIL`、`RAW_MESSAGE`、`RESULT_DETAIL`；仅摘要与存在性标记。
- 证据类型：SQL / 执行计划证据
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-162 超时配置、取消与失败恢复
- 前置条件：代码开发阶段。
- 操作步骤：核对前端 30 秒请求级超时、后端 25 秒语句超时、请求取消/失效与失败保留旧状态。
- 预期结果：前端请求级 30 秒超时不改全局默认；后端语句 25 秒超时映射 `QUERY_TIMEOUT`；旧请求尽量取消、无法取消则标记失效；失败保留旧表单/旧已生效/旧列表/旧游标。
- 证据类型：接口证据、前端单测、后端单测
- 建议执行层级：前端单测、后端单测、接口测试
- 执行状态：PASS

#### LQ-AC-163 开发库功能验证
- 前置条件：开发库可访问；页面可经路由访问。
- 操作步骤：在开发库执行领域 1~27 的全部功能用例。
- 预期结果：功能行为符合各用例预期；开发库验证只证明功能正确性，不得据此外推生产性能（开发库数据不能代表生产值域、分布与选择性）。
- 证据类型：人工 UI 截图、接口证据、集成测试报告
- 建议执行层级：集成测试、人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-164 首页响应时间 ≤3 秒（生产等价）
- 前置条件：最终一级 RANGE 分区、子分区、最终索引、生产 DDL 已确定并执行；生产等价数据规模、值域、分布与并发条件就绪；生产启用前置校验。
- 操作步骤：在生产等价环境执行常用查询第一页，统计响应时间。
- 预期结果：第一页目标响应时间不超过 3 秒（`LQ-PERF-09`）；未满足时按 `LQ-PERF-12` 停止并报告 SQL、执行计划、扫描量、索引或分区缺口，不得擅自删条件或改指标。
- 证据类型：性能验证报告、SQL / 执行计划证据
- 建议执行层级：性能验证
- 执行状态：DEFERRED_UNTIL_PHYSICAL_DESIGN

#### LQ-AC-165 下一页响应时间 ≤2 秒（生产等价）
- 前置条件：同 LQ-AC-164。
- 操作步骤：在生产等价环境执行下一页，统计响应时间。
- 预期结果：下一页目标响应时间不超过 2 秒（`LQ-PERF-10`）；不满足时停止并报告缺口。
- 证据类型：性能验证报告、SQL / 执行计划证据
- 建议执行层级：性能验证
- 执行状态：DEFERRED_UNTIL_PHYSICAL_DESIGN

### 6.30 领域 30：回归范围与构建、测试、截图、接口证据要求

#### LQ-AC-166 后端构建通过
- 前置条件：代码开发阶段。
- 操作步骤：执行 `mvn clean test` 与 `mvn clean package`。
- 预期结果：构建与测试通过，报告测试数量与结果；当前功能导致的失败已修复；本功能不得引入无关失败。
- 证据类型：构建日志
- 建议执行层级：集成测试
- 执行状态：PASS

#### LQ-AC-167 前端构建通过
- 前置条件：代码开发阶段。
- 操作步骤：执行 `npm run build`（及项目已有 type-check/lint/test 脚本如适用）。
- 预期结果：前端构建通过，无类型/构建错误。
- 证据类型：构建日志
- 建议执行层级：前端单测
- 执行状态：PASS

#### LQ-AC-168 接口自动化测试证据
- 前置条件：接口测试环境。
- 操作步骤：运行列表/候选/详情/原始消息接口自动化用例。
- 预期结果：覆盖校验、游标、`CDC_LOG_ID`、错误码、大字段隔离、时间规则等关键接口用例通过，证据可复核。
- 证据类型：接口自动化测试
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-169 人工 UI 验收证据
- 前置条件：页面可经路由访问。
- 操作步骤：按领域 1~27 人工走查并截图。
- 预期结果：关键状态（布局、Tab、查询区、列表固定列、加载/慢提示、空数据/错误态、两个弹窗、分页条）均留有人工 UI 截图证据。
- 证据类型：人工 UI 截图
- 建议执行层级：人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-170 回归范围不受影响
- 前置条件：代码开发阶段。
- 操作步骤：1. 执行后端全量测试与前端构建；2. 抽查既有数据源、大屏、任务失败等既有功能。
- 预期结果：日志查询改造不破坏既有功能；全局 HTTP 默认超时、全局样式与公共组件未被改动；"日志查询"菜单始终显示且为单一入口，无其他菜单/路由回归；回归证据（构建日志、抽查截图）可复核。
- 证据类型：构建日志、人工 UI 截图
- 建议执行层级：集成测试
- 执行状态：PASS

### 6.31 领域 31：功能开放、状态接口与页面状态流程

> 本领域新增于 `LOG-QUERY-BASELINE-ADJUSTMENT-001`，覆盖功能开放控制、状态接口 `GET /api/log-query/status` 与未开放/检测失败页面状态（对应 REQUIREMENTS §3.1 `LQ-OPEN-01~13`、API.md `LQ-API-114~119`、DESIGN.md §12.1 `LQ-DESIGN-125~129`、UI.md §2.4 `LQ-UI-006~009 / 019`）。默认关闭取 `fail-closed`；`enabled` 只控制前端页面使用流程，不是认证、鉴权或接口安全控制。

#### LQ-AC-174 状态接口默认关闭且不读数据库
- 前置条件：接口测试环境；后端配置未显式设置 `CDC_LOG_QUERY_ENABLED`。
- 操作步骤：调用 `GET /api/log-query/status`，核对响应与后端日志/SQL。
- 预期结果：响应 `code=200`、`data.enabled=false`（默认 `false`，fail-closed）；状态接口不读取数据库、不操作数据库或 ZooKeeper，只返回当前后端配置解析后的 `enabled`。
- 证据类型：接口证据、代码审查
- 建议执行层级：接口测试、后端单测
- 执行状态：PASS

#### LQ-AC-175 状态接口契约与超时
- 前置条件：接口测试环境。
- 操作步骤：核对状态接口方法、路径、响应结构、超时与错误码。
- 预期结果：方法 GET、路径 `/api/log-query/status`、无请求参数；使用项目既有 `ApiResponse<T>`，`data` 为 `{ "enabled": boolean }`；前端请求级超时 30 秒且不自动重试；不新增"功能未开放"业务错误码（不新增 40310 或其他功能关闭错误码）。
- 证据类型：接口证据、前端单测
- 建议执行层级：接口测试、前端单测
- 执行状态：PASS

#### LQ-AC-176 enabled=false 显示功能未开放页且不调用原四接口
- 前置条件：`CDC_LOG_QUERY_ENABLED=false`（默认）。
- 操作步骤：进入/刷新页面，观察页面内容与网络请求。
- 预期结果：页面显示功能未开放页，标题"日志查询功能暂未开放"、说明"当前环境尚未启用日志查询功能。如需使用，请联系系统管理员。"；不加载数据源选项、不查询任何日志；不调用 `data-source-options`、`logs/search`、`detail`、`raw-message` 任一接口；未开放状态下不显示双 Tab、查询区、列表、分页和详情/原始消息按钮。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、接口测试
- 执行状态：PASS

#### LQ-AC-177 enabled=true 正常初始化并默认查询错误日志
- 前置条件：`CDC_LOG_QUERY_ENABLED=true`。
- 操作步骤：进入/刷新页面，观察初始化顺序与网络请求。
- 预期结果：先调用状态接口返回 `enabled=true`；随后加载数据源选项，并按既有规则执行错误日志默认查询（按动作发生时当前自然日）；正确日志第一次切换时只初始化缺省条件不自动查询，等待用户点击"查询"。
- 证据类型：接口证据、人工 UI 截图
- 建议执行层级：接口测试、人工 UI
- 执行状态：NOT_RUN

#### LQ-AC-178 状态接口失败/超时显示独立错误页且无"重新检测"按钮
- 前置条件：可制造状态接口失败或超时场景。
- 操作步骤：1. 使状态接口失败或超时，进入页面观察；2. 观察是否自动重试/轮询/刷新；3. 检查页面是否提供"重新检测"按钮。
- 预期结果：显示"功能状态获取失败"独立错误页，标题"功能状态获取失败"、说明"暂时无法获取日志查询功能状态，请刷新页面或稍后重新进入。"；错误页不提供"重新检测"按钮；不自动重试、不自动轮询、不自动刷新；用户只能通过刷新页面、离开后重新进入、再次点击当前"日志查询"菜单重新发起状态检测。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、前端单测
- 执行状态：PASS

#### LQ-AC-179 状态失败后仅可通过刷新/重新进入/再次点击当前菜单重新检测状态
- 前置条件：状态接口失败后已显示状态失败页。
- 操作步骤：1. 使状态接口恢复并返回 `enabled=true`，分别通过刷新页面、离开后重新进入、再次点击当前"日志查询"菜单重新进入；2. 再次制造 `enabled=false` 场景后以上述任一方式重新进入。
- 预期结果：状态重新检测返回 `true` 后进入正常初始化流程（加载数据源选项并按规则默认查询错误日志）；返回 `false` 后继续显示功能未开放页；页面内不存在任何"重新检测"按钮或自动重试入口。
- 证据类型：人工 UI 截图、接口证据
- 建议执行层级：人工 UI、前端单测
- 执行状态：PASS

#### LQ-AC-180 enabled=false 时直接调用原四接口仍按契约工作
- 前置条件：`CDC_LOG_QUERY_ENABLED=false`；接口测试环境。
- 操作步骤：直接调用 `GET /api/log-query/data-source-options`、`POST /api/log-query/logs/search`、`GET /api/log-query/logs/{logType}/{cdcLogId}/detail`、`GET /api/log-query/logs/{logType}/{cdcLogId}/raw-message`。
- 预期结果：原四接口完全不检查 `enabled`，不拦截、不返回 403、不新增业务错误码、不增加数据库访问保护；`enabled=false` 只约束前端不主动调用，直接调用仍按原契约返回。
- 证据类型：接口证据、代码审查
- 建议执行层级：接口测试
- 执行状态：PASS

#### LQ-AC-181 进入/刷新/返回/再次点击当前菜单先调用状态接口并完整重新初始化
- 前置条件：`enabled=true`；两 Tab 均有表单、列表、游标与在途请求；弹窗已打开。
- 操作步骤：分别以四种方式进入：进入页面、浏览器刷新、离开后从其他菜单返回、再次点击左侧"日志查询"菜单（即使当前已在该路由）。
- 预期结果：四种方式均先调用状态接口；立即作废两个 Tab 在途的列表、详情、原始消息和状态请求，关闭并清理弹窗内容，清空两 Tab 表单条件、已生效条件、列表、游标历史、错误、加载、等待秒数和 `initialQueryAttempted`；再次点击当前菜单不依赖组件必然卸载重挂载，必须通过可靠触发机制完成重新初始化；`enabled=true` 时恢复默认错误日志 Tab 并按动作发生时当前自然日执行默认查询；`enabled=false` 时显示功能未开放页；不使用 LocalStorage/SessionStorage 持久化状态。
- 证据类型：人工 UI 截图、接口证据、前端单测
- 建议执行层级：人工 UI、接口测试、前端单测
- 执行状态：PASS

#### LQ-AC-182 前端自动化测试覆盖要求
- 前置条件：实现阶段引入前端测试框架（推荐 Vitest / Vue Test Utils / jsdom，由实现 Agent 依据 Vue 3、Vite 5、TypeScript 与 Node 环境兼容性决定；本用例不要求下载依赖）。
- 操作步骤：执行前端自动化测试并核对覆盖项。
- 预期结果：至少覆盖 DESIGN §13.1 `LQ-DESIGN-170~182` 列出的场景，包括："全部"与具体数据源双向互斥；"全部"状态下请求不携带具体 ID（后端无相应 `IN` 条件由后端测试覆盖）；重置清除校验错误但保留列表、已生效条件和游标；两 Tab 状态独立；三页游标序列及前后翻页失败原子性；再次点击当前菜单清空状态并重新初始化；`enabled=false` 显示未开放页且前端不调用原四接口；`enabled=true` 后按顺序加载选项并默认查询错误日志；状态接口失败后显示状态失败页（无"重新检测"按钮）且无自动重试；旧请求与旧弹窗响应失效；数据源降级展示不矛盾、不重复 ID；`cdcLogId` 始终以字符串处理；原始消息保持纯文本安全，不使用 `v-html`。
- 证据类型：前端单测
- 建议执行层级：前端单测
- 执行状态：PASS

## 7. 上游规则到验收用例的追踪矩阵

> 允许按连续规则区间归组；不采用"全部覆盖"代替可核查映射。每条上游规则区间至少映射到一个验收用例区间。

| 上游规则区间 | 验收用例 | 覆盖说明 |
|---|---|---|
| LQ-SCOPE-01~05 | LQ-AC-001~003 | 单菜单、路由、双 Tab 顺序、默认 Tab、首次查询 |
| LQ-SCOPE-06~09 | LQ-AC-005~006 | 数据源表映射、共用规范、只读、无总数 |
| LQ-SCOPE-10~11 | LQ-AC-004, 004A~004D, 012 | 共用规范、正确日志首次切换只初始化不自动查询 |
| LQ-SCOPE-12~15 | LQ-AC-155, 163~165, 173, 032 | 规模、开发库性能边界、生产启用前置（`CDC_LOG_QUERY_ENABLED=true`）、时间必填默认 |
| LQ-OPEN-01~13 | LQ-AC-174~181 | 功能开放控制、状态接口、未开放/检测失败页面状态与重新初始化 |
| LQ-FILTER-01~04 | LQ-AC-021, 150 | 查询区顺序与响应式布局 |
| LQ-FILTER-10~18 | LQ-AC-022~028 | 源库多选与"全部" |
| LQ-FILTER-20~24 | LQ-AC-029~031 | 源表名输入 |
| LQ-FILTER-30~38 | LQ-AC-022~028 | 目标库多选与"全部" |
| LQ-FILTER-40~44 | LQ-AC-029~031 | 目标表名输入 |
| LQ-FILTER-50~60 | LQ-AC-032~034, 041~045, 092~093 | 时间控件、默认、校验、半开区间 |
| LQ-FILTER-61~68 | LQ-AC-036~040, 044 | 自然日时点、正确日志首次切换只填充缺省时间不查询、不自动推进、后端独立校验 |
| LQ-FILTER-70~75 | LQ-AC-050, 073, 159 | 条件组合与仅时间范围首查 |
| LQ-DATA-01~06 | LQ-AC-077~078, 083, 146 | 数据源单次读取、候选、N+1 禁止 |
| LQ-DATA-07~10 | LQ-AC-079~081, 134 | 名称映射三态与悬停 |
| LQ-DATA-11~12 | LQ-AC-083, 098 | 不跨请求缓存、详情名称复用 |
| LQ-TAB-01~08 | LQ-AC-011, 013, 020 | 两 Tab 独立状态 |
| LQ-TAB-10~12 | LQ-AC-046, 050 | 表单/已生效分离 |
| LQ-TAB-20~24 | LQ-AC-003~004, 004A~004D, 012, 020 | 首次进入与切换（正确日志首次切换只初始化不自动查询） |
| LQ-TAB-30~35 | LQ-AC-047~049 | 查询原子状态 |
| LQ-TAB-40~41 | LQ-AC-051~054 | 重置 |
| LQ-TAB-50~57 | LQ-AC-015~019, 181 | 重新进入、响应失效与再次点击当前菜单可靠重新初始化 |
| LQ-LIST-01~14 | LQ-AC-084, 074 | 12 列顺序与字段来源 |
| LQ-LIST-20~27 | LQ-AC-085~090, 149 | 固定列、滚动、省略、Tooltip |
| LQ-PAGE-01~10 | LQ-AC-057~060, 063~067 | 分页与游标栈 |
| LQ-PAGE-20~26 | LQ-AC-055~056, 071 | 固定排序不重不漏 |
| LQ-PAGE-27~28 | LQ-AC-074~076 | CDC_LOG_ID 字符串 |
| LQ-DETAIL-01~13 | LQ-AC-095~097, 100~101 | 摘要与按钮禁用 |
| LQ-DETAIL-20~28 | LQ-AC-098, 102~103, 099 | 详情弹窗 |
| LQ-DETAIL-30~41 | LQ-AC-100, 102, 104~112 | 原始消息弹窗 |
| LQ-DETAIL-50~53 | LQ-AC-113~115 | 内容安全 |
| LQ-LOAD-01~02 | LQ-AC-135 | 仅四类事件触发查询（正确日志不自动查询） |
| LQ-LOAD-10~22 | LQ-AC-116~119, 122 | 加载态与 Tab 独立 |
| LQ-LOAD-30~39 | LQ-AC-120~121, 123~130 | 超时与失败恢复 |
| LQ-VALID-01~07 | LQ-AC-131~133, 044, 137, 140 | 空数据、校验、内存态 |
| LQ-TIME-01~04 | LQ-AC-093, 045 | 时间语义 |
| LQ-SEC-01~04 | LQ-AC-113~115 | 安全 |
| LQ-PERF-01~08 | LQ-AC-057~062, 159~162 | 性能形态与 SQL 约束 |
| LQ-PERF-09~13 | LQ-AC-164, 165, 173 | 性能目标与不达标处理、生产启用阻断 |
| LQ-PERF-14~15 | LQ-AC-163, 155 | 开发库性能边界与规模 |
| LQ-DB-01~05 | LQ-AC-156~157, 159, 171 | 物理设计边界与最终方案批准 |
| LQ-DB-06~09 | LQ-AC-156~157, 171~172 | Oracle 19c+、分区与索引延期与落地 |
| LQ-DB-10~12 | LQ-AC-156, 158, 171~172 | 离线窗口、分布分析与 DDL 执行 |
| LQ-DB-13~14 | LQ-AC-155, 173 | 访问路径、菜单始终显示与生产启用前置 |
| LQ-NONGOAL-01~20 | LQ-AC-007, 135~137, 056, 060, 113~115 | 非目标反向校验 |
| LQ-API-01~10 | LQ-AC-138, 005, 057 | 契约与只读 |
| LQ-API-11~15 | LQ-AC-001~003, 023, 098, 102 | 接口清单与 POST 语义 |
| LQ-API-114~119 | LQ-AC-174~181 | 状态接口契约、不读数据库、原四接口不受开关控制 |
| LQ-API-20~27 | LQ-AC-023~024, 077~078 | 候选接口与全表读取 |
| LQ-API-30~40 | LQ-AC-031, 034, 044, 140, 142 | 请求校验 |
| LQ-API-41~48 | LQ-AC-057~062, 095, 097, 099 | 返回约束与摘要/数字字段 |
| LQ-API-50~60 | LQ-AC-063~073 | 游标协议与快照边界 |
| LQ-API-61~66 | LQ-AC-077~083, 134 | 名称映射与降级 |
| LQ-API-70~79 | LQ-AC-098, 102, 104~112 | 详情与原始消息 |
| LQ-API-80~99 | LQ-AC-123~130, 139~142, 120~121 | 错误码、超时、CDC_LOG_ID 绑定 |
| LQ-API-110~113 | LQ-AC-063~070 | 游标栈失败原子性 |
| LQ-DESIGN-01~22 | LQ-AC-138~141 | 分层、无会话、安全映射 |
| LQ-DESIGN-30~38 | LQ-AC-044, 077, 159~162 | 列表流程 |
| LQ-DESIGN-40~49 | LQ-AC-057~062, 077~078, 160 | 逻辑 SQL 约束 |
| LQ-DESIGN-50~56 | LQ-AC-143~145, 076 | 三类查询隔离与数值绑定 |
| LQ-DESIGN-60~63 | LQ-AC-126~127 | 游标生成与验证 |
| LQ-DESIGN-64~69 | LQ-AC-063~070, 073 | 游标栈模型 |
| LQ-DESIGN-70~73 | LQ-AC-047~048, 051~054, 015~019, 181 | 状态替换、失效与重新进入 |
| LQ-DESIGN-80~83 | LQ-AC-079~083, 134 | 名称映射四态降级 |
| LQ-DESIGN-125~129 | LQ-AC-174~182 | 状态接口、功能开放配置、启动状态流程、可靠重新进入与前端测试策略 |
| LQ-DESIGN-170~182 | LQ-AC-182 | 前端自动化测试覆盖要求 |
| LQ-DESIGN-90~95 | LQ-AC-095, 143~145 | 大字段隔离 |
| LQ-DESIGN-100~104 | LQ-AC-071~073 | 并发与一致性 |
| LQ-DESIGN-110~115 | LQ-AC-116~130 | 超时、取消与失败恢复 |
| LQ-DESIGN-120~124 | LQ-AC-155~158, 171~173 | 物理解耦边界检查与物理方案落地 |
| LQ-DESIGN-130~149 | LQ-AC-062, 071, 076, 082, 143~146, 148 | 测试要点覆盖 |
| LQ-DESIGN-150~161 | LQ-AC-099, 120~121, 142 | 已确认设计决策 |
| LQ-UI-030~038 | LQ-AC-003~004, 011~014, 020 | Tab 结构与独立状态 |
| LQ-UI-040~068 | LQ-AC-021~045 | 查询区、下拉、时间、校验 |
| LQ-UI-070~076 | LQ-AC-046~054 | 查询与重置 |
| LQ-UI-080~094 | LQ-AC-055, 084~091 | 列表列、固定列、滚动 |
| LQ-UI-100~105 | LQ-AC-095~097, 113~115 | 展示边界与安全 |
| LQ-UI-110~126 | LQ-AC-098, 102, 104~112, 130 | 两个弹窗 |
| LQ-UI-130~136 | LQ-AC-057~073 | 游标分页 |
| LQ-UI-006~009, 019 | LQ-AC-174~181 | 功能开放控制、状态检测流程与未开放/检测失败页面状态 |
| LQ-UI-127~128 | LQ-AC-182 | 前端测试框架授权与自动化测试覆盖 |
| LQ-UI-140~147 | LQ-AC-011~019, 181 | 交互状态机、失效与再次点击当前菜单可靠重新初始化 |
| LQ-UI-160~168 | LQ-AC-116~122 | 加载、慢提示、超时 |
| LQ-UI-180~187 | LQ-AC-123~134 | 空数据、空值、异常态 |
| LQ-UI-219 | LQ-AC-174~181 | 页面级状态模型（statusLoading/enabled/statusError）与状态请求失效 |
| LQ-UI-190~194 | LQ-AC-147~150 | 布局与滚动 |
| LQ-UI-200~206 | LQ-AC-151~154 | 无障碍与键盘 |
| LQ-UI-210~219 | LQ-AC-011~019, 174~181 | 组件与页面级状态模型、状态检测流程与状态请求失效 |
| LQ-UI-220~221 | 全文 | 追踪关系与冲突处理 |

## 8. 性能验收分层

> 性能验收必须与物理设计延期保持严格边界：开发阶段可执行的形态检查与开发库功能验证 ≠ 生产性能结论。生产启用（将 `CDC_LOG_QUERY_ENABLED` 置为 `true`）以 B 层全部通过为前提（`LQ-DB-14`、`LQ-PERF-09~13`）。

### A. 代码开发阶段可以执行

| 用例 | 验收内容 | 结论边界 |
|---|---|---|
| LQ-AC-156 | 代码与当前设计不绑定最终 RANGE 粒度 | 只验证代码/设计边界，不代表最终物理设计已完成 |
| LQ-AC-157 | 当前实现未提前绑定子分区、索引或生产 DDL | 只验证代码/文档/任务边界，不代表最终物理设计已完成 |
| LQ-AC-158 | 物理调整保持完全离线能力边界 | 只验证能力边界，不实际执行 DDL |
| LQ-AC-159 | SQL 形态检查：必带 `TARGET_TIME`、无 `OFFSET`、无 `COUNT(*)`、无日志大表与数据源表大范围 JOIN | 只证明 SQL 形态正确，不构成性能结论 |
| LQ-AC-160 | 固定 101 条读取上限 | 同上 |
| LQ-AC-161 | 列表不预取完整大字段 | 同上 |
| LQ-AC-162 | 超时配置、取消和失败恢复 | 验证配置与行为 |
| LQ-AC-163 | 开发库接口响应与功能正确性 | 不以开发库当前查询速度推断生产性能（`LQ-PERF-14`） |
| LQ-AC-077 / 146 | 数据源单次读取、无 N+1、无大表 JOIN | 可结合可获得的执行计划或 SQL 证据记录风险，不外推为生产性能结论 |

> `LQ-AC-156~158` 属于代码开发阶段即可执行的"物理解耦边界检查"，只验证当前代码与当前文档未提前绑定最终分区、子分区、索引或生产 DDL；不代表最终物理设计已完成。

### B. 生产启用前必须执行（受最终物理设计延期约束）

| 用例 | 验收内容 | 前置条件 |
|---|---|---|
| LQ-AC-171 | 最终物理设计完成并批准 | 约 40 个源数据源首次全量完成，取得两张日志表各自真实记录量、`TARGET_TIME` 分布、字段选择性和主要访问路径证据 |
| LQ-AC-172 | 生产 DDL 在完全离线窗口执行并验证 | `LQ-AC-171` 已 PASS；可完全停止日志读写；生产 DDL 与回退方案已批准 |
| LQ-AC-164 | 首页 ≤3 秒 | 生产等价数据规模、值域、分布与并发条件 |
| LQ-AC-165 | 下一页 ≤2 秒 | 同上 |
| LQ-AC-173 | 生产启用（`CDC_LOG_QUERY_ENABLED=true`）前阻断条件全部通过 | `LQ-AC-171 / 172 / 164 / 165` 均已 PASS，且不存在未关闭的阻断性性能或正确性问题 |

> 正确的延期用例集合精确为 `LQ-AC-164 / 165 / 171 / 172 / 173`，共 5 个。`LQ-AC-155` 为贯穿开发期的菜单始终显示持续约束，状态 `NOT_RUN`，可在 B 层表旁说明，但不得算作延期用例。

- B 层受物理设计延期约束的用例（`LQ-AC-171 / 172 / 164 / 165 / 173`）初始状态一律为 `DEFERRED_UNTIL_PHYSICAL_DESIGN`，不得写成通过或失败；`LQ-AC-155` 为当前即可持续检查的菜单始终显示约束，初始状态保持 `NOT_RUN`。
- 若 B 层性能不达标，按 `LQ-PERF-12` 停止并报告 SQL、执行计划、扫描量、索引或分区缺口；不允许为通过验收而删除查询条件、缩小需求范围或静默修改指标（`LQ-PERF-13`）。

## 9. 待用户确认项

待用户确认项：无（产品性待确认项）。

> `LOG-QUERY-BASELINE-ADJUSTMENT-001` 及 `-R1` 完整调整内容已经用户批准收口，本文档状态为 `APPROVED`，不属于"产品性待确认项"。
> 本版已删除原 `LQ-UI-P-01` 待确认项（"当前页次"与"不显示页码"的取舍）。分页条按已批准需求 `LQ-PAGE-05` 严格不显示任何页码/页次，仅保留"上一页 / 下一页"两个按钮；其余验收细节均由已批准上游规则、`UI.md` 与仓库既有规范唯一确定。

## 10. 一致性自检清单

提交前已逐项核对：

1. 页面字段名称、顺序与 API 字段一致（`LQ-AC-084` ↔ `API.md` §6.3）。
2. UI 行为与需求状态机一致（领域 6/7/8/9 ↔ `UI.md` §15 状态转换表、`LQ-TAB-*`）。
3. 分页按钮与请求游标栈算法一致（`LQ-AC-063~070` ↔ `API.md` §7.1、`DESIGN.md` §7.2）。
4. 时间端点语义与 API/后端转换一致（`LQ-AC-045` ↔ `LQ-API-32 / 34`、`LQ-DESIGN-31`）。
5. 弹窗行为与按需加载接口一致（`LQ-AC-098 / 102` ↔ `LQ-API-70~79`）。
6. 超时、错误码与错误展示一致（`LQ-AC-120~127` ↔ `LQ-API-80~96`）。
7. UI 每个关键状态在本文档中至少有一个验收用例（领域 20/21/22 ↔ `UI.md` §16/§17）。
8. 首次查询失败后切回 Tab 不自动重试，仅用户点击"查询"重试（`LQ-AC-020` ↔ `UI.md` `LQ-UI-035` / §15.2）。
9. 受支持分辨率内页面整体不出现纵向滚动条，仅表格数据区出现一条纵向滚动条（`LQ-AC-149` ↔ `UI.md` `LQ-UI-091 / 092 / 193`）。
10. 验收用例未要求上游设计明确禁止的行为（§2.3 反向约束）。
11. 文档无产品性待确认项（§9）；`LOG-QUERY-BASELINE-ADJUSTMENT-001` 及 `-R1` 完整调整内容已经用户批准收口。
12. 本文档状态为 `APPROVED / IN_PROGRESS`。
13. 所有验收用例初始状态为 `NOT_RUN`（含新增 `LQ-AC-174~182`）；延期用例精确为 `LQ-AC-164 / 165 / 171 / 172 / 173`，均为 `DEFERRED_UNTIL_PHYSICAL_DESIGN`；`LQ-AC-156~158` 为当前可执行的物理解耦边界检查，状态 `NOT_RUN`；`LQ-AC-155` 保持 `NOT_RUN`；未伪造执行结果。
14. `LQ-AC-156~158` 只验证当前代码/文档未提前绑定最终 RANGE 粒度、子分区、索引或生产 DDL，不再放入 B 层完成项；未来物理方案落地由延期用例 `LQ-AC-171`（物理设计批准）、`LQ-AC-172`（生产 DDL 离线执行）验收；生产启用（将 `CDC_LOG_QUERY_ENABLED` 置为 `true`）以 `LQ-AC-171 / 172 / 164 / 165` 全部 PASS 且 `LQ-AC-173` 最终批准为前提。
15. 追踪矩阵无悬空引用、无重复 ID；上游关键规则均有验收落点；新增 `LQ-OPEN-01~13`、`LQ-API-114~119`、`LQ-UI-006~009 / 019 / 127~128 / 219`、`LQ-DESIGN-125~129 / 170~182` 均映射到 `LQ-AC-174~182`。

## 11. 提交前自检

提交前已逐项核对：

1. 仅 `LOG-QUERY-BASELINE-ADJUSTMENT-001` 授权的五份目标文档（REQUIREMENTS / API / DESIGN / UI / ACCEPTANCE）发生变化且非空；无代码、配置、测试或实现报告改动。
2. 五份目标文档状态均为 `APPROVED / IN_PROGRESS`，本次调整已经用户批准收口，可进入本次调整相关的代码修订、自动化测试和开发库验证。
3. 未修改代码、配置、测试和实现报告；未操作数据库、ZooKeeper、构建或测试。
4. UI 规则编号唯一（`LQ-UI-001` 起），无悬空引用。
5. 验收用例编号唯一（`LQ-AC-001` 起），无悬空引用。
6. 上游关键规则均有明确追踪（§7 追踪矩阵）。
7. 列表 12 列完整、顺序正确（`LQ-AC-084`）。
8. 查询条件顺序、时间必填、默认当前自然日、最大 7 天规则正确（领域 3/4/5）。
9. 双 Tab 独立状态与重新进入规则完整（领域 2）。
10. 请求游标栈不存在 off-by-one（`LQ-AC-063~067` 覆盖 API/DESIGN 精确转换表；第 3 页栈严格 `[null,C1,C2]`，返回第 2 页弹出 `C2`、请求 `C1`，无 `C3`）。
11. 分页仅"上一页/下一页"，没有总数、页码/页次、跳页、`pageSize`、OFFSET 分页和表头排序（领域 8、§2.3）。
12. 没有自动刷新、自动重试、模糊表名或大小写转换（领域 23、§2.3）。
13. `CDC_LOG_ID` 始终按字符串处理（领域 11）。
14. 列表、详情、原始消息大字段隔离正确（领域 25）。
15. 加载、慢提示、超时与失败原子性完整（领域 20/21）；首次查询失败后切回不自动重试（`LQ-AC-020`）。
16. 受支持分辨率内仅表格数据区一条纵向滚动条，页面级不出现（`LQ-AC-149`、领域 26）。
17. 物理设计与生产性能验收仍明确延期，生产启用阻断对象为 `CDC_LOG_QUERY_ENABLED=true`（§8、领域 28）。
18. 菜单始终显示、始终可点击且为单一入口；生产环境在阻断项通过并获批前 `CDC_LOG_QUERY_ENABLED` 保持 `false`；不删除未来菜单定义（`LQ-AC-001 / 155 / 170 / 173`、领域 28）。
19. 待用户确认项为无（§9）。
20. 所有验收用例初始状态正确：`LQ-AC-156~158` 为 `NOT_RUN`（当前物理解耦边界检查）；新增 `LQ-AC-174~182` 为 `NOT_RUN`；延期用例精确为 `LQ-AC-164 / 165 / 171 / 172 / 173`（`DEFERRED_UNTIL_PHYSICAL_DESIGN`）；`LQ-AC-155` 保持 `NOT_RUN`；未伪造执行结果（§3）。
21. `LQ-AC-156~158` 职责为当前物理解耦边界检查（不绑定最终 RANGE 粒度、子分区、索引或生产 DDL），不再放入 B 层完成项。
22. 新增 `LQ-AC-171~173`（最终物理设计批准、生产 DDL 离线执行、生产启用阻断链）与 `LQ-AC-174~182`（功能开放、状态接口与页面状态流程），定义唯一、无悬空引用。
23. 延期用例精确为 `LQ-AC-164 / 165 / 171 / 172 / 173` 共 5 个；`LQ-AC-155` 继续为 `NOT_RUN`。
24. A 层包含 `LQ-AC-156~163` 及既有数据源/SQL 形态检查；B 层包含 `LQ-AC-171 / 172 / 164 / 165 / 173`。
25. 生产启用（`CDC_LOG_QUERY_ENABLED=true`）以 `LQ-AC-171 / 172 / 164 / 165` 全部 PASS 且 `LQ-AC-173` 最终批准为前提；不再出现"未来物理设计完成后仍验收粒度未确定"的悖论。
26. 功能开放、状态接口、原四接口不受开关控制、"全部"双向互斥、重新进入完整初始化、重置清除校验错误、数据源四态降级与前端测试覆盖要求均已纳入验收（领域 3/7/12/22/31）。
27. Git diff 只包含五份目标文档。
28. 暂存区在精确暂存前为空。
