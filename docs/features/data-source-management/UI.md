# 数据源管理 —— UI 设计基线（UI.md）

> 文档状态：`APPROVED`
> 需求状态：`APPROVED`
> 验收标准状态：`APPROVED`
> 基础基线状态：`APPROVED`
> 验收后调整状态：`APPROVED`
> 当前实现状态：`IMPLEMENTED_PENDING_REVIEW`
> 原正式验收状态：`FAIL`（原正式验收 PASS=103/FAIL=2/BLOCKED=1/NOT_RUN=0）
> 定向正式复验状态：`BLOCKED`（`DATA-SOURCE-FORMAL-REVERIFICATION-001`，PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0，阻塞 `DS-AC-104`/`DS-AC-108`；`DS-AC-052`/`DS-AC-105` 与 `DS-AC-107`/`DS-AC-109~115` 均已复验转为 `PASS`）
> 调整批准任务：`DATA-SOURCE-POST-ACCEPTANCE-ADJUSTMENT-APPROVAL-CLOSEOUT-001`
> 调整批准日期：2026-08-30
> 调整批准人：项目负责人（用户）
> 调整批准依据：用户明确回复“认可，批准本次调整草案”
> 调整 R1 复审提交：`0e79165ab1b8e7d87ff06b753a8770fcd9198afc`
> 调整 R1 复审结论：`REVIEW_PASS`
> 新调整实现状态：`IMPLEMENTED_PENDING_REVIEW`（已实现并完成定向正式复验；`DS-AC-107`/`DS-AC-109~115` 为 `PASS`，`DS-AC-108` 为 `BLOCKED`）
> 设计任务：`DATA-SOURCE-DESIGN-BASELINE-001`
> 授权基准提交：`c24bbb826b252f06f75ec05bcac77e94a9871019`
> 创建日期：2026-08-29
> 批准任务：`DATA-SOURCE-DESIGN-APPROVAL-CLOSEOUT-001`
> 批准日期：2026-08-29
> 批准依据提交：`fdb9ecaf5bc24373e586d853b4174d1a9cd8bbfc`

---

## 0. 声明与边界

- 本设计已获用户正式批准（批准任务 `DATA-SOURCE-DESIGN-APPROVAL-CLOSEOUT-001`，批准日期 2026-08-29，批准依据提交 `fdb9ecaf5bc24373e586d853b4174d1a9cd8bbfc`），成为数据源管理 Feature 当前正式设计基线；不再处于"草案等待批准"状态。
- 设计批准**不代表**代码已实现、构建通过、验收执行或生产可用。
- 115 条验收用例已全部执行（定向正式复验 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0`，结论 `BLOCKED`，阻塞 `DS-AC-104`/`DS-AC-108`；原失败用例 `DS-AC-052`/`DS-AC-105` 已复验转为 `PASS`）；新增调整用例 `DS-AC-107~115`（9 例）已批准并完成复验执行（`DS-AC-107`、`DS-AC-109~115` 为 `PASS`，`DS-AC-108` 为 `BLOCKED`）。
- 本设计不改变任何已批准 `DS-REQ`/`DS-AC`；不新增 DDL、锁、权限、认证或自动刷新等未批准能力。
- 本设计不修改代码、测试、构建文件、配置、菜单、路由、历史候选或任何已批准项目/数据库基线。
- 本页面替换既有 `/config/data-source` 占位页；菜单项"数据源管理"与路由 `/config/data-source`（name `DataSource`）保持既有值不变（`DS-AC-001`~`003`）。

### 0.1 批准声明

- 用户已正式批准初版、R1、R2 共同形成的完整设计与契约内容；本文件成为数据源管理 Feature 当前正式设计基线。
- 允许下一阶段基于已批准需求、验收标准和四份设计基线生成实现任务提示词。
- 批准设计**不代表**：代码已经实现；后端或前端构建已经通过；服务已经启动或联调完成；数据库或 ZooKeeper 已被访问；任何 SQL/DDL 已执行；任何一条验收用例已经执行或通过；功能已经生产可用。
- 上述为历史批准事实：批准时 106 条验收用例全部为 `NOT_RUN`，实现状态为 `NOT_STARTED`。当前事实：115 条用例已全部执行（定向正式复验 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0`，结论 `BLOCKED`，阻塞 `DS-AC-104`/`DS-AC-108`），实现状态为 `IMPLEMENTED_PENDING_REVIEW`；新增调整用例 `DS-AC-107~115`（9 例）已批准并完成复验执行（`DS-AC-107`、`DS-AC-109~115` 为 `PASS`，`DS-AC-108` 为 `BLOCKED`）。
- 第一版仍无数据库 DDL、主键/唯一约束/索引变更；批准本文档不等于批准执行数据库变更。

### 0.2 批准链

1. 需求及验收批准收口：`fed87640e007967ece60c1dad5e83438e2bc4672`
2. 基线影响同步及 R1：`3f8747b7aff076f06fc8fdad214e1f14e0013afe`、`c24bbb826b252f06f75ec05bcac77e94a9871019`
3. 设计草案初版：`f7ea3eb2a1343a0600deb86404ce6775a810dce9`
4. 设计 R1：`3b6496b6a2312450fd69be2edbbd287ceb756810`
5. 设计 R2 与最终复审通过基准：`fdb9ecaf5bc24373e586d853b4174d1a9cd8bbfc`
6. 用户最终批准与本批准收口任务：`DATA-SOURCE-DESIGN-APPROVAL-CLOSEOUT-001`

本文件不伪造独立 ChatGPT 报告，仅陈述本批准收口任务已收到的复审结论（基于 `fdb9ecaf...` 的 `REVIEW_PASS`）与用户批准事实。

---

## 1. 页面布局（§8.1）

```
┌────────────────────────────────────────────────────────────────┐
│ 数据源管理                                     [新增]            │
├────────────────────────────────────────────────────────────────┤
│ 数据源ID [____________]  数据源名称 [____________]               │
│ 主机地址  [____________]                    [查询]  [重置]       │
├────────────────────────────────────────────────────────────────┤
│ 数据源ID │ 名称 │ 角色 │ 类型 │ 主机 │ 端口 │ Service Name/数据库名 │ 用户名 │ 操作        │
│ DS01    │ 源库A │ 源库 │ ORACLE │ ... │ ... │ ...                 │ cdc    │ 编辑 命名策略 删除 │
│ TG01    │ 目标库B│ 目标库 │ MYSQL │ ... │ ... │ ...               │ root   │ 编辑 业务属性 删除 │
│ ...     │ ...   │ ...  │ ...   │ ... │ ... │ ...                 │ ...    │ ...          │
└────────────────────────────────────────────────────────────────┘
```

- **页面标题与操作区**：左上"数据源管理"；右上"新增"按钮。
- **查询区**：三个普通文本输入框——数据源 ID、数据源名称、主机地址；"查询"与"重置"按钮（`DS-REQ-006`/`009`）。三条件均忽略大小写模糊、多条件 AND、先 trim（`DS-REQ-007`/`008`）。重置清空并立即恢复全部有效记录（`DS-REQ-009`）。
- **主表格（无分页）**：一次加载并展示全部 `FG_ACTIVE='1'` 记录，无分页控件、无页码、无"加载更多"（`DS-REQ-005`）。默认按数据源 ID 升序，无其他排序（`DS-REQ-010`）。
- **列清单**：数据源 ID、数据源名称、数据源角色（源库/目标库）、数据库类型、主机地址、端口、Service Name/数据库名、用户名、操作（`DS-REQ-011`）。
- **不得展示列**：密码、`DATA_SOURCE_ORG`、`DATA_SOURCE_BIZ_ATTR`、`DATA_SOURCE_DOMAIN`、`FG_ACTIVE`、创建/更新时间、`SOURCE_APP`（`DS-REQ-012`）。
- **行操作**：编辑；删除；角色为目标库时显示"业务属性"；角色为源库时显示"目标库命名策略"（`DS-REQ-013`）。源库行不显示"业务属性"，目标库行不显示"命名策略"（`DS-AC-008`）。
- **角色展示与按钮判定**：角色列与行按钮按**规范化**角色判定——后端对 `DATA_SOURCE_CATEGORY` 存量忽略大小写识别并返回 `SOURCE`/`TARGET`，前端只按规范化 `SOURCE`/`TARGET` 渲染"源库/目标库"与行操作，不依赖任意大小写写法。
- **双击行 = 编辑**（`DS-REQ-014`/`103`）。
- **不使用**旧方案的新标签页及页面重新聚焦刷新行为（`DS-REQ-105`）。
- 组件：`DataSourcePage.vue`（替换占位页）+ 查询区 + `el-table` 无分页 + 各弹窗。

**追踪**：DS-REQ-001/002/003/005/006/007/008/009/010/011/012/013/014/091/105；DS-AC-001~010/011~019/087/098/100。

> **本轮已批准调整基线局部替代声明（§11，`APPROVED`）**：本节被本轮已批准调整基线**局部替代**的部分如下，替代边界之外的本节结论**继续有效**。
> 1. “页面标题与操作区 / 查询区 / 主表格 / 列清单 / 不得展示列 / 行操作”中被 §10 替代的部分（结果区头部、角色条件、移除编辑按钮、更多菜单）**继续以 §10 为准**；
> 2. 本节“主表格：一次加载并展示全部 `FG_ACTIVE='1'` 记录”被 `DS-REQ-150` **局部替代**为“展示全部记录”，无分页与 ID 升序**不变**；
> 3. 本节“不得展示列：… `FG_ACTIVE` …”被 `DS-REQ-152`~`DS-REQ-154` **局部替代**为“不新增独立状态列，但在数据源 ID 列内以‘停用’/‘异常（原始值=…）’标识表达状态”，其余不得展示列（密码等）**继续有效**；
> 4. 本节“行操作：编辑；删除；角色为目标库时显示‘业务属性’；角色为源库时显示‘目标库命名策略’”被 `DS-REQ-163`~`DS-REQ-167` **局部替代**为“在上述业务入口与红色‘删除’之间**插入**状态相关的启用/停用项”，业务入口本身与“删除”**不变**；
> 5. 本节“重置清空并立即恢复全部有效记录（`DS-REQ-009`）”被 `DS-REQ-139`/`DS-REQ-140` **局部替代**为“重置只清空控件、零请求；点击‘查询’才查询”（§11.5）。
> 详细线框、视觉与追踪见 §11，变更记录见 §12。其余（页面标题、查询区三文本条件、双击行=编辑、无新标签页、组件构成）**不被替代**。

---

## 2. 主弹窗（新增/编辑）（§8.2）

居中的大尺寸弹窗（`DS-REQ-015`），宽约 560–720px，不使用抽屉/新页面/新标签页（`DS-AC-020`）。

**字段顺序与分组（唯一字段集，`DS-REQ-019`）**：

| 分组 | 字段 | 控件 | 必填标识 | 规则 |
|---|---|---|---|---|
| 标识 | 数据源 ID | input | `*` | 必填 ≤32 `[A-Za-z0-9_-]`；编辑可修改 |
| 标识 | 数据源名称 | input | `*` | 必填 ≤30 |
| 角色 | 数据源角色/类别 | select | `*` | `SOURCE` 源库 / `TARGET` 目标库 |
| 角色 | 数据库类型 | select | `*` | 源库仅 `ORACLE`；目标库 `ORACLE`/`MYSQL`/`DORIS`；角色变化导致类型非法时清空（`DS-REQ-024`/`025`） |
| 连接 | 主机地址 | input | `*` | 必填 ≤64，IP/域名/主机名 |
| 连接 | 端口 | input-number | `*` | 整数 1–65535 |
| 连接 | 用户名 | input | `*` | 必填 ≤64 |
| 连接 | 密码 | input-password | 新增 `*` | 新增必填 ≤64；编辑显示掩码（见 §3 密码状态） |
| 连接 | Service Name/数据库名 | input | `*` | 必填 ≤64；Oracle 文案"Service Name"，MySQL/Doris 文案"数据库名"（`DS-REQ-030`） |

- **编辑状态模型**：前端打开编辑弹窗时单独保存不可编辑的 `originalDataSourceId`（原主键）；表单中的"数据源 ID"为可自由修改的新值，不得用作读取持久化旧密码的定位键（`DS-REQ-021`）。
- **端口控件**：端口为数值型 `input-number`，只接受整数 1–65535；提交/展示均为数值（`DS-REQ-027`）。
- 全部用户输入字符串（除业务属性外）保存前 trim（`DS-REQ-031`）。
- **不得展示/编辑**：`DATA_SOURCE_ORG`、`DATA_SOURCE_DOMAIN`、`SOURCE_APP`、`FG_ACTIVE`、时间字段；`DATA_SOURCE_BIZ_ATTR` 走独立弹窗（`DS-REQ-020`/`083`）。
- 密码编辑状态：未修改 → 请求体不含 `password`；已修改 → 发送 trim 后新密码（`DS-REQ-045`）。
- 底部按钮：**测试连接**、取消、保存；保存成功关闭并刷新主列表（`DS-REQ-018`）；请求处理中禁用相应按钮（`DS-REQ-017`）。
- 未保存修改关闭 → 二次确认放弃（`DS-REQ-016`）。
- 冲突/校验失败：展示清晰业务消息（`DS-REQ-036`），不暴露堆栈/密码（`DS-REQ-100`/`107`）。

**追踪**：DS-REQ-015/016/017/018/019/020/021/022/023/024/025/026/027/028/029/030/031/036/043/044/045/046/047；DS-AC-020/021/022/023/024~035/036/037/039/040/048/049/050/051/052/095/096/102/105。

---

## 3. 连接测试交互（§8.3）

新增与编辑弹窗均提供"测试连接"按钮（`DS-REQ-048`）。

- **倒计时文案**：点击后按钮进入禁用态，显示剩余时间 `10, 9, 8, ..., 0` 秒（`DS-REQ-055`）。
- **测试中禁用**：按钮禁用，防止重复点击（`DS-REQ-055`/`101`）。
- **提前返回**：后端提前返回结果时立即停止倒计时（`DS-REQ-056`）。
- **到 0**：显示超时状态，并忽略之后到达的迟返回结果，不覆盖超时状态（`DS-REQ-057`）。
- **迟响应处理**：采用请求代次/序列机制，仅当代次匹配且未超时、未修改连接字段时生效（`DS-REQ-057`/`060`；实现见 `DESIGN.md` §6）。
- **成功**：明确显示"连接成功"（`DS-REQ-058`）。
- **失败**：显示脱敏简短原因（如"认证失败"/"不可达"/"超时"），不返回原始堆栈、密码或敏感连接信息（`DS-REQ-059`/`107`）。
- **字段修改失效**：修改主机/端口/用户名/密码/Service Name/类型等任意连接字段后，之前的成功状态立即失效，需重新测试（`DS-REQ-060`）。
- **非前置条件**：测试失败或未测试均可保存（`DS-REQ-049`）。
- **编辑未改密码测试**：请求携带 `originalDataSourceId`（原主键）、`password` 缺席，后端按 `originalDataSourceId` 定位 `FG_ACTIVE='1'` 记录读取持久化密码；修改表单中当前"数据源 ID"后测试连接且密码未修改时，仍使用原记录密码（`DS-REQ-051`/`052`）。
- **关闭弹窗/切换记录**：取消或忽略旧请求结果（倒计时停止、代次失效）。

**追踪**：DS-REQ-048/049/050/051/054/055/056/057/058/059/060/061；DS-AC-053~064/103/104。

---

## 4. 业务属性弹窗（仅目标库）（§8.4）

- **入口**：仅目标库行显示"业务属性"按钮（`DS-REQ-082`）；点击打开独立弹窗（`DS-REQ-084`）。
- **角色限定**：弹窗仅在规范化角色为目标库时可用；后端独立校验记录 `FG_ACTIVE='1'` 且角色 `TARGET`，角色不符时返回"数据源角色不适用于当前操作"并拒绝读取/保存。
- **编辑器**：普通 JSON 文本编辑器/多行文本框（`el-input type="textarea"`），仅作文本展示与编辑（`DS-REQ-085`）；支持空内容（`DS-REQ-086`）。
- **不做校验**：第一版不做 JSON 合法性、结构、格式或业务字段校验（`DS-REQ-087`）。
- **原样保存**：保存时原样传输，不 trim、不校验（`DS-REQ-088`）。
- **加载/保存/失败**：读取加载态；保存成功后明确反馈并按页面状态刷新主列表（`DS-REQ-089`）；失败脱敏提示 + 重试（`DS-REQ-100`）。
- **关闭确认**：未保存修改关闭弹窗必须确认放弃（`DS-REQ-090`）。
- 弹窗字段：`DATA_SOURCE_BIZ_ATTR` 多行文本；标题含目标库 ID/名称。

**追踪**：DS-REQ-082/083/084/085/086/087/088/089/090/100；DS-AC-065/066/067/068/069/070/095/099。

---

## 5. 命名策略大弹窗（仅源库）（§8.5）

- **入口**：仅源库行显示"目标库命名策略"按钮（`DS-REQ-069`）；页面/文档统一使用"目标库命名策略/命名策略"表述，不使用"扩展配置"（`DS-REQ-068`）。
- **角色限定**：弹窗仅在规范化角色为源库时可用；后端独立校验 `sourceId` 记录 `FG_ACTIVE='1'` 且角色 `SOURCE`、新目标库 `FG_ACTIVE='1'` 且角色 `TARGET`，角色不符时返回"数据源角色不适用于当前操作"并拒绝列表/保存/删除。
- **独立大弹窗**：打开独立的大弹窗（`DS-REQ-070`），标题显示当前源库 ID 与名称。
- **无分页列表**：展示该源库全部命名策略，不分页（`DS-REQ-071`）。
- **列清单**：目标库 ID、目标库名称、数据库类型、命名策略、前缀、后缀、操作（`DS-REQ-072`）。
- **行操作**：编辑、删除（`DS-REQ-073`）；删除二次确认（`DS-REQ-081`）。
- **同弹窗内表单**：新增/编辑表单在同一大弹窗内部切换/展开，不嵌套第二个弹窗（`DS-REQ-074`）。
- **目标库下拉**：仅 `FG_ACTIVE='1' AND DATA_SOURCE_CATEGORY='TARGET'` 记录（`DS-REQ-076`）；新增默认不选任何目标库（`DS-REQ-077`）；`TARGET_DATA_SOURCE_ID` 每条必填（`DS-REQ-078`）。
- **策略字段联动**：
  - `TABLE_MERGE`：前缀/后缀不要求填写；切换为或保存为该策略时清空前缀、后缀（`DS-REQ-079`）。
  - `CUSTOM_PREFIX_SUFFIX`：显示前缀、后缀，二者均必填并按统一规则 trim（`DS-REQ-080`）。
- **组合键重复提示**：新增/编辑触发 `(DATA_SOURCE_ID, TARGET_DATA_SOURCE_ID)` 重复或存量多条异常时，明确提示并阻止保存（`DS-REQ-064`/`067`）。
- **编辑定位**：编辑策略时按"原目标库 ID"定位（路径 `originalTargetId`）；切换目标库时 UI 展示新目标库并发送新目标库 ID（`DS-REQ-073`；契约见 `API.md` §2）。
- **未保存修改确认**：关闭或切换时确认放弃（`DS-REQ-075`）。
- 成功反馈：新增/编辑/删除成功后给出明确反馈并刷新弹窗列表（`DS-REQ-102`）。

**追踪**：DS-REQ-062/063/064/065/066/067/068/069/070/071/072/073/074/075/076/077/078/079/080/081/102；DS-AC-071~086/097/099。

---

## 6. 通用状态与视觉验收（§8.6）

- **加载态**：主列表与各弹窗提供加载态（`DS-REQ-098`）。
- **空数据态**：无匹配有效记录时提供空数据状态（`DS-REQ-099`）。
- **错误/重试**：查询、加载、保存、删除、测试连接失败后提供脱敏提示与合理重试入口（`DS-REQ-100`）。
- **成功反馈**：成功保存主数据、业务属性或命名策略均提供明确反馈（`DS-REQ-102`）。
- **防重复提交**：请求进行中禁用对应按钮（`DS-REQ-101`）。
- **未保存确认**：所有编辑容器（主弹窗、业务属性、命名策略）未保存关闭均需确认（`DS-REQ-104`）。
- **文本溢出/Tooltip**：超长单元格（主机、Service Name、前后缀等）显示省略号 + Tooltip 完整内容。
- **弹窗层级**：新增/编辑、业务属性、命名策略均使用同一弹窗体系，避免多弹窗叠压错乱；命名策略弹窗内表单展开不嵌套二级弹窗。
- **响应式最低宽度**：主列表在窄屏下保持最小可用宽度（建议 ≥1024px 内容区）；关键操作不被挤压隐藏。
- **禁止自动刷新**：页面不设自动刷新/定时刷新；仅在操作成功后按页面规范刷新（`DS-REQ-098`~`102` 边界）。
- **人工视觉验收检查点**：主列表列与行操作按钮与批准一致；掩码密码显示；测试连接倒计时与禁用；命名策略策略字段联动；所有弹窗居中、尺寸合理、关闭确认符合预期；无分页控件；无新标签页行为。

**追踪**：DS-REQ-098/099/100/101/102/103/104/105/106；DS-AC-093~101/105/106。

---

## 7. 追踪（§8.7）

- UI 组件/交互 → DS-REQ/DS-AC 映射见各节"追踪"行。
- 组件与 `API.md` 接口一一对应：列表/详情/新增/编辑/删除 → §4.1~4.5；测试连接 → §4.6；目标候选下拉 → §4.7；业务属性 → §4.8/4.9；命名策略列表/新增/编辑/删除 → §4.10~4.13。
- 字段名（camelCase）与 `API.md`/`DESIGN.md`/`DATABASE.md` 保持一致；密码状态模型（缺席/提供、`originalDataSourceId` 读取旧密码）、`port` 数值类型契约、角色大小写兼容与限定、逻辑组合键（原目标库 + 新目标库）、超时/倒计时（10 秒/10→0）与四文档一致。

---

## 8. 批准收口变更记录（2026-08-29）

- 2026-08-29；
- 文档状态由 `DRAFT_PENDING_USER_REVIEW` 转为 `APPROVED`；
- 技术/产品正文不变；
- 实现状态仍为 `NOT_STARTED`；
- 106 条验收仍为 `NOT_RUN`；
- 依据为本批准任务 `DATA-SOURCE-DESIGN-APPROVAL-CLOSEOUT-001` 及 `fdb9ecaf5bc24373e586d853b4174d1a9cd8bbfc` 最终复审通过基准。

---

## 9. 验收后页面调整（APPROVED）

> 状态：`APPROVED`。以下内容严格落实 `DS-REQ-110~115`（已批准需求，`REQUIREMENTS.md` §20）与 `DS-AC-107~115`（已批准验收用例，`ACCEPTANCE.md` §4.15），为正式验收后五项页面调整的 UI 设计。本设计已于 2026-08-30 由项目负责人批准（批准链：调整草案初始提交 `2a47062` → R1 提交 `0e79165` → ChatGPT 调整 R1 复审 `REVIEW_PASS` → 项目负责人明确回复“认可，批准本次调整草案”；批准任务 `DATA-SOURCE-POST-ACCEPTANCE-ADJUSTMENT-APPROVAL-CLOSEOUT-001`）。批准不代表实现、测试或验收；本组设计已由修复任务（`DATA-SOURCE-POST-ACCEPTANCE-REMEDIATION-001` 及 R1）实现，并经 `DATA-SOURCE-FORMAL-REVERIFICATION-001` 定向正式复验（`DS-AC-107`、`DS-AC-109~115` 为 `PASS`，`DS-AC-108` 为 `BLOCKED`），新调整实现状态 `IMPLEMENTED_PENDING_REVIEW`。原 UI 设计基线（§0~§8）保持 `APPROVED` 不变，本节不重写已批准设计历史。
>
> 定向修复与复验：`DS-AC-052`（密码进入 MyBatis DEBUG 参数日志，违反既有 `DS-REQ-047/107`）与 `DS-AC-105`（`port:"abc"` 返回 HTTP 500 而非批准 API 契约要求的 HTTP 400/code=400）已分别由 `DATA-SOURCE-POST-ACCEPTANCE-REMEDIATION-001` 及 R1 修复，并经 `DATA-SOURCE-FORMAL-REVERIFICATION-001` 真实后端/真实 Oracle 开发库复验转为 `PASS`。`DS-AC-104` 保持环境阻塞（MySQL 授权与 Doris 环境未变，待具备后补验）；`DS-AC-108` 因共享开发库无法安全构造“系统完全无数据”的空状态而记 `BLOCKED`。

### 9.1 主列表两类空状态（DS-REQ-110 / DS-REQ-111）

- 判定依据：以“最后一次实际执行并生效的查询条件”为准，不以尚未点击查询的表单内容判断。
- 有生效查询条件且结果为零：
  - 主提示：“未找到符合当前查询条件的数据源”
  - 辅助提示：“请调整查询条件后重试，或点击上方‘重置’后再点击‘查询’查看全部数据源”（措辞依 `REQUIREMENTS.md` §23.1 的 `DS-REQ-110` 一致性勘误：重置零请求，需再点“查询”才按缺省条件查询全部数据源）
- 无生效查询条件且列表为零：
  - 主提示：“暂无数据源”
  - 辅助提示：引导使用右上角“新增数据源”创建第一条数据源
- 空状态不增加第二个“重置查询”按钮/链接；查询区现有“重置”是唯一重置入口。
- 视觉：中性灰色信息样式，不使用红色/橙色错误或警告色；主提示字号与字重高于辅助提示；主辅提示之间保留足够垂直留白。

### 9.2 可拖动业务弹窗与固定居中确认框（DS-REQ-112）

- 可拖动：新增/编辑数据源、业务属性、目标库命名策略三个业务弹窗。
  - 拖动区域：仅标题栏非控件区域；关闭按钮、输入控件、操作按钮不得触发拖动。
  - 约束：弹窗不得被完全拖出可视区域，至少保留可操作标题栏；浏览器尺寸变化后修正位置；每次重新打开恢复默认居中。
- 不可拖动（固定居中）：删除确认、未保存确认等小型确认框。

### 9.3 表单标签固定宽度左对齐（DS-REQ-113）

- 三个业务弹窗内的表单标签统一固定宽度并左对齐；输入控件左边界一致。
- 必填星号位置固定，有无必填标记的标签文字不因星号错位。
- 动态标签 `Service Name/数据库名` 同样遵守。

### 9.4 命名策略弹窗宽度与列表（DS-REQ-114）

- 桌面端默认宽度约 `1050px`，同时受浏览器可视宽度约束并保留左右安全间距；窗口变窄时仍保留安全间距与可用性。
- 列表不分页，默认空间可完整展示约 5 行记录。
- 七列固定：目标库 ID、目标库名称、数据库类型、命名策略、前缀、后缀、操作，合理分配列宽。
- 长内容（长 ID/名称/前后缀）省略显示，悬停展示完整值（Tooltip）。

### 9.5 命名策略单选卡片（DS-REQ-115）

- `TABLE_MERGE` 与 `CUSTOM_PREFIX_SUFFIX` 使用横向排列的单选卡片。
- 卡片结构：第一行显示单选按钮与策略名称，第二行显示说明；点击整张卡片可选中。
- 选中态：蓝色边框 + 浅蓝背景。
- 固定说明：
  - 表合并：“按表合并规则生成目标表名，无需填写前缀和后缀。”
  - 自定义前后缀：“在源表名基础上添加指定前缀和后缀，生成目标表名。”
- 前后缀联动（保持既有规则）：选择表合并时清空并禁用前后缀；选择自定义前后缀时启用并按既有规则校验（`DS-REQ-079/080`）。

### 9.6 变更记录

| 日期 | 变更 | 依据 |
|---|---|---|
| 2026-08-30 | 新增“验收后页面调整草案”设计章节（§9），严格落实 `DS-REQ-110~115` 与 `DS-AC-107~115`；头部元数据增加基础基线/调整草案/当前实现/原正式验收分层状态；原 UI 设计基线保持 `APPROVED`、原设计历史不重写；实现状态由 `NOT_STARTED` 更新为 `IMPLEMENTED_PENDING_REVIEW`；原 106 条验收用例状态由“全部 NOT_RUN”更新为正式验收 `PASS=103/FAIL=2/BLOCKED=1/NOT_RUN=0`（结论 `FAIL`） | DATA-SOURCE-POST-ACCEPTANCE-ADJUSTMENT-BASELINE-001（正式验收后五项页面调整的 UI 设计草案；纯文档任务） |
| 2026-08-30 | 验收后调整批准收口：§9 由“验收后页面调整草案（DRAFT_PENDING_USER_REVIEW）”改为“验收后页面调整（APPROVED）”；头部元数据补充批准任务、批准日期、批准人、批准依据、调整 R1 复审提交 `0e79165` 与复审结论 `REVIEW_PASS`；新调整实现状态 `NOT_STARTED`；`DS-AC-107~115` 仍全部 `NOT_RUN`，批准不代表实现、测试或验收；§9.1~§9.5 设计正文逐字冻结；原草案记录不改写；原 UI 基线 `APPROVED` 与正式验收 `FAIL` 保持 | DATA-SOURCE-POST-ACCEPTANCE-ADJUSTMENT-APPROVAL-CLOSEOUT-001（验收后页面调整批准收口；纯文档任务） |
| 2026-08-30 | 定向正式复验状态回写：头部元数据增加“定向正式复验状态 `BLOCKED`”（`DATA-SOURCE-FORMAL-REVERIFICATION-001`，`PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0`，阻塞 `DS-AC-104`/`DS-AC-108`）；新调整实现状态由 `NOT_STARTED` 更新为 `IMPLEMENTED_PENDING_REVIEW`；`DS-AC-107`/`DS-AC-109~115` 复验为 `PASS`、`DS-AC-108` 复验为 `BLOCKED`；实现状态保持 `IMPLEMENTED_PENDING_REVIEW` 未置 `IMPLEMENTED_ACCEPTED`；§9.1~§9.5 设计正文逐字冻结 | DATA-SOURCE-FORMAL-REVERIFICATION-001（定向正式复验；纯文档状态回写） |

---

## 10. 列表首页选择性接入查询列表页公共组件（`APPROVED`，`IMPLEMENTED_PENDING_USER_REVIEW`）

> 状态：`APPROVED`。本轮调整基线分层状态：`adjustment_ui_status=APPROVED`、`adjustment_baseline_status=APPROVED`、`implementation_status=IMPLEMENTED_PENDING_USER_REVIEW`、`implementation_authorization_status=GRANTED_IN_THIS_TASK`、`formal_acceptance_execution_status=NOT_RUN`、`new_adjustment_acceptance_status=ALL_NOT_RUN`。以下内容严格落实 `REQUIREMENTS.md` §22 的 `DS-REQ-116~138` 与 `ACCEPTANCE.md` §4.16 的 `DS-AC-116~140`（全部 `NOT_RUN`），为任务 `DATA-SOURCE-LIST-PAGE-SELECTIVE-QUERY-LIST-INTEGRATION-BASELINE-001` 形成的 UI 设计基线，**已获批准**，并已由 `DATA-SOURCE-LIST-PAGE-SELECTIVE-QUERY-LIST-INTEGRATION-IMPLEMENTATION-001` 实现，**尚未验收**。
> 批准链（2026-09-19）：初版草案提交 `01680ee527b8e35cd4afd84c4789b862d34f7a77` → R1 修订提交 `c3fd460bea64a14ccc7b52a554194a133330e29d` → ChatGPT 远程 Git R1 复审结论 `REVIEW_PASS` → 项目负责人 2026-09-19 明确回复“批准这轮调整基线”；批准任务 `DATA-SOURCE-LIST-PAGE-SELECTIVE-QUERY-LIST-INTEGRATION-APPROVAL-CLOSEOUT-001`。
> 实现**不代表**已验收或生产可用：实现状态为 `IMPLEMENTED_PENDING_USER_REVIEW`，实现授权为 `GRANTED_IN_THIS_TASK`，`DS-AC-116~140`（25 条）仍全部 `NOT_RUN`；页面视觉结论以后续项目负责人页面目测为准。
> §0~§9 既有 `APPROVED` UI 基线逐字冻结、不重写。

### 10.1 三段结构与结果区头部（局部替代 §1）

```
┌────────────────────────────────────────────────────────────────┐
│ 数据源管理                                                       │
│ 维护源库与目标库的连接配置                                        │
├────────────────────────────────────────────────────────────────┤
│ 数据源ID [________] 数据源名称 [________] 角色 [全部 ▾] 主机 [____] │
│                                                    [查询][重置] │
├────────────────────────────────────────────────────────────────┤
│ 数据源列表  共 N 条                              [新增数据源]      │
│ [固定错误槽：loadError 的 el-alert；无错误时保留占位]              │
│ ──────────────────────────────────────────────────────────────  │
│ 数据源ID │ 名称 │ 角色 │ 类型 │ 主机 │ 端口 │ Service Name │ 用户名 │ 操作 │
│ DS01     │ 源库A │ 源库 │ ORACLE │ ... │ ... │ ...          │ cdc   │ 更多 │
│ TG01     │ 目标库B│目标库│ MYSQL  │ ... │ ... │ ...          │ root  │ 更多 │
└────────────────────────────────────────────────────────────────┘
```

- 三段结构：① 页面标题与一句话说明（`QueryListPageShell`）；② 查询条件区（`QueryListQueryPanel` + `QueryListActions`）；③ 结果列表区（`QueryListResultPanel`）。
- 结果区头部：左侧“数据源列表”+ 当前结果数量；右侧“新增数据源”。结果区**保留** `QueryListResultPanel` 固定结构：头部 → 固定错误槽 → 固定分隔线 → `body` 主体槽；`loadError` 的 `el-alert` 映射**固定错误槽**（保留现有错误消息展示与关闭行为），数据源表格放入 `body` 主体槽；结果区**不增加**双击提示行或其他辅助说明行。
- 页面**不显示**刷新倒计时、最近刷新时间或“立即刷新”；**不显示**“双击数据行可编辑”提示文案。

> **局部替代声明（§10.1）**：本节替代 §1 的**页面区划与结果区头部/查询区排布**结论——标题由左上标题改为标题 + 一句话说明；结果区头部由“数据源列表 + 新增按钮”改为“数据源列表 + 当前结果数量”与右侧“新增数据源”；查询区新增“角色”`el-select` 单选下拉框。§1 中**列清单**（`DS-REQ-011`）、**不得展示列**（`DS-REQ-012`）、**无分页**（`DS-REQ-005`）、**默认 ID 升序**（`DS-REQ-010`）、**角色规范化展示**、**双击行 = 编辑**（`DS-REQ-014`）等结论**继续有效**，不被本节替代；§1 中“行操作：编辑”一项由 §10.3 局部替代。

### 10.2 查询条件与角色 `el-select` 单选下拉框

- 保留三个文本条件：数据源 ID、数据源名称、主机地址（`DS-REQ-006`）。
- 新增“角色”条件，控件固定为 Element Plus `el-select` **单选下拉框**（**不得**画成 Radio / 单选按钮组）；占位与默认显示“全部”；选项顺序与显示文本固定为：全部、源库、目标库，绑定值依次为空值、`SOURCE`、`TARGET`；控件宽度冻结为 `140px`（本文档中角色下拉只出现该宽度）。
- 宽屏顺序固定为：数据源 ID → 名称 → 角色 → 主机 → 查询 → 重置；窄屏响应式遵循 `QueryListQueryPanel` 已验收的**整组换行**规则，不单独发明本页断点或布局算法。
- 过滤使用规范化代码 `SOURCE`/`TARGET`，不使用中文展示值；与其他非空条件 AND 组合；保留 trim 与忽略大小写包含规则（`DS-REQ-007`/`008`/`126`）。
- 前端**不得**静默纠正非法角色值（不自动转大写）；服务端 `category` 参数的归一化、校验与异常映射见 `API.md` §9（`BindException` → HTTP 400 / `code=400` / 字段级消息），UI 只展示服务端返回的错误消息（`DS-REQ-128`）。
- 点击“查询”按当前草稿条件查询；“重置”恢复三个文本条件为空、角色为“全部”，并**立即恢复全部有效记录**（沿用 `DS-REQ-009`，不套用模板默认“重置不查询”）。
- 查询/重置按钮宽度四值同锁，Loading 期间几何稳定（`DS-REQ-137`）。

### 10.3 操作列与“更多”菜单

- **移除**每行可见“编辑”按钮；编辑入口**只保留双击数据行**。
- 操作列只保留**一个带文字的“更多”**下拉入口（不使用纯三点图标）；菜单内**不含**“编辑”。
- 源库行菜单：`目标库命名策略` / 分隔线 / 红色危险项 `删除`。
- 目标库行菜单：`业务属性` / 分隔线 / 红色危险项 `删除`。
- 菜单与下拉面板事件不冒泡触发双击编辑或其他行事件（`DS-REQ-134`）。
- 删除继续使用既有二次确认、并发阻断、错误提示与“仅删除主表记录”语义（`DS-REQ-091~097` 不变）。

### 10.4 Tooltip（保留既有机制）

- 主列表现有长文本 Tooltip（Element Plus `show-overflow-tooltip`）**保留**，不删除、不弱化、不改变。
- 本轮范围外的命名策略弹窗 Tooltip 同样保持原样（§9.4）。
- 迁移到公共单实例 Tooltip **未纳入本轮**（缺少充分必要性，属可观测行为变化）。

### 10.5 弹窗零变化与零回归

- 新增/编辑数据源弹窗、业务属性弹窗、目标库命名策略弹窗、测试连接行为、删除确认框均**不在本轮调整范围**，其内容、字段、校验、视觉与交互保持 §2~§5、§9 的既有结论不变。
- 其他路由与公共组件既有参考页（`/monitor/data-source-state`）**零回归**；稳定滚动条槽仍只对该路由生效；不引入样式泄漏。
- 本轮**不接入** `QueryListRefreshToolbar`；**继续无分页**（`DS-REQ-005` 保持）。“无分页”**只**表示 `body` 中不放分页组件，**不**删除 `QueryListResultPanel` 的固定错误槽与固定分隔线（`DS-REQ-136`）。

### 10.6 追踪（`DS-REQ-116~138`）

| 需求 | 落点 |
|---|---|
| DS-REQ-116 | §10.5（范围外项零变化） |
| DS-REQ-117 | §10.1（公共组件接入） |
| DS-REQ-118 | §10.1（`toolbar`/`body`/固定错误槽槽位归属） |
| DS-REQ-119 | §10.5（不接入刷新工具栏、稳定滚动条槽不启用） |
| DS-REQ-120 | §10.5（既有业务规则不变） |
| DS-REQ-121 | §10.1（三段结构） |
| DS-REQ-122 | §10.1（无刷新元素） |
| DS-REQ-123 | §10.1（结果区头部） |
| DS-REQ-124 | §10.1、§10.3（无提示文案、双击保留） |
| DS-REQ-125 | §10.2（角色 `el-select` 单选下拉框、`140px`、选项顺序与排列顺序） |
| DS-REQ-126 | §10.2（规范化代码、AND、trim/忽略大小写） |
| DS-REQ-127 | §10.2（查询/重置语义） |
| DS-REQ-128 | §10.2（角色参数归一化/校验与异常映射由 API 承载，见 `API.md` §9；前端不静默纠正非法值） |
| DS-REQ-129 | §10.3（列清单/排序/范围/角色标签保持） |
| DS-REQ-130 | §10.3（移除编辑按钮） |
| DS-REQ-131 | §10.3（“更多”带文字入口） |
| DS-REQ-132 | §10.3（源库/目标库菜单差异） |
| DS-REQ-133 | §10.3（删除语义与危险样式） |
| DS-REQ-134 | §10.3（事件不冒泡） |
| DS-REQ-135 | §10.4（Tooltip 保留） |
| DS-REQ-136 | §10.5（无分页） |
| DS-REQ-137 | §10.2（Loading 几何稳定） |
| DS-REQ-138 | §10.5（零回归、无样式泄漏） |

### 10.7 变更记录

| 日期 | 变更 | 依据 |
|---|---|---|
| 2026-09-19 | 新增“列表首页选择性接入查询列表页公共组件”设计草案章节（§10），严格落实 `DS-REQ-116~138` 与 `DS-AC-116~140`（全部 `NOT_RUN`）；以“局部替代声明”明确 §10.1 对 §1 页面区划/结果区头部/查询区排布的替代边界，以及 §10.3 对 §1“行操作：编辑”项的替代；§0~§9 既有 `APPROVED` UI 基线逐字冻结、不重写；实现状态仍为 `NOT_STARTED`（本轮为纯文档草案，未修改任何业务代码/测试/依赖/配置/SQL，未访问数据库/ZK/Kafka，未启动服务）；既有正式复验统计 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0` 与 `DS-AC-104`/`DS-AC-108` 两个 `BLOCKED` 逐字保留，未置 `IMPLEMENTED_ACCEPTED` | DATA-SOURCE-LIST-PAGE-SELECTIVE-QUERY-LIST-INTEGRATION-BASELINE-001（列表首页调整基线草案；纯文档任务） |
| 2026-09-19 | R1 定向修订（ChatGPT 远程独立复审）：① §10.1 线框与文字删除“头部下方直接进入表格”，明确保留 `QueryListResultPanel` 固定结构（头部 → 固定错误槽 → 固定分隔线 → `body`），`loadError` 的 `el-alert` 映射固定错误槽；② §10.1/§10.2 将角色控件由 Radio 改为 `el-select` 单选下拉框、宽度冻结 `140px`、选项顺序“全部/源库/目标库”、宽屏顺序“数据源ID → 名称 → 角色 → 主机 → 查询 → 重置”、窄屏整组换行；③ §10.2 增加“前端不静默纠正非法角色值”的说明并指向 `API.md` §9；④ §10.5 增加“无分页 ≠ 删除固定错误槽/分隔线”。§0~§9 与 §10.3/§10.4 未修改；`DS-REQ-116~138` 编号与数量未变 | DATA-SOURCE-LIST-PAGE-SELECTIVE-QUERY-LIST-INTEGRATION-BASELINE-001-R1（ChatGPT 远程复审定向修订；纯文档任务） |
| 2026-09-19 | 列表首页选择性接入公共组件调整 UI 设计批准收口：§10 章节标题与状态声明由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`（`adjustment_ui_status=APPROVED`）；记录完整批准链（初版草案提交 `01680ee5...` → R1 修订提交 `c3fd460b...` → ChatGPT 远程 Git R1 复审 `REVIEW_PASS` → 项目负责人 2026-09-19 明确回复“批准这轮调整基线”；批准任务 `DATA-SOURCE-LIST-PAGE-SELECTIVE-QUERY-LIST-INTEGRATION-APPROVAL-CLOSEOUT-001`）；明确批准不等于实现、测试或验收——§10.1~§10.6 技术正文与 R1 冻结方案（`QueryListResultPanel` 固定结构、角色 `el-select` `140px`、无分页/无刷新/Tooltip 保留、双击编辑与“更多”菜单、局部替代声明边界）**零变化**，实现状态仍为 `NOT_STARTED`、实现授权 `NOT_GRANTED_IN_THIS_TASK`、`DS-AC-116~140` 仍全部 `NOT_RUN`；§0~§9 既有 `APPROVED` UI 基线逐字冻结；既有统计 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0` 与 `DS-AC-104`/`DS-AC-108` 两个 `BLOCKED` 逐字保留 | DATA-SOURCE-LIST-PAGE-SELECTIVE-QUERY-LIST-INTEGRATION-APPROVAL-CLOSEOUT-001（项目负责人批准驱动的调整基线批准收口；纯文档任务；未修改任何业务代码/测试/依赖/配置/SQL；未访问数据库/ZK/Kafka；未启动服务） |
| 2026-09-19 | 本轮调整实现状态回写：§10 章节标题改为“（`APPROVED`，`IMPLEMENTED_PENDING_USER_REVIEW`）”，状态声明由 `implementation_status=NOT_STARTED`/`implementation_authorization_status=NOT_GRANTED_IN_THIS_TASK`/`acceptance_execution_status=ALL_NOT_RUN` 更新为 `implementation_status=IMPLEMENTED_PENDING_USER_REVIEW`/`implementation_authorization_status=GRANTED_IN_THIS_TASK`/`formal_acceptance_execution_status=NOT_RUN`/`new_adjustment_acceptance_status=ALL_NOT_RUN`；实现按 §10 已批准 UI 设计落地（`QueryListPageShell`/`QueryListQueryPanel`/`QueryListActions`/`QueryListResultPanel` 接入、角色 `el-select` `140px`、无分页、无刷新工具栏、Tooltip 保留、行双击编辑与“更多”操作列）；§10.1~§10.6 技术正文与 R1 冻结方案**零变化**；`DS-AC-116~140` 仍全部 `NOT_RUN`，实现状态未置 `IMPLEMENTED_ACCEPTED`；页面视觉结论以后续项目负责人页面目测为准；§0~§9 既有 `APPROVED` UI 基线逐字冻结；既有统计 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0` 与两个 `BLOCKED` 逐字保留 | `DATA-SOURCE-LIST-PAGE-SELECTIVE-QUERY-LIST-INTEGRATION-IMPLEMENTATION-001`（已批准调整基线的前后端实现、自动化测试、构建与实现状态回写；未访问数据库/ZK/Kafka；未启动服务） |

## 11. 列表展示全部状态、启用/停用及视觉微调（`APPROVED`，`IMPLEMENTED_PENDING_FINAL_ACCEPTANCE`，正式验收已执行 `PASS=42/FAIL=0/BLOCKED=0/NOT_RUN=0`）

> 本轮调整基线分层状态：`adjustment_document_status=APPROVED`、`adjustment_baseline_status=APPROVED`、`adjustment_ui_status=APPROVED`、`implementation_status=IMPLEMENTED_PENDING_FINAL_ACCEPTANCE`、`implementation_authorization_status=GRANTED_IN_THIS_TASK`、`formal_acceptance_execution_status=EXECUTED_PASSED_LOCAL`、`new_adjustment_acceptance_status=PASS_42_OF_42`（2026-09-20 由 `DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-FORMAL-ACCEPTANCE-001` 在真实浏览器执行；**正式验收执行通过不等于最终验收通过**，未置 `IMPLEMENTED_ACCEPTED`/生产可用）。
>
> 批准链（2026-09-19）：初版草案提交 `4ccd6610...` → R1 修订提交 `c4e1048...` → R2 极小修订提交 `aa906c0...` → ChatGPT 从远程 Git 复审 R2 提交 `aa906c0...` 结论 `REVIEW_PASS`（`blocking_finding_count=0`）→ 项目负责人 2026-09-19 明确回复“批准本轮调整基线” → 批准收口任务 `DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-APPROVAL-CLOSEOUT-001`。批准对象为经初版、R1、R2 修订并由 ChatGPT 远程复审通过的**当前**调整基线，**非仅初版**；批准与实现只代表 UI 基线正式成立且已落地，**不代表**已测试、已验收或生产可用。
>
> 任务：`DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-BASELINE-001`（纯文档任务）。实现任务：`DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-IMPLEMENTATION-001`（2026-09-19）。正式验收任务：`DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-FORMAL-ACCEPTANCE-001`（2026-09-20）。关联需求 `DS-REQ-139`~`DS-REQ-177`，关联验收 `DS-AC-141`~`DS-AC-182`（已由 `NOT_RUN` 更新为 `PASS`）。§0~§10 既有结论与 §11.0~§11.x 视觉结论**逐字冻结**，仅状态分层随正式验收执行更新。页面视觉结论的最终接受仍以项目负责人最终验收决定为准。

### 11.0 对既有 UI 结论的局部替代边界

| # | 被替代 | 替代需求 | 边界 |
|---|---|---|---|
| 1 | §1/§10.1 结果区头部左侧“数据源列表 + 当前结果数量” | `DS-REQ-141` | 只替代头部文字，固定结构（头部 → 固定错误槽 → 固定分隔线 → `body`）不变 |
| 2 | §1/§10 列清单的“第一列为数据源 ID” | `DS-REQ-142`/`DS-REQ-143` | 只替代为“新增序号列作为第一列”，其余列身份不变 |
| 3 | §1/§10 的“展示全部有效记录 / 不得展示 `FG_ACTIVE`” | `DS-REQ-150`~`DS-REQ-154` | 只替代为“展示全部记录 + 数据源 ID 列内状态标识”；不新增独立状态列；密码等不得展示列不变 |
| 4 | §1/§10 行操作/“更多”菜单只有业务入口与删除 | `DS-REQ-163`~`DS-REQ-167` | 只在业务入口与红色删除之间**插入**启用/停用项，异常行菜单收敛 |
| 5 | §1/§10.2“重置后立即恢复全部有效记录” | `DS-REQ-139`/`DS-REQ-140` | 只替代“重置即查询”；控件清空不变 |
| 6 | §10.1 行高/字号/字重的既有效果（偏紧凑、偏小、偏淡） | `DS-REQ-146`~`DS-REQ-149` | 只替代视觉表现；列身份与角色语义不变 |
| 7 | Element Plus 默认蓝色“新增数据源”按钮 | `DS-REQ-174` | 只替代视觉样式；位置、槽位与流程不变 |

### 11.1 页面线框（本轮目标）

```
┌────────────────────────────────────────────────────────────────┐
│ 数据源管理                                                       │
│ 维护源库与目标库的连接配置                                        │
├────────────────────────────────────────────────────────────────┤
│ 数据源ID [________] 数据源名称 [________] 角色 [全部 ▾] 主机 [____] │
│                                                    [查询][重置]  │
├────────────────────────────────────────────────────────────────┤
│ 共 N 条                                          [＋ 新增数据源]  │
│ [固定错误槽：loadError 的 el-alert；无错误时保留占位]              │
│ ──────────────────────────────────────────────────────────────  │
│ 序号 │ 数据源ID [停用] │ 名称 │ 角色 │ 类型 │ 主机 │ 端口 │ ... │ 操作 │
│  1   │ DS01           │ 源库A │ 源库 │ORACLE│ ...  │ ...  │ ... │ 更多 │
│  2   │ TG01  [停用]    │目标库B│目标库│ MYSQL│ ...  │ ...  │ ... │ 更多 │
│  3   │ XX01 [异常（原始值=NULL）] │ ... │ ... │ ... │ ... │ ... │ 更多 │
└────────────────────────────────────────────────────────────────┘
```

- 结果区左上角**只显示 `共 N 条`**；“数据源列表”文字**删除**（`DS-REQ-141`）。
- `QueryListResultPanel` 固定结构与 `loadError` 映射**不变**；**不增加**双击提示行或其他辅助说明行（`DS-REQ-123` 继续有效）。
- 表格第一列为**序号**（从 `1` 开始）；第三列位置仍是既有“名称”等列，列身份不变。

### 11.2 序号列与列宽

- **序号列**：第一列，标题“序号”；取值 `index + 1`；**固定窄宽**（窄至足以容纳两位以上序号即可）且单元格内容**居中**；**不绑定**行数据字段、**不进入**请求参数与响应字段、**不改变**默认排序（`DS-REQ-142`/`DS-REQ-143`）。
- 默认排序仍为 `DATA_SOURCE_ID ASC`（`DS-REQ-144`；`DS-REQ-010` 继续有效）。
- **主机列**：相对调整前**缩窄**列宽；`show-overflow-tooltip` 继续作用于主机列，省略内容仍可悬停查看完整值；主机字段取值、长度与校验规则**不变**（`DS-REQ-145`）。
- **不新增**独立“状态”列；状态以 §11.4 的列内标识表达（`DS-REQ-154`）。

### 11.3 行高与字体（以“源库快照状态”页为参照）

| 对齐项 | 参照（`/monitor/data-source-state` 最终页面） | 本页结论 |
|---|---|---|
| 表格行高 | 该页 `td`/`th` 垂直内边距所决定的行高 | 本页对齐该行高，**不得**继续使用调整前偏紧凑的效果（`DS-REQ-146`） |
| 表头与正文基础字号/字重 | 该页表头（较小字号 + `600` 字重 + 中性灰）与正文基础字号/字重 | 本页表头/正文对齐该参照，**不得**继续使用偏小、偏淡的效果（`DS-REQ-146`） |
| “数据源 ID”正文 | 该页“探针端”列主值（更重字重 + 等宽字体族 + 等宽数字） | 本页“数据源 ID”正文使用同一致清晰字体效果（`DS-REQ-147`） |
| “源库/目标库”角色标签 | 该页“快照状态”标签（固定高度、内边距、无边框、固定圆角、`12px`/`600`） | 清晰度、字号、字重参考该标签；保留标签形态与两类角色可区分颜色（`DS-REQ-148`） |

- **样式作用域**：所有覆盖局部作用于数据源页面（页面根作用域的 `--ql-*` 覆盖 + 页面级 class）；**禁止**写入 `:root`、**禁止**修改公共组件、**禁止**影响其他路由与参考页（`DS-REQ-149`/`DS-REQ-138`）。
- 不得为配合本页视觉而修改“源库快照状态”页面。

### 11.4 状态标识（停用 / 异常）

- `FG_ACTIVE='1'`：**无**附加标识（`DS-REQ-151`）。
- `FG_ACTIVE='0'`：在“数据源 ID”列内**与 ID 同行**显示“停用”胶囊标识；位置与视觉参照该参考页“探针端”列的停用标识（内联、小胶囊、固定行高、警示配色、小字号加粗）（`DS-REQ-152`）。
- `FG_ACTIVE` 为 `NULL` 或非 `0`/`1`：显示 `异常（原始值=…）` 或等价的可定位标识；原值为 `NULL` 时**明确显示 `NULL`**；**不得**静默显示为“停用”（`DS-REQ-153`）。
- 标识**不改变行高**、**不换行**、**不遮挡**数据源 ID 文本；标识文字清晰可读。
- 标识取值**直接来自**列表接口返回的原始 `fgActive`（`API.md` §11.2），前端不做归一化。

### 11.5 重置语义

- 点击“重置”**只**恢复查询控件缺省状态：三个文本条件清空、角色恢复“全部”。
- **不发起**查询请求；**不改变**当前表格、结果总数、错误展示与已应用查询条件；不触发 Loading、不改变 busy。
- 用户随后点击“查询”时才按缺省条件查询全部（`DS-REQ-139`/`DS-REQ-140`）。本节替代 §1/§10.2 中“重置后立即恢复全部有效记录”的结论。

### 11.6 操作列与启用/停用菜单

- 操作列仍**只保留一个带文字的“更多”下拉入口**，菜单内**不含“编辑”**（`DS-REQ-131`/`DS-REQ-130` 继续有效）；双击仍是唯一编辑入口。
- 菜单结构（`DS-REQ-167`）：

| 行类型 | 菜单 |
|---|---|
| 启用源库（`'1'`/`SOURCE`） | `目标库命名策略` → 分隔线 → `停用` → `删除` |
| 停用源库（`'0'`/`SOURCE`） | `目标库命名策略` → 分隔线 → `启用` → `删除` |
| 启用目标库（`'1'`/`TARGET`） | `业务属性` → 分隔线 → `停用` → `删除` |
| 停用目标库（`'0'`/`TARGET`） | `业务属性` → 分隔线 → `启用` → `删除` |
| 异常行 | **只有** `停用`（用于归一化为 `'0'`） |

- **样式**：启用/停用项在“删除”**之前**；“删除”为**最后一项**且红色危险样式；“启用”为普通操作样式；“停用”为警示色，且与红色“删除”**可区分**（`DS-REQ-164`）。
- **确认**：启用与停用**均**弹出二次确认，文案明确包含**数据源 ID** 与**目标动作**（`DS-REQ-165`）。
- **防冒泡**：下拉触发器、菜单项与确认框交互**不冒泡**触发行双击编辑或其他行事件（`DS-REQ-166`；与 `DS-REQ-134` 既有机制一致）。
- **busy 与刷新**：同一行操作期间禁用该行启停与删除入口，避免重复提交；成功后按**当前已应用查询条件**刷新（停用后该行仍保留并出现“停用”标识，启用后标识消失）；失败保留当前列表与已应用条件、恢复 busy、显示明确错误（`DS-REQ-172`/`DS-REQ-173`）。
- **停用/异常记录的入口**：停用记录仍可双击编辑、可删除、可维护业务属性/命名策略、可在编辑弹窗内测试连接；异常行**隐藏或禁用**除“停用”外的所有写入口（`DS-REQ-155`~`DS-REQ-162`）。

### 11.7 “新增数据源”按钮视觉

- 删除 Element Plus 默认 `type="primary"` 蓝色；改为**黑色实心主按钮**，与页面“查询”按钮同一黑白灰视觉语言。
- 白色文字；**保留**前置加号图标。
- `normal` 深色底；`hover` 略浅深灰；`active` 再加深；`disabled` 保持既有禁用语义（不引入新的禁用视觉规范）。
- 尺寸与圆角与公共查询列表页规范一致；**不改变**按钮位置（仍在 `QueryListResultPanel` 的 `toolbar` 槽）与新增业务流程（`DS-REQ-174`）。

### 11.8 明确不变项

- 不分页；不接入刷新工具栏；无自动刷新/立即刷新/最近刷新时间；稳定滚动条槽不启用（仍只对 `/monitor/data-source-state` 生效）（`DS-REQ-175`）。
- 保留既有 `show-overflow-tooltip` 机制，本轮不迁移公共单实例 Tooltip（`DS-REQ-135`/`DS-REQ-175`）。
- 角色查询仍为 `el-select` 单选、宽度 `140px`（`DS-REQ-175`）。
- 三个业务弹窗（新增/编辑数据源、业务属性、目标库命名策略）的布局、字段、宽度与视觉**不做**重新设计；仅为支持停用记录维护而调整**必要的**读/写状态边界（`DS-REQ-176`）。

### 11.9 追踪（`DS-REQ-139~177`）

| 需求 | 本文件落点 |
|---|---|
| DS-REQ-139、DS-REQ-140 | §11.0（#5）、§11.5 |
| DS-REQ-141 | §11.0（#1）、§11.1 |
| DS-REQ-142、DS-REQ-143、DS-REQ-144 | §11.0（#2）、§11.2 |
| DS-REQ-145 | §11.2 |
| DS-REQ-146、DS-REQ-147、DS-REQ-148、DS-REQ-149 | §11.0（#6）、§11.3 |
| DS-REQ-150、DS-REQ-151、DS-REQ-152、DS-REQ-153、DS-REQ-154 | §11.0（#3）、§11.4 |
| DS-REQ-155、DS-REQ-156、DS-REQ-157、DS-REQ-158、DS-REQ-159 | §11.6（停用/异常记录入口） |
| DS-REQ-160、DS-REQ-161、DS-REQ-162 | §11.4、§11.6 |
| DS-REQ-163、DS-REQ-164、DS-REQ-165、DS-REQ-166、DS-REQ-167 | §11.0（#4）、§11.6 |
| DS-REQ-168、DS-REQ-169、DS-REQ-170、DS-REQ-171 | §11.6（后端契约见 `API.md` §11；UI 只负责消息与刷新时机） |
| DS-REQ-172、DS-REQ-173 | §11.6 |
| DS-REQ-174 | §11.0（#7）、§11.7 |
| DS-REQ-175、DS-REQ-176、DS-REQ-177 | §11.8 |

## 12. 本轮调整变更记录

### 12.1 初版草案（2026-09-19，任务 `DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-BASELINE-001`）

- 2026-09-19；
- 新增 §11「列表展示全部状态、启用/停用及视觉微调（`DRAFT_PENDING_USER_REVIEW`，未实现）」与本节；§1 末尾新增“本轮新草案局部替代声明”，逐项给出对 §1 的替代边界；
- §11.0 以七条表格冻结对 §1/§10 的替代边界（结果区头部、序号列、全部状态展示与状态标识、菜单插入启停项、重置语义、行高字体、新增按钮视觉）；
- 冻结目标线框（§11.1）、序号列与主机列宽（§11.2）、行高与字体对齐表（§11.3）、停用/异常标识（§11.4）、重置语义（§11.5）、操作列与启停菜单结构/样式/确认/防冒泡/刷新（§11.6）、新增按钮黑色主按钮四态（§11.7）与明确不变项（§11.8）；
- §0~§10 既有 `APPROVED` UI 基线与 §10.1~§10.7 技术正文逐字冻结、未修改；`DS-REQ-001~138` 编号与正文未改；
- 本轮新增需求 `DS-REQ-139~177`（39 条）与本轮新增验收 `DS-AC-141~182`（42 条）全部为 `NOT_RUN`；上一轮 `DS-AC-116~140`（25 条）仍全部 `NOT_RUN`，未混入本轮统计；页面视觉结论以后续项目负责人页面目测为准；
- 既有正式复验统计 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0`（阻塞 `DS-AC-104`/`DS-AC-108`）逐字保留，未置 `IMPLEMENTED_ACCEPTED`；
- 本轮为纯文档草案：未修改任何 `.vue`/`.ts`/`.java`/测试/依赖/配置/SQL/锁文件，未访问数据库/ZK/Kafka，未启动服务，未运行 Maven/npm 测试或构建。

### 12.2 批准收口（2026-09-19，任务 `DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-APPROVAL-CLOSEOUT-001`）

- 2026-09-19；
- §11 章节标题由“（`DRAFT_PENDING_USER_REVIEW`，未实现）”收口为“（`APPROVED`，未实现）”；分层状态 `adjustment_document_status`/`adjustment_baseline_status` 由 `DRAFT_PENDING_USER_REVIEW` 收口为 `APPROVED`，并同步 `adjustment_ui_status=APPROVED`（`implementation_status`/`implementation_authorization_status`/`formal_acceptance_execution_status`/`new_adjustment_acceptance_status` 保持 `NOT_STARTED`/`NOT_GRANTED_IN_THIS_TASK`/`NOT_RUN`/`ALL_NOT_RUN`）；
- 记录完整批准链：初版草案提交 `4ccd6610...` → R1 修订提交 `c4e1048...` → R2 极小修订提交 `aa906c0...` → ChatGPT 远程 Git 复审 R2 提交 `aa906c0...` 结论 `REVIEW_PASS`（`blocking_finding_count=0`）→ 项目负责人 2026-09-19 明确回复“批准本轮调整基线” → 批准收口任务 `DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-APPROVAL-CLOSEOUT-001`；
- §1 末尾“本轮新草案局部替代声明”改标为“本轮已批准调整基线局部替代声明（§11，`APPROVED`）”；
- §11.0~§11.9 UI 正文（线框、序号列与列宽、行高/字体、停用/异常标识、重置语义、启停菜单、黑色主按钮、明确不变项）**零变化**；§0~§10 既有 `APPROVED` UI 基线与 §10.1~§10.7 技术正文逐字冻结；
- `DS-REQ-001~177` 编号与正文零变化；本轮新增验收 `DS-AC-141~182`（42 条）仍全部 `NOT_RUN`，上一轮 `DS-AC-116~140`（25 条）仍全部 `NOT_RUN`；既有 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0` 逐字保留；
- 批准只代表 UI 基线正式成立，**不代表**已实现、已测试、已验收或生产可用；页面视觉仍须后续项目负责人页面目测，实现须**另行**授权并使用独立任务；未修改任何 `.vue`/`.ts`/`.java`/测试/依赖/配置/SQL，未访问数据库/ZK/Kafka，未启动服务。

### 12.3 实现状态回写（2026-09-19，任务 `DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-IMPLEMENTATION-001`）

- §11 章节标题由“（`APPROVED`，未实现）”更新为“（`APPROVED`，`IMPLEMENTED_PENDING_USER_REVIEW`）”；分层状态由 `implementation_status=NOT_STARTED`/`implementation_authorization_status=NOT_GRANTED_IN_THIS_TASK` 更新为 `IMPLEMENTED_PENDING_USER_REVIEW`/`GRANTED_IN_THIS_TASK`，并补充实现任务号（`adjustment_document_status`/`adjustment_baseline_status`/`adjustment_ui_status` 保持 `APPROVED`，`formal_acceptance_execution_status=NOT_RUN`、`new_adjustment_acceptance_status=ALL_NOT_RUN` 保持）；
- 实现按 §11 已批准 UI 设计落地：重置只恢复查询控件缺省值且零请求（`DS-REQ-139`/`DS-REQ-140`）；结果区左上角只显示 `共 n 条`（`DS-REQ-141`）；第一列新增前端序号、主机列收窄至 `min-width 110`（`DS-REQ-142`~`DS-REQ-145`）；行高与基础字体、数据源 ID 字体、角色标签视觉对齐参考页 `/monitor/data-source-state`，覆盖值只作用于本页面（`DS-REQ-146`~`DS-REQ-149`）；列表展示全部状态与原始 `fgActive`，停用/异常标识与“更多”菜单收敛按 §11.4/§11.6 落地（`DS-REQ-150`~`DS-REQ-167`）；“新增数据源”改为黑色实心主按钮并保留加号图标（`DS-REQ-174`）；
- §11.0~§11.9 UI 正文（线框、序号列与列宽、行高/字体、停用/异常标识、重置语义、启停菜单、黑色主按钮、明确不变项）**零变化**；§0~§10 既有 `APPROVED` UI 基线与 §10.1~§10.7 技术正文逐字冻结；`DS-REQ-139~177` 数量（39 条）与编号未变；
- 本轮新增验收 `DS-AC-141~182`（42 条）仍全部 `NOT_RUN`，上一轮 `DS-AC-116~140`（25 条）仍全部 `NOT_RUN`；既有 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0` 逐字保留；
- 实现状态为 `IMPLEMENTED_PENDING_USER_REVIEW`，**未**置为 `IMPLEMENTED_ACCEPTED`/`ACCEPTED`/生产可用；**页面视觉与交互结论以后续项目负责人页面目测为准，本任务未启动任何服务、未做人工目测**；未访问数据库/ZK/Kafka，未启动/停止/重启任何服务。

### 12.4 实现 R1 复审整改（2026-09-20，任务 `DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-IMPLEMENTATION-001-R1`）

- 2026-09-20；ChatGPT 对实现提交 `6b7ae04...` 的复审结论 `CHANGES_REQUIRED`（6 项阻塞），本 R1 按批准契约整改实现与测试；UI 侧仅作一处**同类别措辞一致性勘误**：
- §9.1「主列表两类空状态」中有生效查询条件时的辅助提示语由“请调整查询条件后重试，或点击上方‘重置’查看全部数据源”改为“请调整查询条件后重试，或点击上方‘重置’后再点击‘查询’查看全部数据源”，并加注其依据为 `REQUIREMENTS.md` §23.1 的 `DS-REQ-110` 一致性勘误（重置零请求，需再点“查询”才按缺省条件查询全部数据源）。原措辞隐含“重置即查询”，与已批准 `DS-REQ-139`/`DS-REQ-140` 冲突，故属同一空状态需求内部的**新旧条款一致性勘误**，**非**新业务决策；
- §9.1 其余结论（判定依据为“最后一次实际执行并生效的查询条件”、有生效条件时主提示“未找到符合当前查询条件的数据源”、无生效条件时主提示“暂无数据源”并引导“新增数据源”、不增加第二个重置入口、中性灰色信息样式）**继续有效、零改动**；§9.2~§9.6 与 §0~§8 既有 `APPROVED` UI 正文逐字冻结；
- 菜单分隔线、停用警示色、异常行“停用”警示色、异常标识（含 `FG_ACTIVE` 未返回时提示契约异常而不显示 `undefined`）等 UI 行为均按 §11.4/§11.6 与 R1 §3.3/§3.6 落地并由自动化测试覆盖，§11.0~§11.9 正文**零变化**；
- `DS-REQ-001~177` 编号与正文零变化；本轮新增验收 `DS-AC-141~182`（42 条）仍全部 `NOT_RUN`，上一轮 `DS-AC-116~140`（25 条）仍全部 `NOT_RUN`；既有 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0` 逐字保留；
- 实现状态保持 `IMPLEMENTED_PENDING_USER_REVIEW`，**未**晋升为 `IMPLEMENTED_ACCEPTED`/`ACCEPTED`/生产可用；**页面视觉与交互结论仍以项目负责人页面目测为准**；本任务未访问数据库/ZK/Kafka，未启动/停止/重启任何服务，未做浏览器人工目测。

### 12.5 正式验收执行与状态回写（2026-09-20，任务 `DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-FORMAL-ACCEPTANCE-001`）

- 依据项目负责人 2026-09-20 授予的正式验收执行授权（含 `database_write_approval_status=GRANTED_FOR_R2`），在真实浏览器 + 真实后端 + 真实 Oracle 开发库下执行 `DS-AC-141~182` 共 42 条，覆盖展示全部状态、逐行启用/停用、二次确认、行级 busy、异常记录停用归一化与头部/列/行高/字体/新增按钮视觉微调。
- **§11 标题与状态块更新**：标题更新为「（`APPROVED`，`IMPLEMENTED_PENDING_FINAL_ACCEPTANCE`，正式验收已执行 `PASS=42/FAIL=0/BLOCKED=0/NOT_RUN=0`）」；`implementation_status` 由 `IMPLEMENTED_PENDING_USER_REVIEW` 更新为 `IMPLEMENTED_PENDING_FINAL_ACCEPTANCE`，`formal_acceptance_execution_status` 由 `NOT_RUN` 更新为 `EXECUTED_PASSED_LOCAL`，`new_adjustment_acceptance_status` 由 `ALL_NOT_RUN` 更新为 `PASS_42_OF_42`。
- **§11.0~§11.9 视觉与交互结论逐字冻结**：`DS-REQ-139~177` 编号与正文未改；`DS-AC-001~182` 编号、前置条件、操作步骤、预期结果零变化，仅 `DS-AC-141~182` 状态列由 `NOT_RUN` 更新为 `PASS`；`DS-AC-175`（行高/视觉量测）按任务 §9.4 以定向自动化测试 + 实现路径审计作为补充证据，并在报告中明示证据层级。
- 既有 `PASS=113/FAIL=0/BLOCKED=2/NOT_RUN=0` 逐字保留；上一轮 `DS-AC-116~140`（25 条）仍全部 `NOT_RUN`；实现状态**未**晋升为 `IMPLEMENTED_ACCEPTED`/`ACCEPTED`/生产可用；**本轮正式验收执行不等于项目负责人最终视觉验收结论**，最终视觉验收仍以项目负责人判定为准。
- 本任务未修改任何业务代码/测试/配置/依赖/锁文件/SQL/DDL；ZooKeeper 只读、无 Kafka、未访问业务源库/目标库；临时验收服务已按精确 PID 停止。报告见 `reports/DATA-SOURCE-LIST-ALL-STATUS-ENABLE-DISABLE-UI-ADJUSTMENT-FORMAL-ACCEPTANCE-001.md`。
