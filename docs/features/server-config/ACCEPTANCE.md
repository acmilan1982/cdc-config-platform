# 中心端配置 Feature 验收基线（ACCEPTANCE）

## 1. 文档元数据与状态

| 项目 | 值 |
|---|---|
| Feature 中文名称 | 中心端配置 |
| Feature 标识 | `server-config` |
| 正式菜单 | 中心端配置（直接演进自既有“服务端配置”占位菜单，不新增第二套菜单） |
| 既有路由 | `/config/server`（保持不变，不另建重复路由） |
| 目标文档 | `docs/features/server-config/ACCEPTANCE.md` |
| 文档状态 | `APPROVED` |
| 实现状态 | `IMPLEMENTED_ACCEPTED`（旧批准需求版本与本次两项已批准调整均已实现并验收接受，见 §5 变更记录） |
| 验收用例状态/正式验收状态 | `66 PASSED / 0 FAILED（ACCEPTED）` |
| 收口任务 | `SERVER-CONFIG-FORMAL-ACCEPTANCE-CLOSEOUT-001` |
| 收口日期 | 2026-08-29 |
| 验收证据提交 | `b5aeec28eaf29e20a56dd7012e4077dee8b891a4` |
| 项目负责人最终人工测试 | 保存、刷新、恢复均正常 |
| PENDING_USER_CONFIRMATION | 0 |
| 任务编号 | `SERVER-CONFIG-FEATURE-BASELINE-001` |
| 授权基线提交 | `7ea9d702e831245fbe8f0e84691bf0aea093dbdf` |
| 候选基线初始任务 | `SERVER-CONFIG-FEATURE-BASELINE-001` |
| 候选基线 R1 任务 | `SERVER-CONFIG-FEATURE-BASELINE-001-R1` |
| 批准任务 | `SERVER-CONFIG-FEATURE-BASELINE-APPROVAL-001` |
| 批准日期 | 2026-08-27 |
| 批准人 | 项目负责人 |
| ChatGPT 复审通过候选提交 | `4e55493a0292b462885e4dde0d789e5e1ca48df2` |
| 依据需求 | `docs/features/server-config/REQUIREMENTS.md`（已批准，状态 `APPROVED`） |
| 创建日期 | 2026-08-27 |
| 候选调整任务 | `SERVER-CONFIG-PRE-ACCEPTANCE-ADJUSTMENT-BASELINE-001` |
| 候选调整授权基线提交 | `c0b9d4973e2b6bdd3e7b02a3748816ffc55362ba` |
| 候选调整批准任务 | `SERVER-CONFIG-PRE-ACCEPTANCE-ADJUSTMENT-BASELINE-APPROVAL-001` |
| 候选调整批准日期 | 2026-08-28 |
| 候选调整批准人 | 项目负责人 |
| ChatGPT 最终复审通过提交 | `b1c5349df43c73bc855b5ca8b3ea92acb6faa7ee` |

重要声明：本文件把所有需求转换为可客观验收的场景，使用唯一、稳定的验收编号。所有用例初始状态为 `NOT_RUN`（未执行）；`PASS / FAIL / BLOCKED` 是执行后状态，任何用例状态只有在执行并取得与步骤匹配的客观证据后才允许更新。对需要构造数据库异常数据的验收场景，本文件只定义期望行为，不授权任何测试数据写入；任何数据库写操作仍需按项目数据库审批规则另行获得授权。

状态含义必须清楚，三者不得混淆：本文件 `APPROVED` 表示验收标准已正式批准（含本次验收前两项调整：`CONFIG_DESC` 人工换行、按 `ID_SERVER_CONFIG ASC` 排序）；用例 `NOT_RUN` 表示尚未执行、不能推定通过，`PASS / FAIL / BLOCKED` 表示已执行；实现状态 `IMPLEMENTED_ACCEPTED` 表示旧批准版本与本次两项已批准调整均已实现并验收接受。本文件当前最终执行结果为 `66 PASSED / 0 FAILED / 0 BLOCKED / 0 NOT_RUN`，正式验收状态 `ACCEPTED`。正式验收的详细证据链见 §6.4 最终验收证据索引及各验收报告。

## 2. 验收结果状态模型

| 状态 | 含义 |
|---|---|
| `NOT_RUN` | 尚未执行，不能推定通过（所有用例初始状态） |
| `PASS` | 已执行且符合预期，须附证据 |
| `FAIL` | 已执行且不符合预期，须记录失败点 |
| `BLOCKED` | 受环境或前置条件阻塞，须记录阻断原因 |
| `DEFERRED_UNTIL_*` | 经批准延期到明确后续阶段 |

## 3. 验收领域分类与数量

| 分类 | 编号范围 | 数量 |
|---|---|---|
| 菜单与路由 | SC-AC-001 ~ SC-AC-003 | 3 |
| 页面结构与列布局 | SC-AC-004 ~ SC-AC-012 | 9 |
| 数据加载与异常 | SC-AC-013 ~ SC-AC-018 | 6 |
| 说明多行与人工换行 | SC-AC-066 | 1 |
| 可编辑性判定 | SC-AC-019 ~ SC-AC-023、SC-AC-065 | 6 |
| 布尔配置校验 | SC-AC-024 ~ SC-AC-026 | 3 |
| 原始消息存储策略校验 | SC-AC-027 ~ SC-AC-028 | 2 |
| 实时批量插入数据库类型校验 | SC-AC-029 ~ SC-AC-033 | 5 |
| 快照批次大小校验 | SC-AC-034 ~ SC-AC-036 | 3 |
| 源表删除策略校验 | SC-AC-037 ~ SC-AC-038 | 2 |
| 当前只读配置 | SC-AC-039 ~ SC-AC-040 | 2 |
| 通用非空与物理长度 | SC-AC-041 ~ SC-AC-042 | 2 |
| 编辑、撤销与脏值 | SC-AC-043 ~ SC-AC-047 | 5 |
| 保存确认框 | SC-AC-048 ~ SC-AC-051 | 4 |
| 后端校验与事务 | SC-AC-052 ~ SC-AC-061 | 10 |
| 安全与非目标 | SC-AC-062 ~ SC-AC-064 | 3 |
| **合计** | SC-AC-001 ~ SC-AC-066 | **66** |

## 4. 验收用例

### 4.1 菜单与路由（对应 REQUIREMENTS §7.1）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-001 | 平台已登录并进入任意已登录页面 | 打开左侧“配置管理”菜单 | “服务端配置”菜单显示名称为“中心端配置”；不存在第二个相同名称菜单项；菜单数量与替换前相同（仅名称变化） |
| SC-AC-002 | 菜单“中心端配置”可见 | 点击“中心端配置”菜单并观察地址栏 | 浏览器地址为 `/config/server`，成功进入中心端配置页面；路由复用既有 `/config/server`，无 404 |
| SC-AC-003 | 平台路由表为当前 Git 已提交路由 | 检查 `frontend/src/router/index.ts` 路由定义 | 仅存在一条 `/config/server` 路由记录（name `ServerConfig`），不存在重复路由 |
| SC-AC-004 | 页面尚未打开 | 直接访问 `/config/server` 并观察页面 | 显示中心端配置页面；顶部展示唯一中心端 `SERVER_ID`（与验收运行环境中查询到的唯一 `CDC_SERVER` 记录一致，不固定依赖数据库基线的历史快照值）和配置项总数（与已加载配置数量一致） |
| SC-AC-005 | 已加载唯一中心端全部配置 | 观察配置列表各列与表头 | 不展示“是否可编辑”字段或状态列；页面任意位置不单独展示 `IS_EDITABLE` 值；可编辑性仅通过编辑控件体现 |
| SC-AC-006 | 已加载唯一中心端全部配置 | 观察配置列表各列 | 不展示 `ID_SERVER_CONFIG` 列；不展示 `SERVER_ID` 列 |
| SC-AC-007 | 已加载配置中含较长 `CONFIG_DESC` | 观察配置项说明（主内容）列 | 配置项说明列占据列表最大可用宽度；`CONFIG_DESC` 正常时主内容显示说明内容，普通过长文本按列宽自动折行、多行完整展示，无大段省略；页面不把英文 Key 作为主内容持续展示 |
| SC-AC-008 | 已加载配置 | 观察页面主体列结构；定位 `CONFIG_DESC` 为 NULL/空/纯空格及 `CONFIG_DESC` 与 `CONFIG_KEY` 均为空的记录（构造该数据库状态需另行获得写授权；仅定义期望行为） | 页面主体为“配置项说明 + 配置值”两列：配置项说明列占据最大可用宽度，配置值列相对紧凑但可操作，无 `CONFIG_KEY` 独立列；`CONFIG_DESC` 缺失时主内容回退显示 `CONFIG_KEY`；两者均为空时显示“未定义配置项”且该行只读、不允许保存；兜底不修改数据库的 `CONFIG_DESC` 或 `CONFIG_KEY` |
| SC-AC-009 | 已加载配置；存在只读配置值超出列宽的情况 | 悬停配置项说明旁的信息图标；悬停超宽只读配置值 | 信息图标 Tooltip 完整显示 `配置Key：{CONFIG_KEY}`，不截断、不脱敏；超宽只读值显示省略，悬停弹出完整原文，原文与数据库值完全一致，不脱敏、不截断 |
| SC-AC-010 | 某支持编辑配置进入编辑状态 | 编辑布尔/枚举/多选/数字控件并观察 | 编辑控件充分使用配置值列可用宽度；枚举下拉可完整阅读选项说明，多选与数字输入可正常操作，未被窄列挤压 |
| SC-AC-011 | 已进入中心端配置页面 | 观察页面元素 | 页面无搜索、筛选、分页、自动刷新控件；无新增、复制、删除配置入口 |
| SC-AC-012 | 已进入中心端配置页面 | 观察页面顶部 | 无中心端下拉框或切换器；页面不提供切换/选择其他中心端的能力 |

### 4.2 数据加载与异常（对应 REQUIREMENTS §8、§9）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-013 | `CDC_SERVER` 恰好一条且存在配置记录 | 进入页面 | 页面顶部显示该中心端 `SERVER_ID` 与配置总数；列表加载该中心端全部配置记录 |
| SC-AC-014 | `CDC_SERVER` 为零条（构造该数据库状态需另行获得写授权；仅定义期望行为） | 进入页面 | 页面显示“中心端尚未注册，请先启动 sync-server”；不加载任何配置；所有编辑控件和“保存全部”不可用；页面不发起批量保存请求 |
| SC-AC-015 | `CDC_SERVER` 超过一条（构造该数据库状态需另行获得写授权；仅定义期望行为） | 进入页面 | 页面提示“检测到多个中心端，当前功能仅支持唯一中心端”；不加载配置、不编辑、不保存；不自行选择第一条中心端 |
| SC-AC-016 | 唯一中心端存在但 `CDC_SERVER_CONFIG` 为零条（构造该数据库状态需另行获得写授权；仅定义期望行为） | 进入页面 | 页面显示空状态；无新增配置入口；“保存全部”不可用 |
| SC-AC-017 | 唯一中心端存在且有多条配置 | 进入页面并观察列表顺序 | 一次加载并展示该中心端全部配置；列表按 `ID_SERVER_CONFIG` 升序排列 |
| SC-AC-018 | 唯一中心端存在且有多条配置 | 观察列表列内容 | 每行主体仅展示两列：配置项说明、配置值；不得出现 `CONFIG_KEY` 独立列 |

### 4.3 可编辑性判定（对应 REQUIREMENTS §10）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-019 | 存在 `IS_EDITABLE='1'` 且属于可编辑白名单的配置 | 观察该行 | 显示对应专用编辑控件（下拉/多选/整数输入），可编辑 |
| SC-AC-020 | 存在 `IS_EDITABLE='0'` 的配置 | 观察该行 | 该行只展示只读配置值，不显示编辑控件，不可编辑 |
| SC-AC-021 | 存在 `IS_EDITABLE` 为 NULL、空白或非 `'1'` 异常值的配置（构造该数据库状态需另行获得写授权；仅定义期望行为） | 观察该行 | 该行只读展示配置值，不显示编辑控件，不可编辑 |
| SC-AC-022 | 存在 `CONFIG_KEY` 不在可编辑白名单的未知配置且 `IS_EDITABLE='1'`（构造该数据库状态需另行获得写授权；仅定义期望行为） | 观察该行 | 该行仍完整展示：主内容显示配置项说明（缺失时按兜底名称），Key 仅通过信息图标 Tooltip 查看；该行只读，不提供通用文本编辑；即使 `IS_EDITABLE='1'` 也不可编辑 |
| SC-AC-023 | 存在 `CONFIG_KEY` 已知但尚未内置控件和校验规则的配置（未来新增 key 场景） | 观察该行 | 该行只读展示；前端与后端均不允许修改其值 |
| SC-AC-065 | 存在已支持可编辑 Key 且 `IS_EDITABLE='1'`，但数据库当前 `CONFIG_VALUE` 为空或不符合该 Key 专门规则（构造该数据库状态需另行获得写授权；本用例只定义期望行为） | 进入页面，观察并尝试保存；然后把值改为合法值并保存 | 页面显示对应专用控件与当前值异常提示，不因值异常变只读；未修正前不能提交（“保存全部”不得提交包含非法值的批次）；改为合法值后可进入确认并按正常批量规则保存；若 `IS_EDITABLE` 不为 `'1'` 或 Key 不受支持，仍保持只读 |

### 4.4 布尔配置校验（对应 REQUIREMENTS §11.1）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-024 | `auto-create-table` 处于编辑状态 | 依次选择 `true`、`false` 并保存 | 两种小写值均可保存成功；保存后重新加载显示对应小写 `true`/`false` |
| SC-AC-025 | `auto-expand-column-length` 处于编辑状态 | 依次选择 `true`、`false` 并保存 | 两种小写值均可保存成功；保存后重新加载显示对应小写 `true`/`false` |
| SC-AC-026 | `auto-create-table` 或 `auto-expand-column-length` 处于编辑状态 | 尝试输入/提交 `TRUE`、`False`、`1`、`0`、空值或任意变体 | 前端拒绝并不允许提交；后端收到该类值则整批拒绝（不改变数据库值） |

### 4.5 原始消息存储策略校验（对应 REQUIREMENTS §11.2）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-027 | `raw-message-storage-strategy` 处于编辑状态 | 依次选择 `NONE`、`PLAIN`、`COMPRESS` 并保存 | 三个大写枚举均可保存成功；保存后重新加载显示精确大写枚举值 |
| SC-AC-028 | `raw-message-storage-strategy` 处于编辑状态 | 尝试提交小写 `none`、混合大小写、空值或其他非枚举值 | 前端拒绝并不允许提交；后端收到该类值则整批拒绝（不改变数据库值） |

### 4.6 实时批量插入数据库类型校验（对应 REQUIREMENTS §11.3）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-029 | `realtime-insert-batch-enabled-database-types` 处于编辑状态 | 只选择一种数据库类型并保存 | 保存成功；保存值仅含该类型小写值，无多余空格 |
| SC-AC-030 | 同上 | 输入带首尾空格的 Token、大写 Token 及重复 Token（如 ` MySQL, mysql, ORACLE `）后保存 | Token 去除首尾空格、统一转小写、自动去重；保存值为去重后的小写逗号连接字符串，不含空格 |
| SC-AC-031 | 同上 | 提交包含白名单外类型（如 `sqlserver`、`postgres`）的值 | 前端拒绝并不允许提交；后端收到该类值则整批拒绝（不改变数据库值） |
| SC-AC-032 | 同上 | 同一集合以不同选择顺序（如先 Doris 后 MySQL 与先 MySQL 后 Doris）分别编辑 | 规范化后两者不视为实际修改；保存值固定为 `doris,oracle,mysql` 的子序列（示例：选择 MySQL 和 Doris → `doris,mysql`），仅顺序不同不产生脏值 |
| SC-AC-033 | 同上 | 尝试清空选择（不选任何类型）后保存 | 前端拒绝“至少选择一种”并不允许提交；后端收到空值则整批拒绝 |

### 4.7 快照批次大小校验（对应 REQUIREMENTS §11.4）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-034 | `snapshotBatchSize` 处于编辑状态 | 输入 `100`、`10000` 及中间值（如 `500`）并保存 | 均保存成功；保存值为无多余空格、无前导零的标准十进制字符串 |
| SC-AC-035 | 同上 | 输入 `99`、`10001` 并尝试保存 | 前端拒绝（超出最小值/最大值）并不允许提交；后端收到该类值则整批拒绝（不改变数据库值） |
| SC-AC-036 | 同上 | 输入小数（如 `100.5`）、科学计数法（如 `1e3`）、带符号（如 `+100`、`-100`）、空值或非数字字符并尝试保存 | 前端拒绝并不允许提交；后端收到该类值则整批拒绝（不改变数据库值） |

### 4.8 源表删除策略校验（对应 REQUIREMENTS §11.5）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-037 | `tableRowDeleteStrategy` 处于编辑状态 | 依次选择 `DELETE`、`DELETE_FLAG` 并保存 | 两个大写枚举均可保存成功；保存后重新加载显示精确大写枚举值 |
| SC-AC-038 | 同上 | 尝试提交小写 `delete`、混合大小写、空值或其他非枚举值 | 前端拒绝并不允许提交；后端收到该类值则整批拒绝（不改变数据库值） |

### 4.9 当前只读配置（对应 REQUIREMENTS §12）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-039 | 配置列表已加载 | 观察 `monitor-metric-topic-name`、`server-log-topic-name` 两行 | 两行只展示只读配置值，无编辑控件，不可编辑、不可保存 |
| SC-AC-040 | 假设数据库将上述 key 的 `IS_EDITABLE` 改为 `'1'`（该状态为未来边界，仅定义期望行为，不授权改库） | 观察页面 | 在前后端正式增加专门规则前，两行仍只读；不因 `IS_EDITABLE` 变化开放编辑 |

### 4.10 通用非空与物理长度（对应 REQUIREMENTS §11.7）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-041 | 任一支持编辑配置处于编辑状态 | 将 `CONFIG_VALUE` 改为空、NULL 或仅含空白字符并尝试保存 | 前端拒绝（基于去除首尾空格后的值判定非空）并不允许提交；后端收到空值则整批拒绝 |
| SC-AC-042 | 任一支持编辑配置处于编辑状态 | 提交该 Key 专门值域内的合法值（长度≤64）并保存；直接构造超过 64 字符的新值批量请求 | 专门值域内的合法值保存成功（当前布尔/枚举/多选/整数 Key 的成功值必须同时满足专门值域与长度≤64，不要求其接受一个 64 字符值）；超过 64 字符的新值请求由后端在写库前拒绝并整批回滚，不依赖数据库截断作为校验方式；未来新增自由文本型可编辑 Key 时，再对该 Key 执行 64 字符成功/65 字符失败的精确边界验收 |

### 4.11 编辑、撤销与脏值（对应 REQUIREMENTS §13）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-043 | 多条支持编辑配置处于可编辑状态 | 连续修改两条及以上配置的值 | 全部编辑内容保留；多条修改可一并进入保存确认流程 |
| SC-AC-044 | 页面加载后未做任何实际修改 | 观察“保存全部”和“撤销修改”按钮 | 两个按钮均不可用（禁用状态） |
| SC-AC-045 | 已修改若干配置值 | 点击“撤销修改” | 所有编辑控件恢复为“最近一次成功加载”的值；不发起任何网络请求，数据库值不变 |
| SC-AC-046 | 多选配置当前保存值为 `doris,mysql` | 以不同选择顺序选择相同集合（如先 MySQL 后 Doris 与先 Doris 后 MySQL），使规范化后的固定顺序值仍为 `doris,mysql` | 用户选择顺序不同，但规范化后的固定顺序值相同，不视为实际修改；不进入确认框变更列表，“保存全部”保持不可用（若全页仅此变化） |
| SC-AC-047 | 用户正在编辑配置且未保存 | 等待或触发任何后台刷新/重入 | 页面不自动刷新；用户未保存的编辑内容不被覆盖 |

### 4.12 保存确认框（对应 REQUIREMENTS §14）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-048 | 已修改至少一个支持编辑配置 | 点击“保存全部” | 先完成前端全部校验；若存在非法值，不弹出确认框并显示校验错误；校验通过才弹出确认框 |
| SC-AC-049 | 存在实际变更的配置 | 弹出确认框后观察 | 确认框只列出实际发生变化的配置；每项主要展示配置项显示名称、原值、新值，不突出 `CONFIG_KEY`；技术 Key 通过信息图标 Tooltip 按需查看；未变更配置不出现 |
| SC-AC-050 | 确认框已弹出 | 点击“取消” | 不发送任何保存请求；页面编辑内容全部保留 |
| SC-AC-051 | 确认框已弹出 | 点击“确认” | 发送一次批量保存请求；请求仅携带既有记录主键与新 `CONFIG_VALUE` |

### 4.13 后端校验与事务（对应 REQUIREMENTS §10、§15、§16）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-052 | 构造包含不可编辑记录（`IS_EDITABLE='0'`、NULL/空白/异常值）的批量请求（构造请求不涉及数据库写授权） | 直接向后端提交该请求 | 后端重新读取真实记录并判定不可编辑，整批拒绝；不改变数据库任何值 |
| SC-AC-053 | 构造包含未知/不支持 `CONFIG_KEY` 记录的批量请求 | 直接向后端提交该请求 | 后端判定 key 不在白名单，整批拒绝；不改变数据库任何值 |
| SC-AC-054 | 构造包含非唯一中心端归属记录的批量请求（目标记录 `SERVER_ID` 与唯一中心端不符） | 直接向后端提交该请求 | 后端依据主键重新读取并判定归属错误，整批拒绝；不改变数据库任何值 |
| SC-AC-055 | 构造包含不存在主键记录的批量请求 | 直接向后端提交该请求 | 后端判定记录不存在，整批拒绝；不改变数据库任何值 |
| SC-AC-056 | 构造尝试修改 `CONFIG_KEY`、`CONFIG_DESC`、`SERVER_ID`、`ID_SERVER_CONFIG`、`IS_EDITABLE` 中任一字段的批量请求 | 直接向后端提交该请求 | 后端拒绝修改非 `CONFIG_VALUE` 字段；不改变数据库任何值 |
| SC-AC-057 | 批量请求包含多条全部合法的目标记录 | 提交并观察结果 | 全部记录在同一数据库事务内更新成功；保存成功反馈后重新查询并展示最新配置 |
| SC-AC-058 | 批量请求中至少一条记录触发校验失败或更新失败（构造失败路径，不涉及数据库写授权） | 提交并观察结果 | 整批回滚，禁止部分成功；数据库中所有目标记录值均保持原值 |
| SC-AC-059 | 两次并发保存场景 | 先后提交两个合法批量保存请求 | 本 Feature 不做并发保护、不使用旧值/版本号/更新时间作更新条件；后一次合法保存覆盖数据库当时值，即“最后一次成功保存生效”；确认框原值不参与并发比较 |
| SC-AC-060 | 保存成功 | 观察保存结果 | 页面给出成功反馈，并重新查询唯一中心端及全部配置；重新加载结果成为新的原始值 |
| SC-AC-061 | 保存失败（后端拒绝或数据库异常） | 观察保存结果 | 页面给出明确但不泄露底层敏感堆栈的错误信息；数据库整批回滚；页面保留用户编辑内容，便于修改或重试 |

### 4.14 安全与非目标（对应 REQUIREMENTS §16、§17、§18）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-062 | 配置列表与确认框可见 | 观察所有 `CONFIG_VALUE` 展示 | 所有值完整展示，不脱敏、不掩码；只读值超宽省略时悬停展示完整原文；不存在配置Key独立列，Key 仅通过信息图标 Tooltip 展示 |
| SC-AC-063 | 已保存配置 | 观察保存后页面与可用操作 | 页面不触发、不调用、不提示 `sync-server` 重启，不提供生效通知或生效检测操作，不判断配置是否已被 `sync-server` 加载 |
| SC-AC-064 | 全功能范围 | 审查接口、页面与变更清单 | 不执行任何数据库 DDL、不新增索引/约束/物理外键；不提供 `CDC_SERVER` 维护能力（无新增/修改/删除中心端记录的页面或接口）；不新增、删除 `CDC_SERVER_CONFIG` 记录 |

### 4.15 说明多行与人工换行（对应 REQUIREMENTS §7.6）

| 编号 | 前置条件 | 操作/输入 | 预期结果 |
|---|---|---|---|
| SC-AC-066 | 配置列表已加载，其中存在 `CONFIG_DESC` 含真实 LF 换行字符（必要时也覆盖 CRLF）的记录（构造该数据库状态需另行获得写授权；本用例只定义期望行为） | 进入页面并观察配置项说明列 | 说明在数据库真实换行位置分行显示；同一列表其他普通过长文本仍按列宽自动折行；`<br>` 与字面量 `\n` 不被解析为换行协议（作为普通文本显示）；`CONFIG_DESC` 中的 HTML 内容保持文本转义（不渲染为 HTML，无 `v-html`）；多行说明不造成横向溢出，信息图标/Key Tooltip 仍可悬停查看完整 Key |

## 5. 文档级变更记录

| 日期 | 变更 | 依据 |
|---|---|---|
| 2026-08-27 | 建立“中心端配置”Feature 验收基线（DRAFT_PENDING_USER_REVIEW；共 64 项，全部 NOT_RUN） | SERVER-CONFIG-FEATURE-BASELINE-001（纯文档任务） |
| 2026-08-27 | R1 修订：显示结构改按“配置项说明 + 配置值”两列验收，新增信息图标 Key Tooltip 与显示名称兜底覆盖；修订 SC-AC-004/007/008/009/018/022/046/049/062；修正 SC-AC-042 为“专门值域 + 长度≤64”口径；新增 SC-AC-065 非法当前值纠正用例；合计更新为 65，全部保持 NOT_RUN | SERVER-CONFIG-FEATURE-BASELINE-001-R1（ChatGPT 复审 + 项目负责人确认；纯文档修订，状态保持 DRAFT_PENDING_USER_REVIEW） |
| 2026-08-27 | 批准：文档状态由 `DRAFT_PENDING_USER_REVIEW` 改为 `APPROVED`；`依据需求`更新为已批准的 `REQUIREMENTS.md`；记录批准任务、批准日期、批准人（项目负责人）与 ChatGPT 复审通过的候选提交 `4e55493a...`；65 条用例 `SC-AC-001`～`SC-AC-065` 全部保持 `NOT_RUN`，编号连续、唯一且内容不变；不得将需求批准误写为验收执行通过 | SERVER-CONFIG-FEATURE-BASELINE-APPROVAL-001（项目负责人批准驱动的 Feature 需求与验收基线收口；纯文档任务，不连接数据库，不修改代码） |
| 2026-08-28 | 候选调整（预验收）：SC-AC-017 预期改为 `ID_SERVER_CONFIG ASC`；SC-AC-007 修订为聚焦普通过长文本自动折行；新增 SC-AC-066 人工换行用例（真实 LF/CRLF 分行、`<br>`/字面量 `\n` 不作为协议、HTML 文本转义、无横向溢出、Key Tooltip 可用）；验收合计更新为 66，全部保持 `NOT_RUN`；文档状态迁移为 `DRAFT_ADJUSTMENT_PENDING_USER_REVIEW`、实现状态迁移为 `IMPLEMENTED_ADJUSTMENT_PENDING` | SERVER-CONFIG-PRE-ACCEPTANCE-ADJUSTMENT-BASELINE-001（纯文档候选基线任务；待用户复审） |
| 2026-08-28 | 批准收口：文档状态由 `DRAFT_ADJUSTMENT_PENDING_USER_REVIEW` 迁移为 `APPROVED`；记录批准任务、批准日期、批准人（项目负责人）与 ChatGPT 最终复审通过提交 `b1c5349...`；`SC-AC-001`～`SC-AC-066` 共 66 条全部保持 `NOT_RUN`，编号连续唯一；实现状态保持 `IMPLEMENTED_ADJUSTMENT_PENDING`；所有用例内容与预期不变 | SERVER-CONFIG-PRE-ACCEPTANCE-ADJUSTMENT-BASELINE-APPROVAL-001（项目负责人批准驱动的验收前调整基线收口；纯文档任务，不连接数据库，不修改代码） |
| 2026-08-29 | 最终收口：实现状态由 `IMPLEMENTED_ADJUSTMENT_PENDING` 迁移为 `IMPLEMENTED_ACCEPTED`；新增元数据行（验收用例状态/正式验收状态 = `66 PASSED / 0 FAILED（ACCEPTED）`、收口任务、收口日期、验收证据提交 `b5aeec2...`、项目负责人最终人工测试、`PENDING_USER_CONFIRMATION=0`）；明确正式验收最终执行结果 `66 PASSED / 0 FAILED / 0 BLOCKED / 0 NOT_RUN`；新增 §6 最终验收证据索引。本行仅收口状态，不改动任何用例编号、前置条件、步骤或预期结果；历史变更记录中此前的 `DRAFT_*`/`APPROVED`/`NOT_RUN`/`IMPLEMENTED_ADJUSTMENT_PENDING` 等历史状态原样保留 | SERVER-CONFIG-FORMAL-ACCEPTANCE-CLOSEOUT-001（纯文档正式验收收口；不修改代码/测试，不连接数据库，不重新执行验收） |

## 6. 最终验收证据索引

以下为正式验收与收口的完整证据链。`SC-AC-001`～`SC-AC-066` 的编号、前置条件、步骤与预期结果始终未变；本节仅索引执行与收口证据，不构成对用例定义本身的改动。

| # | 证据项 | 说明 | 提交/文件 |
|---|---|---|---|
| 1 | 初次正式验收报告 | 66 条初次执行；经 R1 事实修正后历史结果为 `64 PASSED / 2 FAILED`（失败项 `SC-AC-009`、`SC-AC-062`） | `docs/features/server-config/reports/SERVER-CONFIG-FORMAL-ACCEPTANCE-001.md`（R1 事实修正提交 `dea3360...`） |
| 2 | R1 事实修正 | 修正初次验收报告中的事实性偏差，保留当时 `64/2` 历史 | 提交 `dea3360...` |
| 3 | 缺陷修复报告 | 修复 `SC-AC-009`/`SC-AC-062` 两项显示缺陷（Key Tooltip 前缀、超宽只读值单行省略与悬停原文） | 提交 `70382a6...`；`docs/features/server-config/reports/SERVER-CONFIG-FORMAL-ACCEPTANCE-DEFECT-FIX-001.md` |
| 4 | 定向重验报告 | 真实浏览器定向重验 `SC-AC-009`、`SC-AC-062`：`2 PASSED / 0 FAILED`，数据库临时数据已恢复 | 提交 `b5aeec2...`；`docs/features/server-config/reports/SERVER-CONFIG-FORMAL-ACCEPTANCE-TARGETED-RETEST-001.md` |
| 5 | ChatGPT 最终复审 | 对缺陷修复 `70382a6` 及定向重验 `b5aeec2` 的代码范围、浏览器测量、Tooltip 行为、数据库恢复和报告一致性复审：`PASSED`，无需 R1 | 见定向重验报告与最终收口报告 |
| 6 | 项目负责人最终人工测试 | 项目负责人（2026-08-29）明确确认“保存、刷新、恢复”都正常 | 见最终收口报告 |
| 7 | 最终收口报告 | 综合判定 `66 PASSED / 0 FAILED / 0 BLOCKED / 0 NOT_RUN`，`formal_acceptance_status=ACCEPTED`，`implementation_status=IMPLEMENTED_ACCEPTED` | `docs/features/server-config/reports/SERVER-CONFIG-FORMAL-ACCEPTANCE-CLOSEOUT-001.md` |

说明：`SC-AC-014`、`SC-AC-015`（中心端零条/多条等需构造异常数据库状态的场景）始终由后端测试、前端测试与代码证据交叉通过，不作为真实数据库构造验收；本收口不改变该披露口径。
